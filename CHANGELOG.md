# Changelog

## [Unreleased]

_کارهای انجام‌شده که هنوز ریلیز نشده‌اند._

## [1.111.0]

### Added — عیب‌یابیِ ارتباطِ ایران↔خارج: اختلال از کدام سمت است؟

برگه: `docs/specs/2026-09-26-link-diagnosis-and-traffic.md`

«تانل کند است» پنج علتِ متفاوت دارد و هر کدام کارِ متفاوتی می‌خواهد. تا
امروز پنل هیچ‌کدام را از هم جدا نمی‌کرد. حالا «تانل ← عیب‌یابیِ ارتباط»
هر پنج دقیقه پنج مسیر را از دو سرور می‌سنجد:

| مسیر | چه چیزی را می‌گوید |
|---|---|
| ایران ← داخل | خودِ سرور/دیتاسنترِ ایران سالم است؟ |
| ایران ← بین‌الملل | خروجیِ بین‌المللِ ایران باز است؟ |
| ایران ← خارج | مسیرِ مستقیم تا سرورِ خارج |
| خارج ← ایران | **دقیقاً مسیرِ خودِ تانل** (پورتِ تانل روی سرورِ ایران) |
| خارج ← بین‌الملل | خودِ سرورِ خارج سالم است؟ |

- **حکم:** بالای هر سرور یک حکم به زبانِ ساده می‌آید، همراه با «چه کنم»:
  - مشکلِ سرورِ ایران؛
  - اختلالِ سراسریِ بین‌الملل؛
  - مشکلِ سرورِ خارج؛
  - مسیرِ بینِ دو سرور (معمولاً آی‌پیِ محدودشده)؛
  - یا همه سالم (پس مشکل از تانل است).
- **تاریخچه:** هر مسیر یک نوارِ ۲۴ ساعته دارد تا زمانِ اختلال‌ها کنارِ هم
  دیده شود.
- **یک پیاده‌سازی:** منطق در `backend/linkcheck.py` است. پنل آن را برای
  سمتِ خارج اجرا می‌کند و ایجنت همان فایل را برای سمتِ ایران از پنل
  می‌گیرد.
- **آی‌پیِ سرورِ خارج** از اتصال‌های برقرار روی پورتِ تانل پیدا می‌شود.
  پنل آن را جایی ذخیره نمی‌کرد: همه‌ی موتورها از خارج به ایران زنگ
  می‌زنند و `remote_host` آدرسِ ایران است.

### Added — حجمِ ترافیکِ هر سرور

«تانل ← حجمِ ترافیک»: سرورِ پنل و هر نود؛ امروز، ۷ روز و ۳۰ روز، دریافت و
ارسال جدا، و نمودارِ ساعتی و روزانه. تا امروز فقط ترافیکِ لحظه‌ای خوانده
می‌شد و هیچ حجمی ذخیره نمی‌شد.

- شمارنده‌های کارتِ شبکه هر پنج دقیقه خوانده و در سطل‌های ساعتی جمع
  می‌شوند.
- شمارنده‌ی کوچک‌تر از قبل یعنی ری‌استارت: حجمِ بعد از ری‌استارت شمرده
  می‌شود، نه منفی و نه صفر.
- فیلترِ کارت‌ها همان `monitor` است، تا «لحظه‌ای» و «امروز» با هم بخوانند.

### Fixed — «کیفیتِ تانل» مسیرِ ایران↔خارج را نمی‌سنجید

سنجشِ کیفیت به `127.0.0.1:پورتِ تانل` روی خودِ سرورِ ایران زده می‌شد. آن
اتصال را موتورِ تانل همان‌جا جواب می‌دهد، پس عددش تاخیرِ ایران↔خارج نبود.
با این حال برچسبش می‌گفت «همان چیزی که ترافیکِ واقعی حس می‌کند»، و تانلِ
کند «عالی» دیده می‌شد. برچسب حالا می‌گوید واقعاً چه چیزی را می‌سنجد و به
عیب‌یابیِ ارتباط ارجاع می‌دهد.

### ⚠️ ایجنت باید به‌روز شود (۱.۶.۰)

سنجشِ سمتِ ایران و حجمِ ترافیکِ نودها دستورِ تازه‌ی `pathcheck` را لازم
دارند. ایجنتِ قدیمی خودکار به‌روز نمی‌شود. صفحه‌ی عیب‌یابی صریح می‌گوید کدام
سرور قدیمی است و دکمه‌ی به‌روزرسانی دارد. تا آن موقع، مسیرهای سمتِ خارج و
حجمِ سرورِ پنل کار می‌کنند.

### Changed — دروازه‌ها

- `tools/test-linkcheck.py` (۴۳ بررسی): هر ردیفِ جدولِ حکم، تفاضلِ ترافیک
  با ری‌استارت، برابریِ فیلترِ کارت‌ها با `monitor`، و اینکه نتیجه‌ی یک نود
  روی نودِ دیگر نوشته نشود.
- همین تست پیش از ریلیز یک باگ گرفت: `pathcheck` در فهرستِ مجازِ کارهای
  `tunnels.py` نبود و حلقه‌ی پنج‌دقیقه‌ای هر دور با خطا می‌ایستاد.

## [1.110.0]

### Added — اشتراکِ ماهانه‌ی ربات و مینی‌اپِ نماینده

برگه: `docs/specs/2026-09-26-reseller-store-subscription.md`

نماینده برای اینکه ربات و مینی‌اپش بفروشد، اشتراکِ ماهانه می‌خرد. بی
اشتراک، خرید، تمدید (دستی و خودکار)، شارژِ کیف پول و تستِ رایگان
می‌ایستند و مشتری یک جمله می‌بیند: «فروشِ این فروشگاه موقتاً متوقف است».
کانفیگ‌ها، لینک‌ها و صندوقِ مشتری دست نمی‌خورند. رسیدِ سفارشی که پیش از
بسته‌شدن ساخته شده هم پذیرفته می‌شود، چون پولش در راه است.

- **پیش‌فرض قیمتِ صفر است، یعنی رایگان برای همه.** به‌روزرسانی فروشِ هیچ
  نماینده‌ای را قطع نمی‌کند تا مالک قیمت بگذارد. عمداً به قیمتِ «پوسته‌ی
  شخصی» گره نخورد: هر نماینده‌ای که پوسته نخریده بود، با آپدیت فروشش
  بی‌خبر قطع می‌شد.
- **مالک:** «نمایندگی → نماینده‌ها و دسترسی»، زیرِ فهرست. قیمت و مدت، وضعیتِ
  هر نماینده، و بازکردنِ دستی برای پرداختِ آفلاین.
- **نماینده:** نوارِ وضعیت در پرتال. وقتی فروشگاه بسته است یا کمتر از ۵ روز
  مانده، روی همه‌ی صفحه‌ها دیده می‌شود؛ وگرنه فقط در داشبورد. خرید از
  اعتبارِ خودش است. تمدیدِ زودهنگام روزهای مانده را نمی‌سوزاند.
- **قاعده یک‌جاست:** `core.addon_open` در `bot/core.py`. ربات و بکند هر دو
  همان را صدا می‌زنند؛ قفلِ پوسته هم حالا از همین‌جا می‌خواند و نسخه‌ی خودش
  در `app.py` حذف شد. خریدِ هر دو افزونه از یک هسته (`_addon_buy`) رد
  می‌شود: اول کسر، بعد تاریخ، و برگشتِ پول اگر نوشتن شکست بخورد.
- **دروازه در هسته‌هاست، نه پوسته‌ها:** `card_order`، `wallet_purchase`،
  `topup_order`، `give_trial` و `auto_renew_subscription`. مینی‌اپ از همین
  هسته‌ها رد می‌شود، پس جدا بسته نشد. `test_flow` با ast می‌سنجد که هر پنج
  تا `store_gate` را صدا بزنند و `receipt_submit` صدا نزند. `test_reseller`
  رفتار را با پولِ واقعی می‌شمارد.
  - با خاموش‌کردنِ عمدیِ دروازه، `test_reseller` قرمز شد.
- پوسته‌ی `checkout` هر دیکشنریِ خطا را «سکه‌ی کافی نیست» فرض می‌کرد و روی
  دلیلِ تازه `KeyError` می‌داد. `mini_buy` هم هر دلیلِ ناشناسی را «این پلن
  دیگر در دسترس نیست» می‌گفت. هر دو حالا شاخه‌ی خودشان را دارند.

### Fixed — مینی‌اپ اولش لوگو و رنگِ فروشگاه را نشان نمی‌داد

سه علت داشت، و هر سه «اولش نمی‌آید» دیده می‌شدند:

- **بارِ اول، بی‌کش:** اسپلش خنثی بود و بعد به لوگو و رنگ می‌پرید. حالا
  اگر نشانی `?shop=` دارد (ربات همیشه می‌گذارد)، پیش از اولین فریم
  `/api/mini/brand` پرسیده می‌شود. سقفِ این پرسش ۹۰۰ میلی‌ثانیه است و شکستش
  همان اسپلشِ خنثیِ قبلی را می‌دهد، نه صفحه‌ی سفید. هارنس هم تا امروز
  `?shop=` را از نشانی می‌انداخت، پس این مسیر هرگز در آن اجرا نشده بود.
- **فروشگاهِ خودِ مالک:** همین که مالک برای نماینده‌ها قیمتِ پوسته می‌گذاشت،
  مینی‌اپِ **خودش** هم به پوسته‌ی پیش‌فرض برمی‌گشت. مستاجرِ ریشه دیگر هرگز
  قفل نیست.
- **مالک اصلاً ویرایشگر نداشت.** استودیوی پوسته فقط در پرتالِ نماینده بود.
  حالا «ربات تلگرام → پوسته‌ی مینی‌اپ» همان استودیو را با مسیرهای مالک باز
  می‌کند. استودیوی دومی ساخته نشد: `ThemeBox` به `portal/studio.jsx` رفت و
  مسیرهایش را از یک `themeApi` می‌گیرد.

### Added — چند سبکِ صفحه‌ی ورود

«نوار»، «حلقه»، «موج»، «نقطه‌ها» و «فقط لوگو». مالک و نماینده هر دو در گامِ
۴ِ استودیو انتخاب می‌کنند. انتخاب همان لحظه در گوشیِ پیش‌نمایش پخش می‌شود و
کادرِ لوگوی گامِ ۳ هم همان سبک را نشان می‌دهد.

- سبک روی قالب سوار است: قالب رنگ و زمینه را می‌دهد، سبک «چه چیزی حرکت
  می‌کند» را.
- شناسه‌ها سه جا نوشته شده‌اند: بکند، `mini-themes.js`، و `mark.jsx` که
  عمداً به ماژولی وابسته نیست. `test-portal-dashboard` برابریِ هر سه را
  می‌سنجد.
- سبکِ ناشناس ۴۰۰ می‌گیرد، نه «نوار»ِ بی‌صدا.

### Fixed — همکارِ فروش نمی‌دانست کدام لینک را بدهد

- پنلِ همکار (`/aff`) از نو چیده شد:
  - لینکِ معرفیِ آماده با کپی و اشتراک در تلگرام؛
  - «چطور کار می‌کند» در سه قدم؛
  - آمار؛
  - ستون‌های مشتری و تسویه.
- خطای شبکه دیگر `Failed to fetch`ِ انگلیسی نیست.
- لینکِ همکار حالا یک‌جا در بکند ساخته می‌شود (`_aff_link_fmt`). مالک در
  «همکاری در فروش» لینک و «پیام برای همکار» (نشانیِ پنل، کد، لینک، درصد) را
  با یک دکمه کپی می‌کند. `test-seams` برابریِ شکلِ لینک در ربات و بکند را
  می‌سنجد.

### Fixed — صفحه‌ی اشتراک

- **نوارِ پایین روی دسکتاپ جا می‌ماند.** قاعده‌ای پایین‌ترِ فایل
  `max-width: 480px` داشت و قاعده‌ی ۵۶۰ پیکسلیِ دسکتاپ را می‌بُرد: نوارِ ۴۸۰
  پیکسلی زیرِ صفحه‌ی ۵۶۰ پیکسلی. حالا هر دو از یک متغیر (`--page-w`)
  می‌خوانند و روی دسکتاپ نوار گوشه و لبه‌ی کارت‌ها را دارد.
- **رنگِ نوار با پالت نمی‌خواند.** رنگش یک مشکیِ ثابت بود؛ حالا از
  `--surface` پالت ساخته می‌شود.
- **نوارِ پیمایشِ سفید روی دسکتاپ،** کنارِ صفحه‌ی تیره. صفحه `color-scheme`
  نداشت.

### Fixed — پیش‌نمایشِ زنده‌ی صفحه‌ی اشتراک

- روی گوشی، قابِ ۳۹۰ پیکسلی در کارتِ ۳۵۸ پیکسلی جا نمی‌شد و سمتِ چپِ صفحه
  بریده می‌شد. حالا قاب خودش را با جا تطبیق می‌دهد و درصدِ واقعی را نشان
  می‌دهد.
- در بزرگ‌نماییِ کمتر از ۱۰۰٪، قاب با پهنای اصلی از وسطِ خودش کوچک می‌شد و
  به راست می‌لغزید. ارتفاعِ ۷۶۰ هم هرگز کم نمی‌شد.
- هر بار بازشدن، یک خطای قرمز در کنسول می‌آمد: درخواست به آدرسِ نمونه‌ای
  که همیشه شکست می‌خورد.

### Fixed — استودیوی پوسته روی گوشی

- **نوارِ ذخیره:** «ذخیره‌نشده» روی گوشی حرف‌به‌حرف زیرِ هم می‌شکست. سه دکمه و
  وضعیت در ۳۶۰ پیکسل جا نمی‌شدند؛ حالا دکمه‌ها روی گوشی فقط آیکون دارند و
  برچسبشان در `aria` است.
- **لوگو:** دکمه‌ی آپلود بی‌برچسب بالای ستون می‌نشست. حالا یک ردیف مثلِ «قاب»
  و «زمینه» است، با آنچه پذیرفته می‌شود. تصویر را روی پیش‌نمایش هم می‌شود
  رها کرد.

### Changed — دروازه‌ها

- `test-contract`: ستون‌هایی که کوئری‌های `billing.db` می‌خوانند، در اسکیمای
  واقعی سنجیده می‌شوند. با یک ستونِ غلطِ عمدی قرمز شد.
- `test-seams`: `/api/mini/brand` تنها مسیرِ مینی‌اپ بی‌امضای تلگرام است، با
  دلیل. و سنجیده می‌شود که جز `_shop_brand` چیزی برنگرداند.

## [1.109.0]

### Fixed — هشدارِ سلامتِ سرور هرگز فرستاده نشده بود

`_health_alert` (دیسکِ پر، سرویسِ خاموش، گواهیِ رو به انقضا) ستون‌های
`admin_id` و `group_id` را از جدولِ `tenants` می‌خواند. این دو ستون هرگز
وجود نداشته‌اند؛ نامشان `owner_tg_id` و `admin_group_id` است. پس کوئری
**همیشه** خطا می‌داد و `except Exception: pass` آن را می‌بلعید. **هیچ
هشدارِ سروری هرگز به تلگرام نرسید**، و هیچ جایی هم نشانه‌ای نبود.

تستِ خودِ این قابلیت (`test-health`) جدولی دست‌ساز با **همان نام‌های غلط**
می‌ساخت، پس با باگ هم‌نظر بود و سبز می‌ماند.

- **کوئری:** حالا ستون‌های واقعی را می‌خواند. مسیریابی همان `staff_chat`ِ
  ربات است: گروهِ مدیریت با تاپیکِ `alerts`، وگرنه پیویِ صاحبِ ربات. هر
  شکستِ ارسال لاگ می‌شود.
- **`test-health`:** اسکیمای `tenants` را از خودِ `init_db()`ِ ربات
  می‌سازد. روی کدِ قبلی شش بررسی قرمز می‌شود.
- **دروازه‌ی تازه در `test-seams`:** هر ستونی که کوئریِ تک‌جدولیِ بکند یا
  ربات می‌خواند یا می‌نویسد، در اسکیمای واقعیِ ربات (همه‌ی ۱۷ جدول، با
  مهاجرت‌ها) باشد. روی کدِ قبلی دقیقاً همین دو ستون را گرفت.

### Fixed — دیگر هیچ `except: pass`ِ بی‌دلیلی نیست

همین شکل بود که باگِ بالا را سال‌ها پنهان کرد. حدودِ هفتاد جا در بکند و
ربات حالا لاگ می‌کنند: `app.py`، `tunnels`، `firewall`، `monitor`،
`netid`، `health`، `handlers`، `db`، `xui` و `tg`.

- **warning** برای چیزهایی که اثر دارند:
  - نشدنِ نخِ حلقه‌ی سلامت و نگهداری؛
  - پاک‌نشدنِ فایلِ توکنِ گیت‌هاب؛
  - پاک‌نشدنِ پرچمِ برگشتِ فایروال؛
  - ذخیره یا خوانده‌نشدنِ فهرستِ آی‌پی‌های بسته پس از ری‌استارت؛
  - نرسیدنِ پیامِ ردِ رسید یا شکستِ تمدید به مشتری؛
  - شکستِ مهاجرتِ دیتابیسِ ربات.
- **debug** برای بقیه.
- نه استثنا با دلیل در فهرست ماندند، مثلاً `getloadavg` روی ویندوز.
- **دروازه:** `test-seams` هر `except: pass`ِ تازه را در این فایل‌ها رد
  می‌کند.

### Fixed — ورودیِ هزینه‌ها

- **نرخِ دستیِ نامعتبر** بی‌صدا نادیده گرفته می‌شد و نرخِ بازار جایش
  می‌نشست. هزینه‌ای که مدیر با نرخِ خودش وارد کرده بود با عددِ دیگری
  ذخیره می‌شد. حالا ۴۰۰ می‌گیرد. نرخِ اعشاری پذیرفته و گرد می‌شود.
- **تاریخِ خرابِ هزینه** خام ذخیره می‌شد و بعد در شمسی و فیلترِ بازه از قلم
  می‌افتاد. **حجمِ ناخوانا** بی‌صدا حذف می‌شد. هر دو حالا ۴۰۰ می‌گیرند.

### Fixed — شکست‌های بی‌صدای رابط

- **داشبوردِ صفحه‌ی اشتراک، کارتِ «بدهیِ نماینده‌ها»:** خطای HTTP اسکلتِ
  همیشگی می‌ماند، و خطای شبکه کارت را پنهان می‌کرد، انگار حسابداری تنظیم
  نشده باشد.
- **کانال:** شکستِ پیشنهادها و پلن‌ها بی‌صدا بود. شکستِ خواندنِ وضعیتِ هوش
  مصنوعی راهنمای «آدرس را تنظیم کنید» نشان می‌داد، در حالی که مشکل خواندن
  بود.
- **پرتال، قیفِ فروش:** خطا کارت را حذف می‌کرد.
- **«اجرا کن الان»ِ نگهداری:** شکستِ بی‌پیام «انجام شد» را در رنگِ خطا
  نشان می‌داد.
- **بازیابیِ ربات و حسابداری:** بی نسخه‌ی امن هشدار می‌دهند
  (`safetyWarning`، حالا در هر دو). قطعیِ شبکه «فایل معتبر نبود» گفته
  نمی‌شود. شمار با رقمِ فارسی می‌آید.

### Tests

- **`test-load-errors`:** ۶۱ بررسی. کانال (پیشنهاد، هوش مصنوعی)، کارتِ
  بدهیِ داشبورد و قیفِ پرتال اضافه شدند؛ روی کدِ قبلی هر چهار قرمزند.
- **`test-bot-buttons`:** بررسیِ «dispatch استثنا را می‌گیرد» روی catchِ
  *درونی* سبز می‌شد، نه روی catchِ بیرونی که قرار بود بسنجد. حالا هر دو
  شکل را می‌شناسد.
- **`test-backup` و `test-admin-api`:** نسخه‌ی امنِ ناموفق، ورودیِ هزینه‌ها،
  و ارسالِ واقعیِ هشدار به گروه و تاپیک.

## [1.108.1]

### Fixed — رقمِ لاتین وسطِ پیامِ فارسی

پیام‌هایی که سرور به رابط می‌فرستد عدد را با f-stringِ خام می‌گذاشتند.
چند نمونه:

- «موجودی کافی نیست — 150,000 تومان کم دارید» (مینی‌اپِ مشتری)؛
- «قیمتِ «ارزان» (110,000 تومان) زیرِ کفِ شماست»؛
- «2 بخشِ تازه‌تر …».

حالا شمار و مبلغ در بیست‌وچند پیام با `_fnum` می‌آیند، همان شکلِ `faNum`ِ
رابط («۱۵۰٬۰۰۰»). مسیر، آی‌پی، پورت و نسخه عمداً لاتین می‌مانند؛ به همین
دلیل تبدیل در سرور و فقط روی عدد است، نه روی کلِ پیام در رابط.

دو پیامِ اعتبارِ ناکافیِ ربات (ساخت و تمدید) هم حالا از `core.toman`
می‌گذرند، مثلِ بقیه‌ی ربات.

### Tests

- **دروازه‌ی تازه در `test-seams`:** هر `{x:,}` داخلِ f-stringِ فارسی در بکند
  یا `handlers.py` رد می‌شود. PDF مستثناست. روی کدِ قبلی شش جا را گرفت.

## [1.108.0]

### Fixed — بازیابیِ پشتیبانِ قدیمیِ تنظیمات، بخش‌های تازه‌تر را پاک می‌کرد

«بازیابی از فایل» در صفحه‌ی اشتراک کلِ تنظیمات را با محتوای فایل جایگزین
می‌کرد. پشتیبانی که پیش از ساختنِ بخشی گرفته شده بود (واسطه‌ها، ربات،
پاپ‌آپ، …) آن بخش را بی‌صدا پاک می‌کرد.

حالا همان قاعده‌ی بازگردانیِ ربات اجرا می‌شود: **بخشی که در فایل نیست
یعنی «آن موقع وجود نداشت»، نه «حذفش کن».** پیامِ موفقیت می‌گوید چند بخشِ
تازه‌تر دست نخورد.

- قطعیِ شبکه دیگر «فایل معتبر نیست» گفته نمی‌شود. آن پیام فقط برای JSONِ
  ناخواناست؛ بقیه‌ی خطاها دلیلِ خودشان را می‌گویند.
- کپیِ امنِ پیش از بازیابی اگر شکست بخورد، لاگ می‌شود.

### Fixed — دسترسیِ فایل‌های راز

اگر محدودکردنِ فایلِ هشِ رمز یا فایلِ توکنِ گیت‌هاب به ۰۶۰۰ شکست می‌خورد،
بی‌صدا می‌گذشت. یعنی راز برای بقیه‌ی کاربرانِ سرور خوانا می‌ماند. حالا
لاگ می‌شود.

## [1.107.0]

### Fixed — پرتالِ نماینده و مینی‌اپ

- **مینی‌اپ — سفارش‌های باز بی‌صدا غیب می‌شدند.** اگر خواندنِ سفارش‌ها شکست
  می‌خورد، فهرست خالی می‌شد و کارتِ «رسیدتان در انتظار تایید است» می‌رفت.
  مشتری فکر می‌کرد پرداختش نرسیده و دوباره پول می‌داد. حالا اپ می‌گوید
  وضعیتِ سفارش‌های قبلی خوانده نشد و پیش از پرداختِ دوباره صبر کنید.
- **پرتال — پلن‌ها و پوسته‌ی مینی‌اپ:** خطای خواندن فقط داخلِ شاخه‌ی
  «داده رسید» نشان داده می‌شد. خواندنِ ناموفق هرگز به آن‌جا نمی‌رسید و
  اسکلتِ بارگذاری برای همیشه می‌ماند. حالا خطا با دکمه‌ی «دوباره» می‌آید.

### Tests

- **`test-load-errors`** حالا دو اپِ دیگر را هم کامل mount می‌کند:
  - **پرتال** با توکن و صفحه از آدرس، هشت صفحه؛
  - **مینی‌اپ** با یک WebAppِ ساختگیِ تلگرام، چهار مسیر.
- هر صفحه مسیرِ خودش را می‌شکند، نه مسیرهای پوسته را، تا خطای پوسته صفحه‌ی
  خراب را سبز نکند.
- روی کدِ پیش از این اصلاح دقیقاً همان سه مورد قرمز است. جمعاً ۵۶ صفحه
  و مسیر.

## [1.106.0]

### Fixed — ذخیره‌ای که پس از خواندنِ ناموفق همه‌چیز را پاک می‌کرد

`PUT /api/admin/bot/settings` کلِ دیکشنریِ تنظیماتِ ربات را **جایگزین**
می‌کند، و `PUT /api/admin/config` کلِ تنظیماتِ پنل را. چند صفحه اگر
خواندنِ تنظیمات شکست می‌خورد، با دیکشنریِ خالی یا پاسخِ خطا ادامه می‌دادند.
پس ذخیره‌ی بعدی فقط یک کلید را پس می‌فرستاد و **همه‌ی متن‌ها، سکه،
کارت‌ها و بقیه‌ی تنظیماتِ ربات پاک می‌شد**:

- **پیگیریِ تست** (کدهای تخفیف): `catch { setT({ settings: {} }) }` فرمِ
  پیش‌فرض را باز می‌کرد؛
- **پاسخ‌های آماده** (پیام‌ها): تنظیمات با `{}` شروع می‌شد؛
- **آدرسِ کانال و هوش مصنوعی** (کانال): بعد از خطا، فرم با `cfg = null`
  می‌ماند؛
- **مسیرِ x-ui** (تنظیماتِ حسابداری): کلِ تنظیماتِ پنل را با `r.json()`
  بی‌سنجش می‌خواند و یک کلید عوض می‌کرد. اگر خواندن ۵۰۰ می‌گرفت، `{detail}`
  به‌جای تنظیمات ذخیره می‌شد. نسخه را هم نمی‌فرستاد، پس هر تغییرِ
  هم‌زمانِ جای دیگر بی‌صدا پاک می‌شد.

حالا دو لایه جلویش را می‌گیرد:

- **رابط:** این صفحه‌ها بی‌تنظیماتِ خوانده‌شده ذخیره نمی‌کنند و خطا را
  نشان می‌دهند. مسیرِ x-ui مسیرِ جدای `PUT /api/admin/billing/xui-path` دارد
  و دیگر کلِ تنظیمات را جابه‌جا نمی‌کند.
- **بکند:**
  - ذخیره‌ای که بیش از نیمی از بخش‌های تنظیماتِ ربات را حذف کند، ۴۰۰
    می‌گیرد.
  - تنظیماتِ پنلی که کمتر از نیمی از بخش‌ها را داشته باشد هم ۴۰۰
    می‌گیرد.
  - برای صفحه‌ی بعدی که همین اشتباه را تکرار کند.

### Fixed — خطا به‌جای «خالی است»، شش صفحه‌ی دیگر

- **پشتیبانِ حسابداری:** پاسخِ خطا به‌عنوانِ «پشتیبان» دانلود می‌شد و پیام
  «دانلود شد — ۰ پرداخت، ۰ گروه» می‌آمد. مدیر فکر می‌کرد پشتیبان دارد.
- **هزینه‌ها:** خطا صفحه را با «۰ تومان هزینه» می‌ساخت.
- **نماینده‌ها و اینباندِ نماینده‌ها:** خطا، حتی خطای خودِ بکند، «هنوز
  نماینده‌ای ساخته نشده» می‌گفت.
- **اتصالِ ربات:** خطا چند ثانیه بعد به «ماژول ربات ممکن است نصب نشده
  باشد» برمی‌گشت.
- **تنظیماتِ حسابداری:** خطای خواندن اسکلتِ همیشگی می‌ماند. حالا فقط
  کارتِ مسیر خطا را نشان می‌دهد و پشتیبان‌گیری در دسترس می‌ماند.
- **قفلِ کانالِ نماینده** (پرتال): خطا فرم را با «خاموش» باز می‌کرد و ذخیره
  قفلِ واقعی را خاموش می‌کرد.
- **تنظیماتِ ناخوانای یک مستاجر** با `{miniapp_url}` بازنویسی نمی‌شود؛
  داده‌ی قابلِ نجات می‌ماند و لاگ می‌شود.

### Tests

- **`test-load-errors`** از ۱۹ به ۴۴ صفحه رسید: همه‌ی حسابداری، همه‌ی
  بخش‌های ربات، کانال، نفوذ، نماینده‌ها، و سه موردِ «خواندنِ تنظیمات
  شکست خورد».
- **دروازه‌ی `r.json()`** در `test-ui-safety` شکلِ بی‌پرانتز
  (`r => r.json()`) را هم می‌گیرد. سه موردی که از زیرِ نسخه‌ی اول رد
  شده بودند، همین‌ها بودند.
- **`test-admin-api`**:
  - نگهبانِ تنظیماتِ ناقص؛ با حذفِ شرط، دو بررسی قرمز می‌شود؛
  - مسیرِ جدای x-ui که بقیه‌ی تنظیمات را دست نمی‌زند.

## [1.105.1]

### Fixed

- **مینی‌اپ روی گوشیِ کوچک:** نامِ پلن در کارتِ سفارشِ در انتظار یک‌خطی
  بریده می‌شد. نام را مالک می‌نویسد و می‌تواند بلند باشد («… ۱۰۰ گیگ —
  نامحدود کاربر»)؛ روی ۳۲۰ پیکسل درست همان «نامحدود» می‌رفت. حالا تا دو
  خط می‌پیچد.
- **داشبوردِ نماینده:** زیرنویسِ کارت‌های شاخص («۱۱ تازه و ۱۹ تمدید در این
  ماه») روی ۳۲۰ پیکسل بریده می‌شد؛ حالا تا دو خط می‌پیچد.

ممیزیِ واکنش‌گرای مینی‌اپ و پرتال در ۳۲۰ و ۳۹۰ پیکسل حالا بی‌بُرش است.

## [1.105.0]

### Fixed

- **راهنمای رفرال غلط بود.** می‌گفت برای پاداشِ خودکار «باید منطقِ
  جداگانه‌ای در ربات پیاده‌سازی شود»، در حالی که ربات از خیلی قبل «سکه و
  دعوت» را داشت. یعنی مدیر را به ساختنِ چیزی می‌فرستاد که وجود داشت. حالا
  می‌گوید این کارت دقیقاً چه می‌کند: دوستِ مشتری را با پیامِ آماده به
  پشتیبانی می‌فرستد و پاداشش دستی است. پاداشِ خودکار در «ربات تلگرام ←
  سکه و دعوت» است.
- **ربات:** سه مسیرِ بی‌صدا حالا لاگ می‌شوند:
  - مهلتِ ناخوانای سفارش، که قاعده‌ی مهلت را اجرا نمی‌کرد؛
  - تیکتی که بعد از پاسخ بسته نشد و ممکن بود دوباره جواب بگیرد؛
  - علامت‌نخوردنِ کاربری که ربات را بلاک کرده.
- **واسطه‌ها روی گوشی:** پیشوند و دامنه‌ی هر واسطه بریده می‌شد؛ حالا به خطِ
  بعد می‌رود.
- **بنرهای هشدار:** رقم‌های لاتین وسطِ جمله‌ی فارسی بودند («٪15»،
  «3 روز»)؛ حالا فارسی‌اند.
- **«دانلود برنامه‌ها»** در صفحه‌ی اشتراک و پیش‌نمایش نیم‌فاصله نداشت.
- **پاپ‌آپ:** ورودیِ ایموجیِ دلخواه مقدارِ فعلی را نشان می‌داد و شبیهِ یک
  دکمه‌ی تکراری کنارِ فهرست بود. حالا خط‌چین است و «دلخواه» نوشته دارد.

## [1.104.0]

### Fixed — کارهای پس‌زمینه که بی‌صدا از کار می‌افتادند

- **نگهداریِ خودکار.** حلقه‌ای که هر پنج دقیقه نمونه‌ی مصرف می‌گیرد،
  نگهداری را اجرا می‌کند و از نودها گزارش می‌خواهد، هر سه شکستش را با
  `pass` می‌بلعید. اگر نگهداری از کار می‌افتاد، نه لاگ چیزی می‌گفت و نه
  پنل، و «اجرای بعدی: …» همچنان نشان داده می‌شد. حالا:
  - هر شکست با دلیل در لاگ می‌آید؛ فقط وقتی متنش عوض شود، نه هر پنج دقیقه؛
  - شکستِ زمان‌بندِ نگهداری با زمانش روی کارتِ نگهداری نشان داده
    می‌شود.
- **سفارشِ تحویل‌نشده.** وقتی ساختِ کانفیگِ یک سفارشِ تاییدشده شکست
  می‌خورد، سفارش به «در انتظار» برمی‌گردد. اگر همین برگرداندن هم شکست
  می‌خورد، بی‌صدا بود و سفارش «تاییدشده» می‌ماند بی‌آنکه کانفیگی ساخته
  شده باشد. حالا خطا و رویدادِ ربات ثبت می‌شود.
- **خواندنِ گروه‌های x-ui** و **به‌روزرسانیِ «آخرین دیدن»** کانفیگ‌ها
  بی‌صدا شکست می‌خوردند. حالا در لاگ می‌آیند.

## [1.103.0]

### Fixed — ورودیِ خرابِ حسابداری، بی‌صدا صفر یا نادیده

- **«نرخ حجمی»ِ نامعتبر بی‌صدا ۰ ذخیره می‌شد.** از آن به بعد واسطه‌ی حجمی
  هیچ بدهی‌ای نداشت. ردیف‌های نرخ از قبل ۴۰۰ می‌گرفتند، ولی این فیلد نه.
  حالا ۴۰۰ می‌گیرد؛ «طولِ دوره» هم همین‌طور.
- **«تسویه‌شده تا» و «شروع همکاری» بی‌سنجش ذخیره می‌شدند.** تاریخِ خراب
  بعدها در `_bill_since` بی‌صدا نادیده گرفته می‌شد و صورتحساب از «شروع
  همکاری» حساب می‌شد، یعنی ماه‌های تسویه‌شده دوباره روی فاکتور می‌آمدند.
  - حالا تاریخِ نامعتبر هنگامِ ذخیره رد می‌شود.
  - تاریخِ خرابی که از قبل ذخیره شده، در برچسبِ منبعِ صورتحساب گفته
    می‌شود («شروع همکاری — «تسویه‌شده تا» نامعتبر بود»).
  - صفحه‌ی «صورتحسابِ دوره» هم هشدار می‌دهد.

### Fixed — بازگردانیِ پشتیبان

- **جدولی که خالی نمی‌شد، بی‌صدا رد می‌شد.** بعد ردیف‌های پشتیبان روی
  ردیف‌های قدیمی ریخته می‌شدند: نه داده‌ی قبلی، نه پشتیبان، بلکه مخلوطی
  که هیچ‌جا ثبت نبود. حالا بازگردانی همان‌جا می‌ایستد و همه چیز برمی‌گردد.
- **ردیفِ جامانده** شمرده می‌شد، ولی دلیلش گفته نمی‌شد. حالا اولین خطای هر
  جدول در هشدار می‌آید؛ مثلاً «NOT NULL constraint failed: users.tg_id».
- **نسخه‌ی امنِ پیش از بازگردانیِ حسابداری**: اگر گرفته نمی‌شد، بی‌صدا
  می‌گذشت. حالا ثبت و گزارش می‌شود.
- **دو نسخه‌ی همین حلقه** در ربات و حسابداری یکی شدند
  (`_restore_tables`). تستِ برابری می‌سنجد که هیچ‌کدام حلقه‌ی خودش را
  دوباره ننویسد.

### Fixed — شکست‌های بی‌صدای دیگر

- **پرتالِ نماینده:** اگر نرخ‌ها خوانده نمی‌شدند، «کانفیگ تازه» هیچ پله‌ای
  نداشت و تمدید قیمت نشان نمی‌داد، بی‌هیچ توضیحی. اگر آمار خوانده
  نمی‌شد، کارت‌های فروش بی‌صدا غیب می‌شدند. حالا هر دو گفته می‌شوند.
- **مینی‌اپ:** اگر عکسِ رسید خوانده نمی‌شد، هیچ اتفاقی نمی‌افتاد. حالا خطا
  نشان داده می‌شود.

## [1.102.0]

### Fixed — خطای خواندن، به‌جای «خالی است»

بیست‌وشش جای پنل پاسخ را با `fetch(...).then((r) => r.json())` می‌خواندند و
وضعیت را نمی‌سنجیدند. ده جای دیگر خطا را کلاً می‌بلعیدند. پس پاسخِ ۵۰۰
مثلِ داده در صفحه می‌نشست، یا شاخه‌ی «خالی» را می‌گرفت. نتیجه هر بار یک
دروغِ آرام بود. چند نمونه‌ی خطرناک:

- **قواعدِ فایروال** «ufw نصب نیست» می‌گفت و دستورِ `apt install ufw` را
  روی سروری نشان می‌داد که ufw داشت. خطای خودِ بکند («خواندن وضعیت
  ناموفق») هم فهرستِ خالیِ قواعد می‌ساخت.
- **نگهداریِ خودکار** با مقدارهای خالی باز می‌شد، و «ذخیره» همان
  `{error: true}` را به‌جای زمان‌بندی روی سرور می‌نوشت.
- **کانفیگِ سرورِ خارج** یک جعبه‌ی خالی نشان می‌داد، با دکمه‌ی «کپی کانفیگ».
- **بازگشت به نسخه‌ی قبلی** «هنوز نسخه‌ای ذخیره نشده» می‌گفت، درست وقتی کسی
  بعد از یک به‌روزرسانیِ خراب دنبالِ راهِ برگشت است.
- **بررسیِ نسخه** «به‌روزرسانی خودکار تنظیم نشده» می‌گفت و دستورِ تنظیم
  نشان می‌داد.
- **آی‌پی‌های بسته‌شده** «۰» نشان می‌داد، یعنی «هیچ‌کس بسته نیست».
- **ساعت‌شمارِ برگشتِ فایروال:** نخواندنش بی‌صدا بود. اگر ساعت‌شمار فعال
  بود و دیده نمی‌شد، دکمه‌ی تأیید نمی‌آمد و فایروال خودش برمی‌گشت.

به‌علاوه‌ی این‌ها: قیف و گزارشِ فروش، قالب‌ها، همکاری در فروش، وضعیتِ
سیستم، تاریخچه‌ی مصرف، پرمصرف‌ترین مشتری‌ها، سرورهای دیگر، داشبورد و
کیفیتِ تانل، «چرا نود آفلاین است»، واسطه‌ها و نرخ، نرخِ ارز، و بررسیِ
سنگینِ مانیتورینگ (دکمه می‌چرخید و انگار کلیک ثبت نشده بود).

حالا هر خواندن از `okJson` رد می‌شود و با دلیلِ خودِ بکند می‌ایستد.
`errMsg` قطعیِ شبکه را به فارسی می‌گوید، نه «Failed to fetch» مرورگر.
`LoadError` یک حالتِ خطای یکسان است با دکمه‌ی «دوباره». آمارِ داشبورد هم
اگر خوانده نشود، این را می‌گوید.

### Tests

- **`test-load-errors`** (تازه): نوزده صفحه را با پاسخِ ۵۰۰ برای مسیرِ خودشان
  واقعاً mount می‌کند و می‌سنجد دلیلِ سرور روی صفحه آمده باشد.
  - روی کدِ پیش از این اصلاح ۱۹ از ۱۹ قرمز است؛ حالا همه سبزند.
  - یک بررسی هم مطمئن می‌شود آزمون کور نیست.
  - در `release-check` و CI اجرا می‌شود.
- **`test-ui-safety`** سه شکلِ متنیِ همین اشتباه را رد می‌کند:
  - catchِ خالی یا «بی‌صدا» پشتِ یک fetch؛
  - `if (res.ok) setX(...)` بدونِ else؛
  - `.then((r) => r.json())`.

  catchی که دلیلش را در کامنت می‌گوید، تصمیم است و رد نمی‌شود.

## [1.101.0]

### Fixed — رسیدی که در یک صفحه منتظر بود و در صفحه‌ی دیگر نبود

وضعیتِ قدیمیِ `review` همان «در انتظار» است. کدِ امروز آن را نمی‌نویسد،
ولی تایید و رد هنوز می‌پذیرندش. در ۱.۹۸ این وضعیت به فهرستِ سفارش‌ها و
شمارِ داشبورد اضافه شد، ولی چهار جای دیگر هنوز فقط `awaiting` را
می‌شمردند:

- گزارشِ فروش («… رسید منتظر بررسی شماست»)؛
- زنگِ هشدار؛
- صفِ بررسیِ داخلِ خودِ ربات؛
- آمارِ ربات.

پس روی دیتابیسِ قدیمی، یک رسید در سفارش‌ها منتظر بود، در زنگ نبود، و مدیر
از داخلِ ربات هرگز نمی‌دیدش. به‌جای اصلاحِ هفت کوئری، ربات هنگامِ شروع
این وضعیت را یک‌بار به `awaiting` برمی‌گرداند. این تایید نیست؛ فقط نامِ
یک وضعیتِ در انتظار عوض می‌شود. تستِ `test_bot` مهاجرت را عمداً می‌شکند
تا دو بررسی قرمز شوند.

### Security — آی‌پیِ واقعی در مخزنِ عمومی

- نمونه‌ی ورودی در «آی‌پی‌های بسته‌شده» و «بستنِ دسته‌ای» آدرس‌های
  عمومیِ واقعی بود (`91.99.12.4`، `45.9.148.7`، `185.220.101.0/24`).
- یک خطِ «خروجیِ واقعیِ ss» در `test-fw-safety` آدرسِ یک سرورِ واقعی را
  داشت.

همه با بازه‌های مستند (RFC 5737) عوض شدند. دروازه‌ی تازه‌ای در
`test-seams` هر آی‌پیِ عمومیِ دیگری را در کدِ محصول رد می‌کند: فرانت،
بکند، ربات، صفحه‌ی اشتراک، هارنس و اسکریپت‌ها. تست‌های شناساییِ شبکه
عمداً بیرون‌اند، چون برای سنجیدنِ «آی‌پیِ ایران» بازه‌ی واقعیِ ایران
لازم دارند. آن آدرسِ سرور در تاریخچه‌ی گیت هنوز هست.

### Changed

- **متن‌ها و رفتارِ ربات:** «آدرسِ مینی‌اپ» و «دامنه‌ی لینکِ اشتراک» زیرِ
  برچسبشان و با پهنای کافی می‌آیند. پیش‌تر در ستونی ۲۰۰ پیکسلی بریده
  می‌شدند و هشدارِ خالی‌بودنِ مینی‌اپ در چهار خطِ باریک می‌پیچید.
- **اینباندها:** فهرست روی دسکتاپ دوستونه است. صفحه از ۱۰۶۷ به ۹۰۵
  پیکسل رسید.

### Tests

- **`FIELD_READS`** از ۶ به ۱۷ رندرکننده رسید: همه‌ی کاربران، پلن‌ها،
  کاربرانِ ربات، خریداران، سرورها و تانل‌ها، سلامت، کارهای نود، بدهکاران و
  توصیه‌های نفوذ. امروز هیچ‌کدام فیلدِ ناموجودی نمی‌خوانند؛ این دروازه
  نمی‌گذارد بخوانند. جاروی کاملِ ۹۰ حلقه‌ی `.map` در بخش‌ها هیچ باگِ
  تازه‌ای از این نوع پیدا نکرد.

## [1.100.0]

### Fixed — حسابداری

- **ردیف‌های صورتحساب** همه «بدون نرخ» نشان داده می‌شدند. رابط
  `it.lineTotal` می‌خواند، فیلدی که بکند هرگز نفرستاده بود (نامش
  `amount` است). جمعِ بالای صفحه درست بود، پس فقط کسی می‌دید که ردیف‌ها
  را یکی‌یکی بخواند. حالا مبلغِ هر ردیف، نرخ و مبلغِ کاربرِ اضافه‌اش از
  خودِ بکند می‌آید.
- **پرداخت‌ها:** ثبت، حذف و بارگذاری هیچ خطایی نشان نمی‌دادند. پرداختِ
  ردشده مثلِ ثبت‌شده به نظر می‌رسید. **حذفِ پرداخت با یک کلیک** انجام
  می‌شد و حالا تأیید می‌خواهد.
- **ذخیره‌ی نرخِ گروه** `res.ok` را نمی‌سنجید. اگر همگام‌سازیِ هزینه‌ی
  پلن‌های ربات شکست می‌خورد، بکند چیزی نمی‌گفت؛ حالا هشدار می‌دهد.
- **هزینه‌ها:** شمارِ اقلام و هشدارِ هزینه‌ی حجم از فیلدهایی خوانده
  می‌شدند که بکند نمی‌فرستد (`count`، `trafficCost`)، پس همیشه صفر
  بودند. تاریخِ هزینه‌ها میلادیِ خام بود و حالا شمسی است.
- **داشبورد:** واسطه‌ای که بیش از بدهی‌اش پرداخته بود «مانده‌ی منفی»
  می‌گرفت. حالا «اعتبار» و «پیش‌پرداخت» نشان داده می‌شود.
- **صورتحساب** با عوضِ واسطه خودش ساخته می‌شود و خطای ساخت را نشان
  می‌دهد. پیش‌تر صفحه تا زدنِ دکمه خالی می‌ماند.
- **همکاری در فروش:** حذف، روشن و خاموش‌کردن و ثبتِ پرداخت هیچ خطایی
  نشان نمی‌دادند. حذفِ همکار با یک کلیک انجام می‌شد و حالا تأیید
  می‌خواهد.
- **پایشِ تانل:** ذخیره‌ی تنظیماتِ پایش خطا را نشان نمی‌داد.

### Added — دروازه‌ها

- **فیلدهای خوانده‌شده در برابر پاسخِ بکند** (`test-contract`): شش
  رندرکننده سنجیده می‌شوند. هر کلیدی که JSX از یک ردیف می‌خواند باید در
  پاسخِ واقعیِ بکند باشد. این همان نوعِ باگی است که در این نسخه پنج
  بار پیدا شد: `lineTotal`، `tries`/`blocked` در نفوذ، `totalPaid` در
  همکاری، و `count`/`trafficCost` در هزینه‌ها.
- **پاسخِ نوشتنِ خوانده‌نشده** (`test-ui-safety`): هر `POST`، `PUT` و
  `DELETE` باید پاسخش را بسنجد.
- **تاریخِ خام:** دروازه‌ی تاریخ حالا `spent_at`، `done_at`، `taken_at`
  و تاریخ‌های داخلِ قالب‌رشته را هم می‌گیرد.
- **مسیرِ دستگاهِ سازنده در هارنس** (`test-seams`): داده‌ی حسابداریِ
  هارنس از بکند روی فیکسچرِ موقت ساخته می‌شود. مسیرِ فایلِ موقت، که نامِ
  کاربرِ ویندوز را داشت، همراهش آمده بود.

### Changed

- داده‌ی حسابداریِ هارنس حالا خروجیِ خودِ بکند است، نه یک `BILLING`
  ساختگی که به همه‌ی مسیرها یک جواب می‌داد. قرارداد ۸ مسیرِ حسابداری را
  هم‌شکل می‌سنجد (مجموعاً ۵۰).
- دکمه‌های بازه‌ی زمانی و حذفِ هزینه روی گوشی بزرگ‌تر شدند.

## [1.99.0]

### Fixed — نمایندگی

- **«رمز تازه بساز»** رمزِ فعلیِ نماینده را با یک کلیک باطل می‌کرد و او را
  بیرون می‌انداخت. **«بستن پنل»** هم نشست‌های بازش را با یک کلیک
  می‌انداخت. هر دو حالا تأیید می‌خواهند؛ ساختنِ اولین رمز همچنان
  بی‌پرسش است.
- **هشدارِ «برای اینکه بتواند وارد شود»** وقتی فقط نشانیِ لینک خالی بود
  نمی‌آمد، چون شرطِ بیرونی‌اش نشانی را نمی‌سنجید.
- **تاریخچه‌ی اعتبار** پاسخِ خطا را مثلِ فهرستِ خالی نشان می‌داد.
- **داده‌ی هارنس** برای نماینده‌ها `hasPass` نداشت، پس در هارنس همه «رمز
  ندارد» دیده می‌شدند. فیکسچرِ قرارداد حالا یک نماینده دارد تا کلیدهای
  هر ردیف هم سنجیده شوند (۴۲ مسیرِ «باید هم‌شکل»).

### Changed

- **نماینده‌ها و دسترسی:** هر نماینده یک ردیفِ فشرده است: نام، باز یا
  بسته، آماده‌ی ورود یا نه، گروه، نشانی، اعتبار و دکمه‌ی کپیِ لینک.
  تنظیمات با کلیک باز می‌شود. پیش‌تر هر نماینده یک کارتِ ۳۷۰ پیکسلیِ
  همیشه‌باز بود؛ صفحه‌ی چهار نماینده از ۲۹۰۰ به ۱۰۵۰ پیکسل رسید.
- **اینباندِ نماینده‌ها:** نماینده‌ها به‌شکلِ قرص کنارِ هم می‌آیند و اولی
  خودش انتخاب می‌شود. پیش‌تر صفحه تا انتخاب از منوی کشویی خالی بود.

## [1.98.0]

### Security — رمزِ مدیر در آدرسِ تصویرِ رسید

صفحه‌ی سفارش‌ها هر رسید را با `<img src=".../receipt/12?pw=رمز">` نشان
می‌داد، و بکند رمز را از پارامترِ آدرس هم می‌پذیرفت. پس رمزِ مدیر برای هر
رسیدِ صفحه در لاگِ دسترسیِ nginx، تاریخچه‌ی مرورگر و هدرِ Referer
می‌نشست.

حالا تصویر با fetch و هدر گرفته و از blob نشان داده می‌شود، و مسیرِ رسید
دیگر `pw` را نمی‌پذیرد. شکستِ بارگذاری هم دیده می‌شود. پیش‌تر تصویرِ
خراب بی‌صدا پنهان می‌شد. دروازه‌ی تازه‌ای هر رازی را در پارامترِ آدرس رد
می‌کند.

### Fixed — ربات

- **پلنِ خاموش در پنل روشن دیده می‌شد.** ستونِ `is_active` در دیتابیس
  ۰/۱ است و صفحه آن را با «نابرابر با false» می‌سنجید، که برای ۰ هم درست
  است. کاشیِ بالا «۱ پلن خاموش» می‌گفت و کلیدِ همان پلن روشن بود. دروازه
  این شکل را برای پرچم‌های دیتابیس رد می‌کند.
- **رویدادهای ربات و فهرستِ پست‌های کانال** تاریخِ میلادیِ خام نشان
  می‌دادند (`{e.at || "—"}`). داده‌ی ساختگیِ هارنس از قبل شمسی نوشته شده
  بود و خطا را پنهان می‌کرد. دروازه‌ی تاریخِ خام حالا شکلِ «با جایگزین»
  را هم می‌گیرد.
- **کارتِ سفارش نمی‌گفت مشتری *چه* خریده.** مدیر رسیدِ ۲۸۰ هزار تومانی را
  تایید می‌کرد بی‌آنکه بداند سه‌ماهه است یا شارژِ کیف پول. حالا نامِ پلن،
  نوعِ سفارش (خرید، تمدید، شارژ) و «از کیف پول» روی کارت هست.
- **وضعیتِ قدیمیِ `review`** را تایید و رد هنوز می‌پذیرفتند، ولی هیچ
  زبانه‌ای نشانش نمی‌داد؛ چنین رسیدی فقط در «همه» پیدا می‌شد. حالا در
  «در انتظار» می‌آید.
- **«پرداخت‌شده تا امروز»** در همکاری در فروش از فیلدی می‌خواند که بکند
  نمی‌فرستاد، پس بعد از تسویه هم صفر بود.
- **شکست‌های بی‌صدا:**
  - کاربران `catch { /* بی‌صدا */ }` داشتند.
  - سفارش‌ها و پلن‌ها پاسخِ خطا را مثلِ فهرستِ خالی نشان می‌دادند.

  حالا هر سه دلیل را می‌گویند.
- **«خاموش‌کردنِ ربات»** فروش را با یک کلیک می‌خواباند؛ حالا تأیید
  می‌خواهد.
- **دانلودِ فایل** (بکاپ، صورتحساب، گزارش، خروجیِ آی‌پی‌ها): blob بلافاصله
  بعد از کلیک لغو می‌شد و در فایرفاکس دانلود را پیش از شروع می‌بُرید.
  هفت جا همین شکل را داشتند؛ دروازه‌اش هم اضافه شد.

### Changed

- **پلن‌ها:** فهرستِ فشرده با قیمت، حجم و کلیدِ روشن/خاموش. ویرایشگر فقط
  برای پلنی باز می‌شود که رویش کلیک شود. پیش‌تر هر پلن یک فرمِ ۳۴۰
  پیکسلیِ همیشه‌باز بود. دکمه‌ی ذخیره حالا تغییرِ ذخیره‌نشده را نشان
  می‌دهد.
- **کاربران:**
  - صفحه‌بندیِ شماره‌دار، جای «قبلی/بعدی».
  - کاشی‌ها از شمارِ کلِ دسته‌هاست (خریدار، اشتراکِ فعال، مسدود)، نه
    جمعِ چهل ردیفِ همان صفحه.
- **تاریخِ شمسیِ از قبل تبدیل‌شده** دیگر دوباره تبدیل نمی‌شود. پیش‌تر
  «۲۵ دی ۷۸۳» می‌ساخت.

### Tests

- **`test-contract`:**
  - داده‌ی ربات در هارنس از خودِ بکند ساخته می‌شود (سفارش، کاربر، پلن،
    همکار، رویداد و کاتالوگِ انواعش). ۴۱ مسیرِ «باید هم‌شکل».
  - مسیرِ «باید هم‌شکل» که *مقایسه نشد* هم شکست است. یک جایگزینیِ داده
    در هارنس تابعِ fetch را پاک کرده بود؛ همه‌ی مسیرها «mock ندارد» رد
    شدند و دروازه سبز ماند.

## [1.97.0]

### Fixed — «تلاش برای نفوذ» روی سرورِ واقعی دو کاشیِ همیشه‌صفر داشت

کاشی‌های «جدی» و «از قبل بسته» از `a.tries` و `a.blocked` می‌خواندند.
بکند هیچ‌کدام را نمی‌فرستاد؛ هر دو از داده‌ی ساختگیِ هارنس آمده بودند.
پس هارنس عددِ درست نشان می‌داد و سرور همیشه صفر.

- **داده‌ی تازه:** بکند حالا برای هر آدرس `blocked` را از خودِ فایروال
  می‌سازد (سیاه‌چاله یا قاعده‌ی DENY روی مبدأ). اگر خواندنش شکست
  بخورد، `blockedError` می‌گوید چرا.
- **دکمه‌ی «بستن»:** کنارِ آدرسِ بسته‌شده دیگر نمی‌آید؛ جایش برچسبِ
  «بسته» است.
- **شاخص‌ها:** دو ردیف بودند که با هم تناقض داشتند («۶ مهاجم، ۰ جدی» و
  پایین‌ترش «۲ مهاجم، جدی»). حالا یک ردیف است با یک تعریف: مهاجم یعنی
  آدرسِ پیگیر و ناآشنا.
- **زمان‌ها:**
  - از ۱۵ نویسه‌ی اولِ خطِ لاگ بریده می‌شدند. روی syslog میلادی و
    بی‌سال بودند، و روی لاگِ RFC 3339 اوبونتو ۲۴ به «2026-09-24T12:0»
    بریده می‌شدند.
  - حالا ISO‌اند و با همان قاعده‌ی سالی ساخته می‌شوند که فیلترِ بازه
    دارد. در رابط شمسی دیده می‌شوند.
- **«چه کار کنم»:** جمله‌ی فارسی در جعبه‌ی کدِ چپ‌به‌راست وارونه خوانده
  می‌شد. حالا جمله متن است و فقط دستور کد.

### Fixed — فایروال

- **قاعده‌های IPv6:**
  - پسوندِ « (v6)» جلوی خواندنِ پورت را می‌گرفت. نسخه‌ی v6ِ قاعده‌ی
    SSH «حیاتی» شناخته نمی‌شد و بی‌تأیید حذف می‌شد.
  - هر قاعده دو بار در فهرست می‌آمد و «درهای باز» دوبرابر شمرده می‌شد.
  - حذف فقط نسخه‌ی IPv4 را برمی‌داشت و پورت روی IPv6 باز می‌ماند؛ حالا
    هر دو حذف می‌شوند.
- **سیاستِ پیش‌فرضِ ورودی** همیشه «?» بود. کد در خروجیِ `status
  numbered` دنبالِ خطی می‌گشت که فقط `status verbose` چاپ می‌کند. پس
  یادداشتِ «سیاستِ پیش‌فرض» در رابط هرگز دیده نشد. حالا از
  `/etc/default/ufw` خوانده می‌شود.
- **سوکت‌های موقتِ xray:** «پیش‌بررسیِ روشن‌کردن» آن‌ها را «بی‌قاعده —
  بسته می‌شود» فهرست می‌کرد؛ روی سرورِ واقعی حدود چهل ردیف. «پیشنهاد
  قواعد» همین را از قبل کنار می‌گذاشت. حالا هر دو از یک تابع می‌خوانند.
- **«درهای باز»** قاعده‌ی LIMIT را نمی‌شمرد، پس SSH که معمولاً LIMIT
  است جزوِ درهای باز نبود.
- **کلاسِ `fx-btn-ghost`** در سه صفحه (شش دکمه) به کار رفته بود و هرگز
  تعریف نشد. دکمه‌ها بی‌ظاهر بودند. دروازه‌ی تازه‌ای هر کلاسِ `fx-*`
  تعریف‌نشده را رد می‌کند.
- **توضیحِ زیرِ فیلدها** رنگِ `#475569` داشت. روی زمینه‌ی تیره کنتراستش
  ۲٫۳ بود، زیرِ حدِ خوانایی.

### Changed

- **«روشن‌کردنِ فایروال» وقتی روشن است:** پیش‌تر یک خط بود و بقیه‌ی
  صفحه خالی. حالا جوابِ سؤالِ اصلیِ این وضعیت را می‌دهد: چه چیزی گوش
  می‌دهد ولی قاعده ندارد، یعنی همین حالا از بیرون بسته است. پورتِ
  تازه‌ی x-ui که قاعده نگرفته دقیقاً همین‌جا دیده می‌شود.
- **قواعد:**
  - فرمِ قاعده‌ی تازه در یک ردیف با دکمه‌اش است.
  - «Anywhere» به فارسی نمایش داده می‌شود.
  - برچسبِ «IPv4 + IPv6» برمی‌گردد.
- **گوشی:** همه‌ی دکمه‌ها کمینه‌ی ۳۲ پیکسلِ لمسی دارند.

### Tests

- **`test-contract`** مسیرهای فایروال و نفوذ را با خروجیِ ساختگیِ ufw و
  journalctl و تجزیه‌ی واقعیِ بکند می‌سنجد. ۳۴ مسیرِ «باید هم‌شکل».
  داده‌ی هارنس از خودِ بکند تولید می‌شود.
- **شمارنده‌ی `test-firewall`:** یک متغیرِ `_ok` در همین تغییر شمارنده
  را صفر کرد و سوییتِ ۵۶تایی «۸ پاس» گزارش داد. همان نگهبانی که
  test-admin-api داشت، حالا این‌جا هم هست.
- **جاروی شکست:** هر اصلاح عمداً برگردانده شد تا دروازه‌اش قرمز شود.

## [1.96.0]

### Fixed — وضعیتِ تانل هیچ‌وقت از «در حال اعمال» جلوتر نمی‌رفت

`set_status` در کلِ بکند فقط یک‌جا صدا زده می‌شد: موقعِ اعمال، با
`deploying`. نتیجه‌ی کارهای ایجنت (اعمال، روشن، توقف، ری‌استارت، وضعیت)
به جدولِ jobs می‌رفت و هیچ‌کس آن را به تانل برنمی‌گرداند. پس:

- داشبورد هرگز تانلِ «در حال کار» نشان نمی‌داد و «۰ از ۲ در حال کار» عدد
  همیشگی‌اش بود؛
- تانلی که روی سرور نصب نشده بود هم همان «در حال اعمال» را می‌گرفت؛
  درست و خراب یک شکل بودند.

حالا وضعیت از نتیجه‌ی کار می‌آید و شناسه‌ی تانل از ردیفِ ذخیره‌شده‌ی
کار خوانده می‌شود، نه از پاسخِ ایجنت. سمتِ ایران صاحبِ وضعیت است. سمتِ
خارج فقط می‌تواند «خطا» بگوید، تا پاسخِ دیرترِ خارج خطای ایران را پاک
نکند. `test-nodes` هر گذار را روی دیتابیسِ موقت می‌سنجد و گذارِ سمتِ
خارج را هم جدا.

### Fixed — دکمه‌ی «لاگ» هیچ لاگی نشان نمی‌داد

این دکمه کاری در صف می‌گذاشت و «دستور فرستاده شد» می‌گفت. پاسخِ ایجنت
در جدول می‌ماند، ولی هیچ مسیری آن را نمی‌خواند.

- مسیرِ تازه‌ی `/api/admin/tunnel/{id}/jobs` کارهای اخیرِ تانل را با
  نتیجه برمی‌گرداند. کانفیگ و راز در آن نیست.
- «لاگ» و «وضعیت» حالا پنجره‌ای باز می‌کنند که تا رسیدنِ پاسخ صبر
  می‌کند (ایجنت هر ۳۰ ثانیه سر می‌زند) و خروجی را نشان می‌دهد. اگر
  پاسخی نرسد، بعد از ۹۰ ثانیه می‌گوید چرا.

### Fixed — حذف با یک کلیک، و شکست‌های بی‌صدا

- **حذفِ تانل و حذفِ سرور** بی‌پرسش انجام می‌شدند و خطایشان دیده
  نمی‌شد. حذفِ سرور تانل‌هایش را هم پاک می‌کرد. تانل‌هایی که آن سرور
  سمتِ *خارج*شان بود به شناسه‌ای ناموجود اشاره می‌ماندند؛ حالا جدا
  می‌شوند.
- **«توکنِ جدید»** توکنِ قبلی را همان لحظه باطل می‌کند و سرور را تا
  نصبِ دوباره قطع می‌کند. پیش‌تر با یک کلیک انجام می‌شد؛ حالا هر سه
  عمل تأیید می‌خواهند.
- **گزارشِ خراب از ایجنت.** اگر سنجش، سلامت یا مانیتورینگ قابلِ
  خواندن نبود، در `except: pass` گم می‌شد. کار «موفق» ثبت می‌شد و صفحه
  همچنان «هنوز گزارشی نرسیده» می‌گفت. حالا در رویدادهای همان سرور ثبت
  می‌شود.
- **شکستِ سمتِ خارج هنگامِ اعمال** دیده نمی‌شد و فقط به «دستی» ختم
  می‌شد. حالا دلیلش را می‌گوید.
- **صفحه‌ی سلامت:**
  - خطای خواندن را پیش‌تر «نامشخص» نشان می‌داد.
  - «بررسی دوباره» سه ثانیه بعد گزارشِ قدیمیِ نودها را مثلِ نتیجه‌ی
    تازه نشان می‌داد. حالا می‌گوید گزارش کی می‌رسد و همان موقع دوباره
    می‌خواند.

### Fixed — نمایش

- **`tunnel/overview`** برای هر نود کلِ گزارشِ مانیتورینگ (تا ۶۰
  کیلوبایت) و سلامت (تا ۸ کیلوبایت) را می‌فرستاد، هر چند ثانیه یک‌بار.
  هیچ صفحه‌ای این دو را از این‌جا نمی‌خواند.
- **مانیتورینگِ سرورهای دیگر:**
  - آدرسِ سرور همیشه «—» بود، چون `host` در پاسخ وجود ندارد و نامش
    `public_ip` است. همین اشتباه در بکندِ «تشخیص» هم بود.
  - دکمه‌ی «فایروال» کلاسی داشت که وجود ندارد و بی‌ظاهر بود.
  - پیامِ «ایجنتِ قدیمی» نسخه‌ی ۱.۴ را می‌گفت، ولی مرزِ واقعی ۱.۵ است.
- **رنگِ نمودارِ کیفیت** مرزهای ۶۰/۱۵۰ داشت و خلاصه ۶۰/۱۵۰/۳۰۰. تانلِ
  «خوب» با میله‌های زرد کشیده می‌شد. حالا هر دو از یک ثابت می‌خوانند
  و `test-nodes` برابری‌شان را می‌سنجد.
- **آستانه‌ی مصرف** در «سرورها» ۶۵/۸۵ بود و در مانیتورینگ ۷۵/۹۲.
  پردازنده‌ی ۸۰٪ یک‌جا قرمز بود و جای دیگر زرد. حالا همه‌جا ۷۵/۹۲.
- **تاریخ‌های میلادی:**
  - «اجرای بعدی: 26-09-2026» در زمان‌بندیِ نگهداری؛
  - زمانِ systemd در پنجره‌ی وضعیت؛
  - «آخرین گزارش» در تشخیص.

  دروازه‌ی تاریخِ خام حالا شکلِ `{x.replace("T", …)}` را هم می‌گیرد.
- **وضعیتِ «خطا»:** برچسبش در فهرستِ تانل‌ها «اعمال نشده» بود.

### Changed — بخشِ تانل، از نو

- **داشبورد:**
  - سه شاخص به‌جای چهار: «سرورها ۳» و «آنلاین ۲» یک خبر بودند در دو
    کارت، و جای چهارم به تعدادِ مشکل‌ها رسید.
  - فهرستِ «نیاز به توجه» با خطای هر تانل و آخرین تماسِ هر سرورِ
    آفلاین.
  - سرورها و تانل‌ها کنارِ هم، با نوارِ مصرف.
  - روی گوشی، سه شاخص در یک ردیف.
- **تانل‌ها:**
  - نام، موتور، وضعیت، مسیر و پورت‌ها در یک بلوک. هر کارت از ۱۹۰ به
    ۱۴۲ پیکسل رسید.
  - لبه‌ی رنگیِ وضعیت.
  - دکمه‌های «وضعیت» و «لاگ».
- **مانیتورینگِ سرورهای دیگر:** خودِ سرورها انتخابگرند. کارت‌ها وضعیت،
  آدرس و مصرف را نشان می‌دهند و جای منوی کشویی، سه شاخص و نوارِ
  تکراری را گرفته‌اند.
- **کارتِ سنجه (هر دو مانیتورینگ):**
  - مشکل‌ها اول می‌آیند.
  - «چرا مهم است؟» یک آیکون است، نه یک خطِ تکراری زیرِ دوازده کارت.
  - کارتِ مشکل‌دار «چه کار کنم» را بی‌کلیک نشان می‌دهد.
- **سلامت:**
  - یک نوار با شمارِ هر سطح، جای کارتی ۸۰ پیکسلی برای یک کلمه.
  - زمانِ هر گزارش کنارِ سرورش.
  - «هنوز گزارشی نرسیده» دو بار تکرار نمی‌شود.
- **رویدادها:** گروه‌بندی با روز، صافیِ سطح، و صفحه‌بندیِ شماره‌دار.

### Tests

- **`test-contract`** زیرمسیرهای پارامتریِ تانل را با شناسه‌ی واقعیِ
  فیکسچر می‌سنجد. پیش‌تر هر مسیرِ `{…}`دار کنار گذاشته می‌شد و هارنس
  برای همه‌شان کلِ overview را برمی‌گرداند. ۲۸ مسیرِ «باید هم‌شکل».
- **هارنس** داده‌ی مانیتورینگ را با توابعِ خودِ `monitor.py` می‌سازد؛
  نسخه‌ی قبلی کلیدهایی داشت که در بکند نیستند. `?sections=` را هم مثلِ
  بکند رعایت می‌کند.
- **جاروی شکست:** هر اصلاح عمداً برگردانده شد تا دروازه‌اش قرمز شود.
  دو آزمونِ اول سبز ماندند و قوی‌تر نوشته شدند:
  - گذارِ سمتِ خارج؛
  - شناسه‌ی هم‌پیشوند در LIKE.

## [1.95.0]

### Changed — ظاهرِ تازهٔ کلِ پنل

مالک: «هیچ تغییری نمی‌بینم؛ تمامیِ بخش‌ها؛ این‌همه فضای خالی». حق داشت.
تغییرهای ۱.۹۴ صفحه‌به‌صفحه بودند و در بیشترِ پنل دیده نمی‌شدند. این بار
یک لایهٔ طراحی روی پایه‌های مشترک نشسته، پس هر ۵۱ صفحه هم‌زمان عوض
می‌شوند:

- **سربرگِ کارت.** تیترِ اولِ هر کارت حالا یک نوارِ جدا با آیکونِ قاب‌دار و
  خطِ زیرین است. ۵۵ کارت همین الگو را داشتند و یک‌جا ساختار گرفتند. چشم
  اول نوار را می‌خواند، بعد بدنه را، نه یک تیترِ شناور وسطِ فضای خالی.
- **لبهٔ درخشانِ کارت.** لبه یک شیب است: از بالا روشن و در پایین رنگِ
  برند. کارت‌های شاخص رنگِ لحنِ خودشان را می‌گیرند. این کار بدونِ blur
  انجام شده، پس هزینهٔ GPU ندارد.
- **منو.** صفحهٔ فعال یک قرصِ پُر با رنگِ برند است و از دور دیده می‌شود.
  پیش‌تر یک هالهٔ کم‌رنگ بود. آیتمِ فعال `aria-current` هم گرفت، پس
  صفحه‌خوان هم می‌فهمد کجاست.
- **سایر پایه‌ها.** زمینه و سطح‌ها عمیق‌تر و خنثی‌تر شدند و فاصلهٔ
  روشناییِ بینشان دیده می‌شود. این‌ها هم عوض شدند:
  - نوارِ بالا شیشه‌ای و کم‌ارتفاع؛
  - دکمهٔ اصلی شیب‌دار با درخشش؛
  - ورودی‌ها با حلقهٔ فوکوس؛
  - عنوانِ صفحه درشت‌تر؛
  - فاصله‌ها فشرده‌تر.

### Fixed — بارگذاریِ صفحه‌ها «سفت» بود

مالک: «هر صفحه‌ای که لود می‌شود خیلی لود سفتی دارد». هر تعویضِ صفحه
**چهار حرکتِ روی هم** داشت:

1. ظرفِ صفحه ۱۰ پیکسل بالا می‌آمد.
2. خودِ بخش دوباره ۱۰ پیکسل بالا می‌آمد.
3. اسکلت نشان داده می‌شد و وقتی داده می‌رسید، محتوای واقعی جایش
   می‌نشست و **دوباره** بالا می‌آمد، با چیدمانِ دیگر.
4. هر کارتِ داخلِ شبکه جدا و پلکانی بالا می‌آمد.

حالا:

- صفحه یک حرکتِ نرم دارد: محوشدن، به‌علاوهٔ ۴ پیکسل.
- بخش و شبکه‌ها فقط محو می‌شوند، بی‌جابه‌جایی.
- اسکلت فقط وقتی دیده می‌شود که بارگذاری بیش از ۱۸۰ میلی‌ثانیه طول
  بکشد. صفحهٔ سریع اصلاً اسکلت نشان نمی‌دهد.
- اسکلت به‌جای برقِ تند، آرام نفس می‌کشد.

### Tests

ممیزیِ واکنش‌گرا روی پنلِ مدیر، پرتالِ نماینده و مینی‌اپ صفر خطا داد.
ui-safety و آزمونِ مؤلفه‌ها سبزند.

## [1.94.0]

برگه: `docs/specs/2026-09-24-panel-overhaul.md`. این اولین فضای کاری از شش
فضاست: **صفحهٔ اشتراک**.

### Added — قراردادِ هارنس و بکند (`tools/test-contract.py`)

هارنس تنها جایی است که پنل را می‌بینیم، و داده‌اش را هیچ‌چیز با بکند
هم‌شکل نگه نمی‌داشت. این ابزار هر مسیرِ GETِ `/api/admin` را روی یک بکندِ
واقعی با دیتابیس‌های ساختگی صدا می‌زند و کلیدهایش را با هارنس مقایسه
می‌کند. برای این کار به httpx احتیاج ندارد؛ یک صداکنندهٔ ASGIِ کوچک کافی
است.

**اولین اجرا در ۳۱ مسیر از ۴۰ اختلاف دید.** چند نمونه:

- هارنس برای `/bot/status` شکلِ کاملاً دیگری داشت.
- `/monitor` و همهٔ `/firewall/*` یک شیءِ مشترک برمی‌گرداندند.
- پیکربندیِ صفحهٔ اشتراک این بخش‌ها را اصلاً نداشت: `advanced`، `popup`،
  `banners`، `template` و `palette`.
- واسطه‌ها `{name, slug}` بودند، نه `{domains, emailPrefix, overrides}`.

حاصلش این بود که صفحه‌ها در هارنس شاخه‌ای را نشان می‌دادند که روی سرور
نیست. بیشترِ اختلاف‌ها کهنگیِ هارنس بود، نه باگِ پنل. هر فیلدی که هارنس
اختراع کرده بود بررسی شد که آیا فرانت می‌خواندش یا نه.

- پیکربندی و قالب‌ها حالا عیناً از خودِ بکند می‌آیند:
  `app.DEFAULT_CONFIG`، `TEMPLATES` و `PALETTES`، به‌علاوهٔ دادهٔ نمونهٔ
  واقع‌نما.
- ۱۷ مسیر در فهرستِ «باید هم‌شکل» هستند و از release-check رد می‌شوند.
  با هر فضای کاری این فهرست بزرگ‌تر می‌شود.
- `tools/contract-diff.py` دو پاسخ را کنارِ هم نشان می‌دهد.

### Changed — صفحهٔ اشتراک: کم‌شلوغ‌تر

- **جعبه‌های راهنما یک خط‌اند و با «بیشتر» باز می‌شوند.** ۶۶ جعبه در پنل
  بود، بیشترشان دوسه‌خطی و همیشه باز. هشدارها باز می‌مانند، چون هشدارِ
  پنهان هشدار نیست. این تغییر همهٔ فضاهای کاری را هم‌زمان سبک می‌کند.
- **تنظیماتِ ظاهری از ۳۳۶۲ به ۲۴۰۶ پیکسل رسید.** ده کارت در یک ستون
  بود و «ظاهرِ صفحهٔ مشتری» با «رمزِ پنل» و «بک‌آپ» قاطی بود. حالا دو گروه
  است، «ظاهرِ صفحهٔ اشتراک» و «پنل»، هر کدام با ستون‌های بی‌حفره.
- **به‌روزرسانی از ۱۹۶۴ به ۱۴۵۴ پیکسل رسید.** کار اول آمده (به‌روزرسانی،
  بعد بازگشت) و مرجع آخر. مسیرهای نصب و دستورهای مدیریتی در یک «جزئیاتِ
  فنی» جمع‌شونده رفتند.
- دو مؤلفهٔ مشترکِ تازه برای فضاهای بعدی: `Collapse` (کارتِ جمع‌شونده) و
  `MiniMarkdown`.

### Fixed

- **یادداشتِ انتشار مارک‌داونِ خام بود.** «### Fixed» و «- …» عینِ متن
  نشان داده می‌شد و در راست‌به‌چپ «###» به تهِ خط می‌پرید.
- **پیش‌نمایشِ زنده در هارنس همیشه یک قابِ خاکستری بود.** بکندی نبود که
  `/api/preview` را بدهد. حالا از هارنسِ خودِ صفحهٔ اشتراک می‌آید.
- **پیشوندِ ایمیلِ واسطه با «_» تهِ آن هرگز نمی‌خورد.** پنل «macan»
  می‌خواهد و خودش «_» را اضافه می‌کند. مالکی که «macan_» تایپ می‌کرد،
  یعنی همان چیزی که در ایمیل‌ها می‌بیند، پیشوندِ «macan__» می‌ساخت. مشتریِ
  واسطه بی‌صدا برندِ مالک را می‌دید. `find_reseller` که تصمیم می‌گیرد
  مشتری برندِ چه کسی را ببیند، تا امروز **هیچ تستی نداشت**. حالا هشت تست
  دارد: جداکننده‌ها، پیشوندِ ناقص، تقدمِ دامنه، زیردامنه و www، دامنهٔ
  شبیه، واسطهٔ خاموش، و پیشوندِ خالی.
- **رقم‌های لاتین در دو جا:** شمارِ اپ و واسطه در صفحهٔ به‌روزرسانی، و
  زمان‌بندیِ پاپ‌آپ.

### Tests

- `test-contract.py` در release-check اجرا می‌شود.
- شش حالتِ تازه در `test-components`.
- هشت تست برای `find_reseller`.

هر دو دروازه را عمداً شکستیم و هر دو قرمز شدند.

## [1.93.0]

### Fixed — مینی‌اپ و صفحهٔ اشتراک اول ظاهرِ نکسورا را نشان می‌دادند

مالک: «وقتی لود می‌شود، اول انگار صفحهٔ خودِ نکسورا را می‌آورد، بعد
شخصی‌سازیِ کاربر». با تأخیرِ شبکهٔ واقعی (۲٫۵ ثانیه در هارنس) فریم‌به‌فریم
سنجیدیم. مینی‌اپ حدودِ ۲٫۵ ثانیه با آبیِ نکسورا و عنوانِ خالی نقاشی
می‌شد، **حتی در بارِ دوم**. سه دلیل داشت:

- **اسپلش بعد از ۱٫۷ ثانیه کنار می‌رفت و اپ با رنگِ پیش‌فرض بالا
  می‌آمد** تا `/api/mini/me` برسد. حالا همان اسپلشِ فروشگاه ادامه دارد تا
  فروشگاه معلوم شود. خطا اسپلش را کنار می‌زند.
- **`applyTheme(null)` در همان فاصله پوستهٔ کش‌شده را پاک می‌کرد.** پس کش
  بی‌اثر بود. حالا تا فروشگاه نرسیده دست به ریشه نمی‌خورد. پوسته هم در
  `useLayoutEffect` می‌نشیند، پیش از نقاشی، نه بعدش.
- **کلیدِ کش برای همهٔ فروشگاه‌ها یکی بود.** همه یک `/app` دارند، پس
  مشتری‌ای که از ربات‌ِ مالک و یک نماینده هر دو می‌خرید، اسپلشِ فروشگاهِ
  دیگر را می‌دید. ربات حالا شناسهٔ فروشگاه را به آدرسِ مینی‌اپ می‌چسباند
  (`?shop=`). این شناسه فقط کلیدِ کش است؛ هویت همچنان از امضای initData
  می‌آید. دکمهٔ منوی ربات با راه‌اندازیِ دوباره خودش به‌روز می‌شود.
- **درخواست‌های مینی‌اپ سقفِ زمانی گرفتند:** ۲۰ ثانیه برای خواندن و ۶۰
  برای ارسالِ رسید. بی سقف، سروری که هیچ‌وقت جواب ندهد حالا یعنی اسپلشِ
  بی‌پایان بود؛ با سقف، خطای روشن و دکمهٔ تلاشِ دوباره می‌آید.

**صفحهٔ اشتراک** هم با رنگ و نامِ پیش‌فرض رندر می‌شد و تا ۵ ثانیه بعد،
وقتی تنظیماتِ فروشگاه (یا نماینده) می‌رسید، عوض می‌شد. حالا محتوا پنهان
است تا ظاهرِ خودش بنشیند:

- از کشِ دفعهٔ قبل، همان لحظه. کلید دامنه + ایمیلِ کلاینت است، همان دو
  چیزی که بک‌اند فروشگاه را با آن پیدا می‌کند.
- بارِ اول، بعد از رسیدنِ پاسخ، با سقفِ ۳ ثانیه.
- در این فاصله یک نشانِ خنثیِ بارگذاری است، نه لوگوی نکسورا.

### Tests

- `test-ui-safety.py` هر سه سازوکارِ مینی‌اپ و پنهان‌ماندنِ صفحهٔ اشتراک
  را می‌سنجد.
- `test-admin-api.py` با صدا زدنِ خودِ `miniapp_url` می‌سنجد که شناسهٔ
  فروشگاه در آدرس هست.

هر پنج را عمداً شکستیم و هر پنج قرمز شدند. هارنس `?lat=` گرفت، یعنی
تأخیرِ شبکه. پاسخِ فوری دقیقاً همین باگ را پنهان می‌کرد، چون `me` پیش از
تمام‌شدنِ اسپلش می‌رسید.

## [1.92.0]

### Fixed — واکنش‌گرا در کلِ سایت، اندازه‌گیری‌شده

مالک: «مشکلاتِ زیادی در responsive دیدم». به‌جای نگاه‌کردن، یک ممیزیِ
خودکار ساختیم. هر صفحهٔ هر چهار اپ را در شش عرض باز کرد:

- پنلِ مدیر: ۵۱ صفحه؛
- پرتالِ نماینده: ۱۱ صفحه؛
- مینی‌اپ؛
- صفحهٔ اشتراک.

عرض‌ها ۳۲۰، ۳۶۰، ۳۹۰، ۷۶۸، ۱۰۲۴ و ۱۲۸۰ پیکسل بودند. در هر کدام اندازه
گرفت:

- اسکرولِ افقی؛
- عنصری که از صفحه بیرون می‌زند؛
- عنصری که از کارتش بیرون می‌ریزد؛
- متنِ بریده؛
- هدفِ لمسیِ کوچک.

پیش از اصلاح ۱۴ خطای ساختاری بود؛ حالا در هر چهار اپ و هر شش عرض صفر
است.

- **ریشهٔ بیشترشان یک کلمه بود: `1fr`.** «1fr» یعنی
  `minmax(auto, 1fr)`: ستون از کوچک‌ترین پهنای محتوایش تنگ‌تر نمی‌شود.
  پس یک فیلدِ بلند یا نامِ nowrap کلِ شبکه را از صفحه بیرون می‌داد. این
  صفحه‌ها روی گوشی تا ۶۰ پیکسل بیرون می‌زدند:
  - سکه و دعوت؛
  - کدهای تخفیف؛
  - پاپ‌آپ؛
  - متن‌ها؛
  - استودیوی پوسته؛
  - خانهٔ مینی‌اپ، که روی گوشیِ ۳۲۰ پیکسلی ۵۷ پیکسل اسکرولِ افقی
    داشت.

  هر ۱۹ ستونِ شبکه حالا `minmax(0, 1fr)` است.
- **فهرستِ مشتری‌ها:** نام، شماره، سکه، موجودی و دو دکمه در یک ردیف
  بودند. روی ۳۶۰ پیکسل نام به «سارا م…» بریده می‌شد و شماره از کارت
  بیرون می‌زد. ستونِ کناری حالا به خطِ بعد می‌رود.
- **لپ‌تاپِ ۱۰۲۴ تا ۱۲۸۰ پیکسل:** منوی ۲۱۰ پیکسلی «سفارش‌ها و رسیدها» را
  روی هر صفحه می‌برید. جستجوی ۲۶۰ پیکسلی هم عنوانِ صفحه را. منو حالا ۲۲۸
  پیکسل است و برچسب اگر جا نشد دو خط می‌شود.
- **استودیوی پوسته روی ۳۲۰ پیکسل:** قابِ گوشیِ ۳۲۰ پیکسلی ۳۲ پیکسل از
  صفحه بیرون می‌زد.
- **دکمه‌ی بستنِ منو ۱۸×۱۸ بود**، کمتر از حداقلِ ۲۴ پیکسلیِ WCAG. روی
  صفحهٔ لمسی:
  - منو ۴۰ پیکسل است؛
  - دکمه‌های آیکونی ۳۶؛
  - فیلدهای جستجو تا لبهٔ جعبه‌شان می‌رسند. پیش‌تر ۲۰ پیکسل بودند
    داخلِ جعبهٔ ۴۰ پیکسلی.

  در کلِ سایت هیچ هدفِ لمسیِ زیرِ ۲۴ پیکسلی نمانده.
- کانال، تستِ اتصال، و متن‌های بریده (برچسبِ شاخص، عنوانِ نوار بالا،
  کاشی‌های داشبورد) هم اصلاح شدند.

### Fixed — پنل بدونِ VPN سفید می‌ماند

`telegram-web-app.js` برای هر چهار اپ هم‌زمان در `<head>` بار می‌شد.
telegram.org بدونِ VPN در ایران فیلتر است، پس پنلِ مدیر و پنلِ نماینده
تا وقتی این درخواست به timeout نمی‌رسید اصلاً رندر نمی‌شدند. پیش‌نمایشِ
استودیوی پوسته هم همین‌طور بود. حالا فقط مینی‌اپ، که همیشه داخلِ تلگرام
باز می‌شود، بارش می‌کند.

### Fixed — چهار تاریخِ میلادیِ دیگر

این جاها ستونِ تاریخ را خام نشان می‌دادند:

- تاریخچهٔ نسخه‌های تنظیمات؛
- نرخِ ارز؛
- ورودهای SSH؛
- آخرین گزارشِ سرورهای دیگر.

دروازهٔ قبلی فقط «بریدنِ» تاریخ را می‌شناخت و این‌ها از زیرش رد شدند.

### Tests

`test-ui-safety.py` چهار دروازهٔ تازه دارد:

- هیچ ستونِ شبکه‌ای «1fr» خام نیست. تشخیصش عمقِ پرانتز را می‌فهمد، پس
  `minmax(min(…), 1fr)` را اشتباه نمی‌گیرد.
- اسکریپتِ تلگرام هم‌زمان برای همهٔ اپ‌ها بار نمی‌شود.
- ستونِ تاریخ خام رندر نمی‌شود.
- پیش‌نمایشِ مینی‌اپ بدونِ تلگرام هم بالا می‌آید. نسخهٔ اولِ اصلاحِ
  تلگرام قابِ پیش‌نمایش را خالی کرده بود («باید از داخلِ تلگرام باز
  شود»). ممیزیِ مرورگری گرفتش، پیش از ریلیز.

هر چهار را عمداً شکستیم و هر چهار قرمز شدند. ابزارِ ممیزی بیرون از مخزن است،
چون CI مرورگر ندارد.

## [1.91.0]

### Changed — نمودارِ ستونی به‌جای منحنی، در کلِ پنل

مالک دربارهٔ پنلِ نماینده گفته بود «نمودارِ خطی جالب نیست». سلیقه تنها
دلیل نبود. منحنیِ نرم‌شده بینِ دو روز مقدار «اختراع» می‌کرد: شمارِ سفارش
عدد صحیح است، ولی منحنی بینِ ۲ و ۵ از ۳٫۴ هم رد می‌شد و در روزهای صفر
زیرِ کف فرو می‌رفت. `AreaChart` و `Sparkline` حالا ستونی‌اند، با همان
API، پس هیچ فراخوانی عوض نشد.

- هر ستون یک روز است. تولتیپ تاریخِ شمسی و هر دو سری را نشان می‌دهد.
- سری دوم (تعدادِ سفارش) مقیاسِ خودش را دارد و ردیفی از نقطه است که
  شدتش همان مقدار است. روی ستون‌ها سوار نیست، چون دو مقیاس روی یک
  ستون گمراه‌کننده است.
- روندِ ریزِ کنارِ شاخص‌ها ستون‌های کوچک است و روزِ آخر پررنگ.
- **«کاربرانِ تازه» روندِ سفارش را زیرِ خودش می‌کشید**، چون سریِ کاربر
  وجود ندارد. حذف شد.
- نمودارِ «روند فروش» تا قدِ کارتش کش می‌آید. پیش‌تر زیرش ۱۵۰ پیکسل
  حفره بود.

### Fixed — صفحهٔ اشتراکِ مشتری

- **حلقهٔ درصد یک پای‌چارتِ نصفه بود.** «سوراخِ» وسطِ حلقه یک `::after`
  بود. قاعدهٔ «براقیِ شیشه‌ای»، هزار خط پایین‌تر، همان را دوباره نوشت و
  دیسک را برداشت. برچسبِ آبیِ «باقی‌مانده» روی نیمهٔ آبی خوانده
  نمی‌شد. `conic-gradient` هم انیمیت نمی‌شد، پس «جاروبِ» حلقه هرگز دیده
  نشده بود. حلقه حالا SVG است: سرِ گرد دارد، با `stroke-dasharray`
  جاروب می‌شود، و برچسبش روی دیسکِ تیره می‌نشیند.
- **در قالبِ «کیف پول» حلقه نصفه بیرونِ کارت می‌افتاد.** قاعدهٔ
  `.subscription-card *` هر فرزند را `relative` می‌کرد، از جمله نقطهٔ
  چرخانِ حلقه، که از قبل هم به همین دلیل خراب بود. روی کارتِ رنگی،
  حلقه حالا رنگِ زمینه را می‌گیرد، نه accent روی accent. رنگِ درصد هم
  تیره شد. قاعده‌اش کلاسِ `.value` را هدف گرفته بود که وجود ندارد؛ کلاسِ
  واقعی `.percent` است.
- **رقم‌ها در حالتِ فارسی نیمه‌فارسی بودند.** تاریخ و «۹ دقیقه پیش»
  فارسی بودند و «18 روز»، «50%» و «24.9 GB» لاتین، در یک کارت کنارِ هم.
  حالا هر عددی که نشان داده می‌شود از `locDigits` رد می‌شود.
- روی دسکتاپ ستون ۵۶۰ پیکسل است، نه ۴۸۰.

### Fixed — پنلِ مدیر

- **نوارهای قیف در «آمار و قیف» روی سرورِ واقعی هم خالی بودند.** کد
  `${c}99` روی `var(--accent-2)` رنگِ نامعتبر می‌ساخت و مرورگر کلِ شیب را
  دور می‌ریخت. در همان صفحه «٪NaN نرخ تبدیل» هم نوشته می‌شد.
- **شش تاریخِ میلادیِ دیگر** در این جاها بودند:
  - سفارش‌ها: «تازه‌ترین» و زمانِ هر سفارش؛
  - فهرستِ نماینده‌ها؛
  - بک‌آپ‌ها؛
  - رویدادهای تانل؛
  - برچسبِ فروشِ روزانه.

  تابعِ تازهٔ `isoToJalaliStamp` تاریخ و ساعت را می‌دهد و فقط تقویم را
  عوض می‌کند، نه منطقهٔ زمانی.
- **وضعیتِ «pending» انگلیسیِ خام وسطِ پنل بود.** برای «منتظر رسید»،
  «مهلت گذشت»، «لغوشده» و «در صف رد» برچسبی تعریف نشده بود.
- **سفارش‌ها شبکه شدند.** هر رسید پیش‌تر کارتی ۱۱۴۰ پیکسلی بود، با نام
  در یک لبه و دکمهٔ تایید در لبهٔ دیگر. لبهٔ هر کارت حالا رنگِ وضعیتش
  را دارد و دکمهٔ تایید تمام‌عرض است.
- **رقم و متنِ فارسی در قلمِ مونو** به تاهومای سیستم می‌افتاد، چون قلمِ
  مونو گلیفِ فارسی ندارد. ده‌ها `faNum` داخلِ `var(--mono)` بود. این
  اشکال یک‌بار در یازده جا تک‌تک اصلاح شده بود و دوباره برگشت. حالا
  IRANSansX پشتیبانِ خودِ زنجیرهٔ مونو است، یک‌بار برای همه.
- **دونات:** افسانه خودِ مقدار را هم نشان می‌دهد، نه فقط درصد. تا ۳۲۰
  پیکسل پهن می‌شود؛ پیش‌تر درصد به لبهٔ دیگرِ کارت پرت می‌شد. روی گوشی
  زیرِ دونات می‌نشیند.
- **گوشی:**
  - دو شاخص در هر ردیف. پیش‌تر چهار کارتِ ۱۴۲ پیکسلی کلِ صفحهٔ اول را
    می‌گرفتند.
  - نوارِ «ذخیرهٔ تغییرات» فقط وقتی تغییری هست بالا می‌آید. پیش‌تر ۷۰
    پیکسلِ پایینِ هر صفحه را می‌گرفت، حتی حسابداری و سفارش‌ها.

### Tests

`test-ui-safety.py` چهار دروازهٔ تازه دارد:

- هیچ آلفای هگزی به رنگِ متغیر چسبانده نمی‌شود.
- هر شکلی از بریدنِ خامِ تاریخ ممنوع است: `slice(0, 16)`،
  `replace("T", " ")` و `faNum(String(…))`. نسخهٔ قبلیِ این دروازه فقط
  یک شکل را می‌شناخت.
- قلمِ مونو پشتیبانِ فارسی دارد.
- هیچ نویسهٔ کنترلیِ نامرئی در کد و تست نیست. `\b` در heredocِ گیت‌بش به
  backspace تبدیل شده بود و دروازهٔ تاریخ را بی‌صدا کور کرده بود: سبز
  می‌ماند و چهار تاریخِ میلادی از زیرش رد می‌شد.

هر هشت باگ را عمداً برگرداندیم و هر هشت قرمز شدند. دادهٔ هارنس برای قیف،
سفارش‌ها (فیلترِ `status`) و رویدادهای تانل کهنه بود و صفحه‌ها را
خراب‌تر از پنلِ واقعی نشان می‌داد. هر سه حالا شکلِ بکند را دارند.

## [1.90.0]

### Changed — استودیوی پوسته: سه مرحله، با رنگِ خودِ فروشگاه

مالک: «همه‌چیز عالی شده، فقط این قسمت خیلی ساده است، از همه لحاظ».
استودیو فهرستی از برچسب و دکمه بود. شِماهای قالب قابِ بلندِ تقریباً
خالی بودند و پالت‌ها دو نقطه روی زمینه‌ی تیره.

- **سربرگ با رنگِ فروشگاه:** لوگو، نام و «قالب · پالت»، روی زمینه‌ای که
  از همان پالتِ انتخابی ساخته می‌شود. وضعیتِ اشتراک هم کنارش است:
  چند روز مانده، تا کِی، و تمدید.
- **سه مرحله‌ی شماره‌دار: قالب، طیفِ رنگ، لوگو و نام.** هر انتخاب
  نشانِ تیک و حلقه‌ی رنگی دارد.
- **شِمای هر قالب حالا چیزی نشان می‌دهد.** سربرگ، کارتِ موجودی، دو ردیفِ
  پلن با دکمه و نوارِ زبانه دارد، و هر قالب شکلِ خودش را: مینیمال تخت
  با خطِ مو، پررنگ با سربرگِ رنگی و زبانه‌ی شناور، نئون با لبه‌ی
  درخشان. رنگ‌ها از متغیرهای خودِ پالت می‌آیند (`themeVars`)، نه از
  مقدارِ خام در JSX.
- **پالت‌ها** نمونه‌ی واقعیِ زمینه و شیبِ خودشان‌اند و روی دسکتاپ در
  یک ردیف جا می‌شوند. «رنگِ دلخواه» یک دایره‌ی انتخابِ رنگ دارد و
  کنارش نشان می‌دهد از آن یک رنگ چه ساخته می‌شود: رنگِ اصلی، رنگِ
  دوم، زمینه. پس نماینده می‌فهمد چرا زمینه عوض شد.
- **لوگو** کنارِ یک صفحه‌ی ورودِ کوچک دیده می‌شود. قاب و زمینه نمونه‌ی
  تصویری دارند، نه فقط کلمه. فاصله‌ی صفر «بی‌فاصله» نوشته می‌شود؛
  صفرِ فارسی یک نقطه است و کنارِ لغزنده شبیهِ لکه بود.
- **نوارِ ذخیره‌ی چسبان** می‌گوید چیزی ذخیره‌نشده مانده یا نه، و دکمه‌ی
  «برگرداندن» دارد. روی گوشی، که قابِ پیش‌نمایش زیرِ فرم می‌افتد، دکمه‌ی
  «پیش‌نمایش» مستقیم به آن می‌رود. قبلاً هر انتخاب یعنی تا تهِ صفحه
  پایین رفتن.
- **گوشیِ پیش‌نمایش** روی دسکتاپ کنارِ فرم چسبیده می‌ماند و تیره/روشن
  یک کلیدِ دوحالته است.

### Fixed — پنج تاریخِ میلادی که به کاربر نشان داده می‌شد

«فعال تا 22-10-2026» در استودیو، ISOِ خامی بود که در RTL وارونه هم
خوانده می‌شد. دروازه‌ی تازه چهار نمونه‌ی دیگر را هم پیدا کرد:

- وضعیتِ بازِ نماینده در پنلِ مالک؛
- دیالوگِ بازگشت به نسخه؛
- انقضای کدِ تخفیف؛
- ستونِ «آخرین خرید».

همه حالا از `isoToJalaliLabel` می‌گذرند.

### Tests

`test-ui-safety.py`: هیچ تاریخِ ISOی مستقیم رندر نمی‌شود. `slice(0, 10)`
فقط برای پارامترِ API و نامِ فایل مجاز است. دروازه را شکستیم و قرمز شد.
داده‌ی هارنس برای خریدِ پوسته حالا همان ISOِ میلادیِ بکند را
برمی‌گرداند؛ قبلاً رشته‌ای شبه‌شمسی بود و تبدیلِ تاریخ را هرگز
نمی‌سنجید.

## [1.89.0]

برگه: `docs/specs/2026-09-23-reseller-ui-overhaul.md`

### Changed — پرتالِ نماینده: صفحه‌ی تمام‌عرض، داشبوردِ تازه

مالک: «فضای خالیِ به‌شدت زیاد، flexible نیست، جذابیت ندارد، نمودارِ
خطی نگذار».

- **هر صفحه تمام‌عرض است.** قبلاً هر صفحه (سفارش‌ها، پلن‌ها، ربات،
  پوسته) کارتی ۶۴۰ پیکسلی وسطِ ناحیه‌ای ۱۱۸۰ پیکسلی بود. نصفِ صفحه
  خالی می‌ماند و روی گوشی کارت داخلِ کارت می‌افتاد. حالا خودِ صفحه
  شبکه است و عنوانش را نوارِ بالا می‌گوید. ناحیه‌ی اصلی هم تا ۱۴۴۰
  پیکسل پهن می‌شود.
- **خانه داشبوردی تازه است و هر بلوک یک سؤال را جواب می‌دهد:**
  - چهار عددِ اصلی: فروشِ ماه، مشتریِ فعال، رسیدِ منتظر، اعتبار یا
    بدهی؛
  - وضعیتِ مشتری‌ها در یک نوارِ بخش‌بخش، به‌علاوه‌ی راهنما؛
  - مصرفِ حجم در یک حلقه؛
  - قیف؛
  - کارهای امروز، با دکمه.

  نسخه‌ی قبلی دوازده کاشیِ هم‌وزن بود و دوتایشان خطِ روندِ کوچک
  داشتند. عددهای دوم (فروشِ کل، دریافتیِ کارت‌به‌کارت، تمدیدها) حذف
  نشده‌اند؛ در فهرستِ فشرده‌ی «در یک نگاه» آمده‌اند.
- **قیف «۱۶۲٪ از قبلی» نشان می‌داد.** پله‌ها تو در تو نیستند: ثبتِ
  شماره شرطِ سفارش نیست، پس «سفارش ثبت کردند» می‌تواند از «شماره ثبت
  کردند» بیشتر باشد. حالا هر پله سهمش از «ربات را باز کردند» را نشان
  می‌دهد و درصد همان `pct` خودِ بکند است، نه عددی که فرانت حساب کند.
- **سفارش‌ها و پلن‌ها شبکه‌ی واکنش‌گرا شدند.** لبه‌ی هر سفارش رنگِ
  وضعیتش را دارد، و «پلن تازه» یک کاشیِ خالی در همان شبکه است.
- **«ربات و پرداخت» دو ستون شد:**
  - اتصالِ ربات؛
  - چک‌لیستِ آمادگی، که موارد درست را هم نشان می‌دهد («سه از چهار
    آماده است»)؛
  - رسیدها؛
  - کارت‌ها؛
  - برند.
- **کنارِ نامِ هر پلنِ پولی یک «0» تنها نشسته بود.** `is_trial` از
  SQLite عدد می‌رسد، نه بولی، و `{r.is_trial && …}` در React همان
  صفر را چاپ می‌کند.

### Fixed — صفحه‌ی ورودِ مینی‌اپ و هماهنگیِ رنگ

- **صفحه‌ی ورود مالِ فروشگاه است.** قبلاً اسپلشِ خودِ نکسورا بود که فقط
  لوگویش عوض می‌شد. مدارِ دوّار داشت، روی نامِ فارسی فاصله‌ی حروف
  می‌گذاشت (حروف از هم جدا می‌شدند)، و در قالبِ پررنگ یک نوارِ سیاه
  پایینش بود. حالا لوگو، نام و یک نوارِ پیشرفت است، و هر قالب شکلِ
  خودش را دارد:
  - پررنگ: تمام‌رنگ تا لبه؛
  - مینیمال: بی‌نور؛
  - نئون: حلقه‌ی درخشان.
- **در پوسته‌ی روشن، صفحه‌ی ورود سیاه بود** و بعد ناگهان سفید می‌شد.
  روشن یا تیره را فقط خودِ مینی‌اپ می‌پرسید، آن هم بعد از بارشدنِ
  تکه‌اش. حالا پیش از اولین رندر پرسیده می‌شود، و از یک تابع
  (`tgScheme`) تا دو جا نتوانند دو پاسخ بدهند.
- **در قالبِ پررنگ، لوگوی خودکار (حرفِ اول) دیده نمی‌شد.** کاشی‌اش همان
  شیبِ رنگیِ زمینه بود. روی سطح‌های تمام‌رنگ (اسپلش و سربرگ) حالا
  برعکس می‌شود: زمینه جوهر است و حرف رنگ.
- **رنگِ دوم حالا ۱۶ درجه آن‌طرف‌تر است، نه ۳۰.** با ۳۰ درجه دکمه‌ی
  فروشگاهِ بنفش سرخابی می‌شد، زمرد آبی و رز قرمز، یعنی دو برند به جای
  یکی. کارتِ پاداش هم به جای طلاییِ ثابت رنگِ کم‌رنگِ خودِ فروشگاه را
  می‌گیرد؛ خودِ سکه طلایی می‌ماند.
- پیش‌نمایشِ پرتال دیگر هر بار ۱٫۷ ثانیه صفحه‌ی ورودِ بی‌نام نشان
  نمی‌دهد.

### Tests

`test-ui-safety.py` هفت دروازه‌ی تازه دارد:

- بی‌نمودارِ خطی؛
- صفحه‌ی تمام‌عرض؛
- اسپلشِ فروشگاه؛
- بی‌فاصله‌ی حروف؛
- پوسته پیش از ورود؛
- یک قاعده برای `colorScheme`؛
- صفرِ SQLite.

هر شش باگِ بالا را عمداً برگرداندیم و هر شش قرمز شدند. داده‌ی هارنس
برای `sales` کهنه بود (`{orders, revenue}`) و کاشیِ فروش هرگز در آن
دیده نمی‌شد. حالا همان شکلِ `_portal_sales` است. `?brand=` هم
اضافه شد، چون بدونش هر آزمونِ اسپلش «نکسورا» را می‌دید: همان نامی که
مشتریِ نماینده هرگز نباید ببیند.

## [1.88.0]

برگه: `docs/specs/2026-09-23-mini-theme-studio.md`

### Added — استودیوی پوسته‌ی مینی‌اپ: قالب × پالت × لوگو

مالک: «پوسته فقط یک پیش‌نمایشِ خیلی عادی نشان می‌دهد و فقط یک سری
جزئیات را عوض می‌کند؛ می‌خواهم مثلِ قالب‌های صفحه‌ی اشتراک همه‌چیز —
حتی صفحه‌ی ورود — کامل عوض شود». درست می‌گفت: پیش‌نمایشِ قبلی ماکتی
دست‌ساز بود و فقط رنگِ دکمه‌اش عوض می‌شد، و پالت زمینه را دست نمی‌زد.

- **چهار قالبِ مخصوصِ مینی‌اپ** (تصمیمِ مالک: جدا از صفحه‌ی اشتراک) که
  ساختار را عوض می‌کنند، نه فقط رنگ — نوارِ بالا، نوارِ زبانه‌ها، شکلِ
  کارت و دکمه، گوشه‌ها، سایه‌ها، و صفحه‌ی ورود:
  **شفق** (ظاهرِ فعلی) · **مینیمال** (تخت، خطِ مو، زبانه با خطِ زیرین،
  ورودِ بی‌مدار) · **پررنگ** (نوارِ بالای رنگی، زبانه‌های شناور، دکمه‌های
  قرصی، ورودِ تمام‌رنگ) · **نئون** (گوشه‌های تیز، درخشش روی لبه‌ها).
- **هشت طیفِ آماده** — رنگِ اصلی، رنگِ دوم، زمینه و سطح‌ها با هم — و
  **رنگِ دلخواه** که از یک رنگ کلِ طیف را می‌سازد، زمینه هم از فامِ خودش.
- **پیش‌نمایشِ واقعی**: خودِ مینی‌اپ در قابِ گوشی (`/app?preview=1`)، با
  داده‌ی نمونه و **پلن‌های واقعیِ خودِ نماینده**. هر کلیک همان لحظه از
  راهِ `postMessage` به آن می‌رسد. دکمه‌ی «صفحه‌ی ورود» ورود را با همان
  قالب و لوگو پخش می‌کند، و «تلگرامِ روشن/تیره» هر دو را نشان می‌دهد.
  پیش‌نمایش هیچ درخواستی به سرور نمی‌فرستد.
- **لوگو**: برش و زوم موقعِ آپلود (هر اندازه‌ای می‌آید، مربعِ ۵۱۲ پیکسل
  ذخیره می‌شود)؛ قاب (گوشه‌گرد/دایره/مربع)، زمینه (بی‌زمینه/روشن/رنگِ
  پالت) و فاصله؛ و وقتی لوگو نیست، **نشانِ خودکار** از حرفِ اولِ نامِ
  فروشگاه با رنگِ پالت — به‌جای مربعِ خنثی. لوگو حالا در سربرگِ مینی‌اپ
  همیشه دیده می‌شود (قبلاً وقتی مشتری عکس داشت، عکسِ او جایش می‌نشست) و
  صفحه‌ی ورود هم همان قاب و زمینه را دارد.
- همه پشتِ اشتراکِ «پوسته‌ی شخصی»، **پیش‌نمایش آزاد** (تصمیمِ مالک)؛ قفل
  در بکند است. آپلودِ خودِ لوگو که از قبل رایگان بود رایگان می‌ماند. اگر
  اشتراک تمام شود، مینی‌اپ به پیش‌فرض برمی‌گردد بی‌آنکه انتخاب‌ها پاک شوند.

### Fixed — ریزه‌ها

- قالبِ نئون ارقامِ فارسی را با فونتِ تک‌فاصله «۲ ۴ ۰ , ۰ ۰ ۰» می‌کرد —
  حالا ارقامِ هم‌پهنا بی‌عوض‌کردنِ فونت.
- نشانِ خودکار برای نامِ فارسی-لاتین «Vح» می‌شد؛ حالا یک حرف.
- `nexora reseller-why` حالا قالب و پالت را هم می‌گوید، نه فقط رنگ.

### Tests

- `test-palette`: هر طیفِ آماده و کلِ چرخه‌ی رنگِ دلخواه — متنِ اصلی ≥ ۷
  و متنِ کم‌رنگ ≥ ۴٫۵ روی زمینه و سطح‌های خودش؛ در پوسته‌ی روشن زمینه
  دست نمی‌خورد.
- `test-portal-dashboard`: قفل در بکند، فهرستِ مجاز، ذخیره‌ی جزئی، رفتارِ
  بعد از تمامِ اشتراک، و برابریِ فهرست‌ها با رابط.
- `test-ui-safety`: هر قالب نوار و زبانه و کارت و دکمه و صفحه‌ی ورود را
  عوض می‌کند؛ پیش‌نمایش خودِ اپ است و به سرور نمی‌زند.
- اندازه‌گیری در کروم: هر چهار قالب در هر دو پوسته، بی‌سرریز؛ ورودِ دفعه‌ی
  دوم با قالبِ خودش؛ برشِ لوگو. ده باگ یکی‌یکی برگردانده شدند و هر ده
  بار تستِ درست قرمز شد.

## [1.87.0]

برگه: `docs/specs/2026-09-23-reseller-dashboard.md`

### Added — داشبوردِ کاملِ نماینده، با منوی کناری

پنلِ نماینده حالا شکلِ پنلِ مالک را دارد: منوی کناری (روی گوشی کشوی
کناری)، و ده صفحه — داشبورد، کانفیگ‌ها، سفارش‌ها، مشتری‌ها، **چت با
مشتری**، پلن‌ها، ربات و پرداخت، متن‌ها و تنظیمات، سکه و دعوت، رویدادها،
پوسته‌ی مینی‌اپ.

صفحه‌های مشتری‌ها، چت، متن‌ها، سکه و رویدادها **کپی نیستند** — همان
صفحه‌های پنلِ مالک‌اند که از `lib/botsrc.js` می‌خوانند: برای مالک از
مسیرهای مدیر، برای نماینده از `/api/portal/*`. پس هر اصلاحی که به پنلِ
مالک برسد به نماینده هم می‌رسد. در بکند هم همین است: هر صفحه یک هسته
دارد (`_users_page`، `_inbox_read`، `_funnel`، `_events_page`…) که
مستاجر می‌گیرد.

تنظیماتِ ربات برای نماینده **فهرستِ مجاز** است: متن‌ها، یادآوری، سکه
و دعوت، قفلِ کانال، پرسیدنِ شماره، مهلتِ پرداخت و پاسخ‌های آماده —
نه پیشوندِ شناسه، آدرسِ مینی‌اپ یا پایه‌ی لینکِ اشتراک. و ذخیره روی
تنظیماتِ فعلی **ادغام** می‌شود؛ کارت و برند و لوگو دست نمی‌خورند.

نسخه‌ی نکسورا پایینِ منو نشان داده می‌شود — «هنوز نمی‌بینمش» معمولاً
یعنی سرور به‌روز نشده، و تا امروز هیچ راهی برای فهمیدنش نبود.

### Added — تستِ رایگان در رباتِ نماینده

تصمیمِ مالک: مجاز و رایگان، هزینه با مالک. کانفیگِ تستِ نماینده
**بی‌گروه** ساخته می‌شود — در صورتحسابش نمی‌آید و از اعتبارش کم
نمی‌شود — ولی **از تستِ خودِ مالک بزرگ‌تر نمی‌شود** (حجم، روز، کاربر).
بدونِ سقف، هر پلنی با برچسبِ «تست» کانفیگِ رایگانِ بی‌حد می‌شد. سقف هم
موقعِ ذخیره سنجیده می‌شود هم موقعِ ساخت: اگر مالک تستش را کوچک کند،
تستِ قدیمیِ نماینده دیگر ساخته نمی‌شود. مالک تست ندارد → نماینده هم
نمی‌تواند، و پرتال همین را می‌گوید.

### Fixed — رنگِ فروشگاه فقط به نیمی از مینی‌اپ می‌رسید

مالک گفت «پوسته و رنگ اعمال نمی‌شود». اعمال می‌شد — به نصفِ صفحه:

- دکمه‌های اصلیِ مینی‌اپ (خرید، قیمت، زبانه‌ی فعال، پرداخت، ارسالِ
  پیام، اشتراک‌گذاری) رنگِ دومِ برند (`--cy`، فیروزه‌ای) را داشتند و
  فقط رنگِ اول عوض می‌شد.
- چند رنگ ثابت در CSS نوشته شده بود — کارتِ موجودی، دکمه‌ی خرید،
  زبانه‌ی فعال، واژه‌ی صفحه‌ی ورود.
- متغیرها روی `.mn-app` می‌نشستند و پنجره‌هایی که از راهِ پورتال روی
  `body` می‌روند بیرونِ آن بودند.

حالا `lib/palette.js` از **یک** رنگ کلِ پالت را می‌سازد — رنگِ دوم،
سرهای شیب، پله‌های کم‌رنگ، و رنگِ متنِ روی هر سطح — و روی ریشه
می‌گذارد. صفحه‌ی ورود هم از دفعه‌ی دوم با رنگِ فروشگاه باز می‌شود.
پیش‌نمایشِ پرتال همان تابع را صدا می‌زند، پس نماینده همان را می‌بیند که
مشتری‌اش.

**«رنگ‌های دیگر با آن سازگار باشند»:** رنگِ دوم همسایه‌ی رنگیِ اولی است
(۳۰ درجه آن‌طرف‌تر)، ولی با **روشناییِ هم‌اندازه** ساخته می‌شود نه با
عددِ L یکسان — سبز و آبیِ هم‌L روشناییِ کاملاً متفاوت دارند، و نسخه‌ی
اول روی سبز در پوسته‌ی روشن کنتراستِ ۲٫۶۷ می‌داد. رنگِ متن (سیاه یا
سفید) از روی بدترین سرِ هر شیب انتخاب می‌شود.

`tools/test-palette.cjs` این را روی **کلِ چرخه‌ی رنگ** می‌سنجد (۳۶ فام
× ۴ اشباع × ۷ روشنایی × دو پوسته): متنِ دکمه همیشه ≥ ۴٫۵. بدونِ رنگِ
نماینده هیچ‌چیز عوض نمی‌شود — پیش‌فرض‌های تازه همان رنگ‌هایی‌اند که
ثابت نوشته شده بود.

### Fixed — پنلِ مالک مشتری‌ها و فروشِ نماینده‌ها را هم می‌شمرد

تا وقتی فقط یک ربات بود، هیچ کوئریِ پنل شرطِ مستاجر نداشت. با آمدنِ
رباتِ نماینده‌ها، کاربران، سفارش‌ها، قیف، گزارش و «فروش کل»ِ پنلِ مالک
همه‌ی مستاجرها را با هم می‌شمردند — فروشِ نماینده در درآمدِ مالک می‌آمد.
و کسی که هم از رباتِ مالک خریده بود هم از نماینده، پرونده و پیامش از
ردیفِ تصادفی خوانده می‌شد (پیام می‌توانست از رباتِ اشتباه برود).

راه‌حل `_tenant_conn`: یک نمای موقت به نامِ هر جدولِ مستاجری که فقط
ردیف‌های یک مستاجر را دارد. SQLite نامِ بی‌پیشوند را اول در `temp`
می‌گردد، پس **همه‌ی کوئری‌های موجود** — گزارش به‌تنهایی دوازده‌تا — بدونِ
دست‌زدن محدود می‌شوند؛ اضافه‌کردنِ شرط به تک‌تکشان یعنی روزی یکی جا
بماند.

### Fixed — ریزه‌ها

- دکمه‌ی «تست رایگان» در ربات وقتی تست روشن بود ولی پلنِ تستی نبود
  نشان داده می‌شد و جوابش «فعال نیست» بود. حالا فقط وقتی پلن هست.
- وصل‌شدنِ صاحبِ فروشگاه به ربات دو نوشتنِ جدا بود؛ دو Start هم‌زمان
  هر دو رد می‌شدند. حالا یک `UPDATE` شرطی.
- پنجره‌های پرتال که از صفحه بلندتر بودند، سرشان بالای صفحه بریده
  می‌شد و با اسکرول هم برنمی‌گشت (۱۴۱ پیکسل روی لپ‌تاپِ ۶۸۰ پیکسلی).
- `sections/bot/api.js`: importِ `errText` داخلِ کامنت افتاده بود.

### Tests

- `tools/test-portal-dashboard.py` — روی اسکیمای واقعیِ ربات: مالک و
  نماینده هر کدام فقط مالِ خودشان را می‌بینند، حتی کسی که در هر دو ربات
  هست؛ تنظیماتِ مجاز؛ سقفِ تست. ۳۶ سنجه.
- `tools/test-palette.cjs` — خوانایی روی کلِ چرخه‌ی رنگ.
- `bot/test_reseller.py` — تستِ رایگانِ نماینده و مسابقه‌ی وصل‌شدن.
  ۵۲ سنجه.
- دروازه‌ی ui-safety: هیچ رنگِ ثابتِ برندی در مینی‌اپ نمانده.
- چهارده باگ یکی‌یکی برگردانده شدند؛ هر چهارده بار تستِ درست قرمز شد.

## [1.86.0]

### Fixed — رباتِ هیچ نماینده‌ای نمی‌توانست بفروشد

مالک گفت «کامل مشکلاتِ قسمتِ نماینده را برطرف کن». وقتی مسیرِ فروش
از رسید تا کانفیگ واقعاً اجرا شد — نه خوانده — معلوم شد رباتِ
نماینده **هیچ‌وقت حتی یک کانفیگ نساخته بود.** پنج چیز پشتِ سرِ هم
شکسته بودند و هر کدام به‌تنهایی کافی بود:

- **به هیچ پنلی وصل نبود.** نماینده پنلِ جدا ندارد؛ پرتال این را
  می‌دانست و به پنلِ مالک وصل می‌شد، ولی ربات با ستون‌های خالیِ
  ردیفِ نماینده وصل می‌شد — یعنی به هیچ‌جا. تاییدِ سفارش از پرتال
  هم (که همان کدِ ربات را صدا می‌زند) با همین خطا می‌ایستاد. حالا
  قاعده‌ی «کدام پنل» در ربات هم هست و تستِ برابری دو نسخه را
  می‌سنجد.
- **گروه نمی‌فرستاد.** گروه تنها چیزی است که می‌گوید کانفیگ مالِ
  کیست. بدونش کانفیگ در پنلِ مالک ساخته می‌شد و در صورتحسابِ
  **هیچ‌کس** نمی‌آمد. حالا کانفیگِ نماینده در گروهِ خودش ساخته
  می‌شود، و نماینده‌ای که گروه ندارد اصلاً کانفیگ نمی‌سازد — با
  پیامی که می‌گوید چرا — پیش از اینکه پولی جابه‌جا شود.
- **شناسه‌ی کانفیگ فارسی می‌شد.** پیشوند از نامِ ردیف می‌آمد («حسین
  دهلگی») و `isalnum` حرفِ فارسی را نگه می‌دارد. حالا مثلِ پرتال
  از نشانیِ لینکِ نماینده می‌آید — همان که صفحه‌ی اشتراک برند را
  از رویش تشخیص می‌دهد.
- **کارت نداشت.** ربات کارت را از تنظیماتِ *خودِ* نماینده می‌خواند
  و نماینده هیچ راهی برای نوشتنش نداشت. پس هر خرید با کارت و هر
  شارژِ کیف پول می‌ایستاد — و چون کیف پول هم فقط با کارت شارژ
  می‌شود، ربات هیچ راهی برای گرفتنِ پول نداشت. حالا «ربات من» در
  پرتال کارت‌ها را می‌گیرد (۱۶ رقم، رقمِ فارسی هم پذیرفته می‌شود).
- **رسید به هیچ‌کس نمی‌رسید.** رسید به گروهِ مدیریت می‌رفت، نماینده
  گروه نداشت، و `notify_group` بی‌صدا `None` برمی‌گرداند. حالا
  نماینده با یک دکمه در پرتال («وصلِ من به ربات») خودش را به رباتش
  وصل می‌کند و رسید، هشدار و پیامِ پشتیبانی — فقط همین سه، نه هر
  عضوِ تازه — با دکمه‌ی تایید به پیویِ او می‌آید. لینک یک‌بارمصرف و
  نیم‌ساعته است، چون هرکس آن را داشته باشد می‌تواند سفارش تایید کند.
  چرا لینک و نه «آیدیِ عددی‌تان را وارد کنید»: کسی آیدیِ عددیِ خودش
  را نمی‌داند، و عددِ اشتباه یعنی رسید به غریبه برسد.

### Fixed — اعتبارِ پیش‌پرداخت دست نمی‌خورد

نماینده‌ی پیش‌پرداخت نباید بیشتر از آنچه داده بفروشد. پرتال این را
رعایت می‌کرد و ربات نه. حالا هر فروش و هر تمدید از رباتِ او، کفِ
همان پلن را **اتمی** از اعتبارش کم می‌کند (شرط داخلِ خودِ `UPDATE`)،
و اگر پنل کانفیگ را نسازد یا تمدید نکند، برمی‌گرداند.

کف را ربات حساب نمی‌کند — نرخ‌ها در دیتابیسِ حسابداری‌اند. بکند
همان لحظه‌ی ذخیره‌ی پلن با همان `_line_amount` حسابش می‌کند و در
`plans.cost` می‌گذارد، و هر وقت مالک نرخِ یک گروه را عوض کند دوباره
می‌سازدش؛ پلن‌هایی که پیش از این نسخه ذخیره شده‌اند هم یک‌بار موقعِ
بالاآمدن پر می‌شوند. پیش‌نمایش و ذخیره از یک تابع می‌خوانند
(`_plan_floor`)، پس نماینده همان کفی را می‌بیند که از اعتبارش کم
می‌شود.

صورتحسابِ بدهکارها عوض نشده: کانفیگِ ساخته‌شده در گروه، همان‌طور
شمرده می‌شود که کانفیگِ ساخته‌شده در خودِ x-ui.

### Fixed — پلنِ زیرِ کف ذخیره می‌شد

مالک گفته بود قیمتِ نماینده «باید از نرخِ من بالاتر باشد تا سود
کند». رابط سود و ضرر را نشان می‌داد ولی بکند هر قیمتی را می‌پذیرفت —
حتی صفر. حالا پلنِ زیرِ کف ذخیره نمی‌شود و پیام هر دو عدد را
می‌گوید. کفِ نامعلوم (بدونِ گروه) جلوی ذخیره را نمی‌گیرد؛ پیش‌نمایش
همان‌جا می‌گوید که معلوم نیست.

### Fixed — صفحه‌ی ورودِ مینی‌اپ نامِ ما را لو می‌داد

مالک: «نباید کاربر بفهمد از نکسورا دارد خریداری می‌کند». اسپلشِ
مینی‌اپ نشانِ نکسورا و کلمه‌ی «NEXORA» را به مشتریِ **هر** نماینده
نشان می‌داد، چون `main.jsx` هیچ‌وقت لوگوی فروشگاه را پاس نمی‌داد.
حالا اسپلشِ مینی‌اپ یا لوگو و نامِ همان فروشگاه است (از کشِ دفعه‌ی
قبل)، یا — اگر هنوز نمی‌دانیم کیست — یک نشانِ خنثی. عنوانِ پنجره هم
همین‌طور.

### Added — «ربات شما هنوز نمی‌تواند بفروشد»

داشبوردِ نماینده حالا می‌گوید رباتِ وصل‌شده چه چیزی کم دارد — کارت،
وصل‌بودن، گروه — و گروه را صریح «کارِ مدیر» می‌نامد تا نماینده دنبالِ
دکمه‌ای نگردد که وجود ندارد. تا امروز این را فقط وقتی می‌فهمید که
مشتری شکایت می‌کرد.

`nexora reseller-why` هم پنج قدمِ تازه دارد: کارت، اینکه رسید به
کسی می‌رسد (و به کجا)، پلنِ زیرِ کف، و برای پیش‌پرداخت‌ها پلنِ بی‌کف.

### Tests

`bot/test_reseller.py` — یک فروشِ واقعی از رباتِ نماینده، و هر جا
که می‌تواند بشکند عمداً شکسته می‌شود: پنلی که نمی‌سازد، تمدیدی که
شکست می‌خورد، اعتبارِ ناکافی، نماینده‌ی بی‌گروه، لینکِ منقضی یا
تکراری. ۴۲ سنجه. سیزده باگ یکی‌یکی برگردانده شدند و هر سیزده بار
تست قرمز شد.

## [1.85.0]

### Fixed — ریشه‌ی ماجرا: nginx هدر را جلو نمی‌فرستاد

سه ریلیزِ گذشته نشانه را درمان کردند. این یکی خودِ علت را می‌زند.

TLS روی nginx تمام می‌شود، پس بدونِ `X-Forwarded-Proto` درخواست به
uvicorn به‌شکلِ **http** می‌رسد. بک‌اند نتیجه می‌گیرد پنل روی https
نیست و آدرسِ مینی‌اپ را هیچ‌وقت ثبت نمی‌کند — و دکمه در هیچ رباتی،
نه مالک و نه نماینده، ظاهر نمی‌شود.

`install.sh` این هدر را از نسخه‌ی اصلاح به بعد دارد و تستش هم
هست. ولی **نصب‌هایی که پیش از آن بالا آمده‌اند فایلِ قدیمی را
دارند** و `nexora update` هیچ‌وقت آن را بازنویسی نمی‌کرد. یعنی هر
سرورِ موجود تا ابد این باگ را داشت.

`fix-nginx-cache.py` — که از قبل در **هر** به‌روزرسانی اجرا
می‌شود — حالا این را هم تعمیر می‌کند:

- هدر را کنارِ هر `proxy_set_header Host` که نداردش می‌گذارد
- بلوکِ ریدایرکتِ http را دست نمی‌زند (پروکسی نمی‌کند)
- اگر از قبل باشد، دوباره اضافه نمی‌کند
- `nginx -t` می‌گیرد و اگر خراب شد، از پشتیبان برمی‌گرداند

پس با یک `nexora update` سرورهای موجود هم درست می‌شوند و آدرسِ
مینی‌اپ دفعه‌ی بعد که پنل باز شود خودش ثبت می‌شود.

**چرا لنگر روی `Host` و نه `proxy_pass`:** بلوک‌هایی هم هستند که
پروکسی می‌کنند ولی هیچ هدری نمی‌گذارند، و آن‌جا تورفتگی معلوم
نیست. هر بلوکی که هدر می‌گذارد، `Host` را دارد.

### Fixed — و خودِ اسکریپت بی‌صدا بود

اگر `nginx` نصب نبود، زودتر برمی‌گشت و **هیچ‌وقت نمی‌گفت چه عوض
کرده**. تغییر روی دیسک نشسته بود و کاربر بی‌خبر. حالا خلاصه در هر
دو مسیر چاپ می‌شود.

### Added — دروازه برای نصب‌های موجود، نه فقط نصبِ تازه

دروازه‌ی قبلی فقط `install.sh` را می‌سنجید — یعنی فقط نصبِ تازه.
حالا سنجیده می‌شود که تعمیرِ نصب‌های موجود هم وجود دارد **و** در
هر به‌روزرسانی صدا زده می‌شود: «تعمیری که صدا زده نشود، وجود
ندارد».

probe با کانفیگی به شکلِ نصب‌های واقعی (یک بلوکِ ریدایرکت و یک
بلوکِ پروکسی): نُه سنجه، همه سبز.

## [1.84.0]

### Fixed — راهنمای غلطی که باعثِ خودِ خرابی شد

فیلدِ «آدرس مینی‌اپ» در پنل **از قبل وجود داشت**. راهنمایش این بود:

> خالی بماند، خودش از دامنه‌ی لینک اشتراک ساخته می‌شود

**این غلط است.** کد صریحاً از `sub_base_url` نمی‌سازدش و کامنتِ
خودش هم می‌گوید چرا نمی‌سازد («آدرسِ x-ui معمولاً روی پورت دیگری
است؛ حدس‌زدن از رویش صفحه‌ی سفید می‌دهد»). آدرس در واقع از دامنه‌ی
**خودِ پنل** یاد گرفته می‌شود، آن هم فقط وقتی پنل روی https باز شود
و هدرش برسد.

پس زنجیره این بود: پنل گفت «خالی بماند» → مالک خالی گذاشت →
تشخیصِ خودکار روی آن نصب کار نکرد → آدرس خالی ماند → **دکمه‌ی
مینی‌اپ در هیچ رباتی ظاهر نشد**، نه مالک و نه نماینده‌ها → و
هیچ‌جا نگفت چرا.

سه اصلاح:

- **راهنمای درست**: می‌گوید از دامنه‌ی همین پنل می‌آید، و اگر خالی
  مانده یعنی تشخیص کار نکرده و باید دستی گذاشته شود
- **هشدارِ دیده‌شدنی** وقتی خالی است: «دکمه‌ی مینی‌اپ در هیچ رباتی
  ظاهر نمی‌شود». تا امروز فقط یک فیلدِ خالی بود و نتیجه‌اش هیچ‌جا
  گفته نمی‌شد
- **دکمه‌ی «آدرسِ همین پنل را بگذار»** — مرورگر دامنه را می‌داند،
  پس نه تایپ لازم است نه ssh. فقط وقتی صفحه روی https باشد ظاهر
  می‌شود؛ روی http به‌جایش می‌گوید دستی بگذارید، چون آدرسِ حدسی
  بدتر از نبودنش است

و اگر مقدارِ واردشده با `https://` شروع نشود، همان‌جا قرمز می‌شود.

سنجیده‌شده در مرورگر (روی http، همان حالتِ پرتکرار): هشدار درست
ظاهر شد و دکمه‌ی حدس عمداً پنهان ماند.

## [1.83.0]

### Fixed — آدرسِ مینی‌اپ روی نصبِ واقعی هیچ‌وقت نوشته نمی‌شد

`nexora reseller-why` روی سرورِ واقعیِ مالک اجرا شد و دقیقاً همین
را نشان داد:

```
  حسین دهلگی  (blocked)
    ok  portal login             enabled=True slug=unlimited-deh
    ok  x-ui group               unlimited
    ok  rates for that group     1 tier(s)
    ok  telegram bot             @unlimitedhvpnbot
    NO  mini-app url             not set
    NO  plans                    0 plan(s)
```

همه‌چیز سبز جز آدرس. علت: آدرس فقط وقتی یاد گرفته می‌شود که پنل
روی **https** باز شود و هدرش هم به بک‌اند برسد. روی نصب‌هایی که
بلوکِ nginx قدیمی دارند یا پنل با http باز می‌شود، `_panel_host`
رشته‌ی خالی برمی‌گرداند و **هیچ‌وقت چیزی نوشته نمی‌شود** — بی‌صدا.

خودِ کد این را می‌دانست و کامنتش هشدار داده بود («روی نصب‌های واقعی
هیچ‌وقت درست نشد… نصب‌های موجود فایل قدیمی را دارند»)، ولی راهِ
دستی نداشت.

**حالا دارد:**

```bash
nexora reseller-why --set-miniapp https://panel.example.com
```

برای **همه‌ی** مستاجرها می‌نویسد، `‎/app` را خودش اضافه می‌کند، و
`http` را رد می‌کند (تلگرام کلِ پیام را رد می‌کند، نه فقط دکمه را).

### Changed — ابزار می‌گوید *چرا*، نه فقط *چه*

گزارش حالا آدرسِ خودِ مالک را هم نشان می‌دهد. اگر آن هم خالی باشد،
یعنی مشکل از نماینده نیست و تشخیصِ خودکار اصلاً فعال نشده — و
همان‌جا راهِ دستی را پیشنهاد می‌دهد:

```
owner mini-app not set
  the owner has no mini-app url either.
  That means the panel was never opened over https, so the
  address was never learned. Set it once, by hand:
      nexora reseller-why --set-miniapp https://<your-panel>
```

راهنمایی‌ای که به وضعیتِ واقعی نخورد، از نبودش بدتر است: نسخه‌ی
قبلی می‌گفت «پنل را روی https باز کنید» به کسی که همین حالا بازش
کرده بود.

سنجیده‌شده با بازسازیِ همان وضعیتِ سرور: سیزده سنجه، همه سبز —
از ردِ `http` تا «بارِ دوم چیزی عوض نمی‌شود» تا «مانعِ بعدی حالا
پلن‌هاست».

## [1.82.1]

### Fixed — تمدیدِ پوسته، روزهای باقی‌مانده را بی‌صدا می‌سوزاند

ممیزیِ نُه مسیری که در همین جلسه اضافه شده بودند، این را پیدا کرد:

```python
try:
    have = _dt.fromisoformat(cur[:19])
    if have > base:
        base = have
except (TypeError, ValueError):
    pass                     # ← بی‌صدا
```

اگر `theme_until` خراب بود، هر دو مسیرِ تمدید بی‌سروصدا از «الان»
شروع می‌کردند. یعنی نماینده‌ای که بیست روز اعتبار داشت پول می‌داد و
همان بیست روز را **از دست می‌داد** — بدونِ هیچ ردی در هیچ لاگی.

شکستنِ خرید بدتر بود (نماینده اصلاً نمی‌توانست تهیه کند)، پس
برگشت به «الان» ماند — ولی حالا لاگ می‌شود.

### Added — دروازه‌ی «مسیرِ پول بی‌صدا رد نمی‌شود»

قاعده‌ی اولِ `CLAUDE.md` است و تا امروز هیچ دروازه‌ای نداشت. حالا
هر تابعی که پول یا اشتراکِ پرداخت‌شده را دست می‌زند
(`_portal_charge`، `_portal_refund`، `spend_balance`،
`close_order`، `credit`، `theme_until`) نمی‌تواند `except: pass`
بدونِ لاگ داشته باشد.

`except: pass` همه‌جا بد نیست — در مسیری که پول جابه‌جا می‌کند
هست، پس دامنه محدود است.

جاروی اول دو از سه گرفت: مسیرِ `grant` — که خودش پولی جابه‌جا
نمی‌کند ولی روزهای پرداخت‌شده را عوض می‌کند — بیرونِ فهرست مانده
بود. «پول» فقط کسر و واریز نیست؛ تاریخِ اشتراکی که بابتش پول داده
شده هم همان است. جاروی دوم: **سه از سه.**

### ممیزیِ نُه مسیرِ تازه‌ی این جلسه

| سنجه | نتیجه |
|---|---|
| مسیرهای مدیر `check_auth` دارند | ۵ از ۵ |
| مسیرهای پرتال از `portal_tenant` رد می‌شوند | ۴ از ۴ |
| شناسه‌ی مستاجر از بدنه خوانده نمی‌شود | هیچ‌کدام |
| `grant` مالکیتِ ردیف را می‌سنجد | بله (`parent_id`) |
| اول کسر، بعد اعطا | بله |
| شکستِ اعطا پول را برمی‌گرداند | بله |
| قیمت از تنظیماتِ مالک می‌آید، نه از درخواست | بله |

## [1.82.0]

### Added — `nexora reseller-why`

الگوی چند ریلیزِ گذشته این بود: مالک می‌گفت «نماینده نمی‌تواند پلن
تعریف کند»، من حدس می‌زدم، و **چهار بار چهار علتِ متفاوت** بود:

| علت | از پنل دیده می‌شد؟ |
|---|---|
| ورودِ پنل فعال نشده بود | تا حدی |
| گروهِ x-ui نداشت → خطای ۴۰۹ روی نرخ | نه |
| ویرایشگرِ خالی دکمه‌ی افزودن نداشت | نه |
| آدرسِ مینی‌اپ نداشت → دکمه در ربات نبود | نه |

هیچ‌کدام از پنل معلوم نبود و همه یک نشانه داشتند.

حالا یک دستور، هفت قدمِ لازم را به ترتیب می‌سنجد و **اولین** چیزی
را که گیر کرده نشان می‌دهد:

```
  hossein  (blocked)
    ok  portal login             enabled=True slug=hossein password=set
    NO  x-ui group               not set
    NO  rates for that group     none
    ok  telegram bot             @hossein_vpn_bot
    ok  mini-app url             https://panel.example.com/app
    ok  plans                    2 plan(s)
    ok  an active, priced plan   2 of 2

    fix this first: x-ui group
      panel > reseller panel > pick their group from the list.
```

فقط **اولین** مانع راهنمایی می‌گیرد، نه همه — بقیه معمولاً نتیجه‌ی
همان یکی‌اند.

خروجی انگلیسی است (قاعده‌ی مخزن برای ابزارهای ترمینال) و **هیچ
رمز، توکن، نام مشتری یا شماره‌ای چاپ نمی‌کند**، پس قابلِ فرستادن
است.

سنجیده‌شده با هفت نماینده‌ی ساختگی که هر کدام سرِ یک قدمِ متفاوت
گیر کرده‌اند: هر هفت بار انگشت روی قدمِ درست گذاشت.

### Changed — دروازه‌ی راهنما فهرستِ دستی ندارد

`_cmds = {"reseller", "billing-why", "fix-flow", "import-topups"}` یک
فهرستِ ثابت بود، یعنی دستورِ تازه از دیدِ دروازه پنهان می‌ماند و
اگر در راهنما نمی‌آمد کسی پیدایش نمی‌کرد.

حالا شاخه‌های `case` که `tools/*.py` صدا می‌زنند خودشان استخراج
می‌شوند — **هشت** دستور به‌جای چهارتای دستی. همان قاعده‌ای که سرِ
صفحه‌های پنل با `data-navhead` اجرا شد.

جارو: دو شکستِ عمدی (دستوری که در راهنما نیست، و ابزاری که وجود
ندارد)، هر دو قرمز.

## [1.81.0]

### Changed — هویتِ فروشگاهِ نماینده، یک‌جا و کنارِ پیش‌نمایش

نماینده‌ای که می‌خواست «فروشگاهِ خودش» را بسازد، باید سه جای جدا
می‌رفت:

| چه چیزی | کجا بود |
|---|---|
| نام فروشگاه | «ربات من» |
| لوگو | روی سرِ صفحه، کنارِ نامِ گروه |
| رنگ | «پوسته» |

و هیچ‌کدام نشان نمی‌داد نتیجه چه شکلی می‌شود.

حالا هر سه در «پوسته»‌اند، کنارِ همان پیش‌نمایشی که در ۱.۷۷.۰
ساخته شد — نام را که تایپ می‌کند، همان لحظه در پیش‌نمایش
می‌نشیند.

**نام و لوگو رایگان‌اند و پشتِ قفل نمی‌روند؛ فقط رنگ پولی است.**
سنجیده‌شده در مرورگر: در حالتِ قفل، فیلدِ نام و دکمه‌ی لوگو فعالند
و انتخابگرِ رنگ غیرفعال.

هیچ مسیرِ تازه‌ای ساخته نشد — همان `/api/portal/brand` و
`/api/portal/logo` که از قبل بودند. مسیرِ دوم برای یک کلید یعنی
روزی یکی اعتبارسنجی را عوض کند و دیگری نه.

### ممیزی — چه چیزی در مینی‌اپِ نماینده هنوز مالِ مالک است؟

هر هفده مسیرِ `/api/mini/*` بررسی شد: **هیچ‌کدام** از
`load_config()` (تنظیماتِ سراسریِ مالک) نمی‌خوانند. همه به مستاجرِ
خودشان محدودند — برند، لوگو، پشتیبانی، کانال، پلن‌ها، سکه، و
صندوق.

پس مینی‌اپِ نماینده از نظرِ داده کاملاً مالِ خودش است.

## [1.80.0]

مالک دو بار گفت «نماینده نمی‌تواند پلن تعریف کند» و یک‌بار «هیچ
مینی‌اپی هم برایش ساخته نشده». هر دو درست بودند، و **هیچ‌کدام همان
چیزی نبود که در ۱.۷۸.۰ درست شد.**

### Fixed — نماینده‌ی بی‌پلن هیچ راهی برای ساختنِ اولین پلن نداشت

```jsx
) : !rows.length ? (
    <EmptyState text="هنوز پلنی تعریف نکرده‌اید" />   ← بن‌بست
) : (
    <> … <button>پلن تازه</button> <button>ذخیره</button> </>
)
```

شاخه‌ی خالی، همان `<>`ای را که دکمه‌های «پلن تازه» و «ذخیره» در آن
بودند **اصلاً رندر نمی‌کرد**. یعنی هر نماینده‌ی تازه ویرایشگر را باز
می‌کرد، «هنوز پلنی تعریف نکرده‌اید» می‌دید، و تمام.

در ۱.۷۸.۰ خطای ۴۰۹ِ «گروه تعیین نشده» برداشته شد و ویرایشگر **باز
شد** — ولی روی همین بن‌بست باز می‌شد. دو باگِ پشتِ سرِ هم، با یک
نشانه.

نقضِ قاعده‌ی خودِ مخزن در بدترین شکلش: «هر دکمه‌ای که نشان داده
می‌شود باید کار کند» — این‌جا دکمه اصلاً نشان داده نمی‌شد.

حالا حالتِ خالی دکمه‌ی **«ساختِ اولین پلن»** دارد. سنجیده‌شده در
مرورگر: خالی → کلیک → یک ردیف با کفِ قیمت («کفِ شما ۷۵٬۰۰۰ تومان»)
و دکمه‌های «پلن تازه» و «ذخیره».

### Fixed — نماینده اصلاً مینی‌اپ نداشت

دو کوئری، هر دو با یک شکل:

| کجا | چه می‌کرد |
|---|---|
| `_learn_panel_origin` | آدرس را فقط روی `parent_id IS NULL` می‌نوشت |
| `_mini_tenants()` | فقط ریشه را برمی‌گرداند |

نتیجه: `miniapp_url(ctx)` برای نماینده خالی بود، پس **دکمه‌ی
مینی‌اپ در رباتش اصلاً ظاهر نمی‌شد** — و اگر هم آدرس را دستی
می‌گذاشت، امضای initData با توکنِ رباتِ او سنجیده نمی‌شد و ۴۰۱
می‌گرفت.

کامنتِ خودِ `_mini_tenants` نوشته بود: «قدم بعدی دادنِ همین به
نماینده‌هاست». قدمی که برداشته نشده بود.

**یعنی پوسته‌ی شخصیِ نماینده در ۱.۷۷.۰ داشت مینی‌اپی را رنگ می‌کرد
که برای او وجود نداشت.**

حالا:

- آدرس برای **همه‌ی** مستاجرها نوشته می‌شود، حتی آن‌که هنوز ربات
  ندارد — تا لحظه‌ای که وصل کرد، دکمه همان‌جا باشد
- نماینده‌ای که همین حالا ربات وصل می‌کند، آدرس را همان لحظه
  می‌گیرد و منتظرِ بازشدنِ دوباره‌ی پنلِ مالک نمی‌ماند
- امضا با توکنِ **هر** ربات سنجیده می‌شود

سنجیده‌شده با امضای واقعیِ HMAC:

| سنجه | نتیجه |
|---|---|
| نماینده جزو مستاجرهای مینی‌اپ | ✅ |
| امضای توکنِ نماینده → مستاجر نماینده | ✅ |
| برندی که مشتری می‌بیند | «حسین وی‌پی‌ان»، نه «نکسورا» |
| توکنِ مالک به مشتریِ نماینده نمی‌رسد | ۴۰۴ |
| دکمه در رباتِ نماینده | ظاهر شد |

### Fixed — عنوانِ مینی‌اپ نامِ فروشگاه را نشان نمی‌داد

`document.title = "اشتراک من"` ثابت بود. تلگرام همین را بالای
پنجره نشان می‌دهد، پس مشتریِ نماینده نامِ فروشگاهی را که از آن خرید
کرده **هیچ‌جا** نمی‌دید. حالا برندِ همان مستاجر می‌نشیند.

### Added — پنج دروازه، و دو مثبتِ کاذبِ خودم

دروازه‌ی «فقط ریشه نباشد» اول به دو چیزِ بی‌گناه گیر کرد:
`ORDER BY parent_id IS NULL DESC` (ترتیب است، نه فیلتر) و کامنتی
که همین باگِ قدیمی را توضیح می‌داد. دومی دقیقاً همان چیزی است که
`_py_nocomment` برایش هست — دروازه‌ای که به توضیحِ باگ گیر کند،
مجبورمان می‌کند توضیح را پاک کنیم.

و دروازه‌ی «آدرس را موقعِ ثبتِ ربات کپی کن» با وجودِ رشته سبز
می‌ماند؛ به سنجشِ خودِ **انتساب** عوض شد.

جارو: **پنج شکستِ عمدی، هر پنج قرمز.**

### دامِ محیطی، بارِ سوم

heredoc گیت‌بش `\b` را به کاراکترِ backspace تبدیل کرد و یک regexِ
دروازه را خراب کرد. `CLAUDE.md` هشدارش را داده بود؛ اسکریپت‌های
وصله‌ای که کاراکترِ فرار دارند باید با ابزار Write نوشته شوند.

## [1.79.0]

### Added — قالب را ببین، بعد انتخاب کن

مالک خواست «حالت پیش‌نمایش بساز من انتخاب کنم».

چهار چیدمان (کلاسیک، آماری، کیف پول، کنسول) و چند پالت از قبل
وجود داشتند، ولی برای انتخاب بینشان باید این حلقه طی می‌شد:

```
قالب را عوض کن → ذخیره کن → برو به پیش‌نمایش → ببین → برگرد
```

یعنی برای مقایسه‌ی چهار قالب، **چهار بار ذخیره روی تنظیماتِ واقعیِ
مشتری‌ها** — و بینشان، مشتری‌هایی که همان لحظه صفحه را باز کرده
بودند قالبِ نیمه‌انتخاب‌شده را می‌دیدند.

حالا نوارِ انتخاب **بالای خودِ پیش‌نمایش** است:

- روی هر چیدمان بزنید، همان لحظه در نما عوض می‌شود
- روی هر رنگ بزنید، همان‌طور
- تا وقتی «همین را انتخاب کن» را نزده‌اید، **هیچ‌چیز ذخیره
  نمی‌شود** و نوار می‌گوید «فقط دارید نگاه می‌کنید»
- «برگرد به ذخیره‌شده» همیشه هست

`?template=` و `?palette=` روی `/api/preview` فقط همان نما را عوض
می‌کنند. صفحه‌ی مشتری این پارامترها را نمی‌بیند.

**سه قاعده که رعایت شد:**

- **فهرستِ مجاز، نه ممنوع.** مقدارِ قالب مستقیم داخلِ صفحه تزریق
  می‌شود؛ هر رشته‌ای یعنی تزریقِ اسکریپت. فقط چهار شناسه‌ی شناخته‌شده
  می‌گذرند و پالت هم الگوی `[a-z0-9_-]{1,32}` دارد.
- **نگاشتِ دوم ساخته نشد.** «نامِ پالت → رنگ‌ها» را همان
  `resolve_theme()` حل می‌کند که مسیرِ واقعی صدا می‌زند، نه یک
  جدولِ دومِ دستی.
- **انتخابِ نهایی فقط دو کلید را عوض می‌کند.** مسیرِ باریکِ تازه
  `POST /api/admin/themes` به‌جای `PUT /api/admin/config` که کلِ
  تنظیمات را جایگزین می‌کند — فرستادنِ dictِ ناقص یعنی هر چه در آن
  نیست پاک شود.

### اندازه‌گیری، نه نگاه‌کردن

سنجیده‌شده در مرورگر:

| مرحله | نتیجه |
|---|---|
| زدنِ «کیف پول» + رنگ بنفش | نشانیِ نما → `template=wallet&palette=violet` |
| پیش از ذخیره | «فقط دارید نگاه می‌کنید — هنوز ذخیره نشده» |
| بعد از «همین را انتخاب کن» | هشدار رفت، سرور `wallet`/`violet` شد |
| نوار روی ۱۲۸۰ | ۹۸۰ پیکسل، کاملاً در دید، بدون اسکرولِ افقی |

### Added — شش دروازه، جاروی هفت‌موردی

هر هفت شکستِ عمدی قرمز شد — از «قالب هر رشته‌ای را قبول کند» تا
«صفحه‌ی اشتراک بازنویسی را نادیده بگیرد».

## [1.78.0]

### Fixed — نماینده‌ی بی‌گروه اصلاً نمی‌توانست پلن تعریف کند

مالک گزارش داد «نماینده نمی‌تواند پلنی تعریف کند». با اجرای واقعی
روی دیتابیس پیدا شد:

```
portal_bot_plans → _portal_gb_policy → _portal_rates → _portal_group
                                                            ↓
                              HTTP 409 «گروه این نماینده هنوز تعیین
                              نشده — با پشتیبانی تماس بگیرید»
```

یعنی نماینده‌ای که مالک هنوز گروهِ x-ui‌اش را نگذاشته بود:

- **ویرایشگرِ پلن اصلاً باز نمی‌شد** (درخواستِ خواندن می‌مرد)
- و اگر باز هم می‌شد، **ذخیره نمی‌شد** (درخواستِ نوشتن هم می‌مرد)

و پیامش «با پشتیبانی تماس بگیرید» بود — یعنی خودِ نماینده هیچ راهی
نداشت بفهمد مشکل چیست.

**ولی پلنِ ربات کانفیگ نیست.** یک ردیف در جدولِ `plans`ِ خودِ
نماینده، برای رباتِ تلگرامِ خودش. هیچ ربطی به گروهِ x-ui ندارد —
گروه فقط برای *کفِ قیمت* لازم است.

و پنلِ خودِ مالک کارتی دارد که «بدون گروه» را می‌شمارد، یعنی این
حالت **عادی و پرتکرار** است، نه استثنا.

حالا `_portal_group_opt()` اضافه شده که به‌جای خطا رشته‌ی خالی
می‌دهد، و مسیرهای پلن از آن می‌خوانند. کفِ قیمت هم به‌جای مردن
می‌گوید «گروه شما هنوز تعیین نشده — تا مالک تعیینش نکند کفِ قیمت
معلوم نیست».

**قاعده‌ی امنیتی دست‌نخورده ماند:** `portal_configs` و
`portal_summary` همچنان بدونِ گروه چیزی نشان نمی‌دهند — نماینده‌ی
نیمه‌ساخته نباید کلِ مشتری‌های سیستم را ببیند.

### Changed — صفحه‌ی اشتراک: کارتِ بالا نیمه‌خالی بود و شبکه تکراری

اندازه‌گیری روی ۳۷۵ پیکسل:

- پایینِ کارتِ بالا حدود **۶۰ پیکسل خالی** بود
- «۵۰٪ باقی‌مانده» در حلقه و «۲۴.۹ GB حجم باقی‌مانده» در شبکه،
  **همان یک حقیقت دو بار**
- «مصرف کل» = «دانلود» + «آپلود» — سه سلول برای یک عدد

حالا **نوارِ حجم** داخلِ همان فضای خالی نشسته: «حجم باقی‌مانده ·
۲۴.۹ GB از ۵۰ GB» با یک میله‌ی پیشرفت که رنگش از همان درصدی می‌آید
که حلقه از آن می‌خواند — نه یک محاسبه‌ی دوم.

شبکه از شش سلول به **چهار** رسید (مصرف کل، دانلود، آپلود، آخرین
اتصال)، پس در ۴ یا ۲ ستون همیشه کامل پر می‌شود و دیگر جای خالی
ندارد.

حالتِ نامحدود: میله پر و کم‌رنگ می‌ماند و «از ۰ GB» برداشته
می‌شود — صفر نشان‌دادن دقیقاً برعکسِ واقعیت است.

### Fixed — یک نوشتنِ مستقیم، کلِ صفحه را در حالتِ صفر نگه داشت

با جابه‌جایی سلولِ «حجم کلی»، این خط ماند:

```js
document.getElementById("title-limit").innerText = data.titleLimit;
```

عنصر دیگر وجود نداشت، پس `TypeError` داد و **تمامِ خط‌های بعدش در
همان تابع اجرا نشدند**: هیچ برچسبی ترجمه نشد، هیچ عددی پر نشد، و
صفحه در حالتِ صفر ماند — بدونِ هیچ پیامی به کاربر.

خودِ همان فایل `setText()` را دارد و کامنتش می‌گوید «اگر عنصری نبود
به‌جای کرش بی‌سروصدا رد می‌شود» — ولی این چهار خط از آن استفاده
نمی‌کردند. حالا می‌کنند، و دروازه‌ی تازه هر ۳۷ دسترسیِ مستقیم را
با ۱۱۱ شناسه‌ی موجود می‌سنجد.

### Added — شش دروازه، و درسِ دوباره‌ی «متن را نسنج، رفتار را بسنج»

جاروی اول: پنج از شش. موردی که سبز ماند، «مسیرهای کانفیگ همچنان
گروه می‌خواهند» بود — شکستن یک `return` زودهنگام *قبل* از فراخوانی
گذاشت، پس رشته سرِ جایش ماند و دروازه‌ی متنی راضی شد.

سه سنجه‌ی رفتاری به `test-admin-api` اضافه شد که خودِ تابع‌ها را
صدا می‌زنند. جاروی دوم: **شش از شش**.

و جدولِ `plans` در فیکسچرِ آن تست نبود؛ اسکیما از خودِ `bot/db.py`
گرفته می‌شود، نه کپیِ دستی — کپی یعنی منبعِ دومِ حقیقت.

## [1.77.1]

### Fixed — داده‌ی هارنس شکلِ بکند را نداشت و باگِ تقلبی می‌ساخت

دنبالِ «قسمت‌هایی از پنل نمایندگی که کار نمی‌کنند» گشتم. اسکنِ
ایستا تمیز بود، پس این بار **هر دکمه را واقعاً زدم**. چیزی که پیدا
شد، خودِ ابزارِ دیدن بود:

ردیف‌های ساختگیِ سفارش در `harness-boot.js` کلیدهای `name` و
`plan` می‌فرستادند، در حالی که بکند `customer` و `planName`
می‌دهد. نتیجه در هارنس:

- هر ردیف **«#۷۰۰ · » خالی** نشان می‌داد
- فیلترِ برگه‌ها اعمال نمی‌شد، پس سفارشِ **تاییدشده** در برگه‌ی
  «در انتظار» می‌نشست و چون دکمه‌ی تایید/رد ندارد، **شبیهِ باگ**
  به نظر می‌رسید

یعنی داده‌ی بدشکل دو کار می‌کرد، هر دو بد: باگِ تقلبی می‌ساخت و
باگِ واقعی را می‌پوشاند. و چون تنها راهِ نگاه‌کردن به پنل همین
هارنس است، هر بررسیِ چشمیِ این پنل تا امروز روی دادهٔ غلط بوده.

بعد از اصلاح، هر سه برگه درست رفتار می‌کنند — سنجیده‌شده در
مرورگر:

| برگه | ردیف | تایید | رد |
|---|---|---|---|
| در انتظار | ۲ | ۲ | ۲ |
| تاییدشده | ۲ | ۰ | ۰ |
| ردشده | ۲ | ۲ | ۰ |

**خودِ پنل نمایندگی سالم بود.** هیچ دکمه‌ی بی‌هندلر، هیچ مسیرِ
بی‌صداکننده، و هیچ stateِ مرده‌ای پیدا نشد.

### Added — دروازه‌ی «داده‌ی ساختگی شکلِ واقعیت را دارد»

`CLAUDE.md` گفته بود «واقع‌نما نگهش دار»؛ حالا اجرا هم می‌شود:

- کلیدهای ردیفِ ساختگی با کلیدهای `return` همان مسیرِ بکند سنجیده
  می‌شوند
- هارنس باید فیلترِ وضعیت را واقعاً اعمال کند
- داده‌های کلیدی نباید آرایه‌ی خالی باشند — آرایه‌ی خالی یعنی
  شاخه‌ی «هنوز چیزی نیست» و صفحه‌ای که سالم به نظر می‌رسد

جارو: پنج شکستِ عمدی، هر پنج قرمز.

### Added — رنگِ نماینده در هارنسِ مینی‌اپ

پوسته‌ی شخصی تا امروز فقط در پیش‌نمایشِ پرتال دیده می‌شد. حالا
مینی‌اپِ هارنس هم رنگ می‌گیرد و سنجیده شد که واقعاً به عناصر
می‌رسد: `--accent: #7c5cff`، `--accent-rgb: 124, 92, 255`، و چهار
عنصرِ واقعی که رنگ را می‌گیرند. با `?accent=off` حالتِ پیش‌فرض هم
دیده می‌شود.

## [1.77.0]

برگه: `docs/specs/2026-09-22-reseller-and-ui.md`

### Fixed — نماینده کفِ قیمتش را یک‌دوازدهمِ واقعیت می‌دید

مهم‌ترین چیزِ این نسخه. زیرِ هر پلنِ نماینده نوشته می‌شد «برای شما
X تومان». آن X این‌طور حساب می‌شد:

```js
policy.cost[gb]        // فقط حجم
```

ولی صورتحسابِ واقعی از `_line_amount` می‌آید:

```python
(نرخ پایه + نرخ هر کاربر اضافه) × ماه
```

یعنی **`days` و `ip_limit` در عددی که به نماینده نشان داده می‌شد
اصلاً نبودند.** اندازه‌گیری‌شده با نرخ‌های نمونه:

| پلن | نشان داده می‌شد | واقعیت |
|---|---|---|
| ۳۰ گیگ / ۳۰ روز / ۲ کاربر | ۶۰٬۰۰۰ | ۷۵٬۰۰۰ |
| ۳۰ گیگ / ۹۰ روز / ۲ کاربر | ۶۰٬۰۰۰ | **۲۲۵٬۰۰۰** |
| ۵۰ گیگ / ۱۸۰ روز / ۴ کاربر | ۹۰٬۰۰۰ | **۹۰۰٬۰۰۰** |
| نامحدود / ۳۶۵ روز | ۲۰۰٬۰۰۰ | **۲٬۴۰۰٬۰۰۰** |

نماینده «بالای کف» می‌فروخت و تا دوازده برابر ضرر می‌کرد — و فقط
آخرِ ماه می‌فهمید.

حالا مسیرِ `POST /api/portal/plan-cost` همان `_line_amount` را صدا
می‌زند و سه عدد برمی‌گرداند: **کفِ شما**، **قیمتِ شما**، **سود یا
ضرر** — با شکستِ عدد («۲۰۰٬۰۰۰ پایه + ۳ کاربر اضافه × ۱۵٬۰۰۰ × ۳
ماه»). هیچ ضربی در جاوااسکریپت نمانده؛ دروازه‌ی درز اجرایش می‌کند.

نبودِ نرخ **صفر برنمی‌گرداند** — صفر یعنی «رایگان» و این یعنی
«نمی‌دانیم». دو چیزِ متفاوت.

### Added — عدد بدونِ جداکننده خوانده نمی‌شود

«۱۲۰۰۰۰۰» و «۱۲۰۰۰۰۰۰» با یک نگاه فرق نمی‌کنند و یک صفرِ اضافه روی
قیمتِ پلن یعنی ده برابر. `MoneyInput` جداکننده را **همان‌طور که
تایپ می‌شود** نشان می‌دهد و مقدارِ خام را به صداکننده می‌دهد، پس
`Number(e.target.value)` هیچ‌جا عوض نشد.

روی همه‌ی فیلدهای مبلغ: نرخ‌های مالک، قیمتِ پلن، شارژِ اعتبار،
هزینه‌ها، پرداخت‌ها، و مبلغِ دلخواهِ شارژ در مینی‌اپ.

**و فقط مبلغ.** شماره‌ی کارت، شناسه‌ی تلگرام، حجم، روز و تعداد
کاربر جداکننده نمی‌گیرند — همین اشتباه دو بار افتاده. دروازه هر دو
سمت را می‌سنجد.

### Added — پوسته‌ی شخصیِ نماینده، با پیش‌نمایش

نماینده رنگِ فروشگاهش را انتخاب می‌کند و **همان‌جا** می‌بیند
مینی‌اپِ مشتریانش چه شکلی می‌شود. بدونِ پیش‌نمایش، تنها راهِ دیدنِ
نتیجه باز کردنِ مینی‌اپ با یک حسابِ واقعیِ تلگرام بود — یعنی عملاً
هیچ‌وقت.

یک رنگ می‌گیریم و پله‌هایش را خودمان می‌سازیم؛ پنج رنگِ جدا یعنی
ترکیب‌هایی که متن رویشان خوانده نمی‌شود. رنگِ نامعتبر → پوسته‌ی
پیش‌فرض، نه صفحه‌ی سفید.

**قابلیت پولی است و قیمتش را مالک تعیین می‌کند** — سیستم هیچ عددی
حدس نمی‌زند. صفر یعنی رایگان برای همه، نه «خاموش». پرداخت از همان
`tenants.credit` کم می‌شود که از قبل بود.

قفل **در بکند** است، نه فقط در رابط. و اشتراکِ تمام‌شده رنگ را
خاموش می‌کند ولی **پاکش نمی‌کند** — اگر دوباره تهیه شد، همان
پوسته برمی‌گردد.

### Added — داشبوردِ نماینده

یک صفحه: کارهای امروز (سفارشِ در انتظار، اشتراکِ رو به اتمام،
مشتریِ حجم‌تمام‌شده)، وضعیتِ مشتری‌ها، فروشِ ربات، و حسابِ دوره.

**هیچ عددی این‌جا حساب نمی‌شود** — همه از `/api/portal/summary` و
`/api/portal/stats` می‌آید، همان محاسبه‌ای که صورتحسابِ مالک
می‌کند.

### Added — چت خودش خبر می‌دهد

تحویلِ کانفیگ، یادآوریِ انقضا و هشدارِ حجم که تا امروز فقط در
تلگرام می‌رفتند، حالا در صندوقِ مینی‌اپ هم می‌نشینند. مشتری‌ای که
ربات را بلاک کرده یا نوتیفیکیشن را بسته، هیچ‌کدام را نمی‌دید و
اولین خبرش قطع‌شدنِ اتصال بود.

**همان متنِ تلگرام**، فقط بدونِ تگ (`F.plain`) — نه نسخه‌ی دوم، که
روزی یکی‌شان اصلاح می‌شود و دیگری نه. و ثبتش هرگز تحویل را
نمی‌شکند.

### Fixed — مینی‌اپ: لحظه‌ی انتظار

بعد از زدنِ «خرید»، تنها بازخوردِ ساختِ کانفیگ یک اسپینرِ ۱۵
پیکسلی داخلِ دکمه بود. ساخت چند ثانیه طول می‌کشد و مشتری در آن
چند ثانیه نمی‌دانست پولش کم شده یا نه — پس دوباره می‌زد.

حالا یک حالتِ کامل: حلقه‌ی ۵۶ پیکسلی، نام پلن، و **«پولتان کم شد»**
پیش از «این صفحه را نبندید» — چون مشتری اول آن را می‌خواهد بداند.
حلقه `flex: none` و `aspect-ratio: 1` دارد تا روی گوشیِ کوتاه
بیضی نشود.

کارتِ سفارشِ در انتظار هم واحد گرفت: «۲۸۰٬۰۰۰» بدونِ «تومان»
می‌توانست هر چیزی باشد.

### Fixed — صفحه‌ی اشتراک

چهار چیزی که مالک در اسکرین‌شات نشان داد، هر چهار با اندازه‌گیری:

| مشکل | اندازه‌گیری |
|---|---|
| حفره‌ی بزرگ زیرِ شبکه‌ی آمار | `auto-fit` روی ۹۰۰ پیکسل **۴** ستون می‌داد و قلم‌ها شش‌تا بودند → ۲ سلولِ خالی. حالا ۳ ستونِ ثابت (روی موبایل ۲) |
| «نامحدود» آبی روی کارتِ آبی | قالبِ «کیف پول» سه عنصر را تیره می‌کرد و `.days-left` در فهرست نبود |
| «GB 5.06» با واحدِ جابه‌جا | مقدارِ لاتین در جریانِ راست‌به‌چپ وارونه می‌شد → `direction: ltr; unicode-bidi: isolate` |
| لینکِ دعوت با `%D8%B3…` | حالا `t.me/…  ·  نام` دیده می‌شود و دکمه لینکِ کاملِ آماده را کپی می‌کند |

### Added — نُه دروازه، و جاروی دوازده‌موردی

دو دروازه در جاروی اول سبز ماندند و هر دو اصلاح شدند:

- «نبودِ نرخ صفر برنمی‌گرداند» متن را می‌سنجید و رشته جای دیگری هم
  بود؛ حالا خودِ شاخه سنجیده می‌شود
- شکستِ `MoneyInput` در جارو اشتباه هدف‌گیری شده بود (تستِ من غلط
  بود، نه کد)

جاروی نهایی: **دوازده شکستِ عمدی، هر دوازده قرمز.**

و probeی جدا با پول: کسرِ دقیق، تمدیدِ زودهنگام که روزهای
باقی‌مانده را نمی‌سوزاند، اعتبارِ ناکافی که چیزی کم نمی‌کند، و
پنج رنگِ نامعتبر که به دیتابیس نمی‌رسند — ۱۶ سنجه، همه سبز.

### دام‌های محیطی که دوباره خوردم

- **heredoc گیت‌بش `\n` را خورد** و یک دروازه را با
  `SyntaxError` شکست — همان چیزی که `CLAUDE.md` هشدارش را داده.
  با ابزار Write نوشته شد.
- **`Stat` دو بار تعریف شد** — بیلد گرفتش؛ نسخه‌ی دوم برداشته شد.
- **داده‌ی هارنس خالی بود** و کارتِ «سفارشِ در انتظار» هیچ‌وقت
  رندر نمی‌شد. با داده‌ی واقع‌نما دیده شد.

## [1.76.0]

### Added — رویدادهای ربات: شکست‌ها دیگر فقط در لاگِ سرور نمی‌مانند

اسکن نشان داد ربات **۱۷ جا** شکست را فقط با `log.exception` ثبت
می‌کند و ۱۲ تایش داخلِ زمان‌بند است — یعنی جایی که هیچ‌کس نگاه
نمی‌کند. چهار موردشان مستقیم پولِ مشتری‌اند: کانفیگ ساخته نشد،
ساخته شد و نرسید، سفارشِ تاییدشده تحویل نشد، تمدید خودکار شکست.

و جدولِ `events` از قبل در `bot/db.py` بود، دو جا نوشته می‌شد
(`provision` و `signup`) و **هیچ‌جا خوانده نمی‌شد** — نه کوئری، نه
مسیر API، نه صفحه. تانل‌ها «رویدادها» داشتند، ربات نداشت.

حالا صفحه‌ی **«رویدادها»** زیر بخشِ ربات: چه چیزی شکست، کِی، و
برای چه کسی — با فیلترِ «فقط خطاها» و صفحه‌بندیِ شماره‌دار. و برای
آن چهار موردِ پولی، همان لحظه پیام در گروهِ مدیریت، چون صفحه‌ای که
باز نشود فرقی با `journalctl` ندارد.

**یک تعریف، سه مصرف‌کننده.** نگاشتِ «نوع → برچسبِ فارسی، شدت، و
آیا گروه خبر شود» فقط در `bot/events.py` است. ربات مستقیم
می‌خواندش، پنل از راهِ API. اگر این نگاشت دو نسخه می‌شد، روزی چیپِ
صفحه یک چیز می‌گفت و پیامِ تلگرام چیزِ دیگری — همان الگویی که این
مخزن هفت بار دیده.

**تصمیمِ هشدار مالِ صداکننده نیست.** `record()` از `EV.is_alert`
می‌پرسد. اگر هر جای شکست خودش تصمیم می‌گرفت، دهمی فراموشش می‌کرد
— و آن یکی همیشه همانی است که مالک لازمش دارد.

فقط موردهای پولی گروه را خبر می‌کنند. اگر همه چیز هشدار بدهد،
هیچ چیز هشدار نیست.

### Fixed — ثبتِ شکست، پنج راه از شش را نمی‌دید

نسخه‌ی اول فقط دو `except` داخلِ `provision` را ثبت می‌کرد. probe
با پنلِ خاموش نشان داد شکست از راهِ **«اینباند پیدا نشد»** بیرون
می‌رود — یک `return False` ساده، نه استثنا. سفارش شکست می‌خورد،
پول برمی‌گشت، و هیچ رویدادی ثبت نمی‌شد.

حالا بدنه `_provision` است و پوسته تنها نقطه‌ی ثبت. راهِ ششمِ شکست
هم که روزی اضافه شود، خودبه‌خود ثبت می‌شود.

### Fixed — سه باگ که فقط در مرورگر پیدا شدند

- **کلیدِ «فقط خطاها» روشن می‌شد و هرگز خاموش نمی‌شد.** `Toggle`
  رویداد را پاس می‌دهد نه مقدار را، و شیءِ رویداد همیشه truthy
  است. با کلیکِ دوم دیده شد، نه با خواندنِ کد.
- **و برچسبی نداشت.** `label` در `Toggle` فقط `aria-label` است؛
  کاربر یک کلیدِ بی‌اسم می‌دید.
- **شناسه‌ی تلگرام جداکننده‌ی هزارگان می‌گرفت** —
  «۱۸۴٬۹۲۳٬۷۷۱». شناسه عدد نیست؛ حالا مثل بقیه‌ی پنل با
  `fx-idnum` و `dir="ltr"`.

### Fixed — `db.py` لاگر نداشت، و همان `except` که قرار بود خطا را ببلعد خودش می‌ترکید

`TenantDB.log()` عمداً خطایش را می‌بلعد: از داخلِ `except` صدا
زده می‌شود و خطای فرعی نباید جای خطای اصلی را بگیرد. ولی داخلِ
همان `except` یک `log.debug` بود و `log` در `bot/db.py` **تعریف
نشده بود** — یعنی هر ثبتِ ناموفق به‌جای سکوت، `NameError` می‌داد
و دقیقاً مسیرِ جبران (برگرداندنِ پول) را می‌شکست.

این را هیچ دروازه‌ی متنی نگرفت. جاروی شکستن گرفتش.

### Added — هشت دروازه، و جاروی هشت‌موردی

دو دروازه‌ی اول **متن** را می‌سنجیدند و با شکستنِ رفتار سبز
ماندند: با `if False:` به‌جای `if not ok:` متنِ `record(...)`
هنوز در فایل بود، و با `raise` به‌جای `return` هنوز `except` در
فایل بود. هر دو به سنجشِ رفتار عوض شدند — و همان تغییر بود که
باگِ `NameError` بالا را پیدا کرد.

جارو: هشت شکستِ عمدی، هر هشت قرمز.

- شکستِ ساختِ کانفیگ از یک نقطه ثبت شود (و رفتارش سنجیده شود)
- تصمیمِ هشدار فقط در `events.KINDS`
- هر نوعی که ثبت می‌شود در `KINDS` تعریف شده باشد (با `ast`، نه
  regex — regexِ اول شش تا از نُه صدازدن را نمی‌دید)
- هیچ عددِ فروشی از `events` شمرده نشود
- ثبتِ ناموفق صداکننده را نشکند
- جدول سقف داشته باشد (۵۰۰۰ ردیف در هر مستاجر، هرس در خودِ ثبت)
- کوئریِ رویدادها به مستاجر محدود باشد
- صفحه سوار باشد، نه فقط در فهرست

### اندازه‌گیری، نه نگاه‌کردن

| چیز | عدد |
|---|---|
| ردیف روی ۱۴۴۰ | ۶۳ پیکسل، بدون سرریز |
| روی ۳۷۵ (موبایل) | کارت ۳۴۳، صفحه اسکرولِ افقی ندارد |
| «فقط خطاها» | ۸ ردیف → ۵ → ۸ |
| صفحه‌بندی | «۱–۳۰ از ۴۱»، شماره‌دار |

## [1.75.1]

### Fixed — صفحه‌ی کاربران، کسی را «خریدار» می‌خواند که فقط تست گرفته بود

روی همان داده، دو صفحه‌ی پنل دو جواب می‌دادند به یک سوال:

| صفحه | «چند نفر خریدند؟» |
|---|---|
| آمار و قیف → «خرید موفق» | ۱ |
| کاربران ربات → چیپِ «خریدار» | **۲** |

علت همان الگوی همیشگیِ این مخزن: **یک قاعده، دو جا، اصلاح در
یکی.** قیف می‌دانست که گرفتنِ تستِ رایگان خودش یک سفارشِ
`approved` با مبلغ صفر می‌سازد و کنارش می‌گذاشت
(`COALESCE(p.is_trial,0)=0`). صفحه‌ی کاربران همین را نمی‌دانست و
`status='approved'`ِ خام می‌گرفت.

سه جا درست شد و هر سه حالا از همان ثابتِ `SQL_REAL_BUY` می‌خوانند،
نه از کپیِ دستی:

- چیپ و فیلترِ **«خریدار»** در «کاربران ربات»
- شمارنده‌ی کنارِ نامِ هر کاربر — کنارِ کسی که فقط تست گرفته بود
  می‌نوشت «۱ خرید · ۰ تومان»
- همان دو شمارنده در صفحه‌ی جزئیاتِ **همکارِ فروش**

چرا مهم است: چیپِ «خریدار» جایی است که مالک برای پیام‌دادن به
مشتری‌ها فیلتر می‌کند. هر کسی که دکمه‌ی تست را زده بود در آن
فهرست بود.

### Added — دروازه، تا مسیرِ چهارم دوباره فراموش نکند

`tools/test-seams.py`: هر کوئریِ SQL در `backend/app.py` که به
`o.user_id = u.id` گره خورده و `approved` را می‌سنجد، باید پلنِ
تست را کنار بگذارد. هر سه جا با شکستنِ عمدی سنجیده شد — هر سه
قرمز شدند.

### Fixed — دروازه‌ای که نصفِ فایل را نمی‌دید

`_nocomment()` برای JSX نوشته شده بود و هر چیزی بینِ `/*` و `*/`
را حذف می‌کرد. `backend/app.py` داخلِ رشته‌هایش `/*` دارد، پس
اولین اجرای دروازه‌ی بالا **۲۵۶ هزار کاراکتر — نصفِ فایل — را
بی‌صدا دور ریخت** و روی باگی که تازه پیدا شده بود سبز ماند.

`_py_nocomment()` اضافه شد که با `tokenize` کار می‌کند (نه regex،
چون `#` داخلِ رشته کامنت نیست)، و روی خودِ `_nocomment` نوشته شد
که مالِ JS است.

این دقیقاً همان درسِ جلسه‌ی قبل است: **دروازه‌ی سبز تا وقتی عمداً
نشکسته‌ای، هیچ ادعایی نکرده.**

## [1.75.0]

### Added — کدهای تخفیف بالاخره بازخورد دارند

مالک کد می‌ساخت و تنها چیزی که می‌دید `used_count` بود: «چند بار».
سه سوالِ واقعی بی‌جواب می‌ماند — چقدر تخفیف دادم، کدام کد فروش
آورد، و پیگیریِ تست جواب داد یا نه.

حالا بالای صفحه‌ی «کدهای تخفیف»:

- جمعِ سفارش‌های کددار، فروش، تخفیفِ داده‌شده، و تعدادِ کدهای
  استفاده‌شده
- هر کد جدا، مرتب بر اساس فروش
- و **نتیجه‌ی پیگیریِ تست**: «برای ۱۴ نفر کد رفت، ۱ نفر استفاده
  کرد و ۳۰٬۰۰۰ تومان فروش آورد»

اگر هیچ‌کس برنگشته باشد، همین را می‌گوید و پیشنهاد می‌کند درصد یا
متن را عوض کنید — نه اینکه یک صفر بی‌توضیح نشان بدهد.

**هیچ عددِ تازه‌ای حساب نمی‌شود.** تفاوتِ `base_amount` و `amount`
روی خودِ سفارش، همان تخفیفی است که داده شده. فقط خوانده می‌شود.

دو قاعده‌ی خودِ مخزن رعایت شده و تست هر دو را می‌سنجد:

- **تست خرید نیست.** سفارشِ پلنِ تست کنار گذاشته می‌شود، و سنجش
  با `is_trial` است نه با مبلغ — چون سفارشِ صددرصد تخفیف‌خورده هم
  صفر است ولی تبدیل است.
- **«فروش» با «درآمد» یکی نیست.** ستون «فروش» نوشته شده و زیرش
  هم گفته شده که خریدِ از کیف پول شمرده می‌شود چون پولش موقعِ
  شارژ رسیده. ادعای درآمد نمی‌کند.

گزارش به همان مسیرِ موجود چسبید، نه یک مسیرِ تازه: صفحه از قبل
صدایش می‌زد و دو درخواست برای یک صفحه یعنی دو جای خطا. و اگر
گزارش نیاید، فهرستِ کدها همچنان می‌آید.

### Changed — تست‌ها

دروازه‌ی جریانِ ربات از ۲۶۳ به ۲۷۶ سنجه. هر هشت اصلاح عمداً
برگردانده شد — از جمله «تستِ رایگان هم در گزارش بیاید» و «گزارش
مستاجرِ دیگر را هم ببیند» — و هر هشت بار قرمز شد.

## [1.74.0]

### Added — پیشنهادِ پست، از روی رویدادهای واقعیِ پنل (فاز ۲)

پنل چیزهایی می‌داند که هیچ ابزارِ محتوایی نمی‌داند. حالا از همان‌ها
پیشنهاد می‌سازد:

| پیشنهاد | رویدادی که پشتش است |
|---|---|
| اعلامِ کدِ تخفیف | کدی ساخته شده و هیچ‌وقت در کانال نیامده |
| معرفیِ پلنِ تازه | پلنی در سه هفته‌ی اخیر اضافه شده و معرفی نشده |
| یادآوریِ تمدید | سه اشتراک یا بیشتر در هفت روزِ آینده تمام می‌شود |
| معرفیِ تستِ رایگان | تست روشن است ولی در کانال نیامده |
| پستِ آموزشی | هفت روز یا بیشتر چیزی گذاشته نشده |
| پستِ نافرجام | پستی هست که نرفته |

**هیچ پیشنهادی از هوا ساخته نمی‌شود.** هر کارت دلیلش را **با عدد**
می‌گوید — «۲۳ اشتراک در هفت روزِ آینده تمام می‌شود»، نه «شاید بد
نباشد پستی بگذارید». اگر رویدادی نباشد، فهرست خالی است. کانالی که
هر روز پستِ توخالی دارد، از کانالِ ساکت بدتر است.

و **چیزی که قبلاً اعلام شده دوباره پیشنهاد نمی‌شود** — سنجش با
متنِ پست‌های رفته است، نه با پرچمِ جدا. پرچم از واقعیت جدا می‌افتد؛
متنِ پست نه.

کدی که ظرفیتش تمام شده، منقضی شده، یا خاموش است پیشنهاد نمی‌شود:
اعلامِ کدی که کار نمی‌کند، بدتر از نگفتن است. و کدی که به‌زودی تمام
می‌شود، فوریتش داخلِ خودِ متن می‌آید.

منطقش در `bot/ideas.py` است — تابعِ خالص، بدونِ FastAPI و بدونِ
دیتابیس. مسیرِ API فقط ردیف‌ها را می‌خواند؛ هیچ عددی آن‌جا ساخته
نمی‌شود.

### Added — نوشتن با هوش مصنوعی (فاز ۳) — اختیاری

**این ستون نیست، چاشنی است.** بخشِ کانال بدونش کامل کار می‌کند، و
اگر تنظیم نشده باشد دکمه‌ی بی‌جواب نمی‌گذاریم: همان‌جا نوشته می‌شود
که چرا و راهِ تنظیمش کجاست.

چرا «ارائه‌دهنده‌محور» و نه یک سرویسِ مشخص: سرویسِ بی‌کلید ممکن است
فردا بسته شود، لایه‌ی رایگانِ سرویس‌های بزرگ از آی‌پیِ ایران معمولاً
بسته است، و **هر کلیدی که باشد در مخزنِ عمومی نمی‌نشیند**. پس مالک
آدرس و مدل و کلیدِ خودش را می‌گذارد؛ شکلِ درخواست همان
`/v1/chat/completions` است که تقریباً همه می‌پذیرند.

**نام برند، پلن‌ها، اپ‌ها و لحن خودکار به درخواست اضافه می‌شوند.**
بدونِ این، خروجی همان متنِ عمومیِ اینترنتی است که مالک خودش هم
می‌توانست از هر جایی کپی کند.

تگ‌های مجازِ تلگرام در دستور به مدل گفته می‌شوند، و مارک‌داونی که
مدل‌ها عادت دارند بنویسند (`**` و `__`) به تگِ تلگرام تبدیل
می‌شود — تلگرام `**` را تگ نمی‌شناسد و پیام خام می‌رود. هر چه از
این هم رد شود، همان اعتبارسنجِ فاز ۱ پیش از ارسال می‌گیرد.

تصویر هم اختیاری است: نشانیِ سرویس با `{q}` به‌جای موضوع. با این
شکل، مالک هر وقت سرویس عوض شد جایش را عوض می‌کند بدونِ اینکه ما
نسخه بدهیم. اگر نشانی نگذارد، دکمه‌ی تصویر اصلاً نمی‌آید.

خطاها ترجمه می‌شوند: «کلید پذیرفته نشد»، «سرویس موقتاً محدودتان
کرده»، «به سرویس نرسیدیم — آدرس، اینترنتِ سرور، یا فیلترشدنش را
بررسی کنید». `HTTP Error 401` به کسی نمی‌گوید چه کند.

### Security — کلیدِ هوش مصنوعی ماسک می‌شود

`bot_token` و `panel_pass` از قبل بریده برمی‌گشتند، ولی کلیدِ تازه
داخلِ `settings` می‌نشیند و از آن حلقه رد نمی‌شد. حالا جدا ماسک
می‌شود.

و نگهبانِ دومی که لازم بود: **مقدارِ ماسک‌شده روی کلیدِ واقعی
نمی‌نشیند.** صفحه تنظیمات را با کلیدِ بریده می‌خواند و پس
می‌فرستد؛ بدونِ این نگهبان، اولین ذخیره کلید را به `sk-ab…` تبدیل
می‌کرد و سرویس **بی‌صدا** از کار می‌افتاد.

### Fixed — موضوع بعد از «بنویس» پاک می‌شد

بعد از گرفتنِ متن، مالک معمولاً برای همان موضوع تصویر هم می‌خواهد.
پاک‌شدنِ موضوع دکمه‌ی تصویر را غیرفعال می‌کرد بی‌آنکه بگوید چرا.

### Changed — تست‌ها

دروازه‌ی جریانِ ربات از ۲۳۷ به ۲۶۳ سنجه. هر چهارده اصلاح عمداً
برگردانده شد؛ **یکی سبز ماند** و خودِ سنجه ضعیف بود، نه کد: نتیجه
را می‌سنجید نه دلیل را. با برداشتنِ نگهبان هم درخواست شکست می‌خورد
(چون آدرسی نبود) و سنجه راضی می‌شد. حالا تفاوتِ «همین‌جا رد شد» با
«رفت و نرسید» سنجیده می‌شود.

## [1.73.1]

### Fixed — بخشِ کانال به عرضِ پنجره واکنش می‌داد، نه به عرضِ خودش

اندازه‌گیری‌شده، با منوی باز:

| پنجره | صحنه | کادرِ نوشتن | پیش‌نمایش |
|---|---|---|---|
| ۱۱۰۰ | ۸۴۰ | ۴۳۴ | ۳۲۶ |
| ۱۲۸۰ | ۹۸۲ | ۵۱۴ | ۳۸۸ |
| ۱۶۰۰ | ۱۳۰۲ | ۶۹۵ | ۵۲۷ |

`@media (min-width: 1100px)` عرضِ *پنجره* را می‌سنجد، ولی منوی
کناری حدود سیصد پیکسل از آن را می‌برد. پس گرید در پنجره‌ی ۱۱۰۰
دوستونه می‌شد در حالی که صحنه فقط ۸۴۰ بود — دو ستونِ خفه.

حالا با `@container` عرضِ خودِ بخش سنجیده می‌شود:

| پنجره | کادرِ نوشتن |
|---|---|
| ۱۱۰۰ | ۴۳۴ → **۸۰۶** |
| ۱۲۸۰ | ۵۱۴ → **۹۴۸** |

و چیزی که مدیاکوئری هیچ‌وقت نمی‌توانست: در همان پنجره‌ی ۱۲۸۰، اگر
مالک منو را جمع کند صحنه ۹۸۲ → ۱۱۵۰ می‌شود و گرید **همان‌جا**
دوستونه می‌شود (۶۳۲ + ۵۰۶). عرضِ پنجره عوض نشده؛ فضای واقعی عوض
شده.

ورودیِ آدرسِ کانال هم عرضِ ثابتِ ۲۰۰ پیکسل داشت و حالا کشسان است.

روی موبایل هیچ سرریزی نبود و نیست — مشکل فقط در میانه‌ی بازه بود.

### Fixed — تستِ اجرا صفحه‌ی کانال را اصلاً باز نمی‌کرد

`test-apps-runtime.js` فهرستِ دستیِ نامِ بخش‌ها داشت
(`WS = ["صفحه اشتراک", "حسابداری", …]`) و «کانال» در آن نبود. پس
آن صفحه هرگز باز نمی‌شد و تست می‌گفت «هر ۴۹ صفحه‌ی پنل باز
می‌شود — بدون خطا».

با شکستنِ عمدیِ صفحه هم **سبز ماند**. همان بیماریِ فهرستِ دستی که
در ۱.۷۳.۰ در دروازه‌ی درز اصلاح شد، این‌بار در فایلِ دیگر.

حالا سرِ هر بخش `data-navhead` دارد و تست ساختار را می‌خواند نه
متن را. شمارش از ۴۹ به **۵۰** رسید، و با همان شکستنِ عمدی قرمز
می‌شود:

    ✗ 3 صفحه از 50 مشکل دارد
        ▸ channel (کرش کرد)

## [1.73.0]

### Added — بخشِ «کانال»

مالک کانال دارد و نمی‌داند چه بگذارد. این مشکلِ کمبودِ ایده نیست —
مشکلِ **کادرِ خالی** است. پس قاعده‌ی این بخش: پنل هیچ‌وقت کادرِ خالی
نشان ندهد.

بخشِ سطحِ‌اول، کنارِ «فایروال» و «تانل» — نه یک صفحه‌ی دیگر زیرِ
«ربات»، که فهرستش چهارده قلم دارد و مالک از شلوغی‌اش گفته.

- **شش قالبِ آماده** که با دادهٔ خودِ پنل پر می‌شوند: نامِ برند،
  پلن‌ها و قیمت‌هایشان، اپ‌هایی که در تنظیمات هست. نه متنِ عمومی.
- **قالب‌بندیِ تلگرام**: پررنگ، کج، زیرخط، خط‌خورده، کد، اسپویلر،
  نقلِ‌قول و لینک — هر دکمه انتخابِ شما را در تگ می‌پیچد.
- **پیش‌نمایش** کنارِ کادر، با شکلِ حبابِ تلگرام.
- **عکس**، با کوچک‌شدن در مرورگر پیش از بارگذاری.
- **زمان‌بندی** با تاریخِ شمسی.
- **فهرستِ پست‌ها** با سه حالت: رفت، در نوبت، نرفت — و هر «نرفت»
  دلیلش را کنارِ خودش دارد.

**اعتبارسنجِ HTML دوباره نوشته نشد.** تلگرام پیامی که تگش خراب
باشد را اصلاً نمی‌فرستد، و `bot/fmt.py` از قبل این را می‌سنجد —
همان که `tg.send` از آن رد می‌شود. پنل نسخه‌ی جاواسکریپتیِ خودش را
ندارد؛ از بک‌اند می‌پرسد. یک قاعده در دو جا، پرتکرارترین باگِ این
مخزن.

**دامِ سقفِ کپشن:** پیامِ متنی تا ۴۰۹۶ کاراکتر است ولی عکس با کپشن
تا ۱۰۲۴. متنی که بدونِ عکس می‌رفت، با افزودنِ عکس رد می‌شود.
شمارنده با وصل‌کردنِ عکس سقفش را عوض می‌کند و دکمه‌ی ارسال خاموش
می‌شود — پیش از ارسال، نه بعد از خطای تلگرام.

**خطای تلگرام ترجمه می‌شود.** `Forbidden: bot is not a member of
the channel chat` به کسی نمی‌گوید چه کند؛ حالا می‌گوید «ربات در
کانال نیست — اول اضافه‌اش کنید و ادمینش کنید».

پستِ زمان‌بندی‌شده را زمان‌بندِ ربات با **ادعای اتمی** برمی‌دارد.
پستِ تکراری در کانال، برخلاف پیامِ تکراری، جلوی چشمِ همه می‌ماند.

### Fixed — سه چیزی که probe و دروازه‌ها گرفتند

**پستِ نافرجام هیچ‌وقت دوباره تلاش نمی‌شد.** `channel_done` ادعا را
پس می‌داد ولی وضعیت را `failed` می‌کرد، و `channel_claim` فقط
`draft` و `queued` را می‌پذیرفت. کامنتِ خودِ تابع ادعای خلافش را
می‌کرد. جاروی شکستن این را نشان نداد — probe نشان داد، چون رفتار
را می‌سنجد نه شکلِ کد را.

**پیامِ خطا به هویتِ کلاسِ استثنا وابسته بود.** `handlers` با
`from tg import TelegramError` برمی‌دارد و `run.py` با
`from bot.tg import …` — دو شیءِ کلاسِ جدا که فقط اسمشان یکی است،
همان دامی که راهنمای مخزن درباره‌ی `db` هشدار می‌دهد. نتیجه:
`except TelegramError` نمی‌گرفت و مالک متنِ خامِ انگلیسی را می‌دید.
حالا تشخیص از روی متن است، نه کلاس.

**`channel_id` در پنل فیلد نداشت** — و دروازه‌ی درزِ ۱.۷۱.۰ دقیقاً
همین را گرفت، همان چیزی که برایش ساخته شده بود. آدرسِ کانال حالا
در خودِ همان صفحه تنظیم می‌شود.

### Changed — دروازه‌ی درز دیگر فهرستِ دستی ندارد

`_FRONT` پنج فایل را با اسم می‌خواند، و صفحه‌ی «کانال» در آن پنج‌تا
نبود. دروازه‌ای که به‌روز نگه داشتنش کارِ دستی باشد، روزی به‌روز
نمی‌شود. حالا هر فایلی که `/api/admin/bot/settings` را صدا بزند
خودش وارد می‌شود.

و تنظیم‌هایی که فقط پنل مصرفشان می‌کند (مثل `quick_replies`)
فهرستِ مجازِ خودشان را دارند، هر قلم با دلیلش.

### Changed — تست‌ها

دروازه‌ی جریانِ ربات از ۲۱۳ به ۲۳۷ سنجه. هر دوازده اصلاح عمداً
برگردانده شد؛ یکی سبز ماند و خودِ موردِ جارو غلط بود، نه دروازه.

## [1.72.0]

### Added — پاسخ‌های آماده در صندوق پشتیبانی

همان ده جوابی که هر روز تایپ می‌شدند: «لینک را کپی کنید»، «اپ را
نصب کنید»، «رسیدتان تایید شد». حالا شش دکمه بالای کادرِ تایپ‌اند.

سه تصمیم و دلیلشان:

- **کلیک، متن را در کادر می‌گذارد — نمی‌فرستد.** جوابِ آماده تقریباً
  همیشه یک جمله کم دارد؛ دکمه‌ای که خودش بفرستد یعنی آن جمله
  هیچ‌وقت اضافه نمی‌شود. و پیامی که ناقص رفته برنمی‌گردد.
- **نامِ مشتری از همان گفتگو پر می‌شود.** `{name}`، `{brand}` و
  `{support}` — و فقط همین سه‌تا، چون فقط داده‌ی همین‌ها در این صفحه
  هست. جای‌گذاری که پرکننده ندارد وسطِ جمله سوراخ می‌گذارد، پس تست
  می‌سنجد که هر جای‌گذارِ استفاده‌شده پرکننده داشته باشد.
- **پیش‌فرض دارد.** ردیفِ خالی یعنی مالک نمی‌فهمد این‌جا قرار بوده چه
  باشد. شش پاسخ از روزِ اول هست و همه‌شان قابلِ ویرایش‌اند.

کلیکِ دوم به پاسخِ قبلی *اضافه* می‌کند، پس دو جواب را می‌شود پشت هم
چید.

اندازه‌گیری‌شده، نه چشمی: نوار ۴۲ پیکسل است، روی ۱۴۴۰ هر شش دکمه
بدون سرریز جا می‌شوند، و روی ۳۷۵ خودِ نوار افقی اسکرول می‌شود بدون
اینکه صفحه اسکرولِ افقی بگیرد.

### Fixed — رنگِ خلاصه‌ی تست ظاهر، زود حساب می‌شد

`color` وسطِ `test-ui-safety.py` حساب می‌شد ولی چاپش آخرِ فایل است.
هر سنجه‌ای که بعد از آن خط رد می‌شد، عددِ «ناموفق» را بالا می‌برد
ولی خلاصه هنوز **سبز** چاپ می‌شد. کدِ خروج درست بود، پس دروازه
واقعاً قرمز می‌شد — ولی کسی که فقط به خطِ آخر نگاه می‌کرد، سبز
می‌دید.

## [1.71.0]

### Added — یادآوری‌ها بالاخره دستِ مالک است

`run.py` دو جا `reminders.enabled` را می‌خواند — یادآوریِ انقضا و
هشدارِ حجم. **هیچ صفحه‌ای در پنل این کلید را نمی‌نوشت.** سوییچ در
کد بود و راهی به آن نبود.

و آستانه‌ها ثابتِ ماژول بودند: `(7, 3, 1)` روز و `80`٪. مالکی که
می‌خواست ۱۴ روز مانده هم خبر بدهد، یا سرِ ۹۰٪ هشدار بدهد،
نمی‌توانست.

حالا در «متن‌ها و یادآوری‌ها»:

- روشن/خاموشِ کلِ یادآوری‌ها
- سه نوبتِ یادآوریِ انقضا، هر کدام با عددِ خودش — صفر یعنی آن نوبت
  نرود
- درصدِ هشدارِ حجم — صفر یعنی این هشدار خاموش
- متنِ هشدارِ حجم، با جای‌گذارهای `{used}` `{total}` `{left}`
  `{pct}` `{plan}` `{label}`

سه نوبتِ ثابت ماند چون پرچمِ «فرستاده شد» سه ستونِ مشخص است
(`notified_7d/3d/1d`). فهرستِ آزاد یعنی ستونِ `notified_14d` هم
لازم است. سه اسلات که مالک عددشان را عوض می‌کند همان کار را بدونِ
مهاجرت می‌کند.

عوض‌کردنِ عددِ یک نوبت، کسی را که قبلاً همان نوبت را گرفته دوباره
خبر نمی‌کند — سرِ تمدید پرچم‌ها صفر می‌شوند. این عمدی است؛
جایگزینش یعنی همه‌ی مشتری‌ها یک پیامِ تکراری بگیرند. در خودِ صفحه
هم نوشته شده.

### Fixed — متنِ سفارشیِ انقضا دکمه‌ی اشتباه می‌داد

مسیرِ پیش‌فرضِ یادآوری، دکمه‌ی `renew:{id}` می‌دهد — همان اشتراک.
مسیرِ متنِ سفارشی `"buy"` می‌داد: فهرستِ کلِ پلن‌ها. کامنتِ خودِ کد،
سه خط پایین‌تر، می‌گوید چرا این غلط است: «وگرنه کاربر دوباره باید
حدس بزند کدام را انتخاب کند».

یعنی هر مالکی که متنِ یادآوری را عوض می‌کرد، بی‌آنکه بداند دکمه‌ی
هدفمند را هم از دست می‌داد. یک قاعده، دو جا، اصلاح در یکی — هشتمین
بارِ همین شکل در این مخزن.

### Added — دروازه‌ی درزِ تنظیمات، هر دو جهت

باگِ سکه این بود: پنل `settings.coins_per_referral` می‌نوشت و ربات
`settings.coins.per_referral` می‌خواند. هیچ خطایی نداد — عدد فقط
هیچ‌وقت نرسید. دروازه‌ای که بعدش نوشته شد **فقط همان یک کلید** را
می‌سنجید.

حالا عمومی است:

- هر کلیدی که ربات از تنظیماتِ مستاجر می‌خواند، باید در پنل فیلدی
  داشته باشد
- هر کلیدی که پنل زیرِ `settings` می‌نویسد، باید جایی خوانده شود
- و هر کارتی که در صفحه‌های ربات ساخته شده، باید جایی سوار هم باشد

سومی از خودِ جاروی شکستن آمد: برداشتنِ `<Reminders />` از JSX هیچ
دروازه‌ای را قرمز نمی‌کرد، چون کلید هنوز در فایل دیده می‌شد.

اسکن **هر دو** `handlers.py` و `run.py` را می‌بیند. مرورِ دستیِ
قبلی فقط اولی را دیده بود و `reminders` از همان‌جا جا ماند.

راستی‌آزماییِ خودِ دروازه: با برگرداندنِ پنل به وضعیتِ دیروز، قرمز
می‌شود و اسمِ کلید را می‌گوید — `reminders — ربات می‌خواند، هیچ
صفحه‌ای نمی‌نویسد`.

### Fixed — Ctxِ قلابیِ تست‌ها با اصل نمی‌خواند

`Ctx` واقعی `.s` (تنظیمات) و `.bot` دارد؛ قلابیِ
`test_reminders.py` هیچ‌کدام را نداشت. یعنی تستِ سبزی که در عمل
`AttributeError` می‌شد. و `send_expiry_notice` بالای فایل با یک
قلابی جایگزین می‌شد، پس صدازدنش در آخرِ فایل همان قلابی را صدا
می‌زد و متنِ واقعی هرگز ساخته نمی‌شد.

حالا هر دو درست‌اند و سنجه‌ای هست که برابریِ قلابی با اصل را نگه
می‌دارد.

### Changed — تست‌ها

`test_reminders` از ۳۶ به ۵۶ سنجه، `test-seams` از ۵۰ به ۵۴. هر
هشت اصلاح عمداً برگردانده شد؛ **دو تا سبز ماندند** و هر دو اصلاح
شدند: آستانه‌ی حجم فقط مستقیم سنجیده می‌شد نه از راهِ زمان‌بند، و
کارتِ سوارنشده از دید دروازه «فیلد» به حساب می‌آمد.

## [1.70.0]

### Added — کد تخفیف، حالا از داخلِ خودِ ربات

در ۱.۶۹.۰ کد تخفیف وصل شد — ولی **فقط از مینی‌اپ**. مشتری‌ای که از
خودِ ربات می‌خرید هیچ جایی برای واردکردنش نداشت: نه دکمه‌ای، نه
حالتی. `checkout` و `wallet_pay` پارامترِ `discount_code` را که
هسته‌ها می‌پذیرفتند، هیچ‌وقت پاس نمی‌دادند.

و این از یک شکافِ ساده بدتر بود، چون در **همان نسخه** پیگیریِ تست
هم ساخته شد: کدی یک‌بارمصرف که از داخلِ تلگرام برای مشتری فرستاده
می‌شود. یعنی ربات کدی می‌فرستاد که خودش قبولش نمی‌کرد. و اگر
مستاجر مینی‌اپ را تنظیم نکرده بود، پیام حتی دکمه‌ی «دیدن پلن‌ها» هم
نداشت — مشتری کدی می‌گرفت و **هیچ راهی** برای استفاده‌اش نداشت.

حالا در صفحه‌ی هر پلن دکمه‌ی «🎟 کد تخفیف دارم» هست. کد را
می‌فرستد، همان صفحه با قیمتِ تازه دوباره کشیده می‌شود، و هر دو
دکمه‌ی خرید — کارت و کیف پول — کد را با خود می‌برند.

چند تصمیمِ ریز و دلیلشان:

- **کد روی ردیفِ کاربر نگه داشته می‌شود** (`held_discount`)، نه در
  `state_data` که وسطِ همین مسیر با `await_receipt` پاک می‌شود، و
  نه داخلِ `callback_data` که سقفِ ۶۴ بایتیِ تلگرام را با کدِ
  چهل‌کاراکتری لبریز می‌کند.
- **هر بار از نو سنجیده می‌شود.** ظرفیت ممکن است بین دیدنِ صفحه و
  زدنِ دکمه تمام شود؛ قیمتی که نشان داده‌ایم باید همان باشد که
  گرفته می‌شود.
- **کدِ نامعتبر بی‌صدا دور انداخته نمی‌شود** — همان‌جا نوشته
  می‌شود که چه شد، نه اینکه قیمت بی‌توضیح به حالتِ اول برگردد.
- **تا وقتی فعال است دیده می‌شود**، با دکمه‌ی برداشتنش. کدی که روی
  قیمت اثر دارد و یادِ کسی نیست، همان مسیرِ خرابِ بی‌صداست.
- **بعد از ساختِ سفارش برداشته می‌شود** — ماندنش یعنی خریدِ بعدی
  هم بی‌آنکه مشتری بخواهد تخفیف می‌گیرد.

پوسته‌ها هنوز پوسته‌اند: `checkout` و `wallet_pay` فقط کد را به
هسته می‌دهند. تست می‌سنجد که هیچ‌کدام خودشان `core.price_order` یا
`create_order` صدا نزنند.

### Changed — پیامِ پیگیریِ تست

حالا می‌گوید کد را کجا باید زد، و دکمه‌ی «دیدن پلن‌ها» **همیشه**
همراهش است — پیش از این فقط وقتی می‌آمد که مینی‌اپ تنظیم شده باشد.

### Changed — تفکیکِ قیمت

`core.price_order` حالا `code_percent` را هم برمی‌گرداند، کنارِ
مبلغش. بدون آن، صداکننده باید از روی دو عدد دوباره درصد را حساب
کند — همان‌جا که گرد‌کردن عددی می‌سازد که با آنچه به مشتری گفته‌ایم
یکی نیست.

### Changed — تست‌ها

دروازه‌ی جریانِ ربات از ۱۹۲ به ۲۱۳ سنجه رسید. سنجه‌های تازه از راهِ
`dispatch` می‌روند، نه با خواندنِ متنِ کد — دکمه‌ای که در روتر وصل
نشده باشد همان‌جا خودش را نشان می‌دهد.

هر ده اصلاح عمداً برگردانده شد. **یکی از آن‌ها سبز ماند**: سنجه‌ی
«قیمتِ تازه روی دکمه» به کلِ پیام نگاه می‌کرد و قیمتِ تخفیف‌خورده
در متن هم بود، پس برچسبِ غلطِ دکمه را نمی‌دید. حالا فقط به خودِ
دکمه‌ها نگاه می‌کند.

## [1.69.1]

### Fixed — دروازه‌ای که به‌شکلِ تصادفی اتهام می‌زد

تستِ مانیتور می‌سنجد که یک عکس‌برداری چند بار «صبر» می‌کند — چون
قبلاً CPU و شبکه هر کدام پنجره‌ی خودشان را داشتند و ۱.۲ ثانیه در
هر تازه‌سازیِ صفحه تلف می‌شد.

ولی شمارنده‌اش سراسری بود. `MF.time` خودِ ماژولِ استانداردِ `time`
است، نه کپی‌اش — پس `MF.time.sleep = …` آن را برای **کلِ پردازه**
عوض می‌کرد. و `subprocess.run(..., timeout=…)` روی لینوکس در
انتظارِ پایانِ فرمان یک حلقه‌ی پس‌رفتِ نمایی می‌زند:
`delay = min(delay * 2, remaining, .05)`.

یعنی هر بار که `network()` دستورِ `ss` را صدا می‌زد، ده‌ها خواب به
حسابِ مانیتور نوشته می‌شد که مالِ خودش نبود — دقیقاً همان
`[0.001, 0.002, 0.004, …, 0.05, 0.05, …]` که در CI دیده شد.

چرا تا امروز پیدا نشد: روی ویندوز `ss` نیست و انتظارِ subprocess هم
`WaitForSingleObject` است، نه حلقه‌ی خواب. پس محلی همیشه سبز بود و
روی رانر بسته به اینکه iproute2 داشته باشد یا نه، قرمز یا سبز.

حالا به‌جای دست‌زدن به ماژولِ مشترک، یک پوسته‌ی نازک فقط برای همان
ماژولِ مانیتور گذاشته شده. و سنجه‌ای اضافه شد که خودِ این را
می‌سنجد: خوابی که مانیتور نکرده، نباید شمرده شود — که با شیوه‌ی
قبلی روی **هر** سکویی قرمز می‌شود، نه فقط جایی که `ss` هست.

## [1.69.0]

### Added — کد تخفیف، بالاخره وصل شد

جدولِ `discounts`، تابعِ `validate_discount` و ستون‌های
`discount_code`/`discount_pct` روی سفارش‌ها **از قبل در مخزن بودند**
— و هیچ‌جا صدا زده نمی‌شدند. یعنی مالک می‌توانست در دیتابیس کد
بسازد و هیچ مشتری‌ای هرگز نمی‌توانست استفاده‌اش کند. راهنمای پروژه
هم صریح نوشته بود «رویش حساب باز نکن».

حالا هر سه سر وصل شده‌اند: صفحه‌ای در پنل برای ساختن و بستنِ کد،
جای واردکردنش در برگه‌ی پرداختِ مینی‌اپ، و اعمالش در هر دو هسته‌ی
خرید (کارت و کیف پول).

چهار چیز که عمداً این‌طور ساخته شدند:

- **ظرفیت سرِ تایید مصرف می‌شود، نه سرِ ساخت.** سه نفر که برگه‌ی
  پرداخت را باز کنند و بروند، نباید کدِ ده‌نفره را تمام کنند.
- **سفارشی که روی مرزِ ظرفیت است، قیمتش عوض نمی‌شود.** سقف ابزارِ
  بازاریابی است، نه بهانه‌ی گرفتنِ مبلغی جز آنچه به مشتری گفته‌ایم.
- **جست‌وجوی کد همیشه با `tenant_id`.** بدونش مشتریِ یک نماینده
  کدِ نماینده‌ی دیگر را استفاده می‌کند و تخفیف از جیبِ اشتباه
  می‌رود.
- **ترتیب: اول کد، بعد سکه، بعد تخفیفِ عمده.** یک جا، در
  `core.price_order` — نه سه نسخه که روزی از هم دور بیفتند.

و مصرفِ ظرفیت **فقط** داخل `close_order` نوشته شده، چون تنها
جایی است که سفارش `approved` می‌شود. هر جای دیگری یعنی یکی از
چهار مسیرِ خرید فراموشش می‌کند — همان اشتباهی که در این مخزن سه
بار افتاد.

### Fixed — پورسانت روی پولی که نرسیده بود

`_pay_commission` پایه‌ی محاسبه را از صداکننده می‌گرفت و سه
صداکننده سه چیزِ متفاوت می‌دادند. تا وقتی تخفیفی در کار نبود، هر
سه یک عدد می‌شدند و فرق دیده نمی‌شد.

با کد تخفیف این دو از هم جدا می‌شوند: قیمتِ پلن ۲۰۰ هزار، پولِ
رسیده ۱۰۰ هزار — و دو مسیر پورسانتِ همکارِ فروش را روی ۲۰۰ هزار
حساب می‌کردند. یعنی پولی که هرگز دریافت نشده بود.

حالا تابع مبلغ را از خودِ سفارش می‌خواند و هیچ صداکننده‌ای پایه
پاس نمی‌دهد. تست می‌سنجد که این‌طور بماند.

### Added — پیگیریِ کسی که تست گرفت و نخرید

سخت‌ترین قدم را برداشته بود — نصب کرده و وصل شده — و بعد رها
می‌شد. تا امروز هیچ‌کس سراغش نمی‌رفت.

حالا چند ساعت بعد از پایانِ تست، **یک** پیام با یک کدِ تخفیفِ
مخصوصِ خودش می‌رود. مقدارِ درصد، فاصله‌ی زمانی، اعتبارِ کد و متنِ
پیام همه در پنل تنظیم می‌شوند و پیش‌فرض خاموش است.

سه شرط که هر سه لازم‌اند:

- تست گرفته باشد
- **هیچ خریدِ پولی نکرده باشد** — و این با «مبلغش صفر نیست» یکی
  نیست: تستِ رایگان خودش یک سفارشِ `approved` با مبلغ صفر
  می‌سازد، پس سنجش با `COALESCE(p.is_trial,0)=0` است نه با مبلغ
- قبلاً پیگیری نشده باشد

کد **یک‌بارمصرف و زمان‌دار** است؛ کدِ مشترک را یک نفر در گروه
می‌گذارد و همه استفاده می‌کنند. و پرچمِ «پیگیری شد» چه پیام برود
چه نرود زده می‌شود — وگرنه کسی که ربات را بلاک کرده هر ساعت یک
کدِ تازه می‌گیرد.

### Added — عکس در گفتگوی پشتیبانی

مشتری در مینی‌اپ و پشتیبان در پنل حالا می‌توانند عکس بفرستند — با
متن یا بدونش. عکس پیش از ارسال در مرورگر کوچک می‌شود (بیشینه
۱۶۰۰ پیکسل) تا گفتگو روی اینترنتِ موبایل هم باز شود.

نامِ فایل تصادفی است و مسیرِ عمومی‌اش فقط با همان نام باز می‌شود؛
سقفِ ۳ مگابایت هم روی خودِ مسیر گذاشته شده.

### Changed — تست‌ها

دروازه‌ی جریانِ ربات از ۱۷۱ به ۱۹۲ سنجه رسید. هر هفت اصلاحِ این
نسخه عمداً برگردانده شد تا مطمئن شویم دروازه قرمز می‌شود؛ تستی که
با باگ هم سبز بماند، تست نیست.

## [1.68.0]

### Changed — پاسخِ پشتیبانی دیگر ربات را شلوغ نمی‌کند

تا امروز **متنِ کاملِ پاسخ** در گفتگوی ربات هم نوشته می‌شد. یعنی
هر مکالمه دو بار وجود داشت — یک‌بار در مینی‌اپ و یک‌بار در ربات —
و ربات پر می‌شد از چیزی که مشتری همان‌جا در مینی‌اپ داشت.

ولی حذفِ کامل هم درست نبود: مشتری‌ای که مینی‌اپ را باز نکند
هیچ‌وقت نمی‌فهمد جواب آمده.

پس حالا یک خطِ کوتاه با دکمه‌ی بازکردنِ مینی‌اپ می‌رود — بدونِ متنِ
پاسخ — و **فقط وقتی خبرِ خوانده‌نشده‌ای از قبل نمانده باشد**.

اندازه‌گیری‌شده: پنج پاسخِ پشت‌سرهم، **یک** پیام در ربات به‌جای
پنج‌تا. به‌محضِ اینکه مشتری صندوقش را باز کند، چرخه از نو شروع
می‌شود، پس پاسخِ بعدی باز هم خبر می‌دهد.

## [1.67.2]

### Changed — برچسبِ کنارِ عددِ مصرف برداشته شد

دو نسخه‌ی قبل کنارِ عددِ مصرف نوشته می‌شد که چقدرش پیش از ریست
بوده. مالک پرسید «اصلاً برای چه باید برچسب باشد؟ باید اصلِ مصرف
باشد» — و درست می‌گفت.

عددِ مصرف *همان* کلِ مصرف است. آن پرانتز توضیحِ پیاده‌سازی بود، نه
چیزی که به کارِ کسی بیاید؛ و حالا که عدد با پنلِ x-ui یکی است،
چیزی برای توضیح نمانده.

هر کسی تفکیک بخواهد، `nexora usage-why` می‌دهدش.

## [1.67.1]

### Changed — برچسبِ مصرف، به‌جای اصطلاح، خودش توضیح می‌دهد

نسخه‌ی قبل کنارِ عددِ مصرف می‌نوشت «(۴۶۲ گیگ پیش از ریست)». مالک
پرسید یعنی چه — و حق داشت: آن اصطلاح بود، نه توضیح. عددی که قابل
دفاع نباشد، به کار حسابداری نمی‌آید.

حالا خودِ تفکیک نوشته می‌شود:

    ۱٬۰۴۱ گیگ مصرف (۵۷۸٫۶ از آخرین صفرکردن + ۴۶۲٫۶ قبلش)

و روی نگه‌داشتنِ موس:

    ۵۷۸٫۶ گیگ از وقتی شمارنده‌ی این گروه در x-ui صفر شد،
    به‌علاوه‌ی ۴۶۲٫۶ گیگ که پیش از آن مصرف شده بود.
    پنل x-ui هم همین جمع را نشان می‌دهد.

### Fixed — دادهٔ ساختگیِ هارنس مصرفِ گروه نداشت

گروه‌های هارنس `usedGB` و `bankedGB` نداشتند، پس شاخه‌ی این برچسب
هیچ‌وقت رندر نمی‌شد و صفحه «سالم» به نظر می‌رسید — همان دامی که
`CLAUDE.md` هشدارش را می‌دهد.

## [1.67.0]

### Fixed — مصرفِ گروه‌ها با پنلِ x-ui نمی‌خواند

این بالاخره علتِ واقعی بود، و با دیتابیسِ خودِ مالک پیدا شد.

3x-ui وقتی ترافیکِ یک گروه را ریست می‌کند، مقدارِ پیش از ریست را
در `client_groups.reset_up` و `reset_down` نگه می‌دارد و **منفی**
ذخیره‌اش می‌کند. صفحه‌ی گروه‌های خودش این را نشان می‌دهد:

    نمایش = SUM(up + down) − (reset_up + reset_down)

پنلِ ما این دو ستون را نمی‌خواند، پس فقط مصرفِ دوره‌ی جاری را
می‌گفت.

اندازه‌گیری روی دیتابیسِ واقعی، کنارِ اسکرین‌شاتِ خودِ پنلِ x-ui —
هر یازده گروه با این فرمول تا کمتر از یک درصد خواندند:

| گروه | x-ui | پیش از این | جاری | بانک‌شده |
|---|---|---|---|---|
| unlimited | ۶.۳۷ TB | ۳۶۰۳ GB | ۳۶۰۳ | ۲۹۲۱ |
| ali | ۱.۵۰ TB | ۵۲۴ GB | ۵۲۴ | ۱۰۱۷ |
| Hossein naji | ۱.۰۱ TB | ۵۷۹ GB | ۵۷۹ | ۴۶۳ |
| Macan | ۱.۰۰ TB | ۳۱۰ GB | ۳۱۰ | ۷۱۵ |
| yaser | ۴۳۵ GB | ۱۱۱ GB | ۱۱۱ | ۳۲۴ |
| Ali kottah | ۱۸۲ GB | ۱۷ GB | ۱۷ | ۱۶۵ |
| dastani | ۷۳ GB | ۷۳ GB | ۷۳ | ۰ |

سه گروهِ آخر بدونِ ریست بودند و **همیشه** درست می‌خواندند — همان
الگویی که نشان می‌داد مشکل در جمع‌کردن است، نه در خواندن.

روی صورتحساب، در گروه‌های نرخِ حجمی، مستقیم روی مبلغ بود:
«Hossein naji» با نرخِ هزار تومان بر گیگ، ۵۷۸٬۶۰۰ می‌شد به‌جای
۱٬۰۴۱٬۱۶۳.

حالا داشبورد، صورتحساب، صورتحسابِ دوره و پنلِ نماینده همه یک عدد
می‌دهند، و آن عدد همانی است که مالک در پنلِ x-ui می‌بیند.

ردیفِ هر کلاینت مصرفِ *خودش* را می‌گوید — بانک در سطحِ گروه ثبت
شده و به کلاینتِ مشخصی نمی‌چسبد، پس یک‌بار روی جمعِ گروه اضافه
می‌شود. رابط هم کنارِ عدد می‌گوید چقدرش پیش از ریست بوده.

**درباره‌ی ردیابیِ ۱.۶۶.۱:** آن ردیاب برای همین مشکل ساخته شده بود،
با این فرض که x-ui تاریخچه ندارد. دارد — فقط در سطحِ گروه. حالا
بانکِ x-ui معتبر است و ردیابِ خودمان فقط وقتی به کار می‌رود که آن
ستون‌ها نباشند (نسخه‌های قدیمی‌ترِ 3x-ui). هرگز هر دو با هم، چون
ریستِ گروهی شمارنده‌ی همه‌ی کلاینت‌ها را صفر می‌کند و هر دو همان
یک اتفاق را ثبت می‌کنند.

### Fixed — گروهِ خالیِ قابل‌صورتحساب، کلِ حسابداری را می‌انداخت

شاخه‌ی «گروهی که در x-ui هست ولی هنوز کانفیگ ندارد» نُه کلید کم
داشت. یکی‌شان `periodStart` بود و کدِ بعدی با `KeyError` می‌افتاد —
یعنی کلِ صفحه‌ی حسابداری پیام «محاسبه ناموفق» می‌داد، فقط چون یک
گروهِ خالی قابلِ‌صورتحساب علامت خورده بود.

روی دیتابیسِ مالک یک گروهِ خالی وجود داشت و همین کافی بود.

## [1.66.3]

### Fixed — ترافیکِ کاربرانی که در چند اینباند بودند، کم شمرده می‌شد

این علتِ واقعیِ «مصرف با پنل x-ui نمی‌خواند» بود.

`client_traffics` در 3x-ui **یک ردیف به ازای هر (اینباند، ایمیل)**
دارد، نه یک ردیف به ازای هر کاربر. کاربری که هم روی XHTTP است و
هم روی TCP+Vision — همان چیدمانی که خودِ این پروژه پیشنهاد می‌دهد
— دو ردیف دارد.

و کد این را می‌نوشت:

```python
traffic[r["email"]] = dict(r)
```

یعنی **جایگزینی، نه جمع**. فقط آخرین ردیف می‌ماند و بقیه دور
ریخته می‌شد.

اندازه‌گیری روی دادهٔ واقعیِ یک سرور، کنارِ خودِ پنل x-ui:

| گروه | پنل x-ui | پنلِ ما | نسبت |
|---|---|---|---|
| dastani | ۷۳.۳ GB | ۷۳.۳ GB | ۱.۰۰ |
| Pelleaval | ۶۲.۳ GB | ۶۲.۳ GB | ۱.۰۰ |
| sajjad | ۳۹.۵ GB | ۳۹.۵ GB | ۱.۰۰ |
| Hossein naji | ۱.۰۱ TB | ۵۷۶.۸ GB | ۱.۸ |
| unlimited | ۶.۳۷ TB | ۳۶۰۰.۹ GB | ۱.۸ |
| ali | ۱.۵۰ TB | ۵۲۲.۹ GB | ۲.۹ |
| yaser | ۴۳۵.۲ GB | ۱۱۱.۱ GB | ۳.۹ |

الگو گویاست: گروه‌هایی که کاربرانشان یک اینباند دارند **دقیقاً**
می‌خوانند، و بقیه به نسبتِ تعدادِ اینباند کم می‌آیند. تعدادِ
کانفیگ‌ها در هر دو یکی بود، پس هر دو یک فایل را می‌خواندند — فقط
جمع‌کردن غلط بود.

روی صورتحساب یعنی **همه‌ی مصرف‌ها کمتر از واقعیت**، و در گروه‌های
نرخِ حجمی مستقیم روی مبلغ.

حالا ردیف‌ها جمع می‌شوند. انقضا دورترین برداشته می‌شود و «فعال»
اگر حتی یک اینباند باز باشد — چون کاربر با همان یکی وصل می‌شود.

### Changed — `nexora usage-why` تعدادِ اینباندها را هم می‌گوید

اگر کاربری در بیش از یک اینباند باشد، ابزار می‌گوید چند نفر در
این حالت‌اند — تا معلوم باشد چرا عددها با نسخه‌های پیشین فرق
دارند.

## [1.66.2]

### Fixed — پنل می‌توانست فایلِ اشتباهِ x-ui را بخواند، بی‌صدا

مسیرِ دیتابیس x-ui از یک فهرستِ ثابت پیدا می‌شد و **اولینِ موجود**
برداشته می‌شد:

    /etc/x-ui/x-ui.db
    /usr/local/x-ui/x-ui.db
    /opt/x-ui/x-ui.db
    /etc/x-ui/db/x-ui.db

روی سروری که بیش از یکی از این‌ها را دارد — نصبِ دوباره، مهاجرت از
مسیری به مسیرِ دیگر، یا بک‌آپی که کنارش مانده — پنل می‌توانست به
فایلی نگاه کند که x-ui دیگر رویش نمی‌نویسد.

از بیرون این‌طور دیده می‌شد: **مصرف هیچ‌وقت عوض نمی‌شود و با پنلِ
خودِ x-ui هم نمی‌خواند.** و هیچ‌جا نمی‌گفت چرا؛ پنل با خوشحالی
عددِ یک فایلِ مرده را نشان می‌داد.

حالا از میانِ فایل‌های موجود، **تازه‌ترین** برداشته می‌شود. این حدس
نیست: x-ui هر چند ثانیه ترافیک را می‌نویسد، پس فایلِ زنده همیشه
تازه‌ترین `mtime` را دارد. مسیرِ دستی و متغیرِ سرویس مثل قبل مقدم‌اند
— انتخابِ صریح بر حدس مقدم است.

### Added — `nexora usage-why`

وقتی عددِ مصرف با چیزی که انتظار می‌رود نمی‌خواند، این می‌گوید
دقیقاً چرا. روی سرور اجرا می‌شود و چهار چیز را کنار هم می‌گذارد:

- نسخه‌ی پنلی که واقعاً اجرا می‌شود
- همه‌ی فایل‌های `x-ui.db` با زمانِ آخرین نوشتن، و اینکه پنل کدام
  را می‌خواند
- وضعیتِ ردیابیِ ریست: چند کانفیگ مبنا دارند، چقدر مصرفِ
  پیش‌از‌ریست بانک شده
- برای هر کانفیگ: مصرفِ دوره، بانک‌شده، و جمع

خروجی انگلیسی است (متنِ راست‌به‌چپ با ترمینال قاطی می‌شود) و هیچ
رمز، نام مشتری یا شماره‌ای چاپ نمی‌کند، پس قابل فرستادن است.

### Added — تشخیصِ حسابداری چند-فایل‌بودن را می‌گوید

`/api/admin/billing/diagnose` حالا دو چیز تازه گزارش می‌کند: اگر
بیش از یک `x-ui.db` روی سرور باشد همه را با زمانِ آخرین نوشتن
فهرست می‌کند، و اگر فایلِ انتخاب‌شده ساعت‌هاست دست‌نخورده مانده
هشدار می‌دهد — یعنی x-ui رویش نمی‌نویسد.

## [1.66.1]

### Fixed — اصلاحِ ۱.۶۶.۰ فقط یک صفحه را درست کرده بود

جمعِ مصرف (با دوره‌های ریست‌شده) در ۱.۶۶.۰ فقط در «همه کاربران»
حساب می‌شد. بقیه‌ی صفحه‌های حسابداری همان عددِ پس‌از‌ریست را
برمی‌داشتند — یعنی همان «یک قاعده، دو جا، اصلاح در یکی» که این
مخزن اسم دارد رویش، این‌بار در خودِ اصلاح.

چهار جا این عدد را غلط می‌خواندند، و سه‌تایشان روی پول:

- **بدهیِ نرخِ حجمی.** `due` از `cl["used"]` حساب می‌شد. کانفیگی که
  ۹۵۰ گیگ مصرف کرده و ریست خورده بود، ۴۵۰ گیگ صورتحساب می‌گرفت —
  و ریست دستِ خودِ نماینده است.
- **قاعده‌ی «هرگز به کار نیفتاده».** کانفیگی که مصرف داشت، ریست
  خورد و بعد غیرفعال شد، «ساخته شده ولی هرگز به کار نیفتاده»
  شمرده می‌شد و **کاملاً از صورتحساب بیرون می‌افتاد**.
- **برگشتِ اعتبار هنگام حذف.** شرطش `used <= 0` بود. نماینده
  می‌توانست ترافیک را ریست کند، کانفیگ را پاک کند، و پولِ ساختش
  کامل برگردد — در حالی که مشتری حجمش را مصرف کرده بود.
- **مصرفِ گروه و پنلِ نماینده.** عددِ روی کارتِ بدهی و عددی که خودِ
  نماینده می‌دید، هیچ‌کدام جمع نبودند.

حالا یک تابع (`_attach_total_usage`) این جمع را می‌سازد و همه از
همان می‌خوانند. تست هر پنج سطح را با هم می‌سنجد و هر پنج اصلاح
عمداً شکسته شد تا معلوم شود دروازه قرمز می‌شود.

اندازه‌گیری‌شده — ۵۰۰ گیگ، ریست، ۴۵۰ گیگ دیگر:

| صفحه | پیش از این | حالا |
|---|---|---|
| داشبورد حسابداری | ۴۵۰ GB | ۹۵۰ GB |
| بدهیِ نرخِ حجمی | ۴۵۰٬۰۰۰ | ۹۵۰٬۰۰۰ |
| صورتحساب | ۴۵۰ GB | ۹۵۰ GB |
| صورتحسابِ دوره | ۴۵۰٬۰۰۰ | ۹۵۰٬۰۰۰ |
| پنلِ نماینده | ۴۵۰ GB | ۹۵۰ GB |

### Fixed — تابعِ جمع، روی نصبِ تازه کلید را ست نمی‌کرد

وقتی ستونِ `used_before` هنوز ساخته نشده بود، تابع زودهنگام
برمی‌گشت و `usedTotal` اصلاً ست نمی‌شد — یعنی صداکننده `KeyError`
می‌گرفت و صفحه خالی می‌شد. دروازه همین را گرفت. حالا قرارش
بی‌قیدوشرط است: بعد از اجرایش هر کلاینت `usedTotal` دارد، حتی اگر
جمعی برای افزودن نباشد.

## [1.66.0]

### Fixed — مصرفِ پیش از ریست، بی‌صدا گم می‌شد

x-ui با ریستِ ترافیک `up` و `down` را صفر می‌کند و هیچ تاریخچه‌ای
نگه نمی‌دارد. پس کانفیگی که سهمیه‌اش تمام شده، ریست خورده، و دوباره
مصرف کرده، در پنل «کم‌مصرف» به نظر می‌رسید.

اندازه‌گیری‌شده: کانفیگِ ۵۰۰ گیگی که ۵۰۰ مصرف کرد، ریست خورد، و
۴۵۰ گیگِ دیگر — پنل **۴۵۰** می‌گفت. پانصد گیگ رفته بود.

روی صورتحساب یعنی واسطه‌ای که با ریست‌زدن دو برابرِ حجم فروخته،
یک‌بار پول می‌دهد.

حالا همان قاعده‌ی تشخیصِ تمدید روی ترافیک هم اجرا می‌شود: عددِ
قبلی را خودمان به خاطر می‌سپاریم و افتادنش را ریست می‌خوانیم.
فهرست «همه کاربران» کنارِ مصرفِ دوره، **مصرف کل با ریست‌ها** را هم
نشان می‌دهد.

یک نکته که باید گفته شود: این از *مشاهده* ساخته می‌شود. ریستی که
بین دو نگاهِ پنل بیفتد، دیده نمی‌شود — تاریخچه‌ای در x-ui نیست که
بعداً بشود از آن خواند. به همین دلیل تازه‌سازیِ خودکار هم اضافه شد.

### Fixed — صفحه‌های حسابداری خودشان تازه نمی‌شدند

یک‌بار موقع باز شدن می‌خواندند و تمام. مالک صفحه را باز می‌گذاشت و
ساعت‌ها همان عدد را می‌دید. حالا هر ۴۰ ثانیه، و این‌بار برای
درستیِ عدد لازم است نه فقط تازگی‌اش.

### Fixed — درصدِ مصرف، با واحدهای قاطی

`_usage_percent` بایت می‌خواست و هر چهار صداکننده مقدارِ خامِ x-ui
را می‌دادند — که گاهی بایت است و گاهی خودِ عددِ گیگ. روی نصبی که
عددِ گیگ نگه می‌دارد:

    مصرف ۴۵۰ گیگ از سهمیه‌ی ۵۰۰  →  ۹۶٬۶۳۶٬۷۶۴٬۱۶۰٪

در مینی‌اپ `gb` و `usagePct` دو خط فاصله داشتند و همان یک مقدار در
یکی از `_gb_of` رد می‌شد و در دیگری نه. حالا خودِ تابع نرمال
می‌کند، پس هیچ صداکننده‌ای نمی‌تواند اشتباه بدهدش.

### Fixed — سکه‌های مشتری در مینی‌اپ قابل خرج‌کردن نبودند

بک‌اند `useCoins` را از قبل می‌پذیرفت و به `handlers.card_order`
می‌داد — همان هسته‌ای که ربات هم از آن رد می‌شود. ولی مینی‌اپ
هیچ‌وقت نمی‌فرستادش. یعنی مشتری سکه جمع می‌کرد، کارتِ سکه‌اش
«۱۰٪ تخفیف فعال» نشان می‌داد، و از مینی‌اپ قیمتِ کامل می‌داد.

حالا در برگه‌ی پرداخت انتخابش می‌کند. و کارتِ سکه صریح می‌گوید
کجا خرج می‌شود، چون تا حالا معلوم نبود خودکار است یا نه.

**هنوز باز:** خریدِ کیف‌پولی تخفیفِ سکه ندارد — نه در مینی‌اپ و نه
در خودِ ربات. `wallet_purchase` اصلاً پارامترِ سکه ندارد و قیمتِ
خامِ پلن را برمی‌دارد. این یک تصمیمِ قیمت‌گذاری است، نه یک باگِ
چیدمانی، پس دست‌نخورده ماند تا مالک بگوید.

### Fixed — نوارِ پایینِ صفحه‌ی اشتراک از جایش کنده شده بود

`.bottom-nav` با `position: fixed` تعریف شده بود، ولی پایین‌تر در
همان فایل یک قاعده‌ی دیگر `position: relative` می‌داد و پسش
می‌زد — برای اینکه `::after` لنگر داشته باشد، در حالی که `fixed`
خودش لنگر است.

نتیجه: نوار از پایینِ صفحه کنده می‌شد و وسطِ محتوا، درست زیرِ
سوالات متداول، می‌نشست.

### Fixed — دکمه‌ی بی‌نام در صفحه‌ی اشتراک

وقتی مالک «مخفی‌کردنِ لیست کانفیگ‌ها» را روشن می‌کرد، کد *برچسبِ*
دکمه را مخفی می‌کرد نه خودِ دکمه را. یک مربعِ ۸۱ پیکسلی با آیکون و
بدونِ هیچ نوشته‌ای می‌ماند — دکمه‌ای که معلوم نیست چه می‌کند.

### Fixed — نامِ بلندِ مشتری سرصفحه را می‌شکست

`max-width: 140px` یک عددِ جادویی بود: روی صفحه‌ی پهن نامِ جاشونده
را بی‌دلیل می‌برید و روی صفحه‌ی باریک هنوز از جای موجود بیشتر بود.
و `flex-wrap` نام را به خطِ دوم می‌برد و سرصفحه دو خطی می‌شد.

حالا جا را خودِ چیدمان تصمیم می‌گیرد و نامِ کامل روی نگه‌داشتن
دیده می‌شود.

### Changed — دو ستونِ داشبورد با هم می‌خوانند

هر ستون یک استکِ جدا بود، پس کارتِ دومِ هر ستون هر جا که کارتِ
اولش تمام می‌شد شروع می‌کرد. اندازه‌گیری‌شده: ته ستون‌ها ۱۹ پیکسل
فرق داشت و «بدهی نماینده‌ها» ۸۲ پیکسل پایین‌تر از «دسترسی سریع»
شروع می‌شد.

حالا یک شبکه‌ی ۲×۲ است و هر ردیف خودش هم‌قد می‌شود.

### Changed — رنگ

- **پس‌زمینه سه منبع دارد، نه دو سایه از یک آبی.** قبلاً هر دو
  لکه آبی بودند، یعنی کلِ پس‌زمینه یک رنگ بود با دو غلظت.
- **هر کارتِ شاخص رنگِ خودش را دارد.** آیکون، خطِ لبه و درخششِ
  هاور هر سه از `tone` همان کارت می‌آیند؛ تا امروز هر چهارتا یک
  آبیِ ثابت بودند و چشم هیچ‌کدام را از دیگری تشخیص نمی‌داد.
- **سطح‌ها از هم جدا شدند.** ‎#0E1523‎ و ‎#131C2E‎ اختلافِ روشناییِ
  ۲٪ داشتند، پس کارتِ روی کارت دیده نمی‌شد.

### Changed — صفحه‌ی اشتراک: بخش‌هایی که با اندازه‌ی صفحه کنار می‌آیند

ردیفِ دکمه‌ها و کارتِ آمار از شبکه‌ی خودتنظیم استفاده می‌کنند.
دکمه‌ای که مالک خاموش کند، بقیه جایش را پر می‌کنند — بی‌آنکه سه
دکمه بی‌قواره پهن شوند یا چهارمی تنها در ردیفِ دوم بیفتد.

### Changed — شارژ کیف پول، روی خودِ کارتِ موجودی

تا امروز دو راه داشت: ردیفی ته صفحه‌ی تنظیمات، و برگه‌ی پرداخت
وقتی پول کم می‌آمد. یعنی کاربر باید *دنبالش می‌گشت*.

### Changed — کارتِ سکه و دعوت

دو جعبه‌ی جدا بود برای یک موضوع، بدونِ عنوان، و ۵۲٪ از ارتفاع
صفحه را می‌گرفت. حالا یک کارت با عنوان است؛ شمارِ دعوت‌شده‌ها
کنارِ عنوان نشسته و کد و دکمه‌ی فرستادن در یک ردیف‌اند.

### Fixed — فاصله‌ی ۲۲ پیکسلی، جایی که نباید

قاعده‌ی ریتمِ بخش‌های مینی‌اپ به *همه چیز* می‌خورد: بین سه کارتِ
فهرستِ اشتراک‌ها هم ۲۲ پیکسل می‌گذاشت، و بین دو فیلدِ پشت‌سرهمِ یک
فرم هم. ردیفِ فهرست حالا ۱۰، فیلدِ فرم ۱۴، و عنوانِ بخش ۱۲.

### Fixed — عددِ کارتِ شاخص با قلمِ بی‌گلیف

`StatTile` عددش را با `--mono` می‌نوشت و آن قلم گلیفِ فارسی ندارد،
پس بی‌صدا به قلمِ دیگری می‌افتاد. همان اشتباهی که یک‌بار در یازده
جای دیگر پیدا شد.

### Fixed — دروازه‌ی ظاهر یک برخوردِ واقعی را گرفت

کارتِ شاخص هم `.fx-card` است و هم `.fx-kpi`؛ دو بلوکِ `::before`
یعنی هرکدام که دیرتر بیاید دیگری را بی‌صدا پاک می‌کند. رنگِ خطِ
لبه حالا از یک متغیر می‌آید، نه از یک شبه‌عنصرِ دوم.

## [1.65.0]

### Fixed — تنظیماتِ سکه هیچ‌وقت به ربات نمی‌رسید

صفحه‌ی «سکه و دعوت» پنل **کار نمی‌کرد، و بی‌صدا**. مالک عدد
می‌گذاشت، «ذخیره شد» می‌دید، و ربات همان مقدارهای پیش‌فرض را
می‌داد.

دو اشتباه روی هم:

- **جای ذخیره.** پنل `settings.coins_per_referral` می‌نوشت و ربات
  `settings.coins.per_referral` می‌خواند
  (`core.coin_settings(ctx.s.get("coins"))`). کلیدها کنارِ هم
  می‌نشستند و هیچ‌کدام دیگری را نمی‌دید.
- **نامِ فیلد.** پله‌ها `{coins, pct}` نوشته می‌شدند و
  `core.tier_for` `percent` می‌خواهد.

اندازه‌گیری‌شده: مالک «۲۵ سکه به معرف» گذاشت، ربات ۱۰ داد. پله‌ی
«۳۰ سکه = ۱۵٪» ساخت، مشتری صفر تخفیف گرفت — و با `pct` تابع اصلاً
`KeyError` می‌داد.

هیچ تستی نگرفتش چون هر دو طرف جدا درست بودند؛ فقط به هم وصل
نبودند. حالا صفحه همان شکلی را می‌نویسد که `bot/core.py` می‌خواند،
و تستِ درز نه‌تنها نگاشت را می‌سنجد که برابریِ **درصدِ مینی‌اپ با
درصدِ لحظه‌ی خرید** را هم.

سقفِ تخفیف و انقضای سکه هم که تا حالا فقط در کد بودند، حالا در
پنل قابل تنظیم‌اند.

### Added — عکس در گفتگو، هم در مینی‌اپ هم در پنل

بیشترِ چیزی که مشتری در پشتیبانی می‌خواهد بگوید یک تصویر است:
عکسِ خطای اپلیکیشن، رسید، یا صفحه‌ای که کار نمی‌کند. تا حالا باید
توصیفش می‌کرد.

- عکس با متن یا بدونِ متن، از هر دو طرف
- پاسخِ مالک با عکس در خودِ تلگرام هم می‌رسد، و پیامِ مشتری با
  عکس در گروهِ مدیریت — «عکس فرستاد» بدونِ عکس یعنی مالک باید
  پنل را باز کند تا بفهمد موضوع چیست
- در پنل، `Ctrl+V` هم کار می‌کند: مالک معمولاً اسکرین‌شات می‌گیرد
- عکس پیش از فرستادن در خودِ مرورگر کوچک می‌شود (۱۶۰۰ پیکسل).
  عکسِ صفحه‌ی گوشی معمولاً ۳ تا ۸ مگابایت است و base64 حجم را
  یک‌سوم دیگر هم زیاد می‌کند

نشانیِ فایل ۱۲ بایتِ تصادفی دارد — همان قاعده‌ی عکسِ پروفایل،
«آدرس، خودش کلید است»، چون تگِ `<img>` هدرِ احراز هویت نمی‌فرستد.

### Changed — هفده دکمه‌ی فیلتر، سه کنترل شد

صفحه‌ی «کاربران ربات»: یازده چیپِ فیلتر به‌اضافه‌ی شش دکمه‌ی
مرتب‌سازی، در کارتی ۱۵۷ پیکسلی، پیش از آنکه یک ردیف داده دیده
شود. روی پهنای ۹۰۰ پیکسل ۳۱ پیکسل از چیپ‌ها بیرون از دید می‌ماند،
پس بعضی فیلترها عملاً پیدا نمی‌شدند.

حالا جست‌وجو و دو منو — **۱۵۷ پیکسل → ۶۵**، **۱۷ دکمه → ۲**.
فیلترِ فعال به شکلِ چیپی با دکمه‌ی برداشتن روی نوار می‌ماند، چون
همان تنها چیزی است که کاربر باید ببیند.

### Fixed — لگِ جستجوی کاربران ربات

سه چیز روی هم:

- **شمارشِ یازده فیلتر در هر ضربه‌ی کیبورد.** این عددها به عبارتِ
  جستجو ربطی ندارند ولی هر بار دوباره حساب می‌شدند. اندازه‌گیری
  روی ۲۵ هزار کاربر: ۳۶ از ۷۴ میلی‌ثانیه.
- **فهرست با هر ضربه به اسکلت تبدیل می‌شد.** داده ناپدید می‌شد و
  کاربر فکر می‌کرد خراب شده. حالا فهرست سرِ جایش می‌ماند و فقط
  یک نشانِ کوچک در خودِ فیلد می‌چرخد.
- **پاسخ‌های بی‌ترتیب.** تایپِ سریع یعنی چند درخواستِ هم‌زمان، و
  پاسخِ عبارتِ قدیمی‌تر روی نتیجه‌ی تازه می‌نشست.

اندازه‌گیری روی ۲۵ هزار کاربر: یک جستجو **۷۴ → ۲۷ میلی‌ثانیه**،
تایپِ سه‌حرفی **۲۱۴ → ۷۵**. شمارشِ فیلترها هم یک اسکن شد به‌جای
یازده‌تا — ولی شرط‌هایش از همان `_USER_FILTERS` ساخته می‌شوند، نه
یک نسخه‌ی دوم.

### Changed — کارتِ سکه و دعوت در مینی‌اپ

یک جعبه‌ی خاکستری بود با کد و دو دکمه، و سه چیزِ لازم را نمی‌گفت:

- **چند سکه دارم؟** هیچ‌جای مینی‌اپ عددِ سکه دیده نمی‌شد، با اینکه
  داده‌اش از قبل می‌آمد.
- **سکه به چه دردی می‌خورد؟** نردبانِ تخفیف اصلاً نشان داده
  نمی‌شد.
- **دعوت چه سودی دارد؟** «سکه می‌گیرید» بدونِ عدد، وعده نیست.

حالا: موجودی، تخفیفِ فعال، نوارِ پیشرفت تا پله‌ی بعد با عددِ
دقیقِ سکه‌ی لازم، کلِ نردبان با پله‌های بازشده، و اینکه هر دوست
چند سکه می‌آورد.

هیچ‌کدام از این عددها در مینی‌اپ حساب نمی‌شوند: از
`core.coin_progress` می‌آیند، همان تابعی که لحظه‌ی خرید قیمت را
کم می‌کند. دو نسخه یعنی روزی مینی‌اپ ۳۰٪ نشان دهد و صندوق ۲۰٪
کم کند.

### Fixed — دو ایرادِ چیدمانی که با اندازه‌گیری پیدا شدند

- **پیش‌نمایشِ عکسِ انتخاب‌شده بریده می‌شد.** `.mn-chat` یک ستونِ
  فلکس است، پس `inline-block` بلوکی می‌شد و فلکس ارتفاعش را
  می‌فشرد: قابِ ۳۵۸×۳۶ دورِ عکسِ ۱۳۲×۸۸. با چشم «یک نوار نازک»
  به نظر می‌رسید.
- **قابِ ۱۱ پیکسلیِ رنگی دورِ عکسِ داخل حباب** خودش را نشان
  می‌داد نه عکس را.

### Fixed — دروازه‌ی ظاهر، `export async function` را نمی‌شناخت

الگویش فقط `function|const|class` را می‌پذیرفت، پس هر تابعِ
`async` که export می‌شد «export نشده» گزارش می‌شد — یعنی یک هشدار
دروغ که هشدارهای واقعی را بی‌ارزش می‌کند.

## [1.64.0]

این نسخه با **نگاه‌کردن** به صفحه‌ها درست شد، نه با حدس‌زدن.

### Fixed — چسباندگی: بخش‌ها صفر پیکسل از هم فاصله داشتند

چند بار گزارش شد و من پیدایش نکرده بودم. علتش این بود که کلاسِ
`.mn-sec` **هیچ قاعده‌ای در CSS نداشت** — نه حاشیه، نه هیچ چیز.
پس عنوانِ هر بخش دقیقاً به کارتِ بالایی می‌چسبید.

اندازه‌گیریِ فاصله‌ی بین بلوک‌های تبِ خانه: `gapFromPrev: 0` برای
هر سه. حالا ۲۲ پیکسل، و عنوان تا کارت ۱۲.

### Fixed — عکس پروفایل بالا عوض نمی‌شد

آواتارِ هدر `src` نداشت. یعنی کاربر عکسش را در تنظیمات می‌دید ولی
بالای صفحه همان حرفِ اول می‌ماند.

حالا: عکسِ خودِ کاربر، وگرنه لوگوی فروشگاه، وگرنه حرفِ اول. نامِ
فروشگاه همان بالا نوشته شده، پس برند گم نمی‌شود.

### Fixed — گفتگو روی دسکتاپ هم اسکرولِ خودش را دارد

اصلاحِ نسخه‌ی قبل روی هر دو کار می‌کرد ولی تاییدش فقط روی موبایل
انجام شده بود. حالا روی ۱۲۸۰ پیکسل هم سنجیده شد: صفحه ثابت
می‌ماند و فقط لاگ می‌لغزد.

### Changed — حفره‌ی داشبورد، این‌بار با محتوا پر شد

نسخه‌ی قبل کارتِ «محتوای صفحه‌ی اشتراک» را کش داد تا حفره پر شود.
این اشتباه بود: حفره فقط از ته کارت به داخلش منتقل شد.

حالا کارتِ تازه‌ای زیرش می‌نشیند — **بدهی نماینده‌ها** با نوار و
مبلغ. و داده‌اش از `billing/overview` می‌آید که از قبل وجود داشت و
هیچ صفحه‌ای صدایش نمی‌زد؛ در فهرستِ بدهی‌های تستِ درز هم ثبت بود و
حالا از آن فهرست درآمد.

عنوانش «بدهی» است نه «فروش»: این عدد چیزی است که نماینده باید
بدهد، نه چیزی که فروخته.

### Fixed — دکمه‌ی بازگشت هنوز برای لمس کوچک بود

۲۲ پیکسل → ۴۰.

پس از این‌ها، سنجشِ همه‌ی صفحه‌های مینی‌اپ (پنج تب + جزئیات
اشتراک) **صفر ایراد** داد: بدون سرریز، بدون متنِ بریده، بدون
دکمه‌ی کوچک، و بدون هیچ فاصله‌ی کمتر از ۱۲ پیکسل بین بلوک‌ها.

## [1.63.0]

### Fixed — بعد از فرستادن پیام، باید دستی برمی‌گشتی

مهم‌ترین ایرادِ این نسخه، و ریشه‌اش ساده بود: **گفتگو اسکرولِ خودش
را نداشت.**

`.mn-chat-log` روی `overflow: visible` بود، پس با هر پیامِ تازه
**کلِ صفحه** بلندتر می‌شد — اندازه‌گیری: ۷۸۰ به ۱۱۹۰ پیکسل. و
`scrollIntoView` بعد از فرستادن، کلِ صفحه را می‌لغزاند: سرصفحه
می‌رفت بالا و کاربر باید دستی برمی‌گشت.

حالا در تبِ گفتگو، اپ قدِ صفحه را می‌گیرد و خودِ لاگ اسکرول
می‌شود — مثل هر پیام‌رسانی. بقیه‌ی تب‌ها مثل قبل معمولی اسکرول
می‌شوند.

### Fixed — فضای مرده‌ی داشبورد

اندازه‌گیری: زیرِ نمودار **۱۴۱ پیکسل** و زیرِ «محتوای صفحه‌ی
اشتراک» **۲۱۱ پیکسل**.

علتش اصلاحِ نسخه‌ی قبل بود: `align-items: start` جلوی کش‌آمدنِ
کارت‌ها را گرفت (که برای کارتِ تمام‌شده درست است) ولی ستونِ
کوتاه‌تر یک حفره ته خودش گذاشت.

حالا کارتی که محتوایش *می‌تواند* بزرگ‌تر شود — نمودار و شبکه‌ی
کاشی‌ها — تا قدِ ردیف کش می‌آید و آن فضا به خودِ نمودار می‌رسد، نه
به یک حفره. اختلافِ قدِ دو ستون: **صفر**.

### Fixed — دکمه‌ی بازگشت برای لمس کوچک بود

۲۲ پیکسل. روی گوشی یعنی باید دقیق زد. حالا ۴۰، بدون اینکه چیدمان
جابه‌جا شود.

پس از این اصلاح، سنجشِ همه‌ی صفحه‌های مینی‌اپ (پنج تب، جزئیات
اشتراک، برگه‌ی پرداخت) **صفر ایراد** داد: بدون سرریز، بدون متنِ
بریده، بدون چسبیدن به لبه، بدون هدفِ لمسیِ کوچک.

## [1.62.0]

برگه‌اش: `docs/specs/2026-09-17-affiliate-portal.md`

### Fixed — تسویه‌ی پورسانت باقی‌مانده را گم می‌کرد

این اول درست شد، چون رابطِ همکار قرار بود روی همین عدد ساخته شود.

پرداختِ ۶۰ هزار بابتِ پورسانت‌های ۵۰ و ۳۰ هزار، اولی را
«پرداخت‌شده» می‌زد و بعد `break` — آن ۱۰ هزارِ اضافه هیچ‌جا حساب
نمی‌شد. مانده درست بود (چون از دفترِ پرداخت‌ها می‌آمد)، ولی وضعیتِ
ردیف‌ها با واقعیت جور درنمی‌آمد.

اگر رابطِ همکار روی همان وضعیت ساخته می‌شد، همکار عددی می‌دید که با
مانده‌اش نمی‌خواند. حالا وضعیت از **مجموعِ کلِ پرداخت‌ها** بازسازی
می‌شود، پس چند بار اجرا شدنش هم همان جواب را می‌دهد.

### Added — پنل همکار فروش

همکار با کد و رمزِ خودش وارد `/aff` می‌شود و می‌بیند: چند مشتری
آورده و هر کدام چقدر خریده، فهرست پورسانت‌ها با وضعیت، و سه عدد —
کلِ درآمد، دریافتی، و **مانده**. به‌علاوه‌ی پرداخت‌هایی که مالک ثبت
کرده.

رمز را مالک از بخش «همکاری در فروش» تعیین می‌کند؛ خالی‌گذاشتنش
یعنی دسترسی بسته شود، و نشستِ بازش همان لحظه باطل می‌شود.

**همکار هیچ چیزی را عوض نمی‌کند** — نه پرداخت ثبت می‌کند نه درصد.
تستی هست که می‌سنجد مسیرِ نوشتنی اضافه نشود.

و مانده از همان `_affiliate_balance` می‌آید که پنلِ مالک به کار
می‌برد. تستی هست که می‌سنجد این دو عدد یکی بمانند — وگرنه روزی
همکار و مالک سرِ عددی بحث می‌کنند که هیچ‌کدام نمی‌تواند ثابتش کند.

رمز مثل رمزِ پنل هَش می‌شود، و پیامِ «کد نیست» با «رمز غلط» یکی
است: اگر فرق کند، با امتحان‌کردن می‌شود فهمید چه کدهایی وجود دارند.

### Added — دعوت از دوستان، داخل مینی‌اپ

کدِ معرف از قبل برای هر کاربر ساخته می‌شد ولی فقط در منوی ربات
دیده می‌شد — کسی که از مینی‌اپ می‌آمد اصلاً نمی‌دانست چنین چیزی
هست.

حالا در تبِ خانه: کد، دکمه‌ی کپیِ لینک، دکمه‌ی فرستادن (از خودِ
تلگرام)، و اینکه چند نفر تا حالا با آن آمده‌اند.

## [1.61.0]

### Fixed — رقمِ فارسی در قلمی که گلیفِ فارسی ندارد

ریشه‌ی «به‌هم‌ریختگی فونت» که چند بار گزارش شد.

`--mono` گلیفِ فارسی ندارد. هر جا «۳۲» با آن نوشته می‌شد، بی‌صدا به
قلمِ پشتیبان می‌افتاد — و کنارِ واحدش («گیگابایت») که ایران‌سنس
بود، دو قلمِ متفاوت می‌نشست. اندازه‌گیری روی مینی‌اپ: **۱۱ جا**.

توکنِ `--num` اضافه شد: ایران‌سنس با `tabular-nums`، که همان ستونِ
عددِ هم‌عرض را می‌دهد که از مونو می‌خواستیم. شناسه و شماره‌ی کارت و
نامِ کانفیگ عمداً مونو ماندند — آن‌ها لاتین‌اند و کپی می‌شوند.

فاصله‌ی عدد و واحد هم از ۴ به ۷ پیکسل رفت؛ ۴ پیکسل «چسبیده» دیده
می‌شد.

### Fixed — شمارنده‌های پنل رقمِ لاتین داشتند

`CountUp` — که زیرِ همه‌ی KPIهای پنل است — عددِ خام برمی‌گرداند،
پس «۶ روی ۳ پلتفرم» می‌شد «6 روی ۳ پلتفرم».

و همان‌جا یک باگِ دوم: `requestAnimationFrame` در تبِ پنهان اصلاً
اجرا نمی‌شود، پس اگر پنل در تبِ پس‌زمینه باز می‌شد، همه‌ی عددها
**برای همیشه صفر** می‌ماندند.

### Added — شارژ کیف پول داخل خودِ مینی‌اپ

تا امروز ربات را باز می‌کرد و کاربر باید آن‌جا مبلغ می‌زد و رسید
می‌فرستاد — یعنی وسطِ کار از اپ بیرون می‌افتاد.

حالا همان برگه‌ی پرداخت: مبلغ (چهار مبلغ آماده یا دلخواه) ←
کارت ← رسید.

منطقش دوباره نوشته نشد: `handlers.topup_order` از ربات بیرون کشیده
شد و هر دو از همان یکی رد می‌شوند. اگر این‌جا دوباره نوشته می‌شد،
می‌شد مسیرِ دومی که یک روز کمینه‌ی مبلغ یا انتخابِ کارت را فراموش
می‌کند — همان اتفاقی که سرِ سه مسیرِ خرید افتاد.

### Fixed — دکمه‌ی «همه» به جای اشتراک‌ها، به خرید می‌رفت

در «اشتراک‌های اخیر»، `onBuy` بود.

### Removed — دکمه‌ی تازه‌سازی مینی‌اپ

اپ خودش هر سه تا بیست ثانیه، و با برگشتن به آن، تازه می‌شود. دکمه
فقط می‌گفت «شاید تازه نباشد». فقط چرخنده‌ی موقعِ بارگذاری ماند.

### Fixed — کیبورد گوشی: این‌بار از روی فوکوس

اصلاح قبلی جواب نداده بود و دلیلش را پیدا کردیم: آن نسخه
`innerHeight - visualViewport.height` را می‌سنجید. داخل WebViewِ
اندروید **خودِ لایه‌ی چیدمان هم کوچک می‌شود**، پس آن اختلاف تقریباً
صفر می‌ماند و نوارِ تب هیچ‌وقت کنار نمی‌رفت.

حالا نشانه، فوکوسِ جعبه‌ی نوشتن است — هیچ ریاضی‌ای ندارد و روی هر
WebViewی یکسان است.

### Fixed — روی دسکتاپ، نوشتن صفحه را می‌پراند

`scrollIntoView` روی فوکوس بی‌قید اجرا می‌شد. روی دسکتاپ کیبوردی
بالا نمی‌آید، پس آن اسکرول فقط صفحه را بی‌دلیل تکان می‌داد. حالا
فقط روی دستگاهِ لمسی.

## [1.60.0]

### Security — رمز پنل دیگر متنِ ساده نیست

`auth.json` خودِ رمز را داشت. هر کسی که آن فایل را می‌خواند —
پشتیبانِ لو رفته، اسنپ‌شاتِ جابه‌جاشده، یک خطای مسیر — مستقیم به
پنل، رمز x-ui، توکن ربات و داده‌ی همه‌ی مشتری‌ها می‌رسید.

حالا PBKDF2-HMAC-SHA256 با ۲۰۰٬۰۰۰ تکرار و نمکِ تصادفی. نه یک
هَشِ ساده: SHA سریع است و سرعت دقیقاً همان چیزی است که
حدس‌زننده می‌خواهد.

**و هیچ نصبی قفل نمی‌شود.** رمزِ متنِ سادهٔ قدیمی هنوز پذیرفته
می‌شود و اولین ورودِ موفق خودش فایل را ارتقا می‌دهد. کاری لازم
نیست. رمزِ فارسی هم کار می‌کند (`compare_digest` روی رشته‌ی
غیراسکی خطا می‌دهد، پس مقایسه روی بایت انجام می‌شود).

اگر `auth.json` خراب باشد، پنل باز نمی‌شود — به متغیر محیطی
برمی‌گردد، نه به پذیرفتنِ هر چیزی.

### Fixed — یک مسیرِ نماینده با هَش‌شدن بی‌صدا می‌شکست

یک مسیرِ پورتال، تابعِ صورتحسابِ مدیر را مستقیم صدا می‌زد و
رمزِ ذخیره‌شده را به‌عنوان هدر پاس می‌داد. با متنِ ساده کار
می‌کرد؛ با هَش، مقایسه‌ی آن با خودش همیشه غلط می‌شد. حالا
نشانه‌ی درونیِ تصادفیِ هر اجرا.

### Fixed — تست‌ها منطقِ رمز را دور می‌زدند

سوییت `load_password` را وصله می‌زد، که بعد از این تغییر دیگر
درزِ درستی نبود. بدتر: وصله‌زدن به `_stored_password` یعنی خودِ
منطق هیچ‌وقت سنجیده نمی‌شود. حالا از درزِ واقعی می‌رود.

و همان‌جا معلوم شد فایلِ رمزِ تست‌ها **بیرون از مخزن** نوشته
می‌شد (`../data/auth.json`) — همان دامی که `CLAUDE.md` درباره‌ی
مسیرهای نسبی هشدار داده.

## [1.59.1]

بازبینیِ دوباره‌ی همان فهرست — چند مورد ناقص مانده بود.

### Fixed — نوارِ تب هم موقع تعویض می‌پرید

همان باگی که در چیپ‌های فیلتر اصلاح شد، در کامپوننتِ مشترکِ `Tabs`
هم بود: تبِ فعال بوردر نداشت، پس یک پیکسل کوتاه‌تر می‌شد و کلِ
نوار بالا و پایین می‌رفت. اندازه‌گیری روی «سفارش‌ها»: فعال ۳۶
پیکسل، بقیه ۳۷.

چون `Tabs` مشترک است، این یک اصلاح همه‌ی نوارهای تبِ پنل را درست
کرد. و قاعده‌ی «یک ردیف، اسکرول افقی» هم از مدیا-کوئریِ موبایل
بیرون آمد و همیشگی شد — روی دسکتاپ نوارِ پرتب می‌توانست بشکند و
چون پنجره‌ی ما پهن بود هیچ‌وقت دیده نمی‌شد.

### Fixed — پرونده‌ی کاربر: شناسه ناخوانا و تاریخ میلادی

دو چیز که در فهرستِ کاربران درست شده بود ولی در *پرونده* نه:

- شناسه و شماره با ۱۲ پیکسل و کم‌رنگ‌ترین رنگ چاپ می‌شدند
- تاریخِ سفارش‌ها **میلادی** بود (`2026-09-14`) وسطِ یک پرونده‌ی
  کاملاً فارسی. این مسیر ردیفِ خامِ دیتابیس را می‌دهد و فیلدِ
  شمسی ندارد، پس تبدیل در خودِ رابط انجام می‌شود

### Fixed — پرونده‌ی کاربر در هارنس خالی باز می‌شد

مقکی برای `/api/admin/bot/subscriber/` وجود نداشت، پس پرونده با
«بدون نام» و صفر باز می‌شد — یعنی هارنس حالتی را نشان می‌داد که
پنلِ واقعی تقریباً هیچ‌وقت ندارد، و خوانایی‌اش اصلاً سنجیده
نمی‌شد. دقیقاً همان دامی که `CLAUDE.md` درباره‌اش هشدار داده.

## [1.59.0]

همه‌ی این نسخه از یک فهرستِ ایراد آمده که مالک داد. هر کدام
اندازه گرفته شد، نه حدس زده.

### Added — هر صفحه‌ی پنل نشانی دارد

پنل هیچ نشانی‌ای نداشت: رفرش یعنی برگشت به داشبورد، دکمه‌ی «عقب»
یعنی بیرون‌رفتن از پنل، و هیچ صفحه‌ای را نمی‌شد برای کسی فرستاد.
حالا `#/bot-orders` و مانندش.

هش است نه مسیر: یک باندل سه اپ را سرو می‌کند و کدامش بالا بیاید از
`location.pathname` تصمیم گرفته می‌شود. مسیر با آن انتخاب قاطی
می‌شد و به سرور هم دست باید می‌خورد.

### Fixed — دکمه‌های «دسترسی سریع» هیچ‌جا نمی‌بردند

`navigate` فقط `setActive` می‌کرد و فضای کاری را عوض نمی‌کرد، پس
افکتِ نگهبان فوراً برمی‌گرداندش به صفحه‌ی اولِ همان فضا. از بیرون
دقیقاً شبیه دکمه‌ی خراب بود.

چهار جا این کار را جدا نوشته بودند — دسترسی سریع، زنگ اعلان، پالت
فرمان و تعویض فضای کاری. همان باگی که `CLAUDE.md` هفت موردش را
شمرده. حالا یک در، و تستی که می‌سنجد پنجمی ساخته نشود.

### Fixed — پیام‌ها با تأخیر می‌رفتند و می‌آمدند

دو علتِ جدا:

**فرستادن** تا *دو* رفت‌وبرگشت تمام نمی‌شد هیچ چیزی نشان نمی‌داد
(POST، بعد خواندنِ دوباره‌ی گفتگو). روی اینترنت موبایل یعنی یکی دو
ثانیه سکوت، و کاربر دوباره می‌زد. حالا حباب همان لحظه می‌نشیند با
نشانِ «در حال رفتن»، و اگر نرسید قرمز می‌شود — نه اینکه شبیه پیامِ
رفته بماند.

**گرفتن** در پنل هر ۱۵ ثانیه بود و در مینی‌اپ هر ۶. حالا هر ۳
ثانیه، ولی فقط تا دو دقیقه بعد از آخرین حرکت؛ گفتگوی رهاشده نباید
تا ابد به سرور بزند.

### Fixed — کیبورد گوشی جعبه‌ی نوشتن را قایم می‌کرد

جعبه `position: sticky; bottom: 0` است، یعنی به پایینِ *لایه‌ی
چیدمان* می‌چسبد — و کیبورد آن را کوچک نمی‌کند، فقط
`visualViewport` را. نتیجه: جعبه پشتِ کیبورد می‌رفت و نوارِ تب هم
رویش می‌نشست.

حالا ارتفاع کیبورد سنجیده می‌شود، نوار تب کنار می‌رود (همان کاری
که خودِ تلگرام می‌کند)، و جعبه با `scrollIntoView` در دید می‌ماند.

### Fixed — «کاربران ربات» با لگ باز می‌شد

اندازه‌گیری: **دو درخواستِ کاملاً یکسان**، یکی در ۱۰ میلی‌ثانیه و
یکی در ۳۷۲. افکتِ دیبونسِ جست‌وجو هنگام سوارشدن هم اجرا می‌شد (چون
`q` از خالی شروع می‌شود) و دوباره `loading` را روشن می‌کرد — فهرست
می‌آمد و بلافاصله به اسکلت برمی‌گشت. همین در «حسابداری» هم بود.

حالا هر دو از یک قلّابِ مشترک رد می‌شوند که بارِ اول اجرا نمی‌شود.

### Changed — حباب‌های پیام جمع‌وجور شدند

یک خطِ کوتاه **۶۰ پیکسل** ارتفاع می‌گرفت، چون ساعت خطِ جدا
می‌گرفت. حالا ساعت ته همان خط شناور می‌شود — همان کاری که تلگرام
می‌کند — و یک‌خطی **۳۴ پیکسل** است.

### Fixed — مینی‌اپ زیر جعبه‌ی نوشتن ۱۸۶ پیکسل فضای مرده داشت

گفتگو فقط به اندازه‌ی محتوایش کش می‌آمد، پس جعبه وسطِ صفحه
می‌ماند. حالا قدِ صفحه را پر می‌کند: **۱۸۶ → ۲۸ پیکسل**.

### Fixed — نوار فیلتر شلوغ بود

یازده چیپ روی دو ردیف (۷۳ پیکسل) و شش چیپ مرتب‌سازی — ۱۷ دکمه در
یک نوار. حالا یک ردیفِ افقی‌اسکرول با لبه‌های محو: **۷۳ → ۳۴
پیکسل**. فیلتری که هیچ نتیجه‌ای ندارد هم نشان داده نمی‌شود، مگر
خودش انتخاب شده باشد.

و چیپِ انتخاب‌شده بوردر نداشت، پس دو پیکسل کوتاه‌تر می‌شد و کلِ
ردیف موقع انتخاب می‌پرید.

### Fixed — داشبورد فضای خالی داشت

«محتوای صفحه‌ی اشتراک» ۵۳۴ پیکسل بود با **۲۳۱ پیکسل خالی** ته آن،
و «روند فروش» ۴۳۶ با ۱۶۱ خالی — شبکه همه را تا قدِ بلندترین ستون
کش می‌داد. حالا هر کارت به قدِ محتوای خودش است.

### Fixed — مشخصات کاربر خوانده نمی‌شد

شناسه و شماره — همان چیزی که مالک با آن آدم را می‌شناسد — با
۱۲ پیکسل و کم‌رنگ‌ترین رنگِ خط چاپ می‌شدند. حالا روشن‌ترند، رقمشان
هم‌عرض است (ستون در فهرست بلند به‌هم نمی‌ریزد)، و با یک کلیک کاملاً
انتخاب می‌شوند.

### Fixed — تاریخ شمسی با رقم انگلیسی

`1405/07/18` کنارِ «انقضا» و «۲۳ روز مانده» تنها عددِ لاتینِ کادر
بود. حالا در مینی‌اپ، پنل نماینده و حسابداری از یک تابع رد می‌شود.

### Fixed — پروفایل صفحه اشتراک رنگِ بی‌ربط داشت

رنگِ دایره از هشِ نامِ کاربر می‌آمد، پس روی صفحه‌ای که نماینده
رنگش را خودش انتخاب کرده یک دایره‌ی بنفش یا نارنجیِ بی‌ربط
می‌نشست. حالا همان `--accent` است که بقیه‌ی صفحه از آن رنگ گرفته.

### Fixed — نبودِ یک هدر، مالک را از پنل بیرون می‌انداخت

خواندنِ هدرِ نسخه روی پاسخی که `headers` نداشت خطا می‌داد، خطا در
`catch`ِ بیرونی می‌افتاد، و نتیجه‌اش پاک‌شدنِ رمز و بیرون‌انداختنِ
مالک بود — برای یک هدرِ **اختیاری**.

## [1.58.0]

### Changed — تنظیمات روی دیتابیس نشست

تنظیماتِ پنل در یک فایل JSON بود. سه ایراد داشت و هر سه واقعی
بودند:

**۱. نوشتن اتمی نبود.** `save_config` با `open(w)` اول فایل را خالی
می‌کرد بعد می‌نوشت. برق برود یا دیسک پر شود، فایل نصفه می‌ماند.

**۲. و خرابی بی‌صدا بود.** `load_config` وقتی JSON خراب بود
`DEFAULT_CONFIG` برمی‌گرداند — یعنی برند، پالت، لینک‌ها، FAQ و
فهرست نماینده‌ها پاک می‌شدند و هیچ‌جا نمی‌گفت چرا. چون پنل سالم به
نظر می‌رسید، مالک فکر می‌کرد خودش چیزی را خراب کرده. این دقیقاً
همان «مسیر خرابِ بی‌صدا»یی است که `CLAUDE.md` ممنوع کرده.

**۳. آخرین ذخیره همه‌چیز را می‌برد.** دو تبِ باز یعنی یکی بی‌صدا
کارِ دیگری را پاک می‌کرد.

حالا در جدولِ `panel_config` داخل `bot.db` می‌نشیند — یک فایل برای
پشتیبان‌گرفتن و برگرداندن:

- نوشتن داخل `BEGIN IMMEDIATE`: یا کامل می‌نشیند یا اصلاً
- هر ذخیره نسخه‌ی قبلی را در `panel_config_history` نگه می‌دارد
  (پنجاه نسخه‌ی آخر)
- خرابیِ بدنه از تاریخچه ترمیم می‌شود، **نه** با برگشت به پیش‌فرض،
  و لاگِ بلند می‌دهد
- `X-Config-Version`: پنل نسخه‌ای را که گرفته پس می‌فرستد و اگر
  جای دیگری عوض شده باشد ۴۰۹ می‌گیرد، نه پاک‌کردنِ خاموش

**مهاجرت خودکار است.** اولین اجرا از `config.json` پر می‌شود و
**فایل پاک نمی‌شود** — به‌عنوان پشتیبان می‌ماند.

### Added — تاریخچه‌ی تنظیمات و برگشت به عقب

در «تنظیمات»: فهرست نسخه‌های قبلی با تاریخ، و دکمه‌ی بازگرداندن.
خودِ برگشت هم یک ذخیره‌ی تازه است نه پاک‌کردنِ تاریخچه، پس اگر
اشتباه بود از همان راه برمی‌گردد.

### Fixed — خواندن تنظیمات ۳٫۷ برابر سریع‌تر شد

اندازه‌گیری‌شده، نه حدس: روشِ قدیم ۰٫۵۲ میلی‌ثانیه برای هر خواندن،
حالا ۰٫۱۴.

مسیر تا این عدد خودش درس داشت. نسخه‌ی اولِ دیتابیسی **کندتر** از
فایل شد (۱٫۱۷ میلی‌ثانیه)، چون در هر خواندن یک اتصال تازه باز
می‌شد و دو `CREATE TABLE IF NOT EXISTS` می‌خورد. سه اصلاح:
ساختِ جدول یک‌بار در عمرِ پردازه، اتصالِ به‌ازای نخ، و پرسیدنِ
*فقط نسخه* پیش از کشیدنِ بدنه.

### Fixed — نبودِ یک هدر، مالک را از پنل بیرون می‌انداخت

خواندنِ هدرِ نسخه روی پاسخی که `headers` نداشت خطا می‌داد، خطا در
`catch`ِ بیرونی می‌افتاد، و نتیجه‌اش `setAuthed(false)` و پاک‌شدنِ
رمز بود — یعنی بیرون‌انداختنِ مالک، برای یک هدرِ **اختیاری**.
هارنس همان روز گرفتش.

### Fixed — هدرِ نسخه در حالت cross-origin نامرئی بود

هدرِ سفارشی بدون `expose_headers` برای جاوااسکریپت خوانده نمی‌شود،
و پنل روی مبدأ صفحه‌ی اشتراک نیست. بدون این، محافظِ «دو تبِ باز»
بی‌صدا از کار می‌افتاد — درست همان چیزی که قرار بود جلویش را
بگیرد.

## [1.57.0]

### Fixed — صفحه‌ی اشتراک ۴۸ آیکونِ نامرئی داشت

بزرگ‌ترین چیزی که این نسخه درست می‌کند، و بدترین شکلِ خرابی:
**روی مرورگرِ ما درست دیده می‌شد.**

صفحه‌ی اشتراک دارایی‌هایش را از سه CDN می‌گرفت:

| از کجا | چه چیزی |
|---|---|
| `cdnjs.cloudflare.com` | فونت‌آسام — ۴۸ آیکون |
| `fonts.googleapis.com` | JetBrains Mono و Chakra Petch |
| `cdn.jsdelivr.net` | وزیرمتن |
| `flagcdn.com` | پرچمِ کشورِ هر کانفیگ |

هیچ‌کدام در ایران بالا نمی‌آیند. و فونت‌آسام وقتی نرسد *هیچ چیزی*
نشان نمی‌دهد — نه مربع، نه علامت سؤال. پس مشتری ۴۸ جای خالی
می‌دید و ما یک صفحه‌ی سالم. هر بار که با VPN باز می‌کردیم، درست
بود.

حالا آیکون‌ها به‌صورت ماسکِ SVG داخل خودِ فایل نشسته‌اند
(`tools/make-subpage-icons.js`). نشانه‌گذاری دست نخورد — هنوز
`<i class="fa-solid fa-copy">` است — چون آیکون‌ها فقط در HTML
نیستند: در جدولِ ترجمه‌ی چهار زبان، در نگاشتِ پلتفرم، و در
`className =` هم ساخته می‌شوند. و چون رنگ از `currentColor`
می‌آید، `style="color: var(--accent)"`های موجود هنوز کار می‌کنند.

آیکون‌ها از lucide آمده‌اند — همان مجموعه‌ای که پنل استفاده
می‌کند، پس صفحه‌ی مشتری و پنلِ مالک یک زبانِ تصویری دارند.

پرچم‌ها از خودِ کد کشور ساخته می‌شوند (دو نشانگر منطقه‌ای یونیکد)،
بدون هیچ درخواستی.

### Fixed — پنل هم با VPN و بی‌VPN دو شکل داشت

`JetBrains Mono` از گوگل می‌آمد. غیرمسدودکننده بود، پس خرابی دیده
نمی‌شد — ولی نتیجه این بود که هیچ دو نفری یک چیز نمی‌دیدند. از آن
بدتر: آن قلم اصلاً گلیفِ فارسی ندارد، پس عددِ فارسی هیچ‌وقت از آن
استفاده نمی‌کرد. هزینه‌ی یک درخواستِ بلاک‌شونده را می‌دادیم بدون
اینکه چیزی از آن بگیریم.

حالا `--mono` فقط قلم‌هایی را نام می‌برد که روی خودِ سیستم هستند.

یک دروازه هم اضافه شد که هر دارایی‌ِ CDNیِ تازه را رد می‌کند و
اسمِ خودش را می‌گوید.

### Added — زنگِ اعلان: «چه کسی منتظرِ من است، و از کِی»

نشانِ کنارِ منو می‌گفت «۲ رسید»، ولی نمی‌گفت کی فرستاده و چقدر
معطل مانده. مالک باید صفحه‌ی سفارش‌ها را باز می‌کرد تا بفهمد
کدامش دو ساعت است که جواب نگرفته.

زنگ در نوار بالا: نامِ فرستنده، مبلغ یا متنِ پیام، و «۳ ساعت و ۳۴
دقیقه». **قدیمی‌ترین بالا** — نه تازه‌ترین — چون همان دارد دیر
می‌شود. بعد از یک ساعت نارنجی و بعد از سه ساعت قرمز می‌شود. کلیک
روی هر سطر مستقیم به همان صفحه می‌برد.

و دو باگ که همین‌جا پیدا شدند:

- **کوئریِ هشدارها فیلترِ مستاجر نداشت**، پس رسیدِ مشتریِ نماینده
  هم به مالک هشدار می‌داد — کاری که مالِ او نیست. همان دامِ
  «FROM … بدون شرط» که در این مخزن پنج بار پیدا شده.
- **پنل روی پنجره‌ی کوتاه اسکرول می‌خورد.** کفِ `min-height: 420px`
  روی نمایشگرِ ۶۰۳ پیکسلی می‌شد ۶۰۹ — یعنی جعبه‌ی نوشتنِ گفتگو زیر
  لبه می‌رفت و برای جواب‌دادن باید اسکرول می‌کردی.

### Fixed — دکمه‌هایی که ربات را باز می‌کردند ولی جایی نمی‌بردند

`?start=wallet` را `/start` فقط به‌عنوان «کد معرف» می‌خواند و چون
کد معتبری نبود بی‌صدا دورش می‌ریخت. یعنی دکمه *کار می‌کرد* — ربات
باز می‌شد — ولی کاربر در منوی اصلی رها می‌شد.

حالا `wallet`، `buy`، `subs` و `renew_<id>` مقصدِ واقعی دارند. و
این کلمه‌ها جزو «رزرو»اند: کد معرف شش حرفِ بزرگ از همان الفباست،
پس `WALLET` می‌توانست روزی کدِ واقعیِ کسی باشد و هر کسی که روی
«شارژ» می‌زد بی‌صدا زیرمجموعه‌اش می‌شد.

`bot_username` هم فقط وقتی پر می‌شد که توکن از خودِ پنل ثبت شده
باشد؛ با نصب‌کننده یا CLI خالی می‌ماند و **همه‌ی** دکمه‌های «برو به
ربات» بن‌بست می‌شدند. حالا یک‌بار از تلگرام پرسیده و نگه داشته
می‌شود.

### Added — دکمه‌ی تمدید در مینی‌اپ

تا امروز فقط یک جمله بود: «از ربات تمدید کنید». یعنی کاربر باید
خودش ربات را باز می‌کرد، منو را می‌گشت، و بین چند اشتراک همان یکی
را پیدا می‌کرد. حالا دکمه مستقیم روی همان اشتراک در ربات باز
می‌شود.

کادرِ «چند روز مانده» هم وقتی اشتراک منقضی شده قرمز می‌شود و زیر
هفت روز نارنجی — مهم‌ترین خبرِ آن صفحه نباید هم‌رنگِ یک یادداشت
معمولی باشد.

### Changed — مینی‌اپ خودش را تازه می‌کند

سه شکاف بسته شد:

- فقط `visibilitychange` شنیده می‌شد. داخل WebViewِ تلگرام برگشتن
  به مینی‌اپ گاهی فقط `focus` می‌دهد — و وصل‌شدنِ دوباره‌ی اینترنت
  هیچ‌کدام را نمی‌دهد. حالا هر سه.
- داخل گفتگو هر شش ثانیه به‌جای بیست. بیست ثانیه برای شمارنده‌ها
  خوب است، ولی وقتی کاربر *در حالِ چت‌کردن* است خیلی طولانی است.
- پنل هم با برگشتن به تب فوری تازه می‌شود، نه اینکه تا تیکِ بعدی
  صبر کند.

## [1.56.0]

### Changed — چت پنل، مثل یک پیام‌رسان

صندوق پیام کار می‌کرد ولی شبیه یک فرمِ اداری بود: فهرستی از پیام‌ها
با تاریخِ کامل روی هرکدام. این صفحه جایی است که مالک با مشتریِ
عصبانی حرف می‌زند؛ هر چیزی که در تلگرام عادت دارد و این‌جا نباشد یک
لحظه مکث می‌سازد — و مکثِ آن لحظه یعنی جوابِ دیرتر.

حالا همان چیزهایی را دارد که دست آدم رویشان می‌رود بدون فکر:

- دو ستونِ تمام‌قد — گفتگوها کنار، پیام‌ها وسط. زیر ۹۰۰ پیکسل روی هم
- جست‌وجو در گفتگوها (نام، نام‌کاربری، شناسه، متنِ آخرین پیام)
- جداکننده‌ی روز: «امروز»، «دیروز»، بعد تاریخِ شمسی
- پیام‌های پشت‌سرهمِ یک نفر گروه می‌شوند — فقط اولی چهره می‌گیرد،
  وگرنه ده پیامِ پشت‌سرهم ده آواتار می‌شد
- ساعت روی هر پیام، نه تاریخِ کامل
- تیکِ خوانده‌شدن روی پیام‌های خودِ مالک
- دکمه‌ی «برو پایین» — ولی فقط وقتی کاربر خودش بالا رفته باشد.
  پرتاب‌شدن به انتها وسطِ خواندنِ تاریخچه، آزاردهنده است

خبرهای خودکار (تایید و ردِ رسید) با `sender='system'` در همان گفتگو
می‌نشینند، پس مالک دقیقاً همان چیزی را می‌بیند که مشتری دیده — نه یک
روایتِ جدا.

### Added — تنظیمات و پروفایل در مینی‌اپ

`first_name` از تلگرام می‌آید و ممکن است «😎» باشد یا اصلاً نباشد، و
شماره را ربات فقط وقتی دارد که کاربر دکمه‌اش را زده باشد. یعنی
پشتیبانی اغلب نمی‌دانست با که حرف می‌زند، و خودِ مشتری در فهرستِ
سفارش‌ها «بدون نام» بود.

تبِ پنجم: عکس پروفایل، نام، شماره تماس، وضعیتِ کیف پول و سکه، شارژ،
و لینکِ کانال و پشتیبانی.

عکس‌ها نامِ تصادفی می‌گیرند (`<tid>_<uid>_<hex16>.<ext>`). دلیلش این
است که `<img>` نمی‌تواند هدرِ احراز هویت بفرستد، پس مسیرِ عکس عمومی
است — و `7_11.png` یعنی هر کسی با شمردنِ شناسه‌ها عکسِ هر مشتری را
برمی‌دارد. نامِ فایل خودش کلید است.

نوشتن پشتِ `mini_user` است و شناسه از نشست می‌آید نه از بدنه، وگرنه
هر کسی عکسِ هر کسی را عوض می‌کند. `..` و جداکننده‌ی مسیر هم رد
می‌شوند.

### Fixed — ساعتِ پیام در مینی‌اپ رقمِ لاتین داشت

تنها عددِ لاتینِ گفتگو بود و کنارِ متنِ فارسی مثل یک وصله دیده
می‌شد. `faDigits` از قبل در همان فایل import شده بود و فقط همین یک
جا استفاده نشده بود.

(شناسه‌ی سفارش، لینک اشتراک و شماره‌ی کارت عمداً لاتین می‌مانند —
آن‌ها را کاربر کپی می‌کند، ساعت را فقط می‌خواند.)

### Fixed — متنِ خالیِ چت، جای اشتباهی را نشان می‌داد

«از فهرست **کنار**، مشتری‌ای را انتخاب کنید» فقط در چیدمانِ دوستونی
درست بود. زیر ۹۰۰ پیکسل فهرست می‌رود *بالای* گفتگو.

## [1.55.0]

### Fixed — عکس رسید گم می‌شد

مشتری پول داده بود و مدرکش نبود.

عکسِ رسیدِ مینی‌اپ فقط به گروه مدیریت تلگرام آپلود می‌شد و
`file_id`ش نگه داشته می‌شد:

```python
if photo_bytes and gid:
```

اگر گروه مدیریت تنظیم نشده بود — یا آپلود شکست می‌خورد —
`receipt_file` خالی می‌ماند در حالی که `receipt_type` هنوز `"photo"`
بود. و پنل دقیقاً همین را می‌سنجد، پس ۴۰۴ می‌داد و **عکس برای همیشه
گم می‌شد**؛ مالک حتی نمی‌دانست چیزی بوده.

حالا دیسکِ خودمان منبعِ اصلی است و تلگرام فقط راهِ دیدنِ سریع در
گروه. نامِ فایل از بیرون می‌آید، پس `..` و جداکننده‌ی مسیر رد
می‌شوند.

### Fixed — برگه‌ی پرداخت بالایش بریده می‌شد

روی گوشیِ ۸۱۲ پیکسلی جا می‌شد و مشکلی دیده نمی‌شد. داخل تلگرام که
ارتفاعِ قابل‌استفاده کمتر است، برگه ۵۳۴ پیکسل می‌شد در ۴۸۰ — یعنی
دستگیره و عنوانِ «واریز کارت‌به‌کارت» می‌رفتند بالای لبه، و اسکرول
هم نداشت.

اندازه‌گیری: `sheetTop` از ‎−۴۰ به ‎+۲۴.

### Added — صندوق پیام: خبر دادن، و حرف زدن

سه خواسته‌ی جدا به نظر می‌رسیدند — خبرِ تایید/ردِ رسید، چت با
پشتیبانی، و اینکه مالک بفهمد رسیدی آمده. ولی هر سه یک چیزند: پیامی
از یک طرف به طرف دیگر با وضعیتِ خوانده‌نشده.

| برای | چه می‌بیند |
|---|---|
| مشتری | تبِ «پیام‌ها»، با نشان و پاپ‌آپ. ردِ رسید **دقیقاً همان متنی** را می‌آورد که مالک نوشته |
| مالک | بخشِ «پیام‌ها» و نشانِ قرمز روی منو: چند رسید و چند پیام منتظرند |

**بدون تازه‌سازی دستی.** هر بیست ثانیه فقط شمارنده‌ها پرسیده می‌شوند
(نه کلِ داده) و وقتی عددی عوض شد، داده گرفته می‌شود. تبِ پنهان هیچ
درخواستی نمی‌فرستد.

و مرزی که نباید شکسته شود: این صندوق چیزی را تایید یا رد نمی‌کند،
فقط خبر می‌دهد. وگرنه مسیر پنجمِ پول ساخته بودیم.

### Changed — صفحه‌ی ورود نکسورا

نوارِ shimmer قبلی جایش را به یک صحنه داد: مدارِ نقطه‌چین با کمانی
که رویش می‌چرخد و شش گره که یکی‌یکی روشن می‌شوند؛ نشان از تاری و
مقیاسِ ۰٫۸۸ بیرون می‌آید؛ نام از فاصله‌ی حروفِ ۰٫۶۲em به ۰٫۲۲em جمع
می‌شود؛ و دو لکه‌ی نورِ بسیار محو آرام جابه‌جا می‌شوند.

خروج هم نرم است — ۳۸۰ میلی‌ثانیه محو و تار، بعد اپ. قطعِ ناگهانی
نیست.

**هیچ درصدِ ساختگی‌ای نیست.** مدار عمداً نامعین است چون پیشرفتِ
دانلودِ یک تکه را نمی‌شود صادقانه اندازه گرفت. و اگر تکه نیامد،
حالتِ خطا با دکمه‌ی «تلاش دوباره» می‌آید — نه لودینگِ ابدی.

### Fixed — و سه چیز که سرِ راه پیدا شدند

**پنل دو رندرکننده‌ی منو دارد.** نشانِ هشدار فقط به `App.jsx` اضافه
شد و هیچ‌وقت دیده نشد، چون حالتِ پیش‌فرض `shell/workspace.jsx` است.
حالا یک کامپوننتِ `NavAlert` و تستی که هر دو را بسنجد.

**هارنس بی‌صدا می‌مرد.** یک خطای نحوی در `harness-boot.js` صفحه را
نمی‌شکند — فقط `fetch` دزدیده نمی‌شود، پس هارنس به سرورِ واقعی
می‌زند و صفحه‌ی ورود نشان می‌دهد. حالا `make-harness` نحو را می‌سنجد
و با خطا می‌ایستد.

**مسیرهای مینی‌اپ بالاتر از `mini_user` نشسته بودند.** پایتون
`Depends(mini_user)` را موقع تعریف اجرا می‌کند، پس کلِ بکند بالا
نمی‌آمد. تست گرفتش.

## [1.54.0]

### Fixed — جای آپلود لوگو برای خودِ مالک، که اصلاً نبود

قابلیت لوگو در ۱.۵۳.۰ ساخته شد ولی فهرست نماینده‌ها
`WHERE parent_id IS NOT NULL` است، یعنی **خودِ مالک هیچ‌وقت در آن
نیست**. پس تنها جایی که دکمه‌ی آپلود داشت کارت نماینده‌ها بود و
صاحبِ پنل به آن نمی‌رسید.

قابلیتی که کاربرش به آن نمی‌رسد، وجود ندارد.

حالا در **صفحه اشتراک → تنظیمات ظاهری → هویت برند**، کنار نام برند و
رنگ‌ها — همان‌جا که آدم دنبالش می‌گردد.

### Changed — صفحه‌ی اشتراک: دو کارتی که یک چیز را دو بار می‌گفتند

اندازه‌گیری‌شده: صفحه ۱٫۹ نمایشگر بلند بود و همین دو کارت ۲۶۲ پیکسل
از آن را می‌گرفتند —

| کارت | ارتفاع | محتوا |
|---|---|---|
| `sec-stats` | ۹۰ | حجم کلی · **مصرف کل** · دانلود · آپلود |
| `sec-details` | ۱۷۲ | **مصرف کل** · باقی‌مانده · حجم باقی‌مانده · آخرین اتصال |

«مصرف کل» دو بار، و «باقی‌مانده ٪» همان چیزی که حلقه‌ی بالای صفحه
می‌گفت. حالا یک کارت با شش قلمِ یکتا در شبکه‌ی سه‌تایی.

پیش از جابه‌جا کردن چیزی، `setText` اضافه شد: نوشتن روی شناسه‌ای که
نیست، کل رندر را می‌انداخت.

### Changed — چهره‌ی مشتری، آرام‌تر

از ۴۶ به ۳۸ پیکسل، و به‌جای گرادیانِ سیر، تهِ‌رنگِ همان رنگ روی سطحِ
خودِ صفحه با حرفی به رنگِ سیر. در سرصفحه‌ای که بقیه‌اش ۱۳ و ۱۴ پیکسل
است، دایره‌ی بزرگ و پررنگ چشم را از خودِ نام می‌دزدید.

### Changed — «آخرین اتصال»

`toLocaleString` می‌داد «۱۴۰۴/۷/۱۲، ۲۰:۳۷:۱۹» که در سلولِ یک‌سومِ
عرضِ گوشی جا نمی‌شد و آخرش سه‌نقطه می‌خورد. حالا «۷ دقیقه پیش».

و `en-font` از رویش برداشته شد: نثر فارسی است نه عدد، و JetBrains
Mono حرف فارسی ندارد — فقط به فونتی پهن‌تر می‌افتاد و همان یک پیکسل
باعث سه‌نقطه می‌شد.

### Changed — کارت پشتیبانی

۱۴۲ پیکسل برای یک جمله و یک دکمه. حالا یک ردیف: متن و دکمه کنار هم،
۱۱۵ پیکسل.

روی هم: ارتفاع صفحه از ۱۵۷۵ به ۱۴۸۰ پیکسل.

### Fixed — داده‌ی ساختگیِ غلط در هارنس

`lastOnline` را ثانیه می‌داد و 3x-ui میلی‌ثانیه می‌دهد (مثل
`expiryTime`). صفحه «۱۳۴۸/۱۱/۱» نشان می‌داد و من دنبال باگی می‌گشتم
که در خودِ صفحه نبود.

## [1.53.1]

### Fixed — ناحیه‌ی امن تلگرام که در صفر ضرب می‌شد

یک خط در CSS بود که *شبیه* رعایت ناحیه‌ی امن است:

```css
calc(12px + env(safe-area-inset-top, 0px) * 0)
```

ضرب در صفر. از اول هیچ کاری نمی‌کرد — بدترین نوعِ مسیر خرابِ بی‌صدا،
چون به نظر می‌رسد کار شده.

و حتی بدون آن هم `env()` جواب نمی‌داد: مرورگر آن را از سیستم‌عامل
می‌گیرد، ولی داخل WebViewِ تلگرام معمولاً صفر برمی‌گردد و مهم‌تر،
اصلاً چیزی از **نوارِ خودِ تلگرام** نمی‌داند — نواری که روی محتوای ما
می‌نشیند.

حالا عددها از خود تلگرام می‌آیند (`safeAreaInset` برای ناچ دستگاه،
`contentSafeAreaInset` برای نوار تلگرام) و چرخاندن گوشی و بازشدن
صفحه‌کلید هم دنبال می‌شوند.

اندازه‌گیری: بدون اینست ۱۲/۸ پیکسل؛ با ۴۷/۳۴ می‌شود ۵۹/۴۲.

### Fixed — جای نوارِ پایین پنج پیکسل کم بود

۶۸ رزرو شده بود و خودِ نوار ۷۳ است، پس آخرین ردیف زیرش می‌رفت.

### Added — ورودِ صفحه در مینی‌اپ

تب‌ها بی‌هیچ حرکتی جا عوض می‌کردند. پنل مدیر این را داشت و مینی‌اپ
نداشت — و همان تفاوتِ کوچک است که یکی را «اپ» و دیگری را «صفحه‌ی
وب» نشان می‌دهد. ۰٫۲۸ ثانیه، کوتاه‌تر از پنل، چون روی گوشی انگشت
تندتر از موس است.

### Fixed — تستِ اپ‌ها هارنس را پاک می‌کرد

`test-apps-runtime.js` وقتی `dist-test` کهنه باشد خودش بیلد می‌کند، و
بیلد آن پوشه را پاک می‌کند — یعنی `/harness.html` هم با آن می‌رفت.
بعدش سرورِ پیش‌نمایش بی‌صدا خودِ پنل را برمی‌گرداند (SPA fallback) و
آدم فکر می‌کند صفحه خراب شده. حالا همان‌جا دوباره ساخته می‌شوند.

### Added — وقتی مشتری برمی‌گردد، صفحه تازه می‌شود

رسید که فرستاده شد، مشتری می‌رود سراغ کار دیگری و بعد برمی‌گردد
ببیند تایید شده یا نه. بدون این، همان صفحه‌ی کهنه را می‌دید و فکر
می‌کرد هیچ اتفاقی نیفتاده — یعنی می‌رفت از پشتیبانی می‌پرسید.

با فاصله‌ی کمینه‌ی هشت ثانیه، وگرنه هر بار جابه‌جا شدن بین تب‌های
تلگرام یک درخواست می‌زد.

### Added — `?insets=` در هارنس

بیرون از تلگرام ناچ و نوار هر دو صفرند، پس محتوایی که زیرشان می‌رود
هیچ‌وقت دیده نمی‌شد — و دقیقاً همان چیزی است که روی گوشیِ واقعیِ
مشتری خراب است.

## [1.53.0]

### Added — خرید کامل در مینی‌اپ، بدون ربات

کارت‌به‌کارت با رسید. مشتری مبلغ را واریز می‌کند، عکس رسید یا متن
پیامک بانک را می‌فرستد، و روی صفحه‌ی خانه می‌بیند رسیدش در چه حالی
است. هیچ‌جا لازم نیست به گفتگوی ربات برگردد.

و باز هم هیچ منطقی در مینی‌اپ نیست. دو هسته‌ی تازه بیرون کشیده شد و
هر دو مسیر — ربات و مینی‌اپ — همان‌ها را صدا می‌زنند:

| هسته | چه چیزی را نگه می‌دارد |
|---|---|
| `card_order` | قیمت با تخفیف سکه، رزرو سکه، مهلت، انتخاب کارت |
| `receipt_submit` | قاعده‌ی مهلت، ادعای اتمی، اعلان گروه با دکمه‌ها |

نسخه‌ی اولِ `mini_order` همین را دوباره نوشته بود و **دو چیز را جا
انداخت**: تخفیف سکه و `order_ttl_minutes`. یعنی مشتری‌ای که سکه
داشت، از مینی‌اپ قیمتِ کامل می‌داد. تستِ «مینی‌اپ خودش پول جابه‌جا
نمی‌کند» همان را گرفت — دقیقاً کاری که برایش نوشته شده بود.

عکس رسید همان‌جا به گروه مدیریت آپلود می‌شود و `file_id`ش ذخیره،
پس پنل و پنل نماینده بدون هیچ تغییری همان رسید را نشان می‌دهند. یک
خط لوله، نه دو تا.

### Added — لوگوی اختصاصی هر نماینده

مالک و هر نماینده لوگوی خودشان را آپلود می‌کنند، و مینی‌اپِ همان
نماینده با لوگوی خودش دیده می‌شود.

SVG عمداً پذیرفته نمی‌شود: می‌تواند `<script>` داشته باشد و این فایل
از دامنه‌ی خودِ ما سرو می‌شود. فرمت از **بایت‌های اولِ فایل** سنجیده
می‌شود، نه از پسوند و نه از `Content-Type` که هر دو را فرستنده
می‌نویسد.

base64 در JSON، نه multipart: `python-multipart` نصب نیست و افزودنِ
وابستگی به سروری که با `nexora-cli` به‌روز می‌شود، ریسکِ بی‌دلیل
دارد.

### Added — حجم پلن نماینده از نرخ مالک می‌آید

تا امروز نماینده هر حجمی می‌توانست برای ربات خودش تعریف کند — بدون
هیچ ربطی به آنچه مالک برایش نرخ گذاشته. یعنی می‌شد پلنی ساخت که
هیچ نرخی برایش وجود ندارد و آخر ماه صفر حساب شود.

| حالت مالک | نماینده چه می‌تواند |
|---|---|
| نرخ حجمی دارد | هر حجمی — مالک به‌ازای مصرف حساب می‌کند |
| پله تعریف شده | فقط همان پله‌ها |
| هیچ نرخی نیست | آزاد، ولی با گفتنِ صریحِ اینکه چرا |

اعتبارسنجی در بکند است نه فقط در رابط، و پیام خطا **پله‌های مجاز را
می‌گوید**: «نامعتبر» بدون گفتنِ اینکه چه چیزی معتبر است، یعنی
حدس‌زدن.

و هر ردیف حالا هزینه‌ی خودِ نماینده را نشان می‌دهد و اگر زیرش قیمت
گذاشته باشد قرمز می‌شود. بدون این، «زیر قیمت فروختم» فقط یک‌بار —
آخر ماه، روی صورتحساب — معلوم می‌شد.

### Fixed — مینی‌اپ خاکستری بود

`syncTheme` همه‌ی سطح‌ها را مستقیم از `themeParams` برمی‌داشت و
خاکستریِ خنثای تلگرام هر مینی‌اپی را شبیه هم می‌کند. حالا تلگرام فقط
تصمیم می‌گیرد **روشن یا تیره**؛ رنگ را نکسورا تصمیم می‌گیرد و در CSS
تعریف شده، نه داخل JSX.

### Fixed — چهره‌ی صفحه‌ی اشتراک بیضی بود

قابِ ۴۶ پیکسلی داخل یک ستونِ فلکسِ ۵۴ پیکسلی بود و کنارش خطِ
«آفلاین» هم می‌نشست، پس از ۴۶ به ۳۸ فشرده می‌شد — و
`border-radius: 50%` روی ۴۶×۳۸ یعنی بیضی. از اول این‌طور بود؛
گرادیانِ تازه فقط آشکارش کرد.

### Changed — صفحه‌ی اشتراک، به زبان طراحی پنل

این صفحه بی‌سلیقه نبود. چیزی که نداشت همان سه چیزی بود که پنل را
«پنل» می‌کند: سریِ دومِ فیروزه‌ای، خطِ مویِ روشن روی لبه‌ی بالای
کارت‌ها، و نورِ محیطی. به‌علاوه پنج پله‌ی معنایی با همان مقادیر پنل.

### Changed — صفحه‌ی ورود کف زمانی گرفت

تا نشان کامل دیده شود. دانلودِ تکه **پیش از** رندر شروع می‌شود،
وگرنه کف و دانلود پشت سر هم می‌دویدند نه هم‌زمان. با
`prefers-reduced-motion` کف برداشته می‌شود.

### Testing

همان کف، دو تستِ خودمان را افشا کرد: تستِ بالاآمدنِ اپ‌ها متنِ
*صفحه‌ی ورود* را می‌دید و چون برچسبش «پنل نمایندگی» است، انتظارش
اتفاقی برآورده می‌شد — سبز، بدون اینکه اپ اصلاً بالا آمده باشد.

و سه دروازه‌ی دیگر که به اتاق خالی نگاه می‌کردند بسته شدند: یکی
کلمه‌ای را در کامنتِ فارسی پیدا می‌کرد، یکی نامِ تابع را در
docstring، و یکی `lazy(` را در توضیحِ خودِ کد.

## [1.52.0]

### Added — خرید داخل مینی‌اپ

مشتری اشتراک را از خودِ مینی‌اپ می‌خرد و پول می‌دهد، بدون برگشتن به
گفتگوی ربات. برگه‌ی تایید مبلغ، موجودی و «بعد از خرید چقدر می‌ماند»
را کنار هم نشان می‌دهد — چون خرید یک تصمیم کوتاه است و این مقایسه
نباید از جلوی چشم برود.

**ولی هیچ منطق پولی در مینی‌اپ نیست.** هسته‌ی `wallet_pay` به
`wallet_purchase` بیرون کشیده شد و هر دو مسیر — ربات و مینی‌اپ —
همان یک نسخه را صدا می‌زنند. `wallet_pay` حالا فقط پیام می‌سازد.

این مسیر چهارمِ پول در این مخزن است. سه مسیر قبلی هر کدام یک‌بار یک
قاعده را فراموش کردند و سه بار جدا پیدا شدند. این یکی نمی‌تواند،
چون نسخه‌ی خودش را ندارد.

شارژ کیف پول عمداً در ربات ماند (کارت‌به‌کارت و رسید آن‌جاست) و برگه
در همان حالت کاربر را به ربات می‌برد.

### Changed — رنگ: یک نام، یک مقدار

توکن‌های معنایی از قبل وجود داشتند و کامنت بالایشان ادعا می‌کرد کار
تمام شده. نبود: JSX هنوز **۳۲۸ بار** `rgba()` خام می‌نوشت. رنگ هشدار
به‌تنهایی ۶۳ بار با **۱۷ آلفای متفاوت** — `.05` و `.06` و `.07` و
`.08` را هیچ طراحی عمداً انتخاب نمی‌کند؛ ردِ پای کپی‌کردن‌اند.

نتیجه‌اش این بود که هیچ دو چیپِ «هشدار» در پنل دقیقاً یک رنگ نبودند.
همان چیزی که آدم حس می‌کند ولی نمی‌تواند انگشت رویش بگذارد.

حالا پنج پله برای هر وضعیت (wash · soft · fill · line · edge) و
تستی که جلوی برگشتنشان را می‌گیرد.

### Added — نشان نکسورا، و صفحه‌ی ورود

برند سه شکل داشت: سه مربعِ گردِ «N» با سه شیبِ متفاوت، در نوار
کناری و دو صفحه‌ی ورود. سه شکل برای یک برند یعنی هیچ‌کدام شناخته
نمی‌شوند.

حالا یک نشان SVG: همان N با حلقه‌ای که مثل لوگو از پشتش رد می‌شود و
جلویش درمی‌آید. صفحه‌ی ورود جای نوار shimmer قبلی را گرفت.

### Fixed — پنل نماینده تنها جایی بود که حالت‌های مشترک را نداشت

`skeleton: 0` و `EmptyState: 0` — تنها فایلِ صفحه‌ای در کل پروژه با
این وضع. «در حال بارگذاری…» یک `<p>` ساده بود و «هنوز کانفیگی
ندارید» هم. این دلیلِ واقعیِ آن حسِ «انگار پروژه‌ی دیگری است» بود،
نه کارت‌های آمار که از همان کامپوننت مشترک می‌آمدند و دقیقاً همان
۱۴۲ پیکسل بودند.

### Fixed — `var(--chip)` که هیچ‌وقت تعریف نشده بود

نشانِ «سوکت موقت xray» در صفحه‌ی فایروال از اول بی‌زمینه بود. تستِ
تازه‌ی توکن‌ها پیدایش کرد.

### Testing

تستِ ast که می‌سنجید هر مسیر خرید پورسانت و پاداش را بدهد، حالا به
`wallet_purchase` نگاه می‌کند نه به پوسته — و یک تست تازه می‌سنجد که
پوسته خودش `create_order`/`spend_balance` صدا نزند.

مهم‌تر: تستِ رفتاریِ **شکستِ ساخت کانفیگ**. نسخه‌ی اول تست‌ها این را
نمی‌گرفت، چون در مسیر موفق سفارش در هر دو حالت `approved` تمام
می‌شود و تفاوت دیده نمی‌شود. با شکستِ ساخت، تفاوت معلوم شد: بدون
این تست، نوشتنِ `approved` پیش از ساخت سبز می‌ماند و ۲۰۰ هزار تومان
برنمی‌گشت.

## [1.51.0]

### Added — چهره‌ی کاربر، همه‌جا

فهرست‌های این پنل همه یک شکل بودند: صد ردیف با شناسه‌هایی مثل
`nexora_8814_1` که فقط در رقمِ وسط با هم فرق داشتند. چشم روی چنین
فهرستی نمی‌ایستد — باید خواند، و خواندن کند است.

حالا هر کاربر یک چهره دارد: حرفِ اولِ نامش روی رنگی که از هشِ خودِ
نام می‌آید. یعنی همان مشتری همیشه همان رنگ را دارد و ردیفش را از
دور می‌شود شناخت. در پنل مدیر (کاربران ربات، سفارش‌ها، پرفروش‌ها)،
در پنل نماینده (خودِ نماینده و تک‌تک مشتری‌هایش)، در مینی‌اپ، و در
صفحه‌ی اشتراکی که خودِ مشتری می‌بیند.

آن صفحه‌ی آخر تا امروز یک شبحِ خاکستریِ یکسان برای همه نشان می‌داد.

### Fixed — پنل نماینده که «هیچ فرقی نکرده بود»

نکرده بود، و دلیلش این نبود که کارتی عوض نشده: کارت‌های آمارش از
همان کامپوننت مشترک می‌آمدند و دقیقاً همان ۱۴۲ پیکسل بودند. چیزی که
نداشت، سرصفحه بود — یک خط متنِ ۴۷ پیکسلی بدونِ چهره، بدونِ نشانِ
ربات — و کارت‌هایش هیچ‌وقت داده‌ی سری نمی‌گرفتند، پس هر هشت‌تا یک
عددِ خشک بودند کنار فضای خالی.

حالا `/api/portal/stats` یک سریِ چهارده‌روزه هم می‌دهد (کانفیگ تازه
و تمدید) و دو کارت نمودار کوچک دارند. سرصفحه چهره و `@نامِ ربات`
گرفت.

### Fixed — صفحه‌ی اشتراک را هیچ‌کس ندیده بود

و چیزی که دیده نشود خراب می‌ماند. این صفحه را فقط سرورِ 3x-ui با یک
اشتراکِ واقعی رندر می‌کرد، یعنی عملاً هیچ‌وقت.
`tools/make-subpage-harness.js` هر ۲۳ قالبِ Go را با داده‌ی واقع‌نما
پر می‌کند و صفحه را در مرورگر می‌آورد — همان کاری که پارسال برای خودِ
پنل کردیم و بعدش تازه معلوم شد چه چیزهایی خراب است.

### Fixed — «کپی شد» که وارونه می‌افتاد

در مینی‌اپ، تاییدِ کپیِ شناسه داخل جعبه‌ی `dir="ltr"` بود. شناسه
چپ‌به‌راست است ولی «کپی شد» فارسی است و جابه‌جا می‌افتاد.

### Testing

`AVATAR_HUES` عمداً دو جا نوشته شده: صفحه‌ی اشتراک را سرورِ 3x-ui سرو
می‌کند و به باندلِ ما دسترسی ندارد. طبق قاعده‌ی مخزن، تستِ برابری
گرفت — هم فهرستِ رنگ‌ها، هم فرمولِ هش. وگرنه یک مشتری در پنل بنفش
می‌شد و در صفحه‌ی اشتراک سبز.

اولین نسخه‌ی آن تست به اتاق خالی نگاه می‌کرد: دنبال `paintAvatar(`
می‌گشت و *تعریفِ* تابع را می‌دید، پس با برداشتنِ خطِ صدازدن هم سبز
می‌ماند. حالا دنبالِ `paintAvatar(username)` می‌گردد و هر سه شکلِ
خراب‌کردن قرمزش می‌کنند.

## [1.50.1]

### Changed — هفت حالت خالیِ باقی‌مانده

چهارتاشان کامپوننت مشترک شدند و جمله‌ای گرفتند که نداشتند — مثلاً
به‌جای «رسیدی در انتظار تایید نیست» خشک، حالا می‌گوید «هر رسیدی که
مشتری بفرستد همین‌جا می‌آید — و از گروه تلگرام هم می‌شود تاییدش کرد».

سه‌تا **عمداً** دست‌ساز ماندند: آن‌ها پنلِ تشخیص‌اند، نه حالت خالی.
مثلاً پنلِ «دیتابیس ۳x-ui خوانده نشد» مسیرِ پیداشده، دستور
`nexora fix-xui` و راهِ دستی را نشان می‌دهد؛ ریختنش در یک کامپوننت
عمومی یعنی دورانداختنِ همان چیزی که مفیدش می‌کند. فقط کلاسِ ظاهری
مشترک را گرفتند تا یکدست باشند.

### تست‌ها هر دو قاعده را نگه می‌دارند

هیچ `fx-card p-N text-center` با مرزِ نقطه‌چین نباید بیرون از
کامپوننت برگردد، و آن سه صفحه‌ای که یک ستونِ کشیده بودند باید
گرید بمانند. هر دو با شکستنِ عمدی قرمز می‌شوند.


## [1.50.0]

### Fixed — کارت‌های کوتاهی که تمام عرض صفحه را می‌گرفتند

گفتید بعضی جاها فضای خالی زیادی دارد. اندازه‌گیری‌شده، سه شکل مشخص
بود — همه‌شان یک ستون از کارت‌های تمام‌عرض که چیز کمی داشتند:

| صفحه | قبل | بعد |
|---|---|---|
| متن‌های ربات | ۷ کارتِ ۹۷۰ پیکسلی دور یک کادر سه‌خطی · ۲۳۲۴px | دو ستون · **۱۷۳۲px** (−۲۵٪) |
| سوالات متداول | ۳ کارتِ تمام‌عرض · ۱۰۲۵px | دو ستون · **۷۴۷px** (−۲۷٪) |
| سرورهای تانل | ۳ کارتِ ۹۷۶×۱۳۴ | گرید خودکار · **۲۰۵px** (−۵۹٪) |

فهرست واسطه‌ها با همین معیار شبیه این‌ها بود ولی **آکاردئون** است —
هر ردیف باز می‌شود و فرم تنظیمات نشان می‌دهد، پس عرض را لازم دارد و
دست‌نخورده ماند. بقیه‌ی کارت‌های «کم‌محتوا» هم حالت خالی و کارت
حکم بودند که درست است تمام‌عرض باشند.

### Fixed — بیست حالت خالیِ دست‌ساز

همان شکل، بیست بار دستی نوشته شده بود: کارت نقطه‌چین، آیکونِ
کم‌رنگِ شناور، یک جمله. یعنی خالص‌ترین شکلِ قدیمی‌ترین باگ این مخزن —
یک قاعده در بیست جا، پس بهترکردنِ کامپوننت مشترک هیچ‌کدامشان را عوض
نمی‌کرد.

سیزده‌تا حالا کامپوننت مشترک‌اند، از جمله **هر دو تای مینی‌اپ** — جایی
که مشتری حالا به‌جای «هنوز اشتراکی ندارید» خشک، دلیل و قدم بعدی هم
می‌بیند.

خودِ کامپوننت هم بازنویسی شد: آیکون داخل یک مربعِ ملایم می‌نشیند
(آیکونِ تنهای کم‌رنگ بیشتر شبیه «چیزی بارگذاری نشد» بود تا «این‌جا
عمداً خالی است»)، جمله‌ی اول عنوان شد و توضیح زیرش.

هفت‌تا باقی مانده‌اند — هرکدام داخل یک شرط، که جاروی خودکار با
اطمینان نمی‌توانست دست بزند.

### وضعیت

هر ۴۷ صفحه: بدون کرش، بدون سرریز افقی، روی دسکتاپ و ۳۷۵ پیکسل.


## [1.49.1]

### Added — هر سه اپ را می‌شود نگاه کرد، نه فقط پنل مدیر

پنل نماینده روی `/r/<نشانی>` است و مینی‌اپ روی `/app`، و سرورِ
پیش‌نمایش فایلی روی آن مسیرها ندارد — پس هیچ‌کدام دیده نمی‌شدند.
اسکریپتِ داده حالا مسیر را *پیش از* اجرای باندل عوض می‌کند:

| آدرس | چه چیزی |
|---|---|
| `/harness.html` | پنل مدیر |
| `/harness.html?as=portal` | پنل نماینده |
| `/harness.html?as=mini` | مینی‌اپ مشتری |

با داده‌ی واقع‌نما، هر دو تمیزند: بدون کرش، بدون سرریز افقی روی
۱۲۸۰ و ۳۷۵ پیکسل، و هشت کارت شاخصِ پنل نماینده هم‌قد.

نوار مصرفِ مینی‌اپ هم درست از لبه‌ی راست پر می‌شود — **اندازه‌گیری
شد، نه نگاه**: با چشم غلط به نظر می‌رسید.

### Fixed — تستی که به فایلِ کهنه تکیه می‌کرد

`test-apps-runtime.js` اگر `dist-test` وجود داشت دوباره نمی‌ساختش،
پس وقتی کد عوض می‌شد ولی آن پوشه کهنه می‌ماند، تست **کدِ قدیمی** را
می‌سنجید. یک‌بار در دروازه قرمز شد و در اجرای دستی سبز. حالا زمانِ
تغییرِ `frontend/src` را با آن مقایسه می‌کند و اگر کهنه بود، خودش
می‌سازد.

### Docs — درسِ گران‌ترین دو چیز، در CLAUDE.md

**داده‌ی ساختگی را واقع‌نما نگه دار.** با آرایه‌های خالی، بیشتر
صفحه‌ها شاخه‌ی «هنوز تنظیم نشده» را می‌گیرند و سالم به نظر می‌رسند —
شش کرشِ واقعی فقط وقتی پیدا شدند که داده شبیهِ واقعیت شد.

**اندازه بگیر، نگاه نکن.** کارتی که ۱۹۰ پیکسل بود به‌جای ۱۴۲،
تولتیپی که پهنای صفحه را ۴۱ پیکسل زیاد می‌کرد، و نوار بالایی که
اصلاً نمی‌چسبید — هیچ‌کدام با چشم پیدا نشدند و همه با
`getBoundingClientRect` بلافاصله.


## [1.49.0]

### Changed — شانزده صفحه با ردیف خلاصه

شش صفحه‌ی تازه به فهرست اضافه شدند: دفتر کل، واسطه‌ها و نرخ،
نماینده‌ها و دسترسی، اینباندها، مانیتورینگ سرورهای دیگر، و بلوکِ
آمارِ خودِ فهرست کانفیگ‌ها.

دوتاشان جوابِ سؤال‌هایی‌اند که تا امروز فقط با بازکردنِ تک‌تک ردیف‌ها
پیدا می‌شد:

**نماینده‌ها و دسترسی** — چند نفر پنلشان باز است، مجموع اعتباری که
دستشان است چقدر است، و **چند نماینده بدون گروه مانده‌اند**. نماینده‌ی
بی‌گروه کانفیگی می‌سازد که به هیچ صورتحسابی نمی‌چسبد؛ یعنی پولی که
بی‌صدا از دست می‌رود.

**اینباندها** — چندتا فعال است، ربات از چندتا استفاده می‌کند، و
پیش‌فرض کدام است. «اینباند تعریف نشده» یکی از پرتکرارترین شکایت‌ها
بود و جوابش همین سه عدد است.

مانیتورینگ سرور، کارت‌های سنجه‌ی خودش را با گیج نگه داشت — آن صفحه از
اول برای همین ساخته شده بود و ردیف شاخص رویش فقط تکرار می‌شد.

### Fixed — یک لنگرِ متنی که در تابع اشتباه نشست

دو تابع در `portal-admin.jsx` همان endpoint را صدا می‌زنند، پس
محاسبه‌ی ردیف خلاصه در اولی نشست و در دومی — همان که رندرش می‌کرد —
`ReferenceError` داد و صفحه سفید شد. با همان جاروی کاملِ ۴۷ صفحه پیدا
شد.

### وضعیت کل پنل

هر ۴۷ صفحه، با داده‌ی واقع‌نما: **بدون کرش، بدون سرریز افقی، روی
دسکتاپ و روی ۳۷۵ پیکسل.** شانزده صفحه با ردیف خلاصه (پیش از این سه‌تا).


## [1.48.1]

### Fixed — شش صفحه با داده‌ی واقعی کرش می‌کردند

داده‌ی ساختگیِ هارنس `{ready:true}` و آرایه‌های خالی برمی‌گرداند، پس
بیشتر صفحه‌ها شاخه‌ی «هنوز تنظیم نشده» را می‌گرفتند و سالم به نظر
می‌رسیدند. با پاسخ‌های واقع‌نما — قواعد فایروال، تانل و نود، همکاران
فروش، تلاش‌های نفوذ، یک فاکتور، فهرست کانفیگ‌ها — شش صفحه همان رندر
اول می‌افتادند:

| صفحه | علت |
|---|---|
| داشبورد تانل، فهرست تانل‌ها | `data.tunnels` و `t.ports` بدون محافظ |
| صورتحساب دوره | `inv.newConfigs` و `inv.renewals` |
| فهرست کانفیگ‌ها | `data.clients` و `data.stats` |

هرکدام یک `|| []` کم داشتند. تا وقتی کسی با تانلِ تنظیم‌شده یا
نماینده‌ای با فاکتور آن صفحه را باز نمی‌کرد، معلوم نمی‌شد — و بعدش کلِ
آن بخش سفید می‌شد.

**حالا هر ۴۷ صفحه با داده‌ی واقع‌نما باز می‌شوند، بدون کرش، و بدون
سرریز افقی — هم روی دسکتاپ و هم روی ۳۷۵ پیکسل.**

### Changed — دوازده صفحه با ردیف خلاصه، به‌جای سه‌تا

پلن‌ها (فعال، ارزان‌ترین، گران‌ترین)، آی‌پی‌های بسته‌شده (چندتا و از
کدام راه)، تلاش‌های نفوذ (چندتا جدی، چندتا از قبل بسته)، فهرست
کانفیگ‌ها، قواعد فایروال، تانل‌ها، هزینه‌ها، همکاران فروش.

همه از داده‌ای که صفحه از قبل دارد — بدون درخواست تازه. و هر عدد
زمینه‌اش را هم می‌گوید: «۲ از ۳ آنلاین»، «۶۶٫۷٪ نرخ تمدید»،
«۳ آدرس هنوز باز است».

### Changed — آخرین کارت‌های شاخصِ دست‌ساز هم یکی شدند

داشبورد تانل، قواعد فایروال، هزینه‌ها، همکاران فروش، و خودِ `Stat`
پنل نماینده (که حالا به کامپوننت مشترک واگذار می‌کند، پس سیزده جای
استفاده‌اش دست‌نخورده ماند).


## [1.48.0]

### Added — حالا می‌شود به پنل *نگاه* کرد

تا امروز تنها راه دیدن پنل، بالاآوردن بکند و دیتابیس و ربات بود.
یعنی عملاً هیچ‌وقت به آن نگاه نمی‌شد، و ایرادهای چیدمانی فقط وقتی پیدا
می‌شدند که مالک بازش می‌کرد.

`tools/make-harness.js` حالا همان پنلِ ساخته‌شده را با داده‌ی ساختگی
سرو می‌کند:

```
cd frontend && NEXORA_SINGLE_BUNDLE=1 npx vite build
node tools/make-harness.js
cd frontend && npx vite preview --outDir dist-test --port 5180
```

و با همین، هر ۴۷ صفحه‌ی پنل اندازه‌گیری شد — نه نگاه‌کردن، اندازه‌گیری.

### Fixed — چیزهایی که اندازه‌گیری نشان داد

**کارت شاخص ۱۹۰ پیکسل بود، حالا ۱۴۲.** جمله‌ی زیرش سه خط می‌شد و هر
کارت یک قد داشت، پس ردیف شاخص‌ها ناهموار بود. حالا عدد و واحدش روی یک
خط می‌مانند و جمله حداکثر دو خط است.

**نمودارِ داشبورد دو تا بود، حالا یکی.** درآمد و تعداد سفارش روی یک
محور می‌نشینند (هرکدام با مقیاس خودش، چون تومان و «تعداد» روی یک مقیاس
معنا نمی‌دهند). ارتفاع کارت از ۵۶۶ به نصف رسید — همان فضای خالی که
دیده می‌شد.

**تولتیپِ نمودار پهنای کل صفحه را زیاد می‌کرد.** `translate(-50%)`
داشت و در لبه‌ها بیرون می‌زد: کارت ۴۸۰ پیکسل، محتوا ۵۲۱. یعنی یک
اسکرول افقی که هیچ‌کس دلیلش را پیدا نمی‌کرد. حالا داخل نمودار
می‌ماند. **الان هر ۴۷ صفحه، هم روی دسکتاپ و هم روی ۳۷۵ پیکسل، صفر
سرریز افقی دارند.**

**نوار بالا نمی‌چسبید.** در جدول‌های بلند، دکمه‌ی ذخیره و جستجو با
اسکرول می‌رفتند.

**لایه‌ی نور محیطی زیر یک پس‌زمینه‌ی مات گم بود** — ساخته شده بود و
دیده نمی‌شد.

**افسانه‌ی دونات زیرش می‌افتاد** چون کارتِ ستون کناری دقیقاً به اندازه‌ی
دونات + افسانه بود.

### Fixed — پنج صفحه با پاسخِ غیرمنتظره کرش می‌کردند

`data.stats` و `data.nodes` بدون محافظ خوانده می‌شدند. اگر بکند شکل
دیگری برگرداند — خطا، نسخه‌ی قدیمی‌تر، یا مستاجری که آن بخش را ندارد —
کلِ صفحه می‌افتاد. حالا حالت خالی نشان می‌دهند.

پیدا شدنشان کار تستِ تازه بود: `test-apps-runtime.js` حالا **هر ۴۷
صفحه را باز می‌کند** و اگر یکی خالی بماند یا کرش کند، می‌گوید کدام.
پیش از این فقط داشبورد در تستی باز می‌شد.

نکته: اولین نسخه‌ی همین تست **الکی سبز بود** — آکاردئون سایدبار کلید
است و کلیک دوم می‌بندَدش، پس هیچ صفحه‌ای باز نمی‌شد و فهرست خرابی‌ها
خالی می‌ماند. حالا اگر کمتر از ۳۰ صفحه باز شود، خودِ تست شکست می‌خورد
و می‌گوید «این تست کور شده، نه پنل سالم».

### Changed — سفارش‌ها و کاربران هم ردیف خلاصه گرفتند

هر دو صفحه حالا بالای فهرست، خلاصه‌ی همان فهرست را نشان می‌دهند —
تعداد، ارزش کل، در انتظار تایید / کیف پول، شماره‌دارها، بلاک‌شده‌ها.
همه از داده‌ای که صفحه از قبل گرفته، بدون هیچ درخواست تازه‌ای.


## [1.47.1]

### Fixed — پنل نماینده اصلاً باز نمی‌شد

در ۱.۴۷.۰ شرطِ مسیر را به `lib/route.js` بردم و در پنل نماینده این
ماند:

    export { portalSlug } from "../lib/route.js";

این شکل، نام را فقط *عبور* می‌دهد و وارد دامنه‌ی خودِ ماژول
نمی‌کند. کدِ همان فایل دو جا `portalSlug()` را صدا می‌زند، پس پنل
نماینده موقع رندر `ReferenceError` می‌داد و **هیچ چیزی نشان
نمی‌داد**. بیلد سبز بود (نحو درست است) و همه‌ی تست‌های متنی هم سبز
بودند.

از دستِ تست‌ها در رفت چون `test-panel-runtime.js` فقط پنل مدیر را
اجرا می‌کرد؛ پنل نماینده و مینی‌اپ هیچ‌وقت در هیچ تستی *اجرا*
نمی‌شدند و فقط متنشان خوانده می‌شد. حالا `test-apps-runtime.js` هر
سه را روی مسیر واقعی‌شان بالا می‌آورد و اگر یکی ریشه‌ی خالی بدهد یا
خطا بزند، قرمز می‌شود. در دروازه‌ی ریلیز و CI هم هست.

### Changed — حالا واقعاً چیزی عوض شده که دیده شود

نسخه‌ی قبل توکن و موشن و اسکلتون آورد، ولی چیدمانِ خودِ صفحه‌ها
دست‌نخورده ماند — اسکلتون فقط موقع بارگذاری دیده می‌شود، پالت فرمان
پشت `Ctrl+K` است و تقسیم باندل اصلاً دیده نمی‌شود. پس پنلی که باز
می‌شد همان پنل قبلی بود.

**داشبورد.** صفحه‌ای که بعد از ورود می‌بینید چهار شمارنده‌ی محتوا
داشت، با اسپارک‌لاین‌هایی که **عدد ساختگی** بودند — `[2,3,3,4,5,5]`
در خودِ کد. حالا کار واقعی را نشان می‌دهد: کاربر تازه، سفارش موفق،
درآمد و نرخ تبدیل، با نمودارهایی از سریِ روزانه‌ی واقعی؛ نمودار روند
فروش با کلید ۷/۳۰/۹۰ روز؛ دوناتِ وضعیت اشتراک‌ها؛ و فهرست
بیشترین‌خریدها. همه از `/api/admin/bot/users/report` که از قبل بود و
فقط صفحه‌ی گزارش فروش از آن می‌خواند.

**اجزای مشترک.** کارت‌ها شیب و خطِ لبه گرفتند، سرستونِ جدول موقع
اسکرول می‌ماند، ردیفِ زیرِ موس یک لبه‌ی رنگی می‌گیرد، و عنوانِ هر
بخش یک خطِ کوتاهِ اکسنت. این‌ها در یک جا عوض شدند، پس هر ۴۵ صفحه با
هم عوض می‌شوند.

**داشبورد حسابداری.** سه عددِ تخت به کارت شاخص با آیکون و زمینه
تبدیل شدند، دوناتِ تسویه اضافه شد، و هر واسطه حالا با حرفِ اولِ نام
و نشانِ وضعیت می‌آید. همه‌ی خط‌های تشخیصی — حجم بدون نرخ،
کانفیگ‌های حساب‌نشده و دلیلشان — دست‌نخورده‌اند.

### Fixed — دو قاعده‌ی CSS که همدیگر را پاک می‌کردند

`.fx-card` و `.fx-kpi` روی یک عنصر می‌نشینند و هر دو `::before`
می‌ساختند، پس هرکدام دیرتر در فایل می‌آمد دیگری را بی‌صدا از بین
می‌برد. تست تازه‌ای این را می‌گیرد.


## [1.47.0]

### Security — لینک کانفیگ مشتری دیگر به سرویس غریبه نمی‌رود

صفحه‌ی اشتراک QR را از `api.qrserver.com` می‌گرفت و **لینک کاملِ
کانفیگ را در آدرسِ آن درخواست** می‌فرستاد. آن لینک خودِ اعتبارنامه
است: UUID، سرور، پورت — هر چیزی که برای استفاده از اشتراک لازم است.
یعنی هر بار که مشتری صفحه‌ی خودش را باز می‌کرد، برای هر کانفیگ دو
بار، این اطلاعات روی سروری می‌رفت که ما نمی‌گردانیمش و در لاگ‌هایش
می‌نشست. هیچ حمله‌ای لازم نبود؛ خودِ صفحه می‌فرستاد.

حالا QR در مرورگرِ خودِ مشتری ساخته می‌شود و هیچ‌جا نمی‌رود.
سازنده‌اش (qrcode-generator، مجوز MIT) داخل همان فایل است نه یک فایل
دوم — چون این صفحه یک سندِ تکِ خودکفاست که `install.sh` کپی می‌کند، و
فایل دوم یعنی یک مرحله‌ی نصبِ تازه که روزی جا می‌ماند و QR بی‌صدا
خراب می‌شود. اگر ساختن QR به هر دلیلی نشد، جای آن خالی می‌ماند:
نداشتنِ QR بهتر از فرستادنِ لینک به بیرون است.

`}}` های کدِ مینیفای‌شده هم به `} }` تبدیل شدند. در جاوااسکریپت هیچ
فرقی نمی‌کند، ولی ۳x-ui این صفحه را با قالبِ Go رندر می‌کند و آن
نشانه برایش معنا دارد.

### Added — یک زبانِ طراحی برای کل پنل

**توکن‌ها به تیلویند وصل شدند.** `theme.extend` خالی بود، پس تیلویند
از وجود رنگ‌ها و فاصله‌های ما خبر نداشت و هر کامپوننت مجبور بود
`style={{ color: "var(--muted)" }}` دستی بنویسد — ۱۶۶۲ بار در ۱۹
فایل. عوض‌کردن یک رنگ یعنی سر زدن به همه‌شان.

**توکن‌های موشن.** هر کامپوننت زمانِ خودش را ساخته بود (۰٫۱۳ و ۰٫۱۶
و ۰٫۱۸ و ۰٫۲ و ۰٫۲۲ و ۰٫۲۶ و ۰٫۳ ثانیه) و هیچ‌کدام دلیلی نداشت.
حالا سه سرعت و سه منحنی، در پنل و صفحه‌ی اشتراک یکسان.

**کامپوننت‌های مشترک یک‌بار ارتقا یافتند، ۴۵ صفحه مفت گرفتند:**
اسپارک‌لاین‌ها موقع ورود کشیده می‌شوند، نمودار خط راهنما و تولتیپ
شیشه‌ای دارد (به‌جای عددی زیر محور)، کارت شاخص با عوض‌شدن مقدارش یک
تپشِ کوتاه می‌زند، و دونات و میله و اسکلتون اضافه شدند.

**پالت فرمان با `Ctrl+K`.** پنل ۴۵ صفحه در پنج فضای کاری دارد و
جستجوی سایدبار فقط *همان* فضای کاری را فیلتر می‌کرد؛ رسیدن به هر
چیز دیگری یعنی عوض‌کردن فضای کاری، پیداکردن گروه، پیداکردن آیتم.

**سایدبارِ جمع‌شونده** با یادِ ماندگار در مرورگر، تولتیپ در حالت جمع،
و نشانگری که بین آیتم‌ها سُر می‌خورد.

### Fixed — ۳۸ چرخنده، یک مودالِ تله، و یک باندلِ سه‌برابر

**اسکلتون به‌جای چرخنده.** همان چرخنده‌ی وسطِ صفحه ۳۸ بار در ۲۰ فایل
تکرار شده بود. فقط می‌گفت «صبر کن» و وقتی داده می‌رسید کل چیدمان
می‌پرید. چرخنده‌ی داخل دکمه ماند — آن‌جا واقعاً یعنی «این دکمه
مشغول است».

**مودال تله‌ی کیبورد بود.** نه `role="dialog"` داشت نه حبس تمرکز، پس
با Tab می‌شد به صفحه‌ی زیرش رفت و روی دکمه‌ای زد که دیده نمی‌شود.
توست هم `aria-live` نداشت، پس «ذخیره شد» برای صفحه‌خوان هیچ‌وقت
اتفاق نمی‌افتاد.

**سه اپ در یک فایل بودند.** مشتری‌ای که مینی‌اپ را روی موبایل باز
می‌کرد کلِ پنل مدیر را هم دانلود می‌کرد — ۱۷۲ کیلوبایتِ فشرده برای
صفحه‌ای با سه کارت، به همان کسی که اینترنتش محدود است. حالا
مینی‌اپ حدود ۶۵ کیلوبایت است.

**صفحه‌ی اشتراک هیچ‌وقت `prefers-reduced-motion` نداشت** در حالی که
نبضِ وضعیت، رادارِ چرخان، موجِ پس‌زمینه و چند انیمیشنِ فنری دارد.
کسی که حرکت اذیتش می‌کند هیچ راهی برای خاموش‌کردنشان نداشت. دوازده
`transition: all` هم که مرورگر را مجبور می‌کرد چیدمان را برای هر
خصوصیت دوباره حساب کند، صریح شدند.

### Fixed — سه دروازه که به اتاق خالی نگاه می‌کردند

**بررسی قرارداد فرانت و بکند فقط `App.jsx` را می‌خواند.** وقتی پنل
به `sections/` تقسیم شد، صداکردن‌های API هم با آن رفتند — پس این
ابزار در هر ریلیزِ اخیر **۲ آدرس** را می‌سنجید در حالی که فرانت ۱۳۰
تا صدا می‌زد، و هر بار «قرارداد سالم است» چاپ می‌کرد. حالا کلِ
`frontend/src` را می‌خواند و هر دو شکلِ صداکردن را می‌فهمد: هم
`${API_URL}/api/…` و هم مسیرِ خامی که به یک تابع کمکی داده می‌شود —
که بیشترشان همین دومی‌اند. هر ۱۳۰ آدرس مسیر واقعی دارند، یعنی چیزی
خراب نبود؛ فقط هیچ‌وقت سنجیده نشده بود. و حالا کف دارد: اگر کمتر از
۹۰ آدرس پیدا شود، خودش شکست می‌خورد و می‌گوید اسکنر کور شده.

دو اسکنر دیگر هشدارِ دروغ می‌دادند: یکی importـی با پسوند صریح
(`./lib/route.js`) را نمی‌دید و دیگری شکلِ `import React, { Suspense }`
را. هشدارِ دروغ، هشدارِ واقعی را بی‌ارزش می‌کند.

### Changed — مینی‌اپ حالا شبیه خودِ تلگرام است

رنگ‌ها از `themeParams` خودِ تلگرام می‌آیند، رنگِ نوار بالا هم. قبلاً
همیشه سرمه‌ایِ نکسورا بود — روی تلگرامِ روشن مثل صفحه‌ی وبی که
تصادفاً آن‌جا باز شده. و این تنها جایی است که **مشتری نهایی**
می‌بیند. چهار عنوان با رنگ سفیدِ ثابت هم بودند که روی پوسته‌ی روشن
نامرئی می‌شدند.

لرزشِ بازخورد روی کپی و خرید و تعویض تب اضافه شد که هر جا پشتیبانی
نشود بی‌صدا هیچ کاری نمی‌کند، و آیکونِ کپی حالا به تیک تبدیل می‌شود.
هیچ پولی همچنان در مینی‌اپ حساب نمی‌شود؛ خرید در خودِ ربات می‌ماند.

## [1.46.1]

### Fixed — مینی‌اپ روی سرور واقعی هیچ‌وقت خودش فعال نمی‌شد

نسخه‌ی ۱.۴۵.۱ گفت آدرس مینی‌اپ خودکار ثبت می‌شود: پنل که روی https باز
شود، بک‌اند دامنه‌اش را می‌بیند و `https://<دامنه>/app` را می‌نویسد. تست
هم داشت و سبز بود. ولی روی هیچ نصب واقعی‌ای اتفاق نیفتاد.

دلیلش در خودِ `install.sh` بود: بلوک nginx ما هدر `X-Forwarded-Proto` را
جلو نمی‌فرستاد. TLS روی nginx تمام می‌شد و درخواست به شکل http به uvicorn
می‌رسید، پس شرطِ «فقط https» همیشه رد می‌شد. تست‌ها آن هدر را دستی
می‌دادند، و همین باعث شد چیزی که در واقعیت هرگز صدق نمی‌کرد سبز بماند.
نتیجه: قابلیتی که «بدون تنظیم کار می‌کند» اعلام شده بود، بی‌صدا هیچ کاری
نمی‌کرد و هیچ‌جا هم نمی‌گفت چرا.

دو اصلاح، چون نصب‌های موجود فایل nginx قدیمی را دارند:

**بک‌اند** حالا دامنه را از `Referer`/`Origin` هم می‌خواند — همان نوار
آدرسی که خودِ مرورگر می‌گذارد، بدون هیچ وابستگی به تنظیم nginx. سطح
اعتماد عوض نمی‌شود: این مسیر رمز مدیر می‌خواهد، پس فقط مرورگر خود مالک
به آن می‌رسد. http و میزبان آلوده (`@` در آدرس) همچنان رد می‌شوند.

**nginx** هم در `install.sh` اصلاح شد تا نصب‌های تازه از اول درست باشند.

یعنی برای فعال‌شدن مینی‌اپ کافی است پنل را یک‌بار روی https باز کنید و
بعد ربات را ریستارت کنید.

## [1.46.0]

### Added — مدت بر حسب روز، و شمارش از اولین اتصال

ساخت کانفیگ در پنل نمایندگی فقط «۱ ماه / ۲ ماه / ۳ ماه» می‌داد. مشتری‌ای
که ۴۵ روز یا ۱۰ روز می‌خواست، نماینده باید بین دو گزینه یکی را انتخاب
می‌کرد و اختلافش را از جیب خودش یا از سهم مشتری می‌داد. خودِ پنل ۳x-ui
این را بر حسب روز می‌گیرد؛ ما وسط راه محدودش کرده بودیم.

حالا روز مستقیم داده می‌شود (۱ تا ۳۶۶) و همان روز به پنل می‌رود. **مبلغ
ولی همچنان از `_months_from_days` و `_line_amount` می‌آید** — یعنی همان
دو تابعی که کل حسابداری از آن‌ها می‌خواند، نه یک قیمت‌گذاریِ روزانه‌ی
تازه. اگر این‌جا روز را جدا قیمت می‌گذاشتیم، سطح پنجمی می‌شد که عدد خودش
را می‌سازد — همان الگویی که یک‌بار چهار عدد متفاوت داد. «ماه» هم هنوز
پذیرفته می‌شود تا هیچ فراخوانیِ قدیمی نشکند.

روزِ صفر حالا خطا می‌دهد، نه اینکه بی‌صدا یک ماه شود: «نفرستاده» با
«صفر فرستاده» یکی نیست.

**شمارش از اولین اتصال.** گزینه‌ای اضافه شد که انقضا از لحظه‌ی اولین
اتصال مشتری شروع شود، نه از لحظه‌ی ساخت. قرارداد خودِ ۳x-ui است —
`expiryTime` منفی — و پنل از قبل این حالت را «شروع‌نشده» می‌شناخت؛ فقط
راهی برای ساختنش نبود. بدون این، مشتری‌ای که امروز می‌خرد و هفته‌ی بعد
وصل می‌شود، آن هفته را از دست می‌دهد.

### Fixed — اگر گروه روی پنل ننشیند، دیگر بی‌صدا نمی‌ماند

گروه تنها چیزی است که می‌گوید یک کانفیگ مالِ کدام نماینده است — هم برای
صورتحساب، هم برای اینکه خودِ نماینده در فهرستش ببیندش. کد گروه را در
همه‌ی شکل‌های `add_client` می‌فرستاد، ولی نسخه‌های قدیمی‌تر ۳x-ui اصلاً
گروه ندارند و بی‌صدا دورش می‌ریزند. نتیجه: کانفیگ ساخته می‌شد، پولش کم
می‌شد، و بعد در فهرست پیدا نمی‌شد.

حالا بعد از ساخت خوانده می‌شود که گروه واقعاً نشسته یا نه، و اگر ننشسته
`groupWarning` برمی‌گردد و در همان کادرِ نتیجه به نماینده نشان داده
می‌شود. مسیر خرابِ بی‌صدا ممنوع است.

### قاعده‌ای که دو جا نوشته شد، تستِ برابری گرفت

پنل مبلغ را *پیش از* فرستادن نشان می‌دهد، پس مجبور است خودش هم روز را
به ماه تبدیل کند — هشتمین نمونه‌ی قدیمی‌ترین باگ این مخزن: یک قاعده، دو
جا. اگر از هم جدا بیفتند، نماینده یک عدد می‌بیند و عددِ دیگری از
اعتبارش کم می‌شود. تست حالا فرمول را پین می‌کند و برابریِ دو طرف را
برای هر روزی از ۱ تا ۳۶۶ می‌سنجد.

## [1.45.1]

### Fixed — مینی‌اپ بدون هیچ تنظیمی کار می‌کند

نسخه‌ی قبل مینی‌اپ را ساخت ولی دیده‌شدنش دو کارِ دستی می‌خواست: پرکردن
آدرس در پنل، و ساختن دکمه در BotFather. یعنی قابلیتی که ساخته شده و تا
کسی آن دو کار را نکند، هیچ‌کس نمی‌بیندش — همان چیزی که سر پنل نمایندگی
افتاد و سر اینباند پیش‌فرض.

حالا هر دو خودکارند:

**آدرس.** اولین باری که پنل روی https باز می‌شود، بک‌اند دامنه‌ی خودش را
از هدر Host می‌بیند و `https://<همان دامنه>/app` را ثبت می‌کند. مینی‌اپ
از همان `frontend/dist` سرو می‌شود که پنل و nginx هم `try_files` دارد،
پس `/app` بدون هیچ تنظیم تازه‌ای بالا می‌آید.

فقط از مسیرِ رمزدارِ مدیر ثبت می‌شود، نه مسیر عمومی: هدر Host را
فرستنده تعیین می‌کند و روی یک مسیر عمومی هر کسی می‌توانست مینی‌اپِ
مشتری‌ها را به صفحه‌ی خودش ببرد. هاستِ حاوی فاصله یا اسلش یا `?` هم رد
می‌شود، و اگر مالک خودش آدرسی گذاشته باشد دست‌نخورده می‌ماند.

**دکمه.** ربات موقع بالا آمدن `setChatMenuButton` را خودش صدا می‌زند، پس
دکمه‌ی کنار کادر تایپ بدون رفتن به BotFather ظاهر می‌شود. و اگر آدرسی
نباشد، دکمه‌ی قبلی برداشته می‌شود — وگرنه به صفحه‌ای اشاره می‌کرد که
دیگر بالا نمی‌آید.

راهی که **امتحان و رد شد**: ساختن آدرس از روی `sub_base_url`. آن آدرسِ
سرویسِ اشتراکِ x-ui است و معمولاً روی پورت دیگری می‌نشیند؛ حدس‌زدن از
رویش آدرسی می‌ساخت که صفحه‌ی سفید می‌داد — بدتر از نبودنِ دکمه.

## [1.45.0]

### Added — مینی‌اپ تلگرام

مشتری داخل تلگرام دکمه‌ای می‌زند و به‌جای دنبال‌کردن فهرستی از دکمه‌ها،
یک صفحه می‌بیند: اشتراک‌هایش با نوار مصرف و روزهای باقی‌مانده، پلن‌ها
به‌صورت کارت‌های کنار هم و قابل مقایسه، و کیف پول و سکه.

برگه‌اش پیش از کد نوشته شد: `docs/specs/2026-09-16-miniapp.md`.

**خرید در خودِ ربات می‌ماند.** دکمه‌ی خرید مینی‌اپ را می‌بندد و کاربر را
به `t.me/<bot>?start=plan_<id>` می‌برد. اگر خرید این‌جا پیاده می‌شد، قیمت
و پورسانت و سکه در دو جا حساب می‌شدند — همان باگی که این مخزن هفت بار
دیده. به همین دلیل مینی‌اپ **هیچ نرخی حساب نمی‌کند، به `billing.db` دست
نمی‌زند، و هیچ سفارشی را approved نمی‌کند**؛ تستی هم این مرز را نگه
می‌دارد.

**احراز هویت.** تلگرام `initData` امضاشده می‌دهد و امضا با توکنِ همان
ربات سنجیده می‌شود. اگر سنجیده نشود، فرستادن شناسه‌ی دیگری کافی است تا
کسی اشتراک‌های دیگری را ببیند — این تنها دیواری است که بین مشتری‌هاست.
شناسه از خودِ امضا برداشته می‌شود، نه از بدنه‌ی درخواست. امضای کهنه‌تر از
۲۴ ساعت و امضای با تاریخِ آینده هم رد می‌شوند، و مقایسه با
`compare_digest` است نه `==`.

ساختارش برای چند ربات نوشته شده (هر نماینده توکن خودش را دارد)، ولی
فعلاً فقط رباتِ مالک فعال است — تصمیم خودِ مالک.

**ربات مسیر مستقل می‌ماند.** مینی‌اپ از دامنه‌ی ما بارگذاری می‌شود و
مشتری‌های ما همان کسانی‌اند که اینترنتشان محدود است. اگر آن صفحه بالا
نیاید، تلگرام چیزی نمی‌گوید و کاربر صفحه‌ی سفید می‌بیند. پس هیچ دکمه‌ای
از منوی ربات حذف نشد و تستی این را نگه می‌دارد؛ و وقتی API جواب ندهد،
مینی‌اپ می‌گوید چه چیزی نشد و راه ربات را یادآوری می‌کند — نه اسپینرِ
بی‌پایان.

آدرسش در «ربات تلگرام ← متن‌ها» تنظیم می‌شود. خالی بماند، دکمه‌اش اصلاً
نشان داده نمی‌شود. فقط `https` پذیرفته می‌شود — تلگرام با `http` خودِ
*پیام* را رد می‌کند، نه فقط دکمه را، یعنی یک آدرس غلط کل منو را از کار
می‌اندازد.

### Fixed — شمارنده‌ی تست‌ها ۱۳۱ بررسی را گزارش نمی‌کرد

`tools/test-admin-api.py` متغیر `_ok` را دو جا به‌عنوان متغیر معمولی به
کار برده بود — همان نامی که شمارنده‌ی `check` است. هر بار شمارش از صفر
شروع می‌شد.

سوییت ۳۱۳ بررسی اجرا می‌کرد و **۱۸۲** گزارش می‌داد. خودِ تست‌ها درست کار
می‌کردند و شکست‌ها هم شمرده می‌شدند، ولی عددِ پایانی دروغ می‌گفت و هیچ
راهی نبود بفهمی بخشی از سوییت اصلاً اجرا نشده.

حالا نگهبانی هست که اگر شمرده‌شده از تعداد فراخوانی‌های داخل فایل کمتر
باشد، خودش قرمز می‌شود.

### Fixed — پویشگر دکمه‌ها، برچسبِ پویا را نمی‌دید

`{copied ? "کپی شد" : "کپی لینک"}` برچسب است و کاربر می‌بیندش، ولی
پویشگر عبارت‌های `{…}` را حذف می‌کرد و دکمه را «فقط‌آیکون و بی‌برچسب»
می‌شمرد. دو دکمه‌ی سالمِ مینی‌اپ بی‌دلیل قرمز شدند. حالا رشته‌ی داخل
عبارت هم برچسب حساب می‌شود — و همچنان دکمه‌ی واقعاً بی‌برچسب را می‌گیرد.

## [1.44.0]

مالک گزارش داد که بر اساس فاکتور، پولِ دوره‌ی قبل و کاربرانِ قبل را یک بار
دیگر از واسطه گرفته. دنبالش رفتیم و **دو علتِ جدا** پیدا شد که روی هم
همان نتیجه را می‌دادند.

### Fixed — یک ماه، چند بار روی فاکتور

`_period_share` مرزهای ماهانه‌ی هر کانفیگ را می‌شمرد و شرطش این بود:
«تاریخش بعد از تسویه باشد». سقفی نداشت.

کانفیگی که تا ۱۴۰۶ اعتبار دارد، مرزهای ماهانه‌اش تا همان‌جا جلو می‌روند —
و تاریخِ آینده **همیشه** از «تسویه‌شده تا» جلوتر است. پس هر دوره دوباره
شمرده می‌شد.

اندازه‌گیری روی یک گروه واقعی، سه دوره‌ی پشت سر هم:

| دوره | ماهِ صورتحساب | از آن، قبلاً هم شمرده شده |
|---|---|---|
| ۱ | ۵۸ | — |
| ۲ | ۴۱ | **۴۱** |
| ۳ | ۳۰ | **۳۰** |

یعنی تمدیدهای `2026-11-04` و `2026-12-04` و `2027-01-03` یک کانفیگ، در
هر سه دوره روی فاکتور بودند.

حالا ماه وقتی حساب می‌شود که **سپری شده باشد**. جمعِ کلِ عمرِ کانفیگ همان
عدد قبلی است؛ فقط درست پخش می‌شود و هیچ ماهی دو بار نمی‌آید.

این یک تصمیم بود، نه فقط رفع باگ. مدلِ «کلِ دوره را موقع فروش حساب کن»
هم درست است، ولی برای تکرارنشدن باید به خاطر بسپارد کدام ماه‌های آینده
قبلاً فاکتور شده‌اند — و «تسویه‌شده تا» که یک تاریخ است نمی‌تواند این را
نگه دارد. تستی که قاعده‌ی قبلی را ثبت کرده بود با همین توضیح به‌روز شد.

### Fixed — «پرداخت گرفتم» دوره را نمی‌بست

`settled_until` از قبل وجود داشت و فاکتور رعایتش می‌کرد. ولی ثبتِ پرداخت
هیچ‌وقت آن را جلو نمی‌برد، و تنها راهِ تغییرش یک فیلد در صفحه‌ی دیگری
بود.

پس مالک پول را ثبت می‌کرد و دوره بسته نمی‌شد — و فاکتور بعدی همان
کاربران را دوباره می‌آورد. همان شکلِ آشنای این مخزن: پول یک جا ثبت
می‌شود، وضعیت جای دیگر، و از هم دور می‌افتند.

حالا «تسویه» یک کار است: مبلغ را ثبت می‌کند **و** دوره را می‌بندد.
عقب‌بردن تاریخ رد می‌شود، چون یعنی دوباره‌حساب‌کردنِ چیزی که پولش گرفته
شده.

و هر گروهی که پرداخت دارد ولی هیچ دوره‌ای پشتش بسته نشده، حالا همین را
روی صفحه می‌گوید — در داده‌ی آزمایشی سه گروه در این حالت بودند.

### Added — نماینده می‌تواند کانفیگ خودش را حذف کند

با تفکیکی که مالک خواست:

- **مصرف نشده** (مشتری همان لحظه پشیمان شد) → اعتبارِ نماینده برمی‌گردد.
  مبلغ از دفتر اعتبار خوانده می‌شود، نه از نرخِ امروز — نرخ ممکن است در
  این فاصله عوض شده باشد.
- **مصرف داشته** → دوره‌اش را کار کرده، اعتباری برنمی‌گردد. دیالوگِ
  تأیید این را *پیش* از زدن دکمه می‌گوید.

کانفیگ واقعاً از x-ui پاک می‌شود. و چون صورتحساب از فهرستِ زنده‌ی x-ui
خوانده می‌شود، ردیفِ حذف‌شده از فاکتور غیب می‌شود — پس هر حذفی که بدهی
داشته در `deleted_clients` ثبت می‌شود تا مالک ببیند چه چیزی و با چه
مبلغی بیرون رفت.

### Changed — جدول کانفیگ‌های نماینده

نماینده هشتاد ردیف دارد و سوال همیشگی‌اش این است: «کدام مشتری به سقفش
نزدیک است؟» عدد «۱۶۱٫۴ GB (۸۱٪)» را باید خواند و با سقف مقایسه کرد.
حالا کنارش یک نوار هست که فقط باید دیده شود — سبز تا ۷۵٪، زرد تا ۹۰،
قرمز بالاتر.

و روی گوشی، جدولِ هفت‌ستونی پهلو می‌کشید: برای رسیدن به دکمه‌ی تمدید
باید جدول را می‌کشیدی. زیر ۷۲۰ پیکسل هر ردیف یک کارت می‌شود و عنوانِ هر
خانه از `data-label` می‌آید، پس یک نشانه‌گذاری می‌ماند نه دو نسخه‌ی جدا.
اندازه‌گیری‌شده روی ۳۷۵ پیکسل: هیچ اسکرول افقی‌ای نماند، نه در صفحه و نه
در خود جدول.

انقضای نزدیک هم به‌جای متن کوچک، نشانِ رنگی گرفت — «امروز»، «۵ روز»،
«منقضی».

### Added — نماینده می‌تواند برای کانفیگ نام بگذارد

`ali_3f2a9b1c` شد `ali_hossein`. پیشوند همچنان ساختِ سیستم است و این
عمدی است: `find_reseller` برندِ صفحه‌ی اشتراک را از همان تکه می‌خواند.
آزموده شد که جعل‌پذیر نیست — برچسبِ `sajjad_reza` هم `ali_sajjad-reza`
می‌شود و برند همان می‌ماند که باید.

### Fixed — تبدیل گیگابایت هفت جا تکرار شده بود

یک خطِ یکسان، شش بار درون‌خطی در `app.py` و یک‌بار در `billing-why.py`.
هفت کپی از یک قاعده‌ی پول. حالا یک تابع است.

### Added — قاعده‌های تازه‌ای که تست اجرا می‌کند

`test-billing-e2e` بخشِ «یک ماه، یک بار» گرفت: سه دوره‌ی پشت سر هم بسته
می‌شود و جمعِ ماه‌ها با عمرِ واقعیِ کانفیگ سنجیده می‌شود. اگر ماهی دو بار
بیاید، جمع از عمر بیشتر می‌شود.

`test-admin-api` از ۱۶۷ به ۱۸۲ رسید: تسویه، حذف با برگشت اعتبار، و
رفتنِ مصرفِ خام در پاسخ.

هر چهار مورد عمداً دوباره خراب شد تا مطمئن شویم قرمز می‌شود.

## [1.43.0]

پنل با داده‌ی واقع‌نما بالا آورده شد و هر ۴۷ صفحه‌اش در سه عرض — دسکتاپ،
تبلت و موبایل — گشته و **اندازه‌گیری** شد. آنچه پیدا شد، همه‌اش از جنسی
بود که تست‌های موجود نمی‌دیدند و عکسِ کوچک‌شده هم نشان نمی‌داد.

### Fixed — پنل نماینده می‌گفت «اینباند تعریف نشده»، در حالی که تعریف شده بود

مالک برای نماینده اینباند ۴۱ و ۴۵ را انتخاب می‌کرد، نماینده از پنل خودش
کانفیگ می‌ساخت، و پاسخ این بود: **«اینباند پیش‌فرض تنظیم نشده — با
پشتیبانی تماس بگیرید».**

`_portal_inbound` فقط ستون `default_inbound` را نگاه می‌کرد و
`inbound_mode`/`inbound_ids` را اصلاً نمی‌دید — همان انتخابی که در
نسخه‌ی ۱.۳۹.۰ اضافه شده بود. یعنی پیامِ خطا دقیقاً چیزی را انکار می‌کرد
که تعریف شده بود، و نماینده هیچ راهی نداشت بفهمد چه کم است.

اندازه‌گیری روی هر چهار مستاجر، با کد قدیم و جدید کنار هم:

| مستاجر | قدیم | جدید |
|---|---|---|
| نماینده با `custom` و ids=`[41,45]` | **۵۰۳** | ۴۱ |
| نماینده با `default` | **۵۰۳** | ۴۱ |
| نماینده بدون هیچ تنظیمی | **۵۰۳** | ۴۱ (از x-ui) |

حالا ترتیب این است: انتخابِ خودِ نماینده، بعد پیش‌فرضِ خودش، بعد پیش‌فرضِ
مالک، و در آخر اولین اینباندِ فعالِ پنل. آن پله‌ی آخر همان کاری است که
**ربات از قبل می‌کرد** و کامنتش دلیلش را نوشته بود: شکستِ کامل از دید
مشتری یعنی «پول دادم و کانفیگ نگرفتم». پرتالِ نماینده عکسش را می‌کرد.

خطا فقط وقتی می‌آید که واقعاً هیچ اینباند فعالی نباشد — و آن‌وقت همین را
می‌گوید، نه چیز دیگری.

### Fixed — اعتبار نماینده پیش از کاری که ممکن بود شکست بخورد کم می‌شد

در هر دو مسیرِ ساخت و تمدید، ترتیب این بود:

    کسر اعتبار → حل اتصال → حل اینباند → ساخت کانفیگ

و فقط شکستِ آخرین مرحله برگشتِ پول داشت. یعنی همان نماینده‌ای که بالا
خطای «اینباند تعریف نشده» می‌گرفت، **اعتبارش هم کم می‌شد و کانفیگی
نمی‌گرفت**.

اتصال و اینباند فقط می‌خوانند، پس هر دو پیش از کسر اعتبار منتقل شدند.

### Fixed — فیلدهای عددی رقم فارسی را بی‌صدا دور می‌ریختند

`<input type="number">` هر چیزی جز رقمِ لاتین را قبول نمی‌کند. در مرورگر
اندازه گرفته شد:

| تایپ‌شده | آنچه در فیلد می‌ماند |
|---|---|
| `۱۲۳` | **خالی** |
| `۱۲٬۳۴۵` | **خالی** |
| `1,234` | **خالی** |

یعنی کاربر فارسی رقم می‌زد و هیچ چیزی ظاهر نمی‌شد — نه خطایی، نه پیامی.
و فلش‌های بالا/پایینش هم در چیدمان راست‌به‌چپ سر جای درستی نمی‌نشستند
(دو فیلد کلاس `fx-input` نداشتند و اصلاً پنهان نشده بودند).

هر ۲۳ فیلد به `NumberInput` تبدیل شدند: `type="text"` با `inputMode`
عددی — صفحه‌کلید موبایل عددی می‌ماند ولی مرورگر دیگر چیزی دور نمی‌ریزد.
نرمال‌سازی در همان مرز انجام می‌شود: رقم فارسی و عربی، جداکننده‌ی
هزارگان، و ممیزِ فارسی همه به شکل لاتین درمی‌آیند.

**قرارداد با بکند عوض نشده.** آنچه به `onChange` و بعد به سرور می‌رسد
همان رشته‌ی لاتینِ قبلی است. با تایپِ `۹۹۹۰۰۰` و `۶۶` و `۴۵` و `۳`،
بدنه‌ی ارسالی این بود:

    "gb":66, "days":45, "ip_limit":3, "price":999000

عددِ JSON، نه رشته — و در دیتابیس هم `int` ذخیره شد.

### Added — سه قاعده‌ی تازه که تست اجرا می‌کند

- هیچ `<input type="number">` نماند
- `_portal_inbound` انتخابِ خودِ نماینده را ببیند (با x-ui که عمداً
  اینباندِ دیگری می‌دهد، وگرنه تست نمی‌تواند «از انتخابش خواند» را از
  «به x-ui افتاد» تشخیص دهد)
- اتصال و اینباند پیش از کسر اعتبار حل شوند

هر شش مورد عمداً دوباره خراب شد تا مطمئن شویم قرمز می‌شود — و باز هم
همان سوییپ یک حفره در تستِ خودم پیدا کرد.

### Fixed — پنل نماینده روی موبایل اسکرول افقی داشت

پنج دکمه‌ی بالای صفحه در یک ردیفِ نشکن بودند: ردیفِ بیرونی `flex-wrap`
داشت، ردیفِ داخلیِ دکمه‌ها نداشت.

نتیجه روی نمایشگر ۳۷۵ پیکسلی: کلِ صفحه **۴۶۳ پیکسل** می‌شد. نام نماینده
بیرون از کادر می‌افتاد و کارت‌ها نصفه دیده می‌شدند — یعنی صفحه‌ای که
مشتری‌های شما با آن کار می‌کنند، روی گوشی عملاً استفاده‌نشدنی بود.

### Fixed — عددهای میلیونی از کارت‌های داشبورد بیرون می‌زدند

سه جا نوشته شده بود `fx-stat-num text-[21px] font-extrabold` و **هیچ‌کدام
کار نمی‌کرد**. قاعده‌ی `.fx-stat-num` در `index.css` دیرتر از کلاس‌های
تیلویند می‌آید و با ویژگیِ برابر برنده می‌شد، پس به‌جای ۲۱ پیکسل، ۳۲ (و
روی موبایل ۲۸) رندر می‌شد.

حالا آن پیش‌فرض‌ها داخل `:where()` نشسته‌اند تا ویژگی‌شان صفر شود و هر
اندازه‌ای که در JSX نوشته می‌شود واقعاً اعمال شود.

ولی اندازه‌ی قلم به‌تنهایی کافی نبود: کارت‌ها روی موبایل دو ستونه می‌شدند
و ۱۳۲ پیکسل فضای داخلی داشتند، که فقط تا عددِ ۸ رقمی کفاف می‌داد. ۱۲۳
میلیون تومان ۹ رقم است. حالا روی گوشی یک ستون می‌شوند — ۳۱۰ پیکسل — و تا
عددِ میلیاردی هم جا می‌گیرد.

### Fixed — واژه‌های فارسی در جهت و قلمِ اشتباه

سلول‌هایی که `dir="ltr"` و قلم مونو دارند برای تاریخ شمسی و آی‌پی و مبلغ
درست‌اند. مشکل آن‌جا بود که زیرخط‌های فارسیِ داخلشان هم همان را به ارث
می‌بردند. دو چیز خراب می‌شد و هر دو اندازه‌گیری شد، نه حدس:

- **`dir="ltr"` جای عدد و واحدش را عوض می‌کند.** «۸۳ روز» روی صفحه
  «روز ۸۳» خوانده می‌شد.
- **JetBrains Mono هیچ گلیف فارسی ندارد.** حرف‌ها به مونوی سیستم
  می‌افتادند: «اعتبار» ۷۱٪ پهن‌تر از قلمِ خودِ پنل، «تومان» ۳۱٪ — درست
  وسط یک عدد.

راه‌حلِ ساده‌ی «فارسی را به انتهای زنجیره‌ی مونو اضافه کن» امتحان و **رد
شد**: ارقام فارسی در مونو همه ۲۴٫۰۱ پیکسل‌اند و در قلم اصلی ۱۴٫۵ تا
۲۷٫۱ — یعنی ستون‌های عددیِ هر جدول از هم می‌پاشید.

به‌جایش عدد در مونو ماند و واحد به قلم اصلی برگشت. نامِ گروه و ایمیل
کانفیگ — که کاربر خودش می‌نویسد و می‌تواند فارسی باشد — داخل `<bdi>`
نشستند تا جابه‌جا نشوند.

### Fixed — «config» و «months» در رابطی که همه‌جایش فارسی است

دو صفحه‌ی جدا این خط را داشتند:

    {g.name} · {g.configs} config · {g.months} months

در حالی که سرستونِ همان جدول «ماه» می‌نوشت. یک قاعده، دو جا، و هر دو جا
جا مانده بود — همان الگویی که این مخزن بارها دیده.

### Fixed — ۵۱ دکمه‌ی آیکونی هیچ نمی‌گفتند چه می‌کنند

از ۸۴ دکمه‌ی فقط‌آیکون، ۵۱ تا نه `title` داشتند نه `aria-label` — و **۱۲
تایشان دکمه‌ی حذف بودند**. کاربر باید حدس می‌زد. حالا هر ۸۴ تا برچسب
دارند.

### Fixed — عنوان تبِ مرورگر برای نماینده، عنوانِ پنل مدیر بود

نماینده در تبِ مرورگرش «مدیریت صفحه اشتراک» می‌دید — عنوانِ پنلی که اصلاً
به آن دسترسی ندارد.

### Fixed — «تومان» دوباره چاپ می‌شد

در یک خط از صفحه‌ی هزینه‌ها، `toman()` که خودش واحد را می‌چسباند، کنار یک
«تومان»ِ دستی نوشته شده بود.

### Security — `billing.db` و `x-ui.db` در `.gitignore` نبودند

مخزن عمومی است. `bot.db` از قبل نادیده گرفته می‌شد، ولی `billing.db`
(نرخ‌ها و پرداخت‌ها) و `x-ui.db` (ایمیل و کانفیگ مشتری‌ها) نه — و هر سه
دقیقاً کنار هم ساخته می‌شوند. یک `git add -A` کافی بود.

### Added — چهار قاعده‌ی تازه که تست اجرا می‌کند

`tools/test-ui-safety.py` از ۴۸ به **۵۴** بررسی رسید:

- حرف فارسی داخل `dir=ltr` یا مونو نماند (با پیمایشِ پشته‌ی تگ، نه regex)
- هر دکمه‌ی فقط‌آیکون برچسب داشته باشد
- واحدِ انگلیسی در متنِ نمایشی نماند
- هیچ قاعده‌ی `fx-stat-num` بدون `:where` اندازه نگذارد

هر هشت مورد عمداً دوباره خراب شد تا مطمئن شویم تست قرمز می‌شود — و همان
سوییپ یک حفره در تستِ خودم پیدا کرد: راهِ فرار کلِ عنصر را معاف می‌کرد،
حتی اگر متن فارسی بیرونِ آن بود.

### بررسی شد و سالم بود

اینها وقت گرفتند و نتیجه‌شان «مشکلی نیست» بود — ثبت می‌شوند تا دوباره
بررسی نشوند:

- **قلم‌های زیر ۱۱ پیکسل** (۹ مورد) همه داخل ماکتِ گوشی یا نشانِ کوچک‌اند،
  نه متنِ واقعیِ رابط.
- **`IRANSansX` درست بارگذاری می‌شود** — ولی فقط وزن ۴۰۰ و ۷۰۰ دارد، پس
  `font-medium` و `font-semibold` به همان‌ها نگاشت می‌شوند.
- **هیچ مسیر API خرابی نیست** — همه‌ی درخواست‌های ۴۷ صفحه ۲۰۰ برگرداندند.
- **تبلت (۷۶۸ پیکسل) هیچ ایرادی نداشت.**

## [1.42.0]

### Fixed — جعبه‌های راهنما به هر چیزی که زیرشان بود می‌چسبیدند

`InfoBox` فقط `mt-4` داشت و هیچ فاصله‌ای از پایین. اندازه‌گیری روی صفحه‌ی
زنده: **صفر پیکسل** تا عنصر بعدی.

و این کامپوننت بیش از صد بار در نوزده صفحه به کار رفته — یعنی همه‌جای پنل
همین‌طور بود، نه فقط یک صفحه.

حالا از بالا و پایین فاصله دارد، و وقتی آخرین عنصرِ یک کارت باشد فاصله‌ی
پایینش برداشته می‌شود تا ته کارت فضای خالیِ بی‌خود نماند.

بعد از اصلاح، هجده بخشِ پنل دوباره اندازه‌گیری شد: هیچ جفتِ چسبیده‌ای
نماند.

### Fixed — فرم «نماینده‌ی جدید» فقط نصفِ عرض را می‌گرفت

فرم داخل ردیفی رندر می‌شد که برای دکمه‌هاست (`flex justify-between`).
نتیجه این بود که فرم یک ستون می‌گرفت و کنارش یک ستونِ خالیِ بزرگ می‌ماند.

حالا دکمه در ردیف می‌ماند و فرم بیرونِ آن، تمام‌عرض — و دکمه وقتی فرم باز
است «بستن فرم» می‌شود.

### Added — سه نگهبان برای فاصله‌ها

جعبه‌ی راهنما باید از هر دو طرف فاصله داشته باشد، وقتی آخرین است نداشته
باشد، و فرمِ باز‌شونده نباید داخل ردیفِ دکمه‌ها بنشیند.

## [1.41.0]

### Fixed — چهار مشکل در پنل نمایندگی، که با باز کردن خودِ صفحه پیدا شدند

پنل را محلی بالا آوردم و صفحه‌ها را نگاه کردم. چهار چیز:

**خودِ شما به‌عنوان نماینده فهرست می‌شدید.** `tenant_portal_list` همه‌ی
مستاجرها را می‌داد، از جمله مستاجرِ ریشه. یعنی کارتی با نام خودتان، با فرم
«نشانی لینک»، «گروه x-ui»، دکمه‌ی «بازکردن پنل» و حتی هشدارِ «برای اینکه
بتواند وارد شود…» — و می‌شد ناخواسته برای خودتان پنل نمایندگی باز کرد.

**صفحه‌ی «اینباند نماینده‌ها» دو بار رندر می‌شد.** این را خودم ساخته
بودم: اسکریپت وصله روی `App.jsx` و `app.jsx` حلقه زده بود و چون ویندوز به
بزرگی و کوچکی حروف حساس نیست، هر دو یک فایل بودند و خطِ مسیر دو بار اضافه
شد.

**گروهِ ثبت‌شده‌ی هر نماینده در منو دیده نمی‌شد.** فهرست گروه‌ها از x-ui
می‌آید؛ اگر گروهِ نماینده در آن نبود، `select` روی «— انتخاب کنید —»
می‌افتاد. یعنی نماینده‌ای که گروه داشت «بدون گروه» به نظر می‌رسید — و
کافی بود پنل x-ui یک لحظه در دسترس نباشد تا *همه‌ی* نماینده‌ها همین‌طور
دیده شوند.

**و علتش بی‌صدا بود:** خطای خواندنِ گروه‌ها دور ریخته می‌شد. حالا صفحه
می‌گوید چرا فهرست خالی است و یادآوری می‌کند که گروهِ ثبت‌شده سر جایش
است.

### Fixed — متن فارسی داخل فیلدهای چپ‌به‌راست

`placeholder` فارسی روی فیلدی که `dir="ltr"` و فونت mono دارد درست دیده
نمی‌شود: جهتش برعکس می‌شود و فونت mono هم حرف فارسی ندارد.

سه جا بود: مبلغ شارژ نماینده، حجم در نرخ‌ها، و توکن API پنل. قاعده‌ی خودِ
مخزن در بقیه‌ی جاها رعایت شده — فارسی در `label` و `hint`، و `placeholder`
یک نمونه‌ی خنثی. حالا هر سه همین‌طورند.

(`dir="auto"` این را حل نمی‌کند: روی `input` جهت از *مقدار* گرفته می‌شود،
نه از `placeholder` — و مقدارِ خالی یعنی چپ‌به‌راست. امتحان شد.)

### Added — سه نگهبان تازه در تست ظاهر

هیچ‌کدام از اینها را تست نمی‌گرفت. حالا می‌گیرد: مسیری که دو بار وصل شده
باشد، `placeholder` فارسی روی فیلد چپ‌به‌راست، و منوی انتخابی که مقدارِ
ثبت‌شده‌اش را نشان ندهد.

## [1.40.0]

### Fixed — پنل دو نرخ تبدیل متفاوت نشان می‌داد

قیف تبدیل در ۱.۳۷.۰ درست شد: تست رایگان دیگر «خرید موفق» شمرده نمی‌شود.
ولی **گزارش فروش** تعریفِ خودش را داشت و همان اشتباه را ادامه می‌داد.

روی داده‌ی آزمایشی با ده کاربر که شش نفرشان فقط تست گرفته بودند و دو نفر
واقعاً خریده بودند:

| | گزارش فروش | قیف | واقعیت |
|---|---|---|---|
| نرخ تبدیل | ۸۰٪ | ۲۰٪ | ۲۰٪ |
| تعداد سفارش | ۸ | — | ۲ |
| میانگین سفارش | ۶۲٬۵۰۰ | — | ۲۵۰٬۰۰۰ |
| خریدارها | ۸ | ۲ | ۲ |

«میانگین سفارش» بدترینش بود: سفارش‌های صفرتومانیِ تست، میانگین را به یک
چهارم واقعیت می‌کشیدند.

فهرست خریدارها و نمودار روزانه هم همین‌طور بودند.

حالا قاعده‌ی «خرید واقعی» **یک تعریف** دارد و هر سه جا — قیف، گزارش
فروش، و آمار فروشِ پنل نماینده — از همان می‌خوانند. تست هم برابری‌شان را
اجرا می‌کند و هم اینکه کپیِ دستیِ شرط جایی نمانده باشد.

## [1.39.0]

### Fixed — خرید با کیف پول در گروه مدیریت هیچ ردی نداشت

هر رویدادِ پولیِ دیگر خودش را به گروه اعلام می‌کند: شارژ کیف پول، تمدید
خودکار، و خریدِ کارتی که اصلاً با رسیدش به گروه می‌آید و مدیر رویش دکمه
می‌زند.

خریدِ کیف‌پولی تنها موردی بود که هیچ اعلامی نداشت. و چون تاییدِ ادمین هم
نمی‌خواهد، یعنی یک روشِ پرداختِ کامل که فروشش در لحظه اصلاً دیده نمی‌شد —
فقط با باز کردن پنل معلوم می‌شد.

حالا اعلام می‌شود، با نام مشتری و پلن و مبلغ، و تمدید از خرید تازه قابل
تشخیص است.

### Fixed — پنلِ خودِ نماینده اینباندِ اختصاصی‌اش را نادیده می‌گرفت

انتخابِ اینباند برای هر نماینده اضافه شد و **ربات** آن را می‌خواند — ولی
**پنلِ خودِ نماینده** نه. یعنی می‌گفتید «کانفیگ‌های این نماینده روی
اینباند ۴۱ و ۴۵»، نماینده از پنل خودش کانفیگ می‌ساخت، و روی اینباند
پیش‌فرض می‌نشست. بی‌صدا و بدون هیچ خطایی.

همان شکلِ همیشگی: یک قاعده، دو ورودی، نگهبان روی یکی. حالا هر دو یک
منطق را اجرا می‌کنند و چون دو پردازه‌ی جداست، تست برابری‌شان را روی ده
حالت می‌سنجد — از جمله اینکه فهرست خالی به «همه» برگردد، نه به فهرستِ
تهی که یعنی کانفیگ به هیچ اینباندی وصل نشود.

### Added — آمار فروش در پنل نماینده

نماینده ربات خودش را دارد، سفارش می‌گیرد و رسید تایید می‌کند — ولی هیچ
عددی از فروشش نمی‌دید. آمارِ پنلش فقط درباره‌ی مصرف و انقضا بود.

حالا چهار عدد اضافه شده: فروش این ماه، فروش کل، دریافتیِ کارت‌به‌کارت، و
سفارش‌های در انتظار بررسی.

دو قاعده‌ای که این‌جا هم اجرا می‌شوند: **تست رایگان خرید نیست** (خودش یک
سفارشِ approved با مبلغ صفر می‌سازد)، و **«فروش» با «درآمد» یکی نیست**
(خرید از کیف پول فروش هست ولی پول تازه‌ای نیامده — آن پول موقع شارژ
رسیده). پس «دریافتی» فقط کارت را می‌شمارد.

### Added — فیلتر و صفحه‌بندی در پنل نماینده

جدول کانفیگ‌ها فقط جست‌وجوی نام داشت و **همه‌ی ردیف‌ها را یک‌جا
می‌ریخت**. نماینده‌ای با صدها کانفیگ صفحه‌ای می‌دید که ته نداشت، و
«کدام‌ها رو به اتمام‌اند» را نمی‌شد با تایپ‌کردن پیدا کرد.

حالا فیلتر وضعیت دارد — فعال، غیرفعال، رو به اتمام، منقضی، حجم تمام،
بالای ۸۰٪ — و صفحه‌بندی شماره‌دار، همان قاعده‌ای که بقیه‌ی پنل دارد.
فهرست سفارش‌ها هم همین‌طور.

### Fixed — فهرست سفارش‌های نماینده بی‌صدا سر ۵۰ تا بریده می‌شد

`portal_orders` سقف ۵۰ داشت و رابط اصلاً سقفی نمی‌فرستاد. یعنی نماینده
فقط تازه‌ترین ۵۰ سفارش را می‌دید و **هیچ‌جا گفته نمی‌شد که بقیه هست**.
حالا سقف بالاتر است و وقتی بخورد، خودش می‌گوید.

### Fixed — تنظیم اینباند روی *همه‌ی* نماینده‌ها می‌نوشت

این دستور `WHERE` نداشت:

    UPDATE tenants SET inbound_mode=?, inbound_ids=?

یعنی هر بار که اینباندهای خودتان را در بخش ربات ذخیره می‌کردید، همان
تنظیم بی‌صدا روی تک‌تک نماینده‌ها هم می‌نشست و انتخابِ خودشان را پاک
می‌کرد. و خواندنش همیشه از مستاجر ریشه بود، پس تنظیمِ هر نماینده نه
دیده می‌شد و نه قابل تغییر.

ربات از قبل `inbound_mode` را از مستاجرِ خودش می‌خواند — زیرساختش بود،
فقط سمت مدیر خراب بود.

### Added — ساخت نماینده از پنل

تا امروز هیچ راهی نبود. صفحه‌ی پنل نمایندگی می‌گفت «از بخش ربات، مستاجر
بسازید» ولی چنین جایی وجود نداشت: `create_tenant` در کل مخزن فقط از
تست‌ها صدا زده می‌شد. یعنی عملاً به یک نماینده محدود بودید.

حالا فرم «نماینده‌ی جدید» هست: نام، نشانی لینک (که از نام حدس زده
می‌شود)، گروه x-ui، رمز، و پیش‌پرداخت یا بدهکاری.

نماینده‌ی تازه همیشه **فرزندِ مستاجر ریشه** ساخته می‌شود — بدون آن، در
حسابداری «مالک» شمرده می‌شود و درآمد و بدهی‌اش با مالِ شما قاطی می‌شود.
و پنلش **بسته** ساخته می‌شود تا گروه و رمزش ثبت شود.

**حذف نماینده عمداً ساخته نشد:** جدول‌ها `ON DELETE CASCADE` دارند و حذف
یک مستاجر، کاربرها و سفارش‌ها و اشتراک‌ها و کیف پولش را با خودش می‌برد.
«بستن پنل» هست که برگشت‌پذیر است.

### Added — نمایندگی فضای مستقل شد

«پنل نمایندگی» یک قلم داخل حسابداری بود. حالا فضای خودش را دارد — کنار
فایروال و تانل — با دو بخش: «نماینده‌ها و دسترسی» و «اینباند
نماینده‌ها».

### Fixed — سه بررسی سلامت وقتی نمی‌توانستند اندازه بگیرند، ساکت می‌ماندند

قاعده‌ی این مخزن این است که مسیر خرابِ بی‌صدا ممنوع است. `check_disk` و
`check_load` از اول رعایتش می‌کردند — اگر نمی‌توانستند بخوانند، «خوانده
نشد» گزارش می‌دادند. سه بررسی دیگر نه:

**IPv6 بدترینشان بود.** اگر دستور `ip` جواب نمی‌داد، نتیجه می‌شد
**«تنظیم نشده — مشکلی نیست»**. یعنی دقیقاً همان وضعیتی که این بررسی برای
پیدا کردنش ساخته شده — IPv6ِ شکسته که باعث می‌شود سایت‌ها برای مشتری کند
باز شوند — به مدیر «سالم» گزارش می‌شد.

**بررسی پورت‌ها** وقتی `ss` جواب نمی‌داد `[]` برمی‌گرداند: کل بررسی از
صفحه‌ی سلامت غیب می‌شد. همان بررسی‌ای که «مشتری وصل نمی‌شود» را پیدا
می‌کند.

**مدت روشن بودن** `None` برمی‌گرداند و `run_all` حذفش می‌کرد.

و داخل خودِ `run_all`، سه گروه با `except: pass` پوشانده شده بودند —
اگر یکی می‌ترکید، کل آن گروه بی‌صدا از فهرست بیرون می‌رفت، در حالی که
حلقه‌ی بالایشان از اول درست عمل می‌کرد و هشدار می‌گذاشت.

هر چهار مورد حالا می‌گویند چه چیزی را نتوانستند بسنجند و چه دستوری را
دستی اجرا کنید.

### یادداشت — کد تخفیف اصلاً وصل نیست

جدول `discounts`، تابع `validate_discount` و ستون‌های `discount_code` و
`discount_pct` روی سفارش‌ها همه وجود دارند، ولی هیچ‌کدام به چیزی وصل
نیستند: نه مسیری در ربات، نه API در پنل، نه صفحه‌ای در رابط کاربری.
`validate_discount` از هیچ‌جا صدا زده نمی‌شود و `used_count` هیچ‌وقت
بالا نمی‌رود.

باگ نیست — قابلیتی است که نیمه‌کاره مانده. این‌جا نوشته می‌شود تا کسی
(یا خودم در دوری دیگر) فکر نکند کار می‌کند و رویش حساب باز نکند.

## [1.38.0]

### Fixed — پاداش معرف فقط از مسیر کارت پرداخت می‌شد

ربات به کاربر می‌گوید: «هر دوستی که با لینک شما بیاید و **خرید کند**،
N سکه به شما می‌رسد». هیچ حرفی از روشِ پرداخت نیست.

ولی این پاداش فقط در مسیر تاییدِ کارت اجرا می‌شد. دوستی که با **کیف
پول** می‌خرید، یا اشتراکش **خودکار تمدید** می‌شد، هیچ سکه‌ای به معرفش
نمی‌رساند.

جالب اینکه پورسانتِ همکار فروش هر سه مسیر را داشت — یکی‌شان با همین
توضیح که «تمدید خودکار هم فروش است». همان استدلال برای پاداشِ معرف هم
صادق بود و کسی اعمالش نکرده بود.

حالا هر سه مسیر هر دو پرداخت را انجام می‌دهند، و تست با `ast` می‌سنجد که
هیچ‌کدام یکی را بدون دیگری صدا نزند. خودِ پرداخت از قبل اتمی و «یک
پاداش برای هر دوست» بود، پس صداکردنش از چند مسیر چیزی را دوباره
نمی‌پردازد.

### Added — `nexora repair-referrals` برای وعده‌های نگه‌داشته‌نشده

اصلاحِ کد فقط خریدهای تازه را درست می‌کند. کسانی که پیش از این با کیف
پول خریدند و معرفشان سکه‌ای نگرفت، همچنان طلبکارند و هیچ‌جا هم دیده
نمی‌شوند.

این ابزار پیدایشان می‌کند. تست رایگان و سفارش‌های ردشده حساب نمی‌شوند،
و کسی که قبلاً پاداشش را گرفته دوباره نمی‌گیرد.

**سکه تخفیف است — یعنی پول.** پس بدون آرگومان فقط *نشان می‌دهد* چه کسی
چقدر طلبکار است و چیزی نمی‌نویسد؛ پرداخت با `--apply` و تصمیم خودتان
است. اجرای دوباره بی‌اثر است، چون از همان قاعده‌ی «یک پاداش برای هر
دوست» عبور می‌کند که خودِ ربات دارد.

اگر نرخ پاداش صفر باشد چیزی پرداخت نمی‌شود و **دلیلش را می‌گوید** —
سکوت این‌جا یعنی مدیر فکر کند حساب‌ها صاف شده.

## [1.37.0]

### Fixed — قیف تبدیل، تست رایگان را «خرید موفق» می‌شمرد

گرفتنِ تست رایگان خودش یک سفارشِ `approved` می‌سازد — با مبلغ صفر، ولی
`approved`. و قیف تبدیل «خرید موفق» را این‌طور می‌شمرد:

    SELECT COUNT(DISTINCT user_id) FROM orders WHERE status='approved'

یعنی **هر کسی که دکمه‌ی تست رایگان را زده، «خرید موفق» شمرده می‌شد**. و
بخشِ «فقط تست گرفتند» — که کل هدفش شمردنِ کسانی است که تست گرفتند و
نخریدند — هیچ‌وقت نمی‌توانست چیزی جز صفر باشد، چون همان تست برایشان یک
سفارشِ `approved` ثبت کرده بود.

در بازتولید آزمایشی با پنج کاربر که فقط **دو** نفرشان واقعاً خریده
بودند، قیف گفت **چهار** نفر. یکی از آن چهار نفر کسی بود که تستش حتی
ساخته هم نشده بود.

قیف دقیقاً برای جداکردن همین دو گروه ساخته شده. حالا «خرید» یعنی سفارشی
که پلنش تست نبوده، و این قاعده **یک بار** نوشته شده و هر دو شمارش از
همان می‌خوانند. گامِ «سفارش ثبت کردند» هم همین‌طور — زدنِ دکمه‌ی تست،
ثبت سفارش نیست.

### Fixed — سومین جایی که سفارش پیش از ساختِ کانفیگ تایید می‌شد

در ۱.۳۴.۰ این الگو در خرید با کیف پول و تمدید خودکار درست شد. تست رایگان
سومین جایش بود و از قلم افتاده بود: سفارش *قبل* از ساخت `approved`
می‌شد، و اگر ساخت شکست می‌خورد حقِ تست پس داده می‌شد ولی سفارش
`approved` می‌ماند.

نتیجه: کاربری که هیچ‌وقت چیزی نگرفت، در آمار «خرید موفق» می‌نشست — و
چون تستش پس داده شده بود، `trial_used` هم نداشت که جایی جبرانش کند.

حالا هر چهار مسیر از `close_order` می‌گذرند. و برای اینکه مسیر پنجمی
دوباره همین را نسازد، تستِ جریان اسکن می‌کند که هیچ‌جای `handlers` خام
`SET status='approved'` ننویسد.

## [1.36.0]

### Fixed — نتیجه‌ی کاری که سرِ بزنگاه می‌رسید، پاک می‌شد

`requeue_stale` کارهایی را که ایجنت برداشته و جوابشان نیامده دوباره به صف
می‌برد. اول آنها را `SELECT` می‌کند، بعد `UPDATE`. بین این دو، نتیجه‌ی
همان کار می‌تواند از ایجنت برسد و `finish_job` ببنددش — دو اتصال جدا، دو
مسیر جدا.

هیچ‌کدام از آن دو `UPDATE` شرطِ وضعیت نداشت. یعنی کارِ تمام‌شده دوباره
`queued` می‌شد: نتیجه‌اش پاک، و همان کار یک بار دیگر روی نود اجرا
می‌شد. شاخه‌ی دیگر بدتر بود — کاری که بعد از چند تلاش *موفق* شده بود
`failed` علامت می‌خورد و روی نتیجه‌اش نوشته می‌شد «ایجنت این کار را N بار
برداشت و هیچ پاسخی نفرستاد». یعنی پنل یک خطای ساختگی نشان می‌داد برای
کاری که انجام شده بود.

حالا هر دو `AND status='taken'` دارند و شمارش هم از `rowcount` می‌آید،
نه از تعداد تلاش — همان الگویی که بقیه‌ی مخزن دارد: اول ادعا، بعد
پرداخت.

پنجره‌اش باریک است و برای دیدنش باید نتیجه دقیقاً در همان فاصله برسد.
تست آن لحظه را ساختگی ولی دقیق بازسازی می‌کند، برای هر دو شاخه.

### Added — کاتالوگ موتورهای تانل دیگر بی‌نگهبان نیست

فهرست موتورها در پنل یک جا نوشته شده و در ایجنت *چهار* جای دیگر:
مخزن‌ها، نام باینریِ تکی، فهرست باینری‌ها، و پسوند فایل پیکربندی. ایجنت
جدا روی نود دانلود می‌شود، پس این دو کپی روی دو ماشین زندگی می‌کنند.

همین شکل قبلاً واقعاً از هم جدا شده بود (فهرست موتورهای تانل در نسخه‌ی
جایگزینِ `monitor` سه نام کم داشت). این‌بار بررسی شد و **هماهنگ بودند** —
ولی بدون نگهبان، اضافه‌کردن یک موتور به پنل یعنی روی نود «موتور
ناشناخته»، آن هم لحظه‌ی نصب و بعد از اینکه مدیر تانل را ساخته است.

تست تازه هر پنج نگاشت را با هم می‌سنجد: نام موتورها، آدرس مخزن، فهرست
باینری‌ها، پسوند پیکربندی، و اینکه هر موتور در `write_service` شاخه‌ی
اجرای خودش را دارد.

### Fixed — `binary` در کاتالوگ موتورها برای FRP ناقص بود

`ENGINES["frp"]["binary"]` مقدار `"frps"` داشت، در حالی که FRP دو باینری
دارد و کدام اجرا شود به سمتِ تانل بستگی دارد: `frps` روی ایران پورت باز
می‌کند و `frpc` روی خارج سرویس را دارد. ایجنت این را درست انجام می‌داد،
پس اثر عملی نداشت — آن فیلد را هیچ‌کس نمی‌خواند.

حالا `binaries` است و فهرست کامل را دارد، و تستِ برابری از روی همین
می‌سنجد. یعنی متادیتای مرده تبدیل شد به مرجعِ همان چیزی که ادعا می‌کرد.

## [1.35.0]

### Fixed — «مجموع بدهی به همکاران» بدهیِ نماینده‌ها را هم می‌شمرد

صفحه‌ی همکاری در فروش، عددی به نام «مجموع بدهی به همکاران» نشان می‌دهد.
دفتر کل هم همان سؤال را جواب می‌دهد، از راه `_affiliate_money_out`. دو
صفحه، یک سؤال — و دو جواب.

دفتر کل فقط مستاجرهای ریشه را می‌شمارد، و دلیلش را خودش نوشته:
پورسانتِ همکارِ یک نماینده، هزینه‌ی همان نماینده است، نه بدهیِ مالک.
صفحه‌ی همکاری هیچ شرطی نداشت و همه را با هم جمع می‌کرد.

در بازتولیدِ آزمایشی با یک نماینده که ۵۰۰٬۰۰۰ به همکارِ خودش بدهکار بود،
صفحه ۷۰۰٬۰۰۰ نشان داد و دفتر کل ۲۰۰٬۰۰۰. یعنی مالک بدهی‌ای را می‌دید که
مالِ او نبود.

حالا صفحه دقیقاً همان شرطِ دفتر کل را به کار می‌برد — همان عبارت، نه یک
بازنویسیِ موازی — و تست، برابریِ این دو عدد را اجرا می‌کند.

### Changed — همکارهای نماینده‌ها پاک نمی‌شوند، جدا می‌شوند

ساده‌ترین راه این بود که همکارهای نماینده‌ها از فهرست حذف شوند. ولی پنل
نماینده اصلاً بخش همکاری ندارد، پس آن‌وقت هیچ‌کس نمی‌توانست ببیندشان.

به‌جایش: در فهرست می‌مانند، با برچسبِ «همکارِ نماینده» و نامِ همان نماینده،
و بدهی‌شان زیر عنوان جداگانه‌ی «بدهی همکاران نماینده‌ها (هزینه‌ی خودشان،
نه شما)» می‌آید. جمعِ دو عدد همان کل است — چیزی گم نمی‌شود، فقط دیگر به
حساب اشتباه نوشته نمی‌شود.

## [1.34.0]

### Fixed — آمار فروشِ ربات در هر دو جهت غلط بود

وضعیتِ سفارش (`status`) همان ستونی است که همه‌ی آمارهای فروش از رویش
شمرده می‌شوند: مجموع فروش، نمودارها، شمارشِ مشتریِ پرداخت‌کننده. دو مسیرِ
کیف پول آن را اشتباه می‌نوشتند، و جالب اینکه در دو جهتِ مخالف:

**خریدِ کیف پول هیچ‌وقت `approved` نمی‌شد.** کانفیگ ساخته و تحویل می‌شد،
پول کم می‌شد، و سفارش `pending` می‌ماند. جاروکشِ سفارش‌های منقضی نیم‌ساعت
بعد آن را `expired` می‌کرد. یعنی هر فروشِ کیف‌پولی، در تاریخچه‌ی خودِ
مشتری «منقضی شد» دیده می‌شد و در هیچ آماری شمرده نمی‌شد.

**تمدید خودکار برعکس:** سفارش *قبل* از ساختِ کانفیگ `approved` می‌شد. اگر
پنل جواب نمی‌داد، پول به کیف پول برمی‌گشت ولی سفارش `approved` می‌ماند —
پولِ برگشته، «فروش» شمرده می‌شد. و تمدیدِ ناموفق رها نمی‌شود؛ با فاصله‌ی
فزاینده دوباره تلاش می‌کند و هیچ‌وقت تسلیم نمی‌شود. پس یک اشتراکِ گیرکرده
روزی چند فروشِ خیالی به آمار اضافه می‌کرد.

جمعِ این دو، عددی می‌ساخت که *درست به نظر می‌رسید*. در بازتولیدِ آزمایشی،
پنل «۲۰۰٬۰۰۰ فروش» نشان داد: فروشِ واقعی نامرئی بود و یک تمدیدِ ناموفقِ
پول‌برگشته جایش را گرفته بود. این بدترین حالتِ خرابی است، چون هیچ چیز
مشکوک به نظر نمی‌رسد.

حالا هر دو از یک در می‌گذرند: `db.close_order()` وضعیت را *اتمی* ادعا
می‌کند و اگر ادعا را برد — و فقط آن وقت — پول را برمی‌گرداند. مبلغِ
برگشتی از خودِ دفتر حساب می‌شود (خرج منهای آنچه قبلاً برگشته)، پس
برگشتِ دوباره ممکن نیست. سفارشِ `approved` هرگز پس داده نمی‌شود، وگرنه
هر فروش یک اشتراکِ هدیه می‌شد.

### Fixed — شاخه‌ی «موجودی وسطِ کار تمام شد» هرگز اجرا نشده بود

هر دو مسیرِ کیف پول، وقتی موجودی بین بررسی و کسر تمام می‌شد، این را
می‌نوشتند:

    UPDATE orders SET status='rejected', reject_reason='…'

ستون `reject_reason` در جدول سفارش‌ها وجود ندارد و هیچ‌وقت هم نداشته. پس
به‌جای مدیریتِ آن مسابقه، `OperationalError` می‌داد. همان ستونی که پنل
برای دلیلِ رد به کار می‌برد `admin_note` است؛ حالا همان نوشته می‌شود.

### Fixed — سفارشِ نیمه‌تمام پولِ مشتری را نگه می‌داشت

جاروکشِ سفارش‌های منقضی سکه را برمی‌گرداند ولی پولِ کیف پول را نه. اگر
ربات بین کسرِ پول و ساختِ کانفیگ ری‌استارت می‌شد، سفارش نیم‌ساعت بعد
«منقضی» می‌شد و پول همان‌جا می‌ماند. حالا جاروکش هم از `close_order`
می‌گذرد: پول برمی‌گردد و به مشتری خبر داده می‌شود.

### Added — `nexora repair-orders` برای ردیف‌هایی که قبلاً نوشته شده‌اند

اصلاحِ کد فقط سفارش‌های تازه را درست می‌کند. این ابزار ردیف‌های قدیمی را
تصحیح می‌کند: خریدِ تحویل‌شده را `approved` و تمدیدِ ناموفق را `rejected`
می‌کند. نشانه‌اش `sub_id` است — فقط بعد از موفقیتِ پنل پر می‌شود.

بدون آرگومان فقط نشان می‌دهد چه چیزی عوض می‌شود و آمار چقدر جابه‌جا
می‌شود؛ با `--apply` می‌نویسد. اجرای دوباره بی‌اثر است.

سفارش‌هایی که پول گرفته‌اند، کانفیگ نساخته‌اند و پولی هم برنگردانده‌اند
جداگانه *گزارش* می‌شوند و دست نمی‌خورند — آن‌جا مشتری ضرر کرده و تصمیمش
با صاحب سیستم است، نه با یک اسکریپت.

## [1.33.1]

### Fixed — Three tunnel engines missing from the fallback list

Found by going after the shape rather than the symptom. Seven bugs fixed today
were one thing: a rule written in two places and corrected in one. So this round
compared every function in the codebase against every other by token shape and
looked at the near-misses — pairs that are almost, but not quite, the same.

`monitor` carries a fallback copy of `netid` for when that module cannot be
imported. That is deliberate and needed: the agent downloads `netid.py` to a node
separately and that download is wrapped in a `try`, so running without it is a
real state, not a hypothetical.

The copy had drifted. `openvpn`, `iodine` and `udp2raw` had been added to
`netid`'s list of tunnel engines and not to the fallback.

That list decides which established connections count as your own tunnel.
Tunnels are held apart from customer traffic — an Iran node opens hundreds of
connections and would otherwise sit at the top of the "unusual" list and bury
real alerts — and they are protected from the bulk IP block. A missing name means
your own tunnel endpoint is counted as a heavy customer *and* left out of the
protected set.

The list is synced, and a test now asserts the fallback and the real module agree
on all three things they share: the engine list, address normalisation, and the
local-address test, across twenty-one inputs.

### Added — The rule that would have prevented seven bugs

`CLAUDE.md` now says it plainly: a rule written in two places needs a test
asserting the two agree. Where duplication is unavoidable — like this fallback —
the parity test goes in beside it. Otherwise the copies drift apart at exactly the
moment someone fixes one of them.

## [1.33.0]

### Fixed — `nexora update` could leave the panel with no styling at all

A finished `npm run build` is not proof of a usable build. Two failures have been
seen that both exit zero: a JavaScript bundle far too small because something
broke mid-build, and a nearly empty stylesheet because Tailwind never ran — which
brings the panel up with no styling whatever.

Each path checked one of them and not the other. `update` looked at the
JavaScript bundle; `rollback` looked at the stylesheet, and its comment names the
Tailwind incident that put it there.

So the failure that check was written for went uncaught on `update` — the command
run on every single release — and was caught only on `rollback`, which almost
never runs.

One `verify_build` does both now, plus the missing-file cases, and reports which
check failed rather than a bare "Build failed". Both paths call it, so neither can
end up with half the check again. Driven by real bash against fixture builds: a
healthy one, an empty stylesheet, a tiny bundle, and each file missing in turn.

### Added — A guard on the thing that keeps `nexora update` working at all

On a server, `$INSTALL_DIR/.github` is a file holding the repository address —
the one thing `nexora update` reads to find a new release. In the package,
`.github/` is a directory of workflows.

The top-level copy loop uses `"$SRC"/*`, and bash's default glob skips dotfiles,
so the file survives. That is correct but entirely implicit: widening the pattern
to match the nested loop, or enabling `dotglob`, would replace the address with a
directory and every future `nexora update` would answer "no repository
configured".

Now pinned both ways — the pattern is checked, and a real copy run asserts the
address is still there afterwards.

## [1.32.1]

### Fixed — A test that failed roughly one run in three hundred

The 1.32.0 tag build failed on a check asserting that the digits `555` do not
appear in a generated subscription id. The random half of that id is sixteen hex
characters, and `555` turns up in about 0.33% of them by chance — one run in
three hundred, with nothing wrong.

The property that matters is that the id is not derivable, not that a particular
string is absent. It now checks that the old `prefix_tgid_seq` shape is gone —
deterministic, because hex contains no underscore — and that generating an id
twice from the same input gives two different answers, which is the actual reason
scanning cannot work. Both still fail if `make_sub_id` returns the email again,
and twenty-five consecutive runs are clean.

Second flaky test of mine caught this way today. The other put its fixture on an
exact rounding boundary; this one asserted something about random data. Both
passed locally and failed on CI.

## [1.32.0]

### Fixed — "None of these are customers" and "I could not tell" looked the same

`_connected_ips` is the only thing separating an attacker from a customer of
yours who mistyped an SSH password. It gathers addresses from three sources, and
each one sat inside a `try` whose failure went to a debug log and nowhere else.

If all three failed — or the modules had not loaded at all — it returned an empty
set. And an empty set was indistinguishable from "none of these addresses belong
to a customer".

The bulk attacker block read that as permission to block everything, and then
reported `0 آی‌پی چون به سرویس وصل بودند رد شد` — telling the operator the check
had run when it had not. On the most destructive button in the panel, protecting
the thing whose loss is hardest to undo: a paying customer's connection.

It now reports how many sources answered. When none did, the bulk block refuses
with a 503 naming the sources that failed and how to proceed anyway. When only
some failed, the block still runs but the response says which part of the
detection was missing, so a short skip list is not read as a clean bill of
health.

### Checked and sound

Read `netid` closely, since everything above depends on it. Address
normalisation collapses `::ffff:` mapped addresses before any comparison, and
both the storing and the looking-up side go through it, so membership cannot fail
on formatting. `is_local` covers loopback, private, link-local, multicast and
unspecified. `identify` orders its checks local, tunnel, customer, unknown, and
the two modules that parse `ss` output index the peer column identically — which
is where I expected to find a mismatch and found none.

## [1.31.1]

### Fixed — A node's health report could stop updating and never recover

The agent caches the panel's own modules so a remote node reports the same
numbers, with the same thresholds and names, as the main server.

`remote_module` re-downloads when the panel's version changes. That was added
because a cached module would otherwise be kept forever, and the comment
explaining it names the symptom exactly: a new function appears in the panel, the
node still runs the old file, and monitoring silently never arrives.

The `health` action carried a second copy of that logic which had never been
fixed. It downloaded only when the file was missing, so a node that fetched
`health.py` once kept it for good.

Not hypothetical. `run_all` later gained a `services` parameter, and the agent
always passes it. Any node whose cached copy predates that raises
`TypeError: run_all() got an unexpected keyword argument 'services'` on every
check from then on — and nothing refreshes the file, so that node's health never
updates again.

`health` goes through `remote_module` now, which also gives it the corrupt-cache
retry and dependency fetch the inline copy never had. The test drives a real
stale cache: an old module, a panel version bump, a re-download, and a
deliberately broken file that has to be replaced rather than fail forever.

**This one lives on the node.** After updating the panel, send the agent update
from the tunnel page so the node picks it up.

### Checked and sound

Read the rest of the agent boundary and changed nothing. Requests are signed with
HMAC-SHA256 over timestamp and body; the panel verifies with a constant-time
compare and refuses a clock drift over five minutes, so a captured request cannot
be replayed later. `remote_module` is only ever called with fixed module names,
never anything from a payload. The `ping` action refuses a host that is not a
plain name or address, and every command is built as an argument list rather than
a shell string.

## [1.31.0]

### Fixed — Above five thousand chats, the per-chat lock stopped working

The bot keeps one lock per chat so a single user's updates are never processed
twice at once. The table is pruned above five thousand entries to stop it
growing without limit.

The lock was fetched inside the guard, the guard released, and the lock acquired
*after* that. In between, the lock sat in the table without being held — and the
prune condition was exactly `not v.locked()`.

So an update from any other chat could prune a lock that had been handed out and
not yet taken. The next update for that chat then created a fresh lock, and two
handlers ran on the same conversation at the same time. Reproduced
deterministically.

It only happened above five thousand live chats — which is to say, when the bot
is busiest and two updates for one chat are most likely. The protection
disappeared exactly when it was needed.

Each entry now carries a use count, raised inside the guard before the guard is
released, and pruning skips anything in use. The count comes back down in a
`finally`, so an exception cannot leave an entry permanently unprunable.

The locks moved out of the tenant loop into a `ChatLocks` class so they can be
tested at all; the chat-id extraction came with them and is now covered for
messages, inline buttons, and callbacks with no message.

### A note on the tests in this release

Breaking each new test on purpose caught two weaknesses in my own work before
they shipped.

The behavioural test could not reach the fault: with the lock now taken inside
the same call, the window no longer exists for a test to aim at. Restoring the
old condition left every behavioural check green. The rule itself is asserted
instead — pruning must read the use count, never `locked()`.

And the first version of that assertion scanned the whole file, so it matched the
class's own docstring, where the old condition is quoted in the explanation. It
reads executable lines only now. That is the second time today a check has been
satisfied by the comment describing the bug rather than the code.

## [1.30.1]

### Fixed — Three more places that picked whichever tenant came first

`SELECT ... FROM tenants LIMIT 1` — no filter, no ordering — turned up twice
today already, in bot income and in health alerts. A sweep of every `FROM
tenants` query in the codebase found three more.

It reaches the owner only because the owner usually holds the lowest row id.
Delete and recreate that row, which is what re-running setup does, and the
owner's id sits above a reseller's. Nothing errors; everything quietly answers
for the wrong row.

**A new affiliate was attached to whichever tenant came first.** If that was a
reseller, the affiliate never appeared in the owner's list and its commission
stayed out of the owner's ledger — which counts root tenants only, as of
1.28.0. Money filed under the wrong name.

**The inbound picker read panel credentials from an arbitrary row.** A
reseller's row carries no panel URL, so the screen offered no inbounds and said
nothing about why.

**Bot diagnostics read the same credentials the same way**, so it could report
the panel as unconfigured when it was configured. Note that `default_inbound` was
already being read correctly with a root filter elsewhere — the same field, two
different rules, which is how 1.28.2 started.

### Fixed — A test of mine that only failed on a slow machine

The 1.30.0 tag build failed on Python 3.10 while the same commit passed on 3.12
and passed locally. The code was fine; the test was mine and it was flaky.

`_months_from_days` rounds at exact half-months, and I had built the device-rate
fixtures 20 days old expiring in 25 — exactly 45 days, sitting on the boundary.
`NOW_MS` is taken at import and `created_at` a few milliseconds later, and on a
slow runner that drift drops the span below 45: one month instead of two, and
every amount exactly half. A second fixture at 70 + 5 = 75 days sat on the next
boundary up.

Both moved clear of it, 50 and 85 days. Verified by stalling three seconds
between the timestamp and the rows — far more drift than any runner adds — and
the suite still passes.

### Added — A seam test for the whole class

Five occurrences of one mistake in a day is a pattern, not an accident. Every
`FROM tenants ... LIMIT 1` must now either ask for the root tenant or name a
specific id, and any query that asks for the root must also order, because
"first root" is undefined without it. Both halves were verified by putting each
fault back and watching the suite go red.

## [1.30.0]

### Fixed — A server that was already broken never reported it

Health alerts fire on a change of level. The state lives in the panel process's
memory, so every restart clears it — and `nexora update` restarts the panel every
time.

That made the first observation after a restart silent by design, and every
observation after it silent too, because the level had not changed. A server that
came up with a full disk stayed critical forever and sent nothing. For the one
mechanism whose entire job is to tell you something is wrong, the worst possible
failure.

The first observation now sends, but only when it is not `ok`, so restarting a
healthy server still says nothing. Repeats at the same level stay suppressed, and
recovery still reports.

### Fixed — The owner's server alerts could go to a reseller

The alert picked its bot with `SELECT bot_token, admin_id, group_id FROM tenants
LIMIT 1` — no filter, no ordering. It lands on the owner only because the owner
usually holds the lowest row id.

Delete and recreate the owner's tenant row, which is what re-running setup does,
and the owner's id is now higher than a reseller's. The alert then goes out
through the *reseller's* bot, to the *reseller's* group: full disk, stopped
service, certificate expiry and hostnames handed to a third party, while the
owner is told nothing.

It selects the root tenant now, the same rule already used for subscription base
URLs and bot income.

### Checked and sound

Read both monitoring modules closely. `_level` is used with higher-is-worse
semantics at all ten call sites, including disk, which uses percent used rather
than percent free. `memory()` prefers `MemAvailable` over `MemFree`, which is the
difference between a real reading and every server looking exhausted. Network
rates handle the first sample, a missing interface and counter decreases without
producing negative or absurd numbers. Nothing needed changing there.

## [1.29.2]

### Added — Guards for the three ways the subscription page breaks for everyone

No bug this round. The customer subscription page was audited and is sound. What
it did not have was any guard on the three fragilities CONTRIBUTING names, all of
which fail the same way: the page breaks for *every* customer at once, and
nothing on the development machine notices, because Go renders the template on
the server and the local test never sees it.

**Brace balance.** One missing `}}` and Go rejects the whole template; the
customer gets a raw error instead of a page. Counted and compared now.

**Template variables.** Every `{{ .x }}` is checked against the official 3x-ui
list, taken from `docs/custom-subscription-templates.md` in MHSanaei's
repository. A name that is not there renders empty or breaks the template, and
never fails locally. All fifteen currently in use are valid.

**Dangling element ids.** `Cannot set properties of null` stops the rest of the
script, so one typo can disable half the page. Every `getElementById` is now
matched against the ids the markup actually defines. All ninety-five resolve.

The three run *before* the jsdom render rather than after. A template fault is
exactly what makes the render crash, so checking afterwards meant the crash hid
the reason — proved by introducing each fault in turn: before the move the third
one killed the run with a stack trace, after it the test names `stat-limitt`.

### Added — The customer states that are not edge cases

Unlimited quota and no expiry are ordinary, and a customer shown `NaN days` or
`-1` assumes the service is broken. The page handles all three states correctly —
expired, unlimited, and over quota — and four checks now hold it there, including
that an unlimited config is never counted as out of quota.

## [1.29.1]

### Fixed — On New Year's Day the intrusion page dropped that day's attacks

syslog writes `Jan  1 00:10` and no year, so the year has to be inferred. The
code assumed the cutoff's year and corrected only one direction: a timestamp
landing in the future was moved back twelve months.

That misses the other side. On the morning of 1 January the cutoff is still in
the old year, so a line stamped `Jan  1` became 1 January of the *previous* year
— eleven months before the cutoff — and was discarded.

For the first twenty-four hours of every year, SSH attempts from that day were
dropped before anything counted them. The page reported a quiet night while a
brute-force ran. For a screen whose only job is seeing attacks, the worst
possible time to be blind.

The year is now the most recent one that does not place the timestamp in the
future, which is right on both sides of midnight. A line whose date cannot be
read is still kept, because losing one silently is the failure this module exists
to avoid.

`_within` had no test at all. It has nine cases now, including both sides of New
Year, 29 February in a non-leap year, ISO stamps, and lines with no date.

### Checked and sound

Read the blocking paths closely, since blocking a real customer is the worst
outcome this area can produce, and changed nothing:

`block_refusal` validates the address, refuses the one the request itself came
from, and refuses loopback, unspecified, private, link-local and any range wider
than the configured floor. The bulk attacker block requires explicit
confirmation, skips addresses currently connected to the service unless told
otherwise, and caps at a hundred per call. The monitoring page's single block
asks first and says plainly that a customer will be cut off. The firewall page's
block needs the address typed in. Commands are built as argument lists, so
nothing there is shell-interpreted. `_is_private` covers 172.16/12 correctly,
which is where I expected to find a gap and did not.

## [1.29.0]

### Fixed — The agent job table grew without limit

A background thread queues a `health` job for every enabled node every five
minutes, and a `sysmon` job whenever the last one is stale. Nothing ever removed
a finished job: `DELETE FROM jobs` ran only when a node itself was deleted.

Measured against the real module with the real schema, two nodes and thirty
days: **17,280 rows and 68 MB**, on course for roughly 800 MB a year. Each row
carries up to 2,000 characters of result. That sits on the same disk as the bot
and accounting databases, on a 4 GB server.

Events were capped at 500 and metrics at 100 per tunnel from the beginning. The
busiest table of the three had no cap at all.

Finished jobs are now pruned to the last 300 per node on every agent check-in,
alongside the stale-job requeue that already ran there. The same simulation ends
at 680 rows and 3.2 MB and stops growing. Queued and taken jobs are never
touched, and one node's prune cannot reach another's rows.

The deepest reader of this history is the diagnostics page, which shows the last
fifteen entries per node, so 300 is far more than anything needs.

### Added — `nexora doctor` reports the tunnel database

An already-grown file does not shrink on its own: SQLite reuses freed pages
rather than returning them. Doctor now shows the tunnel database size alongside
the bot and accounting ones, and when it is oversized it says how many job rows
remain and gives the exact VACUUM command to reclaim the space.

### Checked and sound

The agent boundary was read closely, since it is the one place a remote machine
talks to the panel. Node tokens are 32 random bytes; the lookup is parameterised
and requires the node to be enabled; `queue_job` validates against an allowlist;
`finish_job` refuses a job belonging to a different node and takes the action
from the stored row rather than from what the agent claims; metrics are refused
unless the tunnel is actually on the reporting node; health and sysmon are keyed
by the token's node, not the payload's. Nothing needed changing there.

## [1.28.3]

### Fixed — Rollback overwrote the panel's databases while the panel was running

`nexora rollback` stopped the bot before replacing `bot.db`. It did not stop the
panel before replacing `billing.db` and `tunnels.db`, which the panel holds open.

Copying a file over a database a live process has open — with its own write-ahead
log and cached pages — is the case SQLite tells you not to create. The window was
not brief either: the frontend rebuild sits between the restore and the restart
and takes minutes, and for all of it the panel runs against a file that was
swapped underneath it and can write over the very data being restored.

Both services stop before any database is replaced now. An `EXIT` trap brings
them back, because two failure paths sit between the restore and the restart — a
failed rebuild and a stylesheet that came out too small — and without it a failed
rollback would leave the panel switched off.

This only ever ran when the operator answered yes to "also restore settings",
which is exactly the moment they are trying to recover data.

A test checks the ordering in the rollback branch: stop, restore, restart, with a
path back up on every exit.

## [1.28.2]

### Fixed — Closing a reseller might not close the session they already had

`portal_enabled` was read three ways. Login used a SQL cast that opens only on
exactly 1. The session check used a denylist — anything that was not `0`, empty
or `None` counted as open. The admin list used the same denylist, so its badge
answered to a third rule.

On 0 and 1 all three agree, which is why nothing had gone wrong yet. On any
other value — `2`, `"true"`, `"yes"` — login refused while an open session kept
working and the panel still showed the reseller as enabled. That is the exact
opposite of what the comment above that check promised: access closing the
moment it is revoked.

This is not hypothetical for this column. It was once created as TEXT, which
stored `1` as `'1'`, and `'1' = 1` is never true in SQLite — no reseller could
log in at all. A flag read three ways will reproduce that kind of incident.

There is one reading now, `_portal_open`, an allowlist, used by all three. The
login query no longer re-implements it in SQL: it selects by slug and decides in
Python, so there is nothing left to drift against.

A test drives both paths over sixteen stored values and requires the same
answer, and refuses the denylist or the SQL cast coming back. It compares what
SQLite actually stored rather than what was handed to it — the column has
integer affinity, so `'1.0'` is already `1` by the time anything reads it, and
comparing against the raw value tests a state that never exists.

## [1.28.1]

### Fixed — `nexora import-topups` refused its own arguments

    $ nexora import-topups --apply
    error: unrecognized arguments: import-topups

`case "$1" in` does not consume the argument, so without a `shift` the command
name stays in `"$@"` and reaches the script. The other tool commands shift; this
one did not. It also now passes the install directory's database paths
explicitly, the way `nexora reseller` does, instead of letting the tool guess.

A seam test now checks every CLI branch that forwards `"$@"`: it must either
shift first or discard its own name explicitly, which is how `rollback` handles
it. The first version of that test passed with the bug still present, because
the comment above the line contained the word "shift" — it strips comments
before scanning now. A test that can be fooled by the explanation next to the
code protects nothing.

It also verifies every tool the CLI invokes exists, and that each tool command
appears in the help text.

## [1.28.0]

Every place in the codebase that counts money was checked against every other
place. They disagreed in six ways.

### Fixed — Profit turned negative the moment a settle date was recorded

Regression from 1.26.1. `billing_ledger` says "from day one" in its own
docstring and sums every expense ever, with no cut. It read `due` and `paid`
off the overview, and once the overview became period-scoped those keys meant
"since the settle date" — three months of revenue against two years of costs.
On test data the headline, labelled سود واقعی تا امروز, flipped from
+5,000,000 to -5,000,000.

The overview carries both pairs now — `dueAll`/`paidAll` for the whole history
and `due`/`paid` for the current period — from one pass rather than two reads
of the x-ui database. Revenue and profit take the lifetime pair; who owes money
takes the period pair, because that is a present-tense question and a settled
period is not a debt.

Its four summary cards stopped reconciling as a result: billed minus paid no
longer equalled what was owed. The difference is exactly the periods marked
settled, so the page names it rather than leaving the reader to guess which
number is broken. `profitIfAllPaid` was billed minus spent, which re-claims any
gap left by a period closed at a discount; it is received plus still-owed now.

### Fixed — Money from prepaid resellers was invisible

Two parallel money systems that never spoke. A postpaid reseller pays at month
end and it lands in `payments`. A prepaid reseller pays up front and it landed
only in `credit_tx`, which no accounting screen reads.

Money that genuinely arrived was missing from دریافت‌شده and from profit
entirely, and a reseller who had paid in advance showed as owing the full
amount. A top-up is the moment the money arrives, so it is recorded there;
spending the credit afterwards moves no new money and records nothing.

`nexora import-topups` brings in the ones made before this. It shows what it
would do and writes nothing without `--apply`, skips refunds, skips resellers
with no group, and tags each row with the `credit_tx` id it came from so a
second run cannot double the revenue.

### Fixed — A reseller's renewal was billed twice

Two mechanisms record renewals and neither knew about the other. The portal logs
one the moment it happens; `_detect_renewals` watches each client's expiry and
treats a forward jump as a renewal, which is how renewals made directly in x-ui
get caught at all.

A renewal made through the portal does both. One renewal, two rows, three months
billed instead of two — fifty percent over, on every portal renewal.
`_portal_log_renewal` carries the new expiry into `client_seen` now so the
watcher has nothing to see, and skips its own insert when the baseline is
already ahead.

### Fixed — The client list ignored the per-user rate entirely

`/api/admin/billing/clients` calls itself the reference view for what a user
owes. It computed `months × price` — the base rate alone. The per-user rate
never reached it, so a four-user config read 200,000 where the invoice said
440,000. It also took lifetime months from a `_months_for` called without the
group start or first-seen fallback, and never saw the settle date.

Rate and amount are kept apart there: a config can have a sound rate and still
be charged nothing, and the "no rate" filter has to keep meaning "needs a rate".
Zero amounts carry their reason into the table, the detail panel and the CSV.

### Fixed — Direct bot sales counted for nothing, or counted twice

The ledger's profit ignored the bot entirely, though it sells to the same
customers and burns the same servers. Counting it needed two guards, both of
which inflate if missed: an order paid from wallet balance is not new money —
it arrived when the wallet was topped up — and orders belonging to a reseller's
own bot are the reseller's income. So: approved, card-paid, root tenants only.

Sales and income are now separate labelled numbers on the bot screen, where an
unlabeled toman figure had been sitting.

### Fixed — Affiliate commission never reached the books

Commission paid out is a cost of selling and was never subtracted, so profit was
reported before paying for the sales that produced it. Commission earned but not
yet paid is a commitment: it does not come off today's profit, but leaving it
invisible means today's profit quietly evaporates later. Both are reported, only
the paid part is subtracted, and both are filtered to root tenants.

### Fixed — The test suite shared one database across every run

`../data/bot.db` resolves against the working directory, so it sat outside the
repo and every run wrote to the same file. A tenant left over from an earlier
run made a new test read zero. The e2e suite points `BOT_DB` at its own temp
directory and passes from a clean slate.

## [1.27.0]

### Fixed — A reseller's renewal was billed twice

Two mechanisms record renewals and neither knew about the other.

The portal logs a renewal the moment it happens, with the real date and month
count. `_detect_renewals` remembers each client's expiry and treats a forward
jump as a renewal — it exists for renewals a reseller makes directly in x-ui,
where nothing is recorded at all.

A renewal made through the portal does both: it writes its own row, and it moves
the expiry. So the watcher recorded it a second time. One renewal, two rows,
three months billed instead of two — fifty percent over, on every portal renewal.

`_portal_log_renewal` now carries the new expiry forward into `client_seen`, so
the watcher has nothing left to see, and it skips its own insert when the
baseline is already ahead — the case where an overview happened to run between
the panel call and the log.

### Fixed — The client list ignored the per-user rate entirely

`/api/admin/billing/clients` calls itself the reference view — "any question
about a user, the answer is here, including what they owe". It computed
`months × price`.

That is the base rate alone. The per-user rate added in 1.22 never reached it, so
a four-user config read 200,000 where the invoice said 440,000. It also read
lifetime months from a `_months_for` called without the group's start date or
first-seen fallback, and it never saw the settle date.

It goes through `_period_share` and `_line_amount` now, like the other two
screens. Rate and amount are kept separate: a config can have a perfectly good
rate and still be charged nothing, and the "no rate" filter has to keep meaning
"needs a rate defined". Zero amounts carry their reason — on screen, in the
detail panel, and as a column in the CSV export.

### Fixed — The period screen billed configs nothing else billed

`_billable_config` reached the overview in 1.26.1 and the invoice in the same
release. صورتحساب دوره was the fourth surface and still counted a config that was
created and never switched on as a sale in the week it was created.

### Changed — "Has renewed" stayed a retention question

With amounts now scoped to a period, the renewed/not-renewed filter and the
retention rate would have started answering "renewed in this period", which would
show a two-year customer as never having renewed whenever the settle date was
recent. Those read lifetime renewals; the period share is reported alongside as
`periodRenewals`.

## [1.26.1]

### Fixed — The dashboard and the invoice disagreed about what was owed

Following through on 1.26.0: the invoice learned to bill from a date, and the
dashboard did not. The card labelled کل بدهی دوره — "total owed this period" —
was still summing every month of every config's life and subtracting every
payment ever made.

On a group with ten configs, 400 days old, settled 90 days ago, the two screens
read 14,000,000 and 3,000,000. The balance happened to match only because the
test settled the earlier period to the rial; any discount, rounding, or payment
made in cash and never recorded pulls them apart.

Both now compute through one function, `_period_share`, which decides how much
of a config's life belongs to the current period. Payments are cut at each
group's own date in the overview too, so a settled period's money no longer
counts as credit against the new one. A config whose every month predates the
settle date is dropped from both and reported as dropped.

### Fixed — The invoice billed configs the dashboard refused to bill

`_billable_config` — the rule that a config created and never switched on is not
billable — existed in exactly one place: the dashboard. The invoice never called
it.

So a config that was created and never used showed on the dashboard as excluded,
with the reason spelled out, and on the invoice as a charge. Two configs where
one had never been touched billed 400,000 instead of 200,000, and the invoice is
the document the reseller receives.

The invoice applies the same rule now and says how many it left out and why —
on screen and in the PDF. A test asserts the dashboard and the invoice return
the same due, months, renewals, paid, and balance, so they cannot drift again.

## [1.26.0]

### Fixed — The invoice billed renewals that had already been paid for

Reported against a real invoice: a reseller who had settled up was billed again,
this month, for the same renewals.

`billing_invoice` — which the PDF is built from — read no date at all. It counted
every month of every config's life, every time, and subtracted every payment ever
made. For a config created 400 days ago that gets renewed monthly, that is 14
months on an invoice that should show 3.

The date was already in the database. `settled_until` (تسویه‌شده تا) and
`period_start` (شروع همکاری) were both being stored, and the صورتحساب دوره screen
honoured them. The invoice and the PDF — the document that actually reaches the
reseller — did not look at either.

They do now, under one rule shared by every screen: an explicitly picked date
first, then تسویه‌شده تا, then شروع همکاری, then the whole history if none is set.
Only months dated on or after that point are billed, and payments are cut at the
same date so money from a settled period cannot turn into credit against the new
one. Configs whose every month falls before the date leave the table and are
counted in a line that says so, because an invoice that is short for a good reason
should say the reason itself.

صورتحساب has a date field for one-off invoices that do not match the group's own
setting, and says which default is in effect when it is left empty. The PDF prints
the period at the top and explains, at the bottom, what it left out.

### Fixed — Renewals were counted twice over, two different ways

Two functions counted renewals independently and disagreed.

`_renewal_dates` threw away its estimate as soon as it found a single logged
renewal, so a config with two years of history and one logged renewal was 25 months
on the invoice and one renewal on the period screen. It also counted a logged
three-month renewal as one renewal, dropping two months of billing.

Counting now happens in one place. `_months_for` decides how many months a config
has; `_renewal_dates` only assigns dates to them, filling the earliest slots with
estimates because logging only started the day Nexora was installed — the logged
renewals are always the recent ones. Both read their origin date from the same
helper, so the two numbers cannot drift apart again.

### Fixed — The «حجم بدون نرخ» warning could never appear

The invoice returned a count where the screen read a list, so `.length` was
`undefined` and the warning was skipped in silence — on exactly the screen where
an unpriced config means money not being charged. It returns the volumes now.

## [1.25.1]

### Fixed — The Jalali date picker would not open

Clicking شروع همکاری or تسویه‌شده تا appeared to do nothing.

It was opening. `.fx-anim`, which wraps whole pages, animates `transform` with
`fill-mode: both`, so the transform stays on the element after the animation
finishes — and any transform other than `none` makes that element the containing
block for `position: fixed` descendants. The calendar was therefore placed
relative to that wrapper while its coordinates came from
`getBoundingClientRect()`, which is relative to the viewport. It landed off-screen.

This is why the earlier fix for the clipping problem did not hold: switching from
`absolute` to `fixed` solved the clipping and introduced this.

The calendar is rendered through a portal onto `document.body` now, outside any
transformed ancestor. That is the only version that works regardless of what a
parent happens to do, since nothing can guarantee no ancestor has a transform.
The outside-click check follows it — the panel is no longer a DOM child of the
field, so clicking inside the calendar used to close it.

Found with a jsdom test that mounts the picker inside a transformed parent, which
is now part of the suite. The earlier test passed because it mounted with no
parent at all.

## [1.25.0]

### Fixed — The reseller had no link to give the customer

Every subscription link in the portal came from a `sub_base_url` that somebody had
to have typed into settings by hand. On an install where nobody had, the reseller
created a config and got nothing to hand over — an empty field, with no
explanation anywhere.

The panel already knows this. The subscription service runs on its own port and
path and x-ui holds those settings; the bot has read them from there since the
first version. The portal now does the same, as a third fallback: the reseller's
own setting, then the owner's, then the panel itself. When even that fails, the
screen says the subscription service may be off rather than showing a blank.

### Changed — The customer view, rebuilt

It was a list of fields in a box. Now it is three tabs, in the order the work
happens: تحویل به مشتری first — the link, a copy button, the QR — since that is
what the window gets opened for. Then مصرف, with a bar, a percentage, and what is
left. Then مشخصات, the dates and the ids.

The header carries the state at a glance: active or not, expired, or "۳ روز
مانده" when it is close.

## [1.24.0]

### Added — Opening a customer, with everything needed to hand the config over

Clicking a name in the reseller's list opens it: the subscription link with a copy
button and a QR code, the usage bar with real numbers, created and expiry dates,
days left, device limit, and renew or disable without leaving the panel.

The QR is generated on the server and never leaves it. A subscription link is
effectively the customer's password; sending it to an online QR generator would
hand that site full access to the service.

### Changed — Statistics that say what to do about them

The old cards counted things: configs, renewals, gigabytes. None of that tells a
reseller which customer is about to be lost, which is the only kind of number they
can act on.

The top of the page is now a "نیاز به پیگیری" line — expiring within a week,
already expired, quota exhausted, above 80% — shown only when there is something
in it. Below that: active users, expiring soon, usage against quota, and this
month's new configs and renewals, counted from the renewal ledger rather than
estimated.

Active means enabled and not expired. A config whose quota ran out is still
active and is counted separately, because they are different problems and the
reseller needs to see both.

## [1.23.0]

### Added — Receipts and approvals in the reseller portal

A reseller's customer orders through their bot and sends a receipt. Until now the
only place to approve it was a Telegram admin group, so a reseller without one had
orders that sat pending forever.

Their portal now has a سفارش‌ها screen: who ordered, what, how much, the receipt —
opened inline — and approve or reject with a reason the customer sees.

Approving calls the bot's own `approve_order`, not a second copy of it. The path
that takes someone's money and gives them a config does not get two
implementations; a fix that reaches one and not the other is a bug nobody sees.
That means the reseller's approval inherits the atomic claim, the provisioning,
the customer message and the commission — including the "another thread already
has this order" answer, so a reseller cannot double-provision by clicking twice.

Two things it refuses. Approving without a bot attached, because the customer
would have paid and been told nothing. And rejecting without a reason, because the
reason is what the customer reads.

Receipts are fetched through the server rather than linked: a Telegram file URL
carries the bot token in it, and handing that to a browser hands over the token.

Everything is scoped to the reseller's own tenant, and another reseller's order
answers "not found" — the same answer as one that does not exist, so the screen
cannot be used to find out which ids are real.

## [1.22.0]

### Added — "نیاز به تنظیم": the groups earning you nothing, in one place

On the real server 94 configs sat in seven groups that contributed nothing to any
invoice, and no screen said so. The only symptom was a total lower than expected,
with nothing to explain the gap — which is how it went unnoticed long enough to
be worth almost as much as everything that was being billed.

The groups page now opens with them: how many configs, which group, and which of
the three reasons it is — billing switched off, no rates defined at all, or rates
that do not cover the sizes those configs actually use. Each row has a button that
opens that group for editing.

Naming the reason is the point. "Nothing is counted here" sends you looking; "no
rates defined" tells you what to do.

## [1.21.0]

### Added — Topping up a reseller's credit, with a ledger

Prepaid resellers worked in every respect except the one that matters: there was
no way to give them credit short of editing the database. The card in
حسابداری ← پنل نمایندگی now shows the balance, takes an amount to add or take
back, and switches a reseller between prepaid and month-end billing.

Every change is written to a ledger, because the `credit` column alone is not a
record — it moves every time the reseller creates or renews anything, so there was
no way to answer "when was this topped up, and by how much". The reseller's own
spending lands in the same ledger, so the balance can always be explained. The two
rejected cases — an amount that would go negative, and zero — write nothing: a
ledger should show what happened, not what was attempted.

Negative credit already means "no ceiling, invoice at month end", so top-up
refuses to take a balance below zero rather than quietly turning a reseller into
an unlimited one.

### Added — A reseller can set up their own bot's plans

Attaching a bot gave a reseller a shop with nothing in it: `plans` is per-tenant
and they had no way to add any. They can now define what their own bot sells —
name, quota, days, devices, price, and whether it is listed.

That price is what they charge their customers. What they owe for each config is a
separate thing and still comes from the rates you set for their group; nothing in
this screen touches it.

Every read and write is scoped to their tenant. Saving their list cannot delete
another reseller's plans, and sending another reseller's plan id changes nothing.
Trial plans stay with the owner — a free config is still created on the owner's
server, so it is the owner's call.

## [1.20.0]

### Added — The reseller can see each customer's subscription link

They saw it once, at the moment they created the config, and then had no way to
find it again — so a customer who lost their link was someone the reseller could
not help. Every row in their list now has a copy button for it.

### Added — `nexora fix-flow`

New configs already carry `xtls-rprx-vision`: it is the default in the panel client
that both the bot and the reseller portal use, and nothing overrides it. Configs
made by hand in x-ui, or left over from older versions, can be missing it — and a
REALITY config without the flow does not connect.

Run it to see which clients are missing or on a different flow; run it with `--fix`
to set them. Only vless inbounds are touched, since flow means nothing on the
others, and it says to restart x-ui afterwards.

## [1.19.2]

### Fixed — A reseller's plans appeared in the owner's plan list

Reading the plans was never scoped to a tenant, while saving them always was.
With one bot that difference was invisible. As soon as a reseller can run their
own bot — shipped yesterday — their plans show up in the owner's list, and
deleting one from there answers `ok` and changes nothing, because the delete is
scoped to the owner's own tenant.

The read is scoped now, the same way the write always was.

## [1.19.1]

### Fixed — A reseller set up from the panel could not log in

Two bugs in the same route, both mine, and together they made a correct password
look wrong.

The setup route never read `group`. The panel sent it with every save and it was
dropped without a word, so `portal_group` stayed empty and the reseller would have
seen nothing even after getting in.

Worse, nothing opened the portal. None of the panel's buttons sent `enabled`, so
`portal_enabled` stayed at zero — and login treats a closed portal exactly like a
wrong password, deliberately, so that the page cannot be used to discover which
links are real. The owner typed the password the panel had just generated for them
and was told it was wrong. The command-line path set the flag; the panel did not.

Generating the first password now opens the portal, provided the slug and group
are already set. Only the first: an owner who deliberately closed a reseller would
otherwise have reopened them by editing their link, which would make closing
meaningless.

And the card now lists what is still missing — no slug, no group, no password, or
closed — instead of leaving the login screen's "wrong password" as the only
feedback.

## [1.19.0]

### Added — A reseller can attach their own Telegram bot

The machinery for this has been there all along: the bot service runs a separate
polling loop for every active tenant that has a token, each with its own branding,
its own panel connection and its own customers, and `sync_workers` picks up
anything new within thirty seconds. What was missing was a way for the reseller to
supply the token.

They paste it in their portal. The token is checked against Telegram's `getMe`
before anything is written — a mistyped token would otherwise produce a bot that
starts, never answers, and gives nobody a reason why. The shape is checked first,
so garbage never reaches the network. Their bot is live within half a minute.

A token already registered to another account is refused without naming who has
it; a reseller should not be able to learn who else is in the system. The token is
never returned by the API, not even to the reseller who set it.

They can also set their brand name and support username. Those go through an
allowlist of three keys rather than the whole settings object — settings also
holds the panel connection and the admin group id, and leaving it open would let a
reseller change things that are not theirs.

## [1.18.0]

### Added — "پنل نمایندگی" in the admin panel

Everything the reseller portal needed existed except the screen to set it up, so
the only way in was the command line. It is a section under حسابداری now: pick the
reseller, choose their x-ui group from a list of the real ones, generate a
password, and copy the link you hand them. Opening and closing their access is one
button, and closing drops their open sessions rather than letting them run on.

The group is a dropdown of groups that actually exist rather than a text field.
A trailing space or a different capital letter would mean the reseller logs in and
sees nothing, with no clue why.

### Fixed — The invoice headline ignored what had been paid

`مبلغ قابل پرداخت` printed the gross total. The amount paid and the remaining
balance appeared only in small print at the bottom of the page. A reseller who had
settled half their account saw the original figure at the top of the invoice and
had every reason to think their payment had never been recorded.

The headline is the balance when anything has been paid, labelled as such, with
`کل X − پرداخت‌شده Y` underneath it. The arithmetic behind it never changed —
only what the invoice chose to show.

## [1.17.0]

### Added — Resellers can create configs from their own panel

The last piece: a reseller picks a quota, a length and a device count, and gets a
config with its subscription link. They never touch the owner's x-ui again.

Creating is more dangerous than renewing, so two things are decided here and not
by the request:

**The group.** It comes from the reseller's own row and is passed to the panel as
`groupName`. It is the only thing that decides whose config this is.

**The name.** The reseller does not send one; it is generated as their own unique
slug plus random hex. If they could name it, they could pick a name matching
another group's pattern — and the subscription page picks its branding from the
email prefix, so their customer would see someone else's brand, and the billing
side might count the row under the wrong group.

The quota must be one of the tiers the owner defined for that group. A reseller
cannot create a size that has no rate, which would otherwise produce a config that
shows up as "بدون نرخ" at month end and that nobody pays for.

Also fixed while testing this: `int(payload.get("months") or 1)` turned a
requested zero into one. Someone asking for zero months would have been given one
and charged for it. Missing and zero are told apart now, on both create and renew.

## [1.16.0]

### Added — Resellers can renew and disable from their own panel

This is the half that makes the accounting real. A renewal done here is written
down the moment it happens — the config, the months, the date, the amount — so the
invoice stops reconstructing history from the gap between creation and expiry and
starts reading a ledger.

Three rules govern the write path, and the tests check each one:

**The group never comes from the request.** It is read from the reseller's own
row, every time.

**Anything about to change is fetched from the panel first and its group checked
there.** A reseller sending someone else's config name proves nothing. A config
from another group answers "not found" — the same answer as one that does not
exist, so the portal cannot be used to discover which names are real. The refusal
happens before any write is attempted.

**Credit is taken atomically and before the work.** The condition lives inside the
`UPDATE`, not in a read followed by a write — that gap is exactly the race closed
in the bot's wallet earlier today. If the panel then fails, the credit goes back.

Price comes from the rates the owner defined for that group, including the
per-user rate, and is recomputed server-side; the amount the portal displays is a
preview and never the figure charged. A reseller on unlimited credit is invoiced
at month end as before.

The portal also finds the config's live record on the panel before writing rather
than acting on the snapshot it listed from, since the unique id and inbound are
not in that snapshot and the write has to land on what is there now.

## [1.15.0]

### Added — Reseller portal

A reseller had to be handed the owner's x-ui panel. That let them see every other
reseller's configs and the owner's own direct customers, change anyone's quota,
and delete configs. It is also why accounting has to guess at renewals: work done
in x-ui is recorded nowhere.

Each reseller now gets their own link — `/r/<slug>` — with their own password.
They see their configs, quota and usage, expiry, device count, and what they owe:
the same figure the owner's invoice shows, so there is nothing to argue about.
Nothing else is reachable.

The reseller API is a separate, small surface rather than the owner's routes. The
admin API has 114 routes and every one assumes you own the system; scoping each
individually means one missed route leaks everything — the same shape as the agent
bug found earlier today, where a node was authenticated but not scoped. A seam test
enforces that every portal route passes through `portal_tenant`, and the portal
frontend is a separate app that cannot import admin code at all.

Scoping is on the reseller's x-ui group, never on anything the request carries. A
reseller with no group set sees nothing rather than everything — the safe default
for a half-finished account.

Set one up with `nexora reseller set <name> --slug hossein --group "<x-ui group>"
--generate`, and close it with `nexora reseller off <name>`, which drops open
sessions rather than letting them run to expiry.

Still to come: creating and renewing from the portal, which is what finally
replaces the renewal guesswork; prepaid credit; and attaching their own bot.

### Added — Reseller portal, first phase: accounts and scoped login

A reseller currently has to be handed the owner's x-ui panel. That means they can
see every other reseller's configs and the owner's own direct customers, change
anyone's quota, and delete configs. It is also the reason accounting has to guess
at renewals: work done in x-ui is recorded nowhere.

This is the foundation. Each reseller gets a slug and a password, reaches the
panel at their own link, and logs into a session scoped to their tenant alone.
The owner turns access on and off; turning it off closes any open session on the
spot rather than letting it run to expiry.

The reseller API is a **separate, small surface, not the owner's routes**. The
admin API has 114 routes and every one of them assumes you own the system; handing
those to a reseller would mean scoping each individually, and one missed route
leaks everything. The same shape of bug turned up in the agent routes earlier
today — a node that was authenticated but not scoped could close another node's
jobs. So: allowlist, not denylist. Anything not written for the portal does not
exist for a reseller. A seam test now enforces that every portal route passes
through `portal_tenant`.

What a reseller sees of themselves is deliberately thin — name, credit, discount,
whether they have a bot. Never the bot token, never the x-ui password.

Writing the tests caught a bug in this code before it shipped: the column was
being created as `TEXT`, so the integer 1 was stored as `'1'`, and in SQLite
`'1' = 1` is never true. No reseller could have logged in, and nothing would have
been logged anywhere. The column has a type now, and the query casts, so servers
that already created it as text still work.

## [1.14.1]

### Fixed — The invoice contradicted its own total

Under "مبلغ قابل پرداخت" the PDF printed "۱۱۰ ماه × ۱۹۰٬۰۰۰" — months times the
first tier's base rate. Once a per-user rate existed that multiplication stopped
matching: the box said 25,300,000 and the line under it worked out to 20,900,000,
missing the 4,400,000 of extra-user charges.

Every row was correct. The explanation was not, which is worse: an invoice that
disagrees with itself makes all of its numbers untrustworthy. It now breaks the
total into base and extra-user charges, which add up to the number above them.

### Fixed — The renewal column ran together into one blue band

The renewal marker filled the whole cell, the full column width and the full row
height. Consecutive renewed configs merged into a single unbroken block starting
at the header, so the numbers were hard to read and it wasn't clear which row each
belonged to. It's a small pill around the number now.

### Changed — Rows no longer sit on the footer, and one more fits per page

The last row on a page could reach below the page number. The bottom margin
accounts for the footer now, and a slightly shorter row height gives back what
that cost.

## [1.14.0]

### Added — A per-user rate on every rate tier

A four-user config was billed exactly like a one-user config, even though three
more people sit on the server for it. The device count was printed in the invoice
and counted for nothing.

Each rate tier now carries its own per-user rate, so the 30 GB tier and the 50 GB
tier can charge differently for extra users. The base rate covers the first user
and each additional one is added on top — so single-user configs and every invoice
issued before this are unchanged.

The rate editor shows the arithmetic while you type: "a 4-user config becomes
340,000 toman a month — 190,000 + 3×50,000". A rate with no per-user amount
behaves exactly as it did.

Configs with no device limit can't have their extra users counted, so they take
the base rate alone.

### Fixed — The date picker opened underneath the card and could not be reached

"شروع همکاری" and "تسویه‌شده تا" sit near the bottom of the group editor. The
calendar opened downward with absolute positioning, so half of it fell below the
card's edge — the day buttons were there but unreachable, and no amount of
scrolling helped.

It is positioned against the viewport now, flips upward when there isn't room
below, and scrolls inside itself if it still doesn't fit.

## [1.13.1]

### Fixed — The invoice PDF said "page 1 of 1" on a six-page document

The footer was drawn at the *top* of each page, from inside `header()`. The page
total is only worked out after the first page has been laid out, so page one
always printed "صفحه ۱ از ۱" — on a 94-config invoice that runs to six pages. The
first page looked finished, with blank space under the last row, and the other 79
configs looked lost.

The total was wrong on the later pages too: it was predicted from row heights and
always assumed one extra page for the notes, whether or not they needed one. Six
pages, numbered "of 7".

Nothing is predicted any more. Pages are held back and the footers are drawn at
the end, when the count is certain.

That change also surfaced a second bug: the final page was never closed, so the
page holding the group totals and the notes was dropped from the file entirely.

### Fixed — Empty cells printed as an empty box

Blank values used an em dash. The Persian font on the server has no glyph for it,
so every "no renewals", every missing expiry and every unknown percentage came out
as a hollow square. The font probe only checks Persian letters, never the
punctuation the table itself draws. A plain hyphen exists in every font.

## [1.13.0]

### Added — Renewals made in the panel are now recorded

The reseller holds the x-ui panel and renews configs there directly. Nothing
recorded it: not the bot, which never saw it, and not x-ui, which keeps no
history at all. So the owner could see how many configs a reseller had, but never
which customer renewed or how many times — which is not enough to settle an
account.

The `renewals` table has been in the schema since the beginning, under a comment
saying x-ui has no history so we record it ourselves. Nothing ever wrote to it.

Now `client_seen` remembers each config's expiry, and every read compares it to
the last one. An expiry that jumped forward by twenty days or more is a renewal:
it gets logged with the config, the months gained, and the date. Smaller moves
are treated as manual corrections, and an expiry moved backwards is not a renewal.

Be clear about the limit: the recorded date is when it was *seen*, not when it
happened. Open accounting once a month and the renewal is still counted, just
dated late. The count is right; the date is approximate.

### Fixed — A recorded renewal used to shrink the bill instead of growing it

`_months_for` returned `1 + recorded renewals` and stopped there. Recording starts
the day Nexora is installed, so a config with two years of history and one
recorded renewal would have billed as two months instead of twenty-five — meaning
the moment renewals started being recorded, invoices would have collapsed.

The recorded count is a floor now, not a replacement. The estimate from creation
to expiry still wins when it is larger, and the recorded count takes over only
when it is bigger — which is exactly the case the estimate cannot see: a renewal
done by setting a fresh expiry date rather than adding days, which shortens the
span and hides the history.

## [1.12.0]

### Fixed — A rate you defined is now used, even when it does not match exactly

On a real server: 257 configs, and only 29 of them were being billed.

The `unlimited` group had one rate — unlimited = 190,000 — but 92 of its 94
configs have a quota (88 at 200 GB, 4 at 20 GB). `Hossein dehlagi` was the same
shape. The code refused to apply an unlimited rate to a config with a quota, on
the reasoning that a volume rate cannot be generalised to unlimited. So 100
configs priced at zero and the invoice was short by about 19.4 million toman a
month.

That reasoning had it backwards. Defining a rate for a group means the group has
a price. "It does not match exactly, so nothing" answers a question nobody asked.

Two symmetric fallbacks now close it: a config with a quota in a group whose only
rate is unlimited takes that rate as the group's flat price, and an unlimited
config in a group with only volume rates takes the highest tier — the same rule
already used for a config larger than every tier.

This can only turn a zero into a price. No line that already had one changes.

### Changed — `nexora billing-why` prints English

Persian right-to-left text mixes badly with left-to-right terminal output, and
this is meant to be copied and pasted. It also now ends with a summary: the
monthly total, how many configs still have no rate, and how many sit in groups
with no billing setup at all.

## [1.11.1]

### Added — `nexora billing-why`

Five situations end in "بدون نرخ" and no amount of reading the code tells you
which one a given server is hitting. This puts the three deciding facts side by
side: the rates recorded for each group, the group names as they actually appear
in x-ui, and which quota matches which rate.

It also catches the one case reading the code never could — a group name in x-ui
that differs from the recorded key by a space or a capital letter, so the rates
silently never apply. The panel just says "بدون نرخ".

No password, token, customer name or phone number is printed, so the output can
be sent as-is.

## [1.11.0]

### Fixed — "بدون نرخ" never said why, and a rate could vanish on save

Five different situations end in "بدون نرخ", and the phrase tells you none of
them apart: no rates on the group at all, an unlimited config with only volume
rates defined, a volume config with only an unlimited rate, rates stored in a form
that cannot be read, or a group whose key does not match. An owner who has
defined a rate and still sees "بدون نرخ" has no way to tell which — which is why
this has come back as "accounting isn't working" more than once.

Every unpriced line now carries the reason, and the reason names the fix: which
button to press, which kind of rate is missing. The group card lists them by
count instead of showing a bare number, and the invoice and period views show
them too.

And one of those five was real: saving a rate row that could not be parsed
silently skipped it. The response said `ok`, the row was gone, and the next thing
the owner saw was "بدون نرخ" for a rate they had definitely entered. It is a 400
now, naming the row.

### Fixed — The ports and clients cards still dumped the whole list

Two cards in monitoring never moved to the numbered pager the rest of the panel
uses. The open-ports card had two ways to render everything at once: with any
filter active there was no cap at all, and the "نمایش همه‌ی N پورت" button opened
the lot. On the Iran node — around eight hundred sockets — that is an
eight-hundred-row table inside one card, which is the section reported as too big
and spilling out of its box. The top-clients card had the same button, on a list
that grows with every config sold.

Both now use `LongList`: ten and six per page, numbered, with search. Risky ports
still sort first, so page one is what needs attention. `LongList` gained an
optional `container` so table rows can sit in a real `<tbody>` instead of a
`<div>` — the reason this card had been skipped.

### Fixed — An affiliate could be their own customer

The friend-referral path refuses a self-invite. The affiliate path had no such
check, so an affiliate who had never used the bot could open their own `aff_`
link, become their own attributed customer, and earn commission on every purchase
they made themselves.

Same rule, both paths now.

### Fixed — Snapshots and safety copies were missing whatever was still in the WAL

`bot.db` runs in WAL mode, so committed data lives in `bot.db-wal` until a
checkpoint moves it into the main file. Both places that copied a database copied
only the main file: the safety copy taken before a restore, and the snapshot
`nexora update` takes before replacing anything — the one `nexora rollback` goes
back to.

In the test, a file copy of a database with fifty rows sitting in the WAL came
back unreadable, not merely short. On a running server the loss would be quieter
and worse: a snapshot missing the last stretch of orders and payments, which
nobody notices until it is the only copy left.

Dropping such a file next to a stale `-wal` is its own hazard, since SQLite may
replay that WAL over it — so the restore path now clears the destination's `-wal`
and `-shm` first.

Both paths now take a consistent copy: SQLite's own backup in Python, `sqlite3
.backup` in the shell, falling back to carrying `-wal` and `-shm` along when the
`sqlite3` binary is missing. `tunnels.db` was in the snapshot but not in the
restore, so it is restored now too.

The restore itself was emptying every table and refilling them with no
transaction around it. It is wrapped in `BEGIN IMMEDIATE` with a rollback, and a
safety copy that fails to be taken is logged instead of silently becoming `None`
while the restore proceeds anyway.

### Fixed — "Block attackers" could block the owner

`firewall.py` opens by saying no operation may cut off the owner's SSH, and every
path to enabling the firewall or deleting a rule goes through that guard. Blocking
an IP did not.

The owner's own address can land in the failed-login list — a few mistyped
passwords is enough. One click on block-attackers then runs `ufw insert 1 deny
from <their address>`: position one, ahead of the rule allowing SSH. That path
has no rollback timer, so the way back is the provider's console.

`block_ip` only checked the string's shape, so it also accepted `127.0.0.1`,
`10.x`, and `0.0.0.0/0` — the last one, inserted first, takes the whole server
off the network.

One `block_refusal` now sits in front of every blocking path — single, bulk, and
blackhole. It refuses the address the request itself came from (and any range
containing it), loopback, private and link-local, "all addresses", and anything
shorter than /8. A /8 is still allowed, since blocking a country is a real use.
The routes pass the caller's address from the same `ContextVar` the login
lockout uses.

### Fixed — Every renewal told the customer their quota was nearly gone

Renewing adds to the panel's quota and does not reset the usage counter, so both
numbers accumulate together and stay comparable. The bot never updated its own
stored `gb`, which kept the size of a single plan.

So from the first renewal onward, lifetime usage was being measured against one
period's allowance. Someone on a 50 GB plan who had used 45 GB renewed to a real
100 GB cap — and the bot still read it as 45 of 50. On day one of the new period
they saw "۹۰٪" and an orange dot in اشتراک‌های من, and the hourly job sent them
the 80% warning telling them to top up. Every renewal after that made it worse:
95 of 50, then 145 of 50.

`extend_subscription` now returns the quota that actually landed on the panel and
the renewal stores that, so the two sides cannot drift. Taking the panel's number
rather than recomputing the same sum in a second place is the point — that is how
they came apart to begin with.

### Fixed — A stuck auto-renewal repeated the same failure every hour

Auto-renew runs hourly over every subscription with a day or less left. When it
failed, nothing recorded that it had: the next hour tried the identical thing and
failed identically. One customer whose panel call was broken produced 24 orders a
day, 24 charge-and-refund cycles against their wallet, and 24 identical warnings
in the admin group — which is also how the next real warning gets lost.

Failures are now counted on the subscription and the next attempt is pushed back
by that many hours, up to six. It is never abandoned: giving up means the
customer is cut off without being told. The admin group hears the first failure
and then every sixth; the rest go to the log. After three, the customer is told
directly, with the manual renew button and the sentence they most need — nothing
was taken from their wallet.

A deleted plan was the silent version of the same thing: `auto_renew_subscription`
returned immediately with no message at all, so auto-renew stayed switched on,
never ran, and the customer believed they were covered right up to being cut off.
They and the admin are now told once.

### Fixed — Cancelling as the order expired refunded the coins twice

`release_coins` read the held rows, credited the coins back, and only then
renamed the hold to `released` — three separate statements. Its docstring said a
double refund was impossible because the rename hides the row from the next pass,
which is only true if nothing runs in between.

Something does. It is called from two threads: the sweeper that expires stale
orders, which lives in the scheduler, and the cancel and reject paths, which live
behind the per-chat lock. The scheduler is not behind that lock — the same
boundary every other race in this file was found on. A customer tapping cancel at
the moment the sweeper expires the same order means both see the same `hold` row
and both pay it back. Coins are a discount, so that is money.

Each hold is now claimed with a conditional update and paid only by the thread
that won it. The order is deliberate: if something dies between the claim and the
payment, the coins are not returned — bad, but visible and fixable. Paying twice
is silent. The test runs the two callers against each other on a barrier.

Also: `add_coins`, `add_balance` and the referral reward wrote their ledger row
whether or not the balance update matched a user, so the books could record a
movement that never happened. Now no row moves, no transaction is written, and
the referral reward gives back the once-per-friend slot it would otherwise have
consumed for nothing. This one has no live trigger — nothing deletes users — but
the ledger should not be able to drift from the balance by construction.

### Security — One node could close another node's jobs and write its metrics

`/api/agent/job-result` authenticated the node, then acted on whatever the body
said. `finish_job` was called with the job id alone, so any node with a valid
token could mark *any* node's job done — the panel would show the work finished
while nothing had happened on that server. `save_metrics` took `tunnel_id`
straight from the body, so a node could write invented latency and loss into any
tunnel's history, including a tunnel it has nothing to do with.

The Iran node is the most exposed machine in this system. Whoever holds one of
those tokens should be able to speak for that node and nothing else.

`finish_job` now takes the node and refuses a job that does not belong to it, and
it returns the job's *stored* action rather than trusting the one in the reply —
so the sender no longer decides where its own result gets filed. Metrics are
written only after `tunnel_on_node` confirms the tunnel really is on that node
(either end of it, since a probe can come from either side). Both refusals are
logged against the node that tried.

### Security — Nothing stopped someone guessing the admin password

The whole system sits behind one password: the panel, customer records, the x-ui
credentials, the bot token. There was no limit on how often it could be guessed —
not on `/api/login`, and not on the admin routes, which take the same password in
a header and are just as open to guessing. The intrusion module watches SSH brute
force; the panel's own door had no guard at all.

Ten failures from one IP within five minutes now lock that IP out for fifteen,
and a correct password during the lock is refused too — otherwise the lock is an
inconvenience rather than a barrier. A successful login clears the record. The
last three attempts before the lock say how many are left, so an owner who has
forgotten their password knows where they stand; earlier than that it says
nothing, since an early warning only tells a guesser where they are.

Behind nginx every request arrives from 127.0.0.1, so `X-Forwarded-For` is used —
but only when the immediate peer is loopback or private, i.e. our own nginx. From
the internet the header is ignored, or the lock could be stepped around with a
made-up header. A `ContextVar` set in middleware carries the address, so none of
the 114 `check_auth` call sites had to change.

The comparison is now constant-time. Writing that test surfaced a bug in the fix
itself: `hmac.compare_digest` raises `TypeError` on non-ASCII strings, so an owner
with a Persian password would have had every request fail with a 500 rather than
a login prompt. It compares encoded bytes.

The login screen showed "رمز عبور نادرست است" for every failure. With a lockout
in place that is the worst possible message — it sends the owner back to try
again exactly when they must not — so it now shows what the server said.

### Security — Subscription links could be derived from a Telegram ID

`create_subscription` set `subId = email`, and the email is `prefix_tgid_seq`.
A 3x-ui subscription path takes no password — no subscription link does; its
whole protection is that the string cannot be guessed. 3x-ui generates a random
`subId` for exactly that reason, and we were replacing it with something
countable.

The real risk is not targeting one customer, it is scanning. Anyone holding one
config of their own knows the prefix and the domain; from there a range of
Telegram IDs with a small `seq` yields other customers' live subscriptions —
their configs, on their quota.

New configs now get `prefix_<16 hex>`. The prefix stays, in case any routing is
keyed on it; the subscription page reads the brand from the client *email*, not
from the URL, so nothing about branding changes. Existing subscriptions are
untouched — their `subId` and link are stored and keep working.

### Fixed — The customer who most needs to renew was the one shown no banner

The subscription page's renew banner fired on `daysLeft >= 0 && daysLeft <=
threshold`. `calculateDaysLeft` returns `-1` for a subscription that has already
expired, so `-1 >= 0` is false and an expired customer fell through to the volume
branch — which needs the quota to be nearly used up.

A customer who ran out of *data* got the banner, because their remaining
percentage hit zero. A customer who ran out of *time* got nothing: no banner, no
renew button, no support link. The days counter said "منقضی" and that was the
whole message.

Expired now gets its own branch, with its own title and text in all four
languages ("اشتراک شما منقضی شده" rather than "رو به اتمام", which is a different
thing), and both are editable from the panel's banner settings. The banner is
still suppressed when the client is disabled, since that banner takes over.

The test drives the page through healthy, expiring, expired, and unlimited.

### Fixed — Yearly costs counted as zero in the monthly burden

The expenses page has a card reading "هزینه‌ی ثابت ماهانه — این مبلغ را هر ماه
باید دربیاورید". It summed only expenses marked `monthly`. An expense marked
`yearly` — a domain, a licence — was accepted by the form, stored, and labelled
"سالانه" in the list, then left out of that figure entirely.

So a 1,200,000-toman yearly renewal contributed 0 to the number that says what
you have to earn each month, instead of 100,000. That is the exact error this
module's own docstring says it exists to prevent: wrong, and always in the
owner's favour.

Yearly expenses now contribute a twelfth. The card says how much of the total
came from them, so the number doesn't just quietly go up.

### Fixed — The HTML guard missed what Telegram rejects and flagged what it accepts

`send` runs every outgoing message through `fmt.check`, and sends it stripped of
all formatting if anything is reported. So a false report flattens a message, and
a missed one means Telegram refuses it and the customer sees nothing at all.

It did both.

Missed: a `<` that never became a complete tag. `check` only looked at fully
formed `<tag>` matches, so a customer name containing `<` with one forgotten
`esc()` sailed through — and Telegram rejects the whole message for an unclosed
start tag. Also missed: an invalid attribute (`<a href="https://x/"anything">`
passed because the test was only whether `href="` appeared anywhere), a link
nested in a link, and an attribute on a tag that takes none. Hex entities like
`&#x27;` were reported as unescaped `&`.

Flagged wrongly: `<pre><code class="language-bash">` — Telegram's own documented
form for a code block with a language, which `fmt.pre(text, lang)` generates. The
first message to use one would have been silently flattened.

All 43 rendered bot screens still pass, so nothing legitimate was caught by the
tighter rules.

### Fixed — A tunnel with every port dead reported "unknown"

`save_metrics` kept the first TCP probe that came back `ok` and discarded the
rest. The agent reports a dead port as `{"ok": false, "loss": 100}` — it knows
exactly how bad it is — but that row was thrown away for not being `ok`.

So a tunnel with every port down stored a sample of nothing: no latency, and no
loss either. The trend chart showed a gap and the quality read "نامشخص" for a
tunnel that was completely down. And a tunnel with half its ports dead read
"عالی", because the one surviving port's numbers were the only ones recorded.

Loss is now averaged over every port, counting a dead one as 100, and latency
comes from the ports that answered. A sample where nothing answered is stored as
100% loss and reads "قطع" — a state the panel now has a colour for. A sample
with no data at all still reads "نامشخص": no news and bad news are different
things.

### Fixed — Editing a tunnel skipped every check that creating one does

`create_tunnel` validates the transport against the engine's list, the bridge
port against 1024–65535, and rejects an empty name or remote host.
`update_tunnel` validated none of them — the same value that was refused at
creation went in through a PUT without a word. The config then generated wrong
and the only symptom was a tunnel that didn't work. An empty bridge port
surfaced Python's own English `int()` message as the API error.

Both doors now share `_check_transport` and `_check_bridge`.

### Fixed — The firewall's SSH guard only knew about port 22

Every guard in `firewall.py` compared against the literal 22: `sshProtected`,
`_ssh_would_break`, the critical-port markers, the preflight blockers, the
"don't deny this" check in apply-plan.

On a server whose SSH has been moved — what every hardening guide tells you to
do — that is the worst outcome the module exists to prevent. A leftover rule on
22 was enough to make `sshProtected` true, so the panel showed no warning, and
`/firewall/toggle` enabled ufw with nothing allowing the real SSH port. That
path has no rollback timer, so the only way back in is the provider's console.

The real ports are now read from `sshd_config` (plus `sshd_config.d/*.conf`,
`ListenAddress host:port`, honouring comments) and, on the paths that already
have the socket list, from the sockets `sshd` actually holds. Config and running
state are unioned: an extra port costs one warning, a missing one costs the
server. With no config readable, the answer is still 22 — unchanged behaviour on
a stock machine. `status()` now returns `sshPorts`, and the panel's warning names
the real port instead of saying 22.

Saved rules (`ufw show added`, what the panel lists while the firewall is off)
now carry the same critical marking as active ones.

### Fixed — The panel-trace button raised NameError instead of tracing

`app.py` imports `sys` as `_sys`; the xui-trace route called bare `sys.path`.
Every click on it returned a 500.

### Added — Reading a variable that nothing assigns is checked

The seam that catches a missing module import missed both of the above: it looks
for `mod.name(`, and `sys.path.insert(` doesn't match that shape. The new check
walks every function's scope with the AST instead, so a name that gets its value
in some *other* function is caught at test time rather than when the user presses
the button it hides behind.

It found the xui-trace bug on its first run, and two of my own in this change.

### Fixed — Losing the claim race erased the winner's claim

Once approval became a conditional claim, `approve_order` could return False for
two very different reasons: provisioning failed, or another thread already owns
the order. Both callers read that False as failure.

The scheduler's version was the serious one. On failure it reset the order to
`awaiting` unconditionally — so when it lost the race it wrote over the winner's
claim while that thread was mid-provision. The config went out, the order still
read "awaiting review", it reappeared in the admin's pending list, and a second
approval passed the `status != 'approved'` check and provisioned a second
config. The double-provisioning bug, back through the side door.

Now the busy answer is a named constant both callers branch on: the scheduler
logs and leaves the status alone, and the admin is told to wait rather than
"creating the config failed", which invites exactly the retry that must not
happen. Both status reverts also carry `AND sub_id IS NULL`, so an order whose
config already shipped can never be sent back to the pending list.

This is the third regression from the same root: every guard I make conditional
turns a sentence that was unconditionally true into one that needs re-checking
at each caller.

### Fixed — Callers still claimed the rejection went through

Making rejection conditional left three callers reporting success regardless. The
admin was told "order #N was rejected and the customer has been informed" even
when nothing had been sent.

That was a regression I introduced in the change above: before rejection could
refuse, the sentence was always true.

All three now read the result. The two admin paths say what actually happened, and
the scheduler logs the real outcome.

The scheduler needed more than a message. A rejection it could not perform left
the order in `panel_reject`, which is exactly what it scans for — so it would have
retried the same order every twenty seconds forever. It now moves the order out of
that state with a note saying the config had already been delivered.

### Fixed — An order could be rejected and approved at the same time

Rejection arrives by two routes as well: the button in the admin group, inside the
per-chat lock, and the panel route, which the scheduler runs outside it. Approval
has the same two routes.

Nothing stopped both from happening. The customer would receive their config, a
message saying the order was rejected, and their reserved coins back — three
outcomes that contradict each other.

Rejection is conditional now: an order whose config already exists cannot be
rejected, and rejecting twice does nothing, so the customer does not get a second
rejection message.

Approval refuses an order rejected in the last five minutes, because a rejection
that recent is a race rather than a decision. An older rejection still allows
approval — an admin who rejected by mistake has to be able to put it right.

### Fixed — A receipt arriving as the deadline passed refunded the coins anyway

`handle_receipt` checked the deadline, then wrote the receipt and set the order to
awaiting — unconditionally.

The sweeper that expires unpaid orders runs on the scheduler thread, outside the
per-chat lock, every two minutes. If it fired between that check and that write,
it expired the order and released the reserved coins. The write then set the
order back to awaiting with the receipt attached, so the admin approved it
normally — while the coins had already gone back to the customer. They kept the
discount and the coins.

The write is conditional on the order still being pending now, and when it is not,
the customer is told the deadline just passed and to send the receipt to support
rather than being left thinking it was filed.

### Fixed — Two renewals at once charged twice and extended once

`extend_subscription` reads the current expiry from the panel and writes the new
one. Two renewals running together both read the same starting point and both
write the same single month.

The customer pays twice and gets one month. Nothing errors, and nothing in the
panel shows it — the expiry looks right, because it is right for one of the two
payments.

Reaching it does not need a double tap — the per-chat lock covers that. It needs
the hourly auto-renew, which runs on the scheduler thread outside that lock,
firing while the customer renews by hand.

A subscription is now locked for the moment it takes to renew it. The second
renewal is refused, and `wallet_pay` already refunds in full when provisioning
fails — so the customer gets their money back rather than a silent loss.

The lock releases whatever happens, including a panel error, and a lock older
than five minutes is taken over, so a thread dying mid-renewal cannot leave a
subscription permanently unrenewable. It is per subscription: the customer's
other subscriptions stay free to renew.

### Fixed — The referral reward asked the wrong question, and my own fix made it worse

`_reward_referrer` paid out when the buyer had no *other* approved order. That is
not the rule the business wants, which is one reward per friend introduced — and
it was read-then-write, so two approvals at once could pay twice.

Claiming orders before provisioning (above) changed which way it failed. With the
status now set early, two concurrent orders from the same customer each see the
other as an earlier approved order, and *neither* pays. I introduced that, and
the fix for it is to stop counting orders at all.

The reward is now one statement: insert the coin transaction only if no referral
row exists for that friend, then credit the coins if the insert happened. The
condition and the payment cannot be separated, and the rule matches what it is
actually for — a second friend still earns a second reward.

### Fixed — The free trial could be taken more than once

Same shape as the approval race, and this one gives away product.

`give_trial` read `trial_used`, provisioned the config, and wrote the flag
afterwards, with a full round trip to x-ui in between.

**Correction to what was written here first:** I described this as reachable by
tapping the button twice. It is not. The bot serialises everything from one chat
behind a per-chat lock, so a customer's second tap waits for the first to finish
and finds the flag already set. This change is hardening, not a live bug — unlike
the three below it, which the scheduler reaches without that lock.

The right to the trial is claimed atomically now — one conditional UPDATE from 0
to 1, and only the winner proceeds.

The failure message tells the customer their trial is still available, so a failed
provision explicitly hands the right back. Without that, the claim would have made
that sentence a lie.

### Fixed — Two approvals at once gave one payment two configs

`approve_order` read the order's status, provisioned the config, and only then
marked it approved. The gap between the read and the mark is a full round trip to
x-ui — seconds.

The bot serialises each chat behind a lock, so two taps on the same button are
safe. The scheduler thread is not behind that lock: it calls `approve_order`
directly when it picks up an order approved from the panel. So a panel approval
landing while an admin presses the button in the Telegram group is two threads
with nothing between them — both pass the guard, both provision, and the customer
gets two configs for one payment. The referrer is rewarded twice as well, since
neither approval is marked yet when the "first purchase?" check runs. Only the
affiliate commission survived, because its table has a uniqueness constraint.

The order is now claimed atomically before anything else happens — one conditional
UPDATE, and whoever loses is told the order is already being processed.

A claim that goes stale is re-claimable after five minutes, so a thread that dies
mid-provision does not strand the order. And a failed provision releases the claim
with the error, which keeps the existing "you can try again" behaviour: without
that, a failure would leave the order marked approved with no config and no way
back.

### Added — Data paths are checked across the installer, the code and the CLI

Three places have to agree about where files live: `install.sh`, which writes the
environment into the systemd unit; the defaults in the code; and `nexora-cli`,
which looks in those places when it takes a snapshot.

If they drift, the panel writes somewhere the snapshot does not look — and the
day a backup is needed, it is empty. That is exactly what happened with the
backup table list earlier in this batch, so it is worth a test rather than a
reading.

They agree today. Verified by removing one line from the installer and watching
the test name the missing variable.

### Fixed — The DNS check hung instead of reporting slow DNS

`check_dns` exists to notice when name resolution is slow. It called
`gethostbyname` with no bound — and that call takes no timeout, because it goes
through the system resolver, which has its own and can take tens of seconds
across several nameservers.

So the check that was written to warn about slow DNS held up the entire health
report when DNS was slow. On the panel and on the Iran node both, since the agent
runs the same code. Measured at 30 seconds against a hung resolver.

It runs with a 4-second deadline now and reports "did not answer" as critical,
which is the warning it was meant to produce. A genuine resolve failure still
reports its own error, so the two are distinguishable.

### Fixed — Reverse DNS could hold the intrusion page for 30 seconds

`rdns_many` looks up names in parallel with a time budget, and the docstring
promises the whole thing is capped. It was not.

The pool was used as `with ThreadPoolExecutor(...)`, and leaving that block calls
`shutdown(wait=True)` — which waits for every lookup already running. Cancelling
the pending ones does nothing for those. With a DNS server that does not answer,
the page waits as long as the resolver does. Measured at 30 seconds against a
1-second budget.

The pool is shut down with `wait=False, cancel_futures=True` now. Lookups still
finish in the background and land in the cache, so the next load gets them free.

### Fixed — A reverse lookup left a 1.2-second timeout on every socket

`rdns` set `socket.setdefaulttimeout(timeout)` and restored the old value
afterwards. That value is global to the process, not to the thread, and
`rdns_many` runs sixteen of these at once: each thread read whatever another had
just set as the "previous" value, and the last one to finish left 1.2 seconds
behind permanently — inherited by every socket in the panel that did not set its
own timeout.

It did not even do what it was for. `gethostbyaddr` goes through the system
resolver, which has its own timeouts and ignores that setting. Bounding the time
belongs to `rdns_many`, and that is where it now happens.

### Added — Owner classification is tested

Deciding whether an attacking address is a datacentre or one of your own
customers had no test, and getting it wrong means blocking someone who pays you.
Real reverse-DNS samples for an Iranian operator, two hosting providers, an
unknown domain and no name at all, plus a check that no hint is short enough to
match everything.

### Added — Interpolated SQL is checked for request data

Values are always parameterised, but table and column *names* cannot be, so 24
queries build those with an f-string. That is fine while the names come from the
code — a fixed list, a whitelist, or the database's own schema. It stops being
fine the moment something from the request body lands in the query text.

All 24 were read: filter fragments from a constant dictionary, `SET` clauses
assembled from literals written in the code, whitelisted keys, table names from
`sqlite_master`, and placeholder strings. None takes request data. No injection.

The check is permanent now — it flags any SQL f-string whose interpolated
expression references the request. Confirmed by planting one and watching the
test name the file, the line and the expression.

### Added — Every query is checked for tenant scoping

The bot is multi-tenant: each shop shares the same tables and only the
`tenant_id` column keeps them apart. A query that omits it shows one shop's data
inside another's bot, and raises nothing — the result looks perfectly valid.

All 85 queries touching tenant tables are scoped. Confirmed by removing one
condition and watching the test name the file and line.

One exception is recorded with its reason: the one-time migration that backfills
`plan_name` runs across all tenants on purpose, joining on a globally unique
`plan_id`.

### Changed — Commission lookups assert the tenant instead of relying on ids

`record_commission` looked up the user and the affiliate by primary key alone.
Those ids are globally unique, so the result was correct — until a caller passes
a `user_id` from one tenant with another tenant's `tenant_id`. The commission
would go to the wrong shop's partner, silently. The condition costs nothing.

### Added — Every route is checked for authentication

An admin endpoint that forgets `check_auth` is an open door, and from the outside
it looks exactly like the others. With 114 of them, reading through is not a
method.

`tools/test-seams.py` now parses `app.py` into a syntax tree and checks every
route. All 114 admin endpoints authenticate — verified, not assumed. Deleting one
`check_auth` makes the test fail and name the route and its line, which is how
this was confirmed to work rather than just pass.

Agent routes are checked for a node token instead, since they are called by the
agent and not by a person. Five are deliberately open and listed with the reason:
they serve *code* — the monitoring and firewall logic and the agent itself — not
data or credentials. Requiring a token would be easy on this side, but `download`
in the agent does not send one, so every agent already installed would lose the
ability to update itself and stay behind permanently. The order has to be the
other way round: the agent learns to send a token first.

Public routes are listed too, so a new one cannot appear without someone
deciding it should.

### Fixed — Every expired subscription said it expired "0 days ago"

`days_left` never returns a negative number. That is deliberate: a lot of code
treats zero as "finished", and the reminder scheduler, auto-renew and the traffic
warning all rely on it.

But the "my subscriptions" screen used the same value to build "expired N days
ago", so N was always zero. A customer whose subscription ended yesterday and one
whose ended three months ago read the same line.

A separate `days_past` answers that question; `days_left` keeps its contract
untouched. The screen also says "expired today" rather than "0 days ago", which
is not a sentence anyone writes.

### Fixed — A new config could land on an existing customer's identifier

The identifier for a new config came from the *count* of a customer's
subscriptions: `len(user_subs) + 1`. That holds while the table only grows, which
it does — nothing deletes from it.

But a count is not a high-water mark. Any gap in the sequence — a row that never
got written because provisioning failed halfway, or a bot database restored from
an older snapshot — moves the number backwards onto an identifier that x-ui still
has, belonging to a customer who is using it right now. `create_subscription`
passes it straight to `add_client` with no check.

The number comes from the highest existing suffix now, not the count. Then the
panel is asked whether that identifier is free, and it steps forward until one
is. One extra request per purchase, against taking over a live customer's config.

If the panel does not answer, the computed identifier is used anyway: stopping a
purchase that has already been paid for, because a safety check could not run, is
the worse outcome.

### Fixed — Three rate limits in a row dropped the message

`call` retried three times, and those three were shared between network errors
and Telegram's 429. They are not the same thing. A network error means something
is broken; a 429 means Telegram is explicitly saying *wait this long and send it
again*.

During a broadcast, three 429s in a row is ordinary. The message was dropped and
the recipient counted as failed.

They have separate budgets now: three attempts for network errors, six waits for
rate limits. Telegram's own `retry_after` is still what decides how long, capped
at 30 seconds.

### Fixed — A rate limit was reported as a network failure

When all attempts were spent on 429s, the error raised was:

    TelegramError("شبکه در دسترس نبود: None")

`last_err` had never been set, because no network error had occurred. An admin
whose broadcast left a few people out went looking for a network problem when
they only needed to send more slowly. The error now says it was a rate limit, how
many times it waited, and carries code 429.

### Added — `bot/test_tg.py`

The Telegram client had no test of its own. 16 checks: the normal path, one 429
and recovery, three in a row still delivering, giving up with a truthful message,
logical errors like `chat not found` raising immediately with no retry and no
wait, and a transient network error not costing the message.

### Changed — Every monitoring refresh cost a second of waiting and an apt run

Both `cpu()` and `network()` measure rates, so each read its counter twice with a
`SAMPLE` wait between. They each took their own wait: 1.2 seconds of pure
sleeping per snapshot, for two numbers that can come from the same window.
`snapshot` now samples once and hands each of them its pair. Called on their own
they still sample themselves, since the agent may use them individually.

`packages()` runs `apt-get -s upgrade` with a 25-second timeout, and it ran on
every refresh. The list of upgradable packages does not change minute to minute;
it is cached for an hour now.

`security()` greps 24 hours of SSH journal on every refresh — the same scan the
intrusion page does. Only that scan is cached, for five minutes.

Caching the whole `security` section was the obvious move and it was wrong: the
existing tests caught that it also froze the firewall and fail2ban status. An
admin who has just switched the firewall on should not be told it is off for the
next five minutes. Same for `xray`, which is left uncached so a stopped service
shows immediately.

### Fixed — The intrusion page said "last 24 hours" and showed weeks

`_auth_lines` asks journald for the window with `--since`, then falls back to
`tail -n 20000 /var/log/auth.log`. `tail` limits how many lines, not how old they
are — so on the fallback path the page counted attempts from as far back as the
file went, while the response still reported `hours: 24`.

An address that hammered the server a month ago appeared as a current attacker,
and every count was inflated.

The fallback also triggered more often than it looks. The check was `if
out.strip()`, so a journald query that ran fine but found nothing — a quiet day —
was treated the same as journald being unavailable, and fell through to the
unbounded file. Success with no output now returns nothing, which is what it
means.

Lines from the file are filtered by timestamp, in both the syslog and ISO
formats, with the year inferred and rolled back when that would put the line in
the future. A line whose timestamp cannot be read is kept: dropping it would be
silent data loss, which is the thing being fixed.

## [1.10.10]

### Fixed — The rollback endpoint built a shell command from its input

`/api/admin/rollback` took the snapshot id from the request body and interpolated
it into a shell string:

    cmd = f"setsid {cli} rollback {snap} --yes --settings={keep}"
    subprocess.Popen(["bash", "-lc", cmd])

Its validation rejected `/` and `..` — a denylist again, which passes anything
nobody thought of, shell metacharacters included. A directory whose name carried
`;` or a backtick would have been accepted and interpreted.

Reaching it requires the admin password, so this is not an open door. But it
turns knowing that password into running arbitrary commands as root, rather than
just using the panel, and it is avoidable in two lines.

The id must now match `\d{8}-\d{6}`, which is the only shape
`date +%Y%m%d-%H%M%S` produces — an allowlist, so an unexpected name is rejected
rather than passed along. And the command runs as an argument list with no shell
at all, so nothing in it can be interpreted. `start_new_session` does what
`setsid` did: closing the panel mid-rollback does not kill it.

### Fixed — A missing log file no longer blocks a rollback

Writing output to the log is now best-effort. The log exists to be watched; the
rollback is the part that has to happen. Its path is also configurable, which is
what let the test exercise this at all.

## [1.10.9]

### Fixed — An expense could be recorded at last week's exchange rate

When tgju cannot be reached, `live()` falls back to the last successful rate and
marks it `stale`. For displaying a number that is reasonable. For recording an
expense it is not — the whole reason the toman amount is stored on the row is to
preserve the rate *at the time of purchase*.

The cache never expired, so if the rate site stayed down the fallback kept
returning the same figure indefinitely. And the expense endpoint never looked at
`stale` at all: the row went in with `fx_source: tgju` and nothing to say the
rate was hours or days old.

Two changes. Past a day old, a cached rate is no longer returned as usable — the
number is still there for display, but `ok` is cleared so nothing calculates with
it, and the hint says to enter the rate by hand. Below that, the expense is still
accepted — the site may be down for a few minutes and the work should not stop —
but the response carries the rate's age, and the panel shows it as a warning that
stays on screen instead of disappearing after four seconds.

The age now travels from `live` through `to_toman` to the endpoint, so the
warning can say how old: "the exchange rate from 3 hours ago was used".

A manual rate still wins over everything and is never marked stale.

## [1.10.8]

Agent 1.5.2.

### Fixed — A half-finished download stayed cached until the panel changed version

The agent fetches `monitor.py` and `firewall.py` from the panel and caches them
next to itself, re-fetching only when the panel's version changes. `download`
wrote straight to the destination file, so a connection that dropped mid-copy
left a truncated Python file in place.

From then on the file existed and the version matched, so it was never fetched
again. Every monitoring job on that node failed with a `SyntaxError` until the
panel happened to be updated.

Downloads are atomic now — written to a temporary file and moved into place — so
a failed transfer leaves the previous working copy untouched and no partial file
behind.

### Fixed — A cached module that will not load is now thrown away

The same stuck state could arrive any other way: a truncated file from an older
version, a disk problem, a bad write. Whatever the cause, the agent kept loading
the same broken file and reporting the same error.

If a cached module fails to load, it is deleted along with its version stamp and
fetched once more. A module that loads fine is still not re-downloaded — a fetch
on every check-in would be pointless traffic.

### Added — The agent's module cache is tested

`tools/test-agent.py` now drives `remote_module` directly: a healthy fetch, a
transfer that dies mid-copy, a syntactically broken cached file, and the two
cases that must *not* re-download. The real `download` is tested separately
against a stream that fails halfway, checking that the previous file survives and
no `.part` file is left.

Confirmed against the previous agent, where four of these fail.

## [1.10.7]

### Fixed — Rolling back could destroy the bot database

`nexora-cli` takes a snapshot in two places: before an update, and before a
rollback. Each had its own hand-written list of what to copy, and the rollback
one was missing eight items — `VERSION`, `.github`, `nexora-cli.sh`, the whole
`bot/` directory, `frontend/.env`, and all three databases.

The comment above it says the current state is saved "so rollback itself is
reversible". It was not.

The damage: rolling back asks whether to restore settings from the snapshot. Say
yes, and it copies that snapshot's `bot.db` over the live one — while the live
one was never saved anywhere. Every user, order, subscription and wallet balance
created since the last update is gone, with no copy to recover from and nothing
on screen to suggest anything was lost.

Rolling back also could not be undone for the bot: its code was not in the
snapshot either, so the bot stayed on the rolled-back version permanently.

Both call sites now use one `snapshot_to` function. Two lists cannot drift when
there is only one.

### Fixed — A flaky test of my own, and the reason it stayed hidden

`bot/test_xui.py` failed about one run in ten, but only on Windows. It blocked
the release gate twice today while passing every CI run on Linux.

The cause was the expired-session branch I added to the fake panel in 1.10.5. It
answered with a login page without first reading the request body. The server
then closed a socket with unread bytes still buffered, the OS sent RST, and the
client saw `ConnectionAborted`. Every other POST path in that handler drains the
body; that one did not. One line.

It stayed hidden because the gate printed `p.stdout or p.stderr` — and stdout was
never empty, so the traceback was never shown. All it displayed was output that
stopped mid-section. The gate prints stderr first now, and eight lines instead of
four.

Verified over 30 consecutive runs with no failures, against a measured 2–4 in 20
before.

### Added — `tools/test-snapshot.py`

Runs the function for real against a fake install tree rather than reading the
script. 13 checks: that both paths call the shared function, that all seventeen
files land in the snapshot, that `bot.db` arrives with its contents intact, that
`venv` and `__pycache__` stay out, and that a partial install — no bot, no
databases — neither errors nor creates empty placeholder files, since a zero-byte
`bot.db` restored later would wipe the database just as thoroughly.

## [1.10.6]

### Fixed — The intrusion page went black (React #310)

My own regression, introduced with the pagination work in 1.9.7.

`usePager` was called *after* the component's `if (!d) return <spinner/>` guard.
On the first render, before the data arrives, that guard returns early and the
hook never runs. On the next render the data exists, the guard is skipped, and
the hook runs — so React sees more hooks than the render before and tears the
whole tree down. Not a broken section: a black page.

All the derived values and the hook moved above the guard, which is where hooks
have to be.

### Added — `tools/test-hooks.cjs`

The component render test did not catch this, and could not: it mounts each
component once with data, so the empty-to-loaded transition that triggers the
error never happens.

This one reads the source instead. For every component it finds the early-return
guards and flags any hook called after one. No browser needed.

It was written wrong the first time — the pattern stopped at `=`, so it missed
`const { shown, pager } = usePager(...)`, which is exactly the line that caused
the bug. Verified now by running it against the broken file and watching it fail
with the file, line, component and code, then against the fix and watching it
pass.

## [1.10.5]

### Fixed — When the panel session expired, renewals silently did nothing

3x-ui redirects to its login page when a session expires. `requests` follows the
redirect, so the response arrives as HTTP 200 with an HTML body — not a 401.

`_req` treated any non-JSON body with a 2xx status as "succeeded, no content" and
returned `None`. That rule exists for endpoints like delete, which legitimately
answer with an empty body. But the login page is not empty, and it was being
counted as success.

So `update_client` returned normally, `extend_subscription` reported the new
expiry date, and the panel had changed nothing. The customer was told their
subscription was renewed. It was not. Blocking a client failed the same way.

The bot holds one panel connection for as long as it runs, so this is not a rare
state — it is what happens every time the session ages out.

Empty body with 2xx still means success. A non-empty body that is not JSON now
triggers one re-login and a retry; if it happens again, it raises with the reason
rather than pretending.

### Added — Session expiry is covered by the simulated panel

`bot/test_xui.py` can now make the fake panel answer with a login page, once or
every time. 30 checks, including that a single expiry heals itself and completes
the renewal, that a persistent one raises instead of reporting success, that the
expiry date is left untouched when it raises, and that it never tries logging in
more than once.

## [1.10.4]

### Fixed — The SSL certificate check never ran on any server

`check_cert` returns immediately unless it is given a domain. The domain came
from `advanced.panelDomain` — a config key that no UI writes, no installer step
sets, and no default provides. It is read in two places and written in none.

So the check existed, was written correctly, and never executed. Nobody was ever
warned that a certificate was about to expire. When it does expire, the panel and
the subscription page stop answering for every customer at once, with no prior
signal.

It finds the certificate itself now. certbot names each directory after its
domain, so `/etc/letsencrypt/live/<domain>/fullchain.pem` supplies both the file
and the name — nothing to configure. With several certificates, the one expiring
soonest is reported.

### Fixed — The expiry date was parsed in a locale-dependent way

`strptime` with `%b` matches month names in the system locale. OpenSSL always
prints English ones, so on a server whose `LC_TIME` is not English the parse
raised, the function returned `None`, and the check disappeared without a word.
Parsed with an explicit month table and compared in UTC now.

### Changed — A certificate that cannot be read is a warning, not silence

Any failure used to return `None`, which removes the row from the health list
entirely. No certificate at all still returns `None` — an HTTP-only server should
not be nagged. But when a certificate file is present and its date cannot be
read, that is now a warning with the command to run: not knowing when your
certificate expires is itself worth saying.

### Added — `tools/test-health.py`

The health checks had no test, and one of them had already shipped broken
(`check_time` in 1.9.7). 18 checks covering certificate discovery, the expiry
thresholds, OpenSSL's two-space day format, the unreadable-certificate case,
picking the soonest of several, and the timedatectl parsing that was wrong before.

## [1.10.3]

### Fixed — The public config endpoint leaked keys nobody added to the filter

`/api/public/config` is served without authentication — every customer's browser
fetches it each time the subscription page opens.

Its filter was a denylist: strip `resellers` and `bot`, publish everything else.
So any key added to the config later became public automatically, because nobody
remembered to add it to the list. `maintenance` went out that way: the server's
scheduled reboot time, the busy threshold, and the text of its last error.

That is the wrong shape for a filter that guards secrets. Forgetting a key in a
denylist leaks it; forgetting one in an allowlist only hides a feature. The
direction you fail in should be the safe one.

It is an allowlist now — the twelve keys the subscription page actually reads.
A future config key stays private until someone adds it deliberately.

Reseller `overrides` were merged *after* the filter, so an override could put a
stripped key back. The merge result goes through the same filter now. These are
written by the admin rather than an attacker, but a filter with a hole in it is
not a filter.

### Added — `tools/test-public-config.py`

23 checks. Among them: a config carrying a bot token, a reseller list, the
maintenance schedule and an invented `smsGateway.apiKey`, asserting none of it
reaches the response — including a plain string search of the whole payload for
the secret values.

## [1.10.2]

### Fixed — Backups were missing four tables, including every affiliate record

The list of tables to back up was written by hand. Each time a feature added a
table, that list was not updated, and nothing ever complained. The backup
downloaded with "ok" and simply did not contain them.

Missing from the bot backup:

    affiliates              the partners themselves
    affiliate_commissions   what they earned
    affiliate_payouts       what has been paid out
    events                  the history log

Missing from the accounting backup: `expenses` — the whole expenses section —
and `client_seen`, which accounting uses as the floor for "how long have we known
this config".

Worse than missing: restoring a bot backup ran `DELETE FROM` on its own table
list, then re-inserted from the file. Affiliate data was not on that list, so it
was left alone on the same machine — but restoring onto a fresh server produced a
panel with no affiliates, no commission history, and no payout records. Money
owed to real people, with nothing to reconstruct it from.

Table lists are read from the schema now. A table cannot be forgotten because
nobody has to remember it.

### Fixed — A half-finished restore reported success

Every row that failed to insert was swallowed by a bare `except: pass`, and the
response was `{"ok": true}` regardless. A restore that dropped half the users
looked exactly like one that worked.

Skipped rows are counted per table and returned, and the panel shows that as an
error rather than a success.

### Fixed — Restoring an older backup wiped tables it did not contain

Accounting restore deleted `group_config`, `payments` and `renewals`
unconditionally, whether or not the file had them. A backup taken before the
expenses feature would now also have cleared expenses. Only tables actually
present in the file are touched.

### Added — `tools/test-backup.py`

Backup and restore had no test at all. 24 checks: a full round trip through every
table, the partial-restore warning, and an old backup restored onto a newer
schema.

## [1.10.1]

Three of the five tunnel engines generated configs that could not carry traffic.
Backhaul and Chisel — the two marked recommended, and the two actually in use —
were correct.

The rule, stated in Backhaul's own docstring: `local` is the port opened on the
Iran server that customers connect to, `remote` is the real service port on the
foreign server. "Mixing these up means the tunnel comes up and no traffic
passes."

None of this was visible from the panel, because the port editor writes one value
into both fields. It surfaces the moment anyone maps 8080 to 80, or calls the API
directly — `validate_ports` has always accepted `{local, remote}` pairs.

### Fixed — Rathole had both sides inverted

The Iran server opened the *foreign* service port and waited for customers there.
The foreign server connected to the *Iran* port, which does not exist on that
machine. Service names were also derived from `remote`; they come from `local` now
so both ends agree.

### Fixed — FRP had localPort and remotePort swapped

In frpc, `localPort` is the service on the machine frpc runs on — the foreign
server — and `remotePort` is what frps publishes on the Iran side. They were the
other way round.

### Fixed — GOST opened no port for customers at all

The foreign side used `handler: tcp` with a chain: a *forward* proxy, opening the
port on the foreign server itself, with no destination to send traffic to. The
Iran config only accepted the relay and never listened for customers, despite its
docstring claiming it did.

Rewritten to use `rtcp`, which is how GOST does reverse forwarding: the foreign
side asks the relay to open the customer port on Iran and forwards arriving
connections to its own service.

### Added — Engine configs are tested

`build_config` had no test. There is one per engine now, asserting the exact
output that carries the port mapping, plus a parse check — TOML for Backhaul,
Rathole and FRP, YAML for GOST — because a malformed config fails on the remote
server where only journalctl would show it.

## [1.10.0]

### Fixed — A job the agent took but never answered was stuck forever

When the agent picks up a job its status becomes `taken`. There was no way out
of that state. If the agent restarted mid-job, lost the network, or was killed,
the job stayed `taken` permanently.

The panel detected this — both `nexora check` and the node diagnose page said
"the agent picked it up and never reported back" — and then did nothing about it.
The admin pressed the button, nothing happened, and the only recourse was to
press it again and hope.

Jobs taken more than five minutes ago are requeued on the agent's next check-in.
Every allowed action is idempotent (`apply`, `restart`, `sysmon`, `update_agent`
and the rest), so retrying is safe. After three attempts the job is marked failed
with the reason and the journalctl command to look at, so one broken job cannot
fill the queue forever.

Five minutes is deliberately longer than the slowest job: `sysmon` on a loaded
server takes a minute or two, and requeueing sooner would run two copies at once.

### Changed — Ports dropped from a tunnel say so

`validate_ports` discarded invalid rows with a bare `continue`. An admin entering
three ports got a tunnel with two, and nothing anywhere said where the third
went. When that port later did not work there was no trail to follow.

Dropped ports are now recorded as a warning event on the tunnel, with the reason
— out of range, not a number, or port 22, which is refused so nobody locks
themselves out by accident. When *every* port is rejected, the error says which
ones and why instead of "at least one valid port is required".

### Added — `tools/test-jobs.py`

The job queue had no functional test; `test-nodes.py` checks the source text
rather than running anything. 28 checks covering the normal lifecycle, recovery,
the attempt ceiling, node isolation, and port validation.

## [1.9.9]

### Fixed — A config on an exact half-month boundary billed unpredictably

The new text-vs-numeric test from 1.9.8 failed on Python 3.10 and passed on 3.12,
which turned out not to be a version difference at all. Month counts came from
`round(days / 30)`, and Python's `round` is banker's rounding:

    round(9.5)  == 10
    round(10.5) == 10      <- this one
    round(11.5) == 12

Half goes to the nearest *even* number. So two configs each sitting exactly half
a month over billed differently depending on whether their month count was odd or
even. 285 days is exactly 9.5 months, and one second either side of it flipped
the answer by a whole month.

`_months_from_days` rounds half up, with a tolerance so sub-second differences in
a creation date cannot change the result. The only bills that change are the ones
that landed exactly on a boundary with an even month count — those now round up,
like the odd ones always did.

### Fixed — The 1.9.8 test compared two different instants

It stored the text timestamp truncated to seconds and the numeric one with
milliseconds, so the two sides were up to a second apart. Both are built from the
same whole second now, which is what makes the comparison meaningful.

## [1.9.8]

### Fixed — Subscription length was blank for every client

`_duration_days` was the third function carrying the assumption fixed in 1.9.7:
`float(created)` raises `ValueError` on a text date, so it returned `None` for
every client whose `created_at` is stored as text. The length column in the group
invoice and in the clients list was empty as a result.

It goes through `_epoch_ms` now, like the rest.

### Added — The numbers no longer depend on how x-ui stores dates

The 1.9.7 bugs all had one shape: code that assumed `created_at` was a number.
The tests never caught them because the fixture left `created_at` as `NULL`,
which takes a different branch entirely.

There is now a test that builds the same five clients twice — once with text
timestamps, once with numeric — and asserts that config count, months, group
balance, invoice line counts, renewal count, invoice total, and period start all
come out identical. With the old code the text side reported zero renewals while
the numeric side reported 49, so this fails loudly on any regression of the same
kind.

### Added — `nexora check` reports how x-ui stores `created_at`

One line in the accounting section, giving the SQLite type and a sample value.
Which storage format a server uses decides which code path the accounting takes,
and there was no way to see it without opening the database by hand.

## [1.9.7]

### Fixed — Every invoice was roughly ten times too small

`_to_jalali` accepted epoch milliseconds only. x-ui stores `created_at` as text
in most versions, and the period invoice passed that value straight through, so
on those installs the page raised `TypeError`.

`_renewal_dates` had the same assumption in a quieter form: `float(created)`
raised `ValueError` on a text date and the function returned an empty list. No
exception surfaced — the invoice simply showed **zero renewals**. A config
created two years ago and renewed every month since counted as one sale.

On the sample data in the tests, the same 30-day invoice goes from 150,000 to
1,500,000 once renewals are counted.

All timestamp parsing now goes through one `_epoch_ms` helper that accepts
seconds, milliseconds, and text, so the numbers no longer depend on which x-ui
version wrote the row.

### Added — An invoice that covers everything, not just this month

The period invoice defaults to the last 30 days. For a reseller two years into a
relationship that shows only configs created this month — the complaint that
accounting "doesn't count anything from before, only the new ones".

There is an "از ابتدا تا امروز" button now. It starts from the creation date of
the group's oldest config, or from `settled_until` when a settlement is
recorded, so settled work does not reappear.

### Fixed — `strftime("%s")` is a glibc extension

Used to convert a renewal date to epoch. It raises `ValueError` outside glibc.
It never ran before because the renewal list was always empty, so no test caught
it. Replaced with `_date_ms`, which is explicit about UTC.

### Fixed — The subscription name was the panel's, not the customer's

`sub_label` appended the inbound's name *instead of* the config number when the
inbound had one. Every config on a shared inbound got an identical label:

    یک‌ماهه · پنل جدید
    یک‌ماهه · پنل جدید

So the renew screen could not say which subscription it was renewing, and the
name shown belonged to the panel rather than to anything the customer bought.
The config number comes first now; the inbound name is added only when there is
more than one inbound, where it actually distinguishes something.

### Fixed — Time sync always reported "not synchronized"

`timedatectl show -p NTPSynchronized,Timezone --value` prints properties in
systemd's own order, which puts Timezone first. The first line was read as the
sync flag, so it was never "yes". Running the suggested command changed nothing
because there was nothing wrong. Parsed as `KEY=value` pairs now, and the check
is skipped entirely when systemd does not report the property.

### Fixed — "Database not readable" flashed on every visit

The period section rendered its error card while `data` was still `null` from
the first fetch. It waits for the load now, like the users section already did.

### Changed — Numbered pagination where the lists were long

Firewall suggestions, blackholed addresses, firewall rules, the intrusion table,
and the active-connections list all rendered in full. "Show more" made the page
longer, which was the problem. They paginate now.

### Fixed — IPv6 addresses overflowed the connections card

The address column was a fixed 130px. An IPv6 address is about three times an
IPv4 one and ran outside the card.

### Changed — Custom billing range uses the Jalali picker

It was two text inputs with Gregorian placeholders, typed by hand.

## [1.9.6]

### Fixed — Coins spent on an unpaid order were lost for good

Coins are reserved when the order is created. `spend_coins` documents the
contract: they come back on rejection, expiry, or cancellation. Three paths
honoured it — the cancel button, opening a lapsed order, and admin rejection.

The automatic sweeper did not. It ran a bare UPDATE:

    UPDATE orders SET status='expired' WHERE ...

and left the reservation open. That sweeper runs every two minutes, so it
reaches a lapsed order long before the customer touches it — which made it the
path that actually handles expiry almost every time. The three correct paths were
effectively unreachable.

So a customer who applied coins to an order and then did not pay in time lost
those coins permanently. Silently, with no error, having received nothing.

The release logic moved to `TenantDB.release_coins` because the sweeper holds no
bot and cannot build a `Ctx` — which is why the logic was out of its reach in the
first place. `handlers._release_coins` now delegates to it, so there is one
implementation. The customer also gets a message saying the deadline passed and
their coins are back; otherwise the balance changes with no explanation.

Orders that are already approved are untouched, as before: that sale happened.

## [1.9.5]

Everything here came out of one `nexora check` run on the production server.

### Fixed — The doctor hid every TCP port

`nexora check` capped the port list at 40 lines. `ss` prints UDP before TCP, and
xray keeps about 40 ephemeral UDP sockets open for its own outbound traffic — so
the cap was reached before a single TCP port was printed. SSH, nginx, the panel
and every tunnel listener were missing from the report, which is exactly the list
you run the report to see.

TCP is printed first now, in full, and the ephemeral xray sockets are collapsed
into one line.

### Fixed — 40 phantom xray ports in the firewall

Those same ephemeral sockets reached the firewall page. The process is named
`xray-linux-amd6`, which matches the known-service list, so all 40 were filed
under "must stay open". Applying the plan would have written 40 permanent ufw
rules for ports that get new random numbers the next time xray restarts.

They are now recognised by checking the port against x-ui's own `inbounds`
table — the only authoritative source for which ports xray actually serves. A
real inbound on a high UDP port (Hysteria, for example) is kept; an ephemeral
socket is dropped and counted. If x-ui's database cannot be read, nothing is
filtered: a wrong guess here closes a customer-facing port.

### Fixed — Agent updates were rejected as "outside the panel"

Three `update_agent` jobs in a row failed with "the update address is not
allowed". The agent refuses any URL that does not start with its own
`PANEL_URL`, which is correct — it should not fetch its own executable from an
arbitrary host.

The mistake was on the panel side: it guessed its external address from nginx
headers and sent a domain, while the agent had been installed pointing at an
address of its own. The panel does not know that value and cannot know it.

The panel now sends no URL at all. The agent builds one from the address it
already check-ins with every minute, so the guard passes and stays intact. This
takes effect on the running 1.5.0 agent without reinstalling it.

### Changed — The doctor names the group missing a start date

With eleven groups, "1 billable group has no start date" meant opening all
eleven to find it. It prints the name now.

## [1.9.4]

### Fixed — The same expiry reminder went out three times

The hourly scheduler checked three thresholds in order and stopped at the first
open one:

    for day, flag in ((7, "notified_7d"), (3, ...), (1, ...)):
        if left <= day and not s[flag]: send(...); set(flag); break

That works for a subscription that enters the window at 30 days. It does not
work for one that enters at 1 day left — a short plan, reminders switched on
after the fact, or a bot that was down for a few hours. Such a subscription sits
inside all three thresholds at once:

    hour 1: the 7-day flag is open  ->  "only one day left"
    hour 2: the 3-day flag is open  ->  "only one day left"
    hour 3: the 1-day flag is open  ->  "only one day left"

Three identical messages in three hours, because all three are built from the
same `left`. Now one message closes every threshold the subscription has already
passed, and later thresholds still fire on their own as the date approaches.

### Added — The traffic warning that was never sent

`notified_80p` has been in the subscriptions table from the start, and both
renewal paths reset it. Nothing ever set it, and nothing ever read it — the
"you're running out of data" warning did not exist.

Unlike expiry, data has no date: a customer can burn a month's quota in one
night. They got no warning at all; the first news was the connection dropping,
with days still left on the subscription.

At 80% used, the customer now gets one message with what is left and a renew
button for that specific subscription. Usage comes from a single panel request
for all clients rather than one per client, and the warning resets with each
renewal, so it fires once per period.

## [1.9.3]

### Fixed — Affiliates were only paid on card purchases

`record_commission` was called from one place: the card-payment approval path.
Wallet purchases and auto-renewals both provision a config and deliver it, and
neither recorded a commission.

So an affiliate who brought a customer earned nothing from that customer's
wallet purchases and nothing from their automatic renewals — every month, for as
long as the subscription lasted. The money simply never appeared, with no error
to notice.

All three sale paths share one helper now. The existing uniqueness constraint on
(tenant, order) still prevents a double payment if an approval runs twice, and a
failure while recording the commission cannot block delivery — the sale already
happened and the customer is waiting.

## [1.9.2]

### Changed — Only configs that were actually used get charged

Every config in a group was billed, including ones created and never switched
on. Accounting now skips those — and only those.

The rule has to hold two things at once, because expiry is where a naive version
goes wrong. 3x-ui disables a config when its term ends, so billing on `enable`
alone would drop every config from the invoice the moment it expired — which is
backwards: expiry means the config **ran its term**. The customer used that
month and the reseller owes for it.

So a config is billed when it is enabled, or has traffic, or has expired. It is
skipped only when all three are false: disabled, no traffic, never expired — a
config that was created and never went into service.

Each group shows how many were skipped and why, so a due figure lower than the
config count explains itself instead of looking like a miscalculation.

## [1.9.1]

### Fixed — Paying to renew created a second config instead

The renew screen's buttons were `chk:` and `wpay:` — the same callbacks a new
purchase uses. `create_order` defaults to `kind="new"`, so pressing **تمدید**
built a brand new subscription: the customer paid to renew, received a second
config, and watched the one they meant to renew expire anyway.

### Fixed — Auto-renewal extended whichever subscription was newest

The renewal path did `subs[0]` — the most recently created subscription, not the
one that had triggered the renewal. A customer with three subscriptions saw the
wrong one extended while the expiring one lapsed.

Orders carry `renew_sub_id` now, set when the order is created and read back at
provisioning time. The lookup is scoped to the paying user, so an order can only
ever extend a subscription that belongs to them.

`bot/test_wallet.py` builds three real subscriptions, renews the middle one, and
checks the order carries that id — and that a subscription belonging to another
user cannot be found by the same query.

## [1.9.0]

### Added — `nexora check`, so the guessing stops

Three separate problems in this project were each chased twice before the real
cause turned up in a log that happened to get pasted. That is a waste of the
maintainer's time and it is on me.

`nexora check` collects, in one pass, everything those investigations needed:
the version on disk against the version actually running, the agent job queue
and exactly where it stalls, which ports the firewall can see and who owns them,
whether accounting can read x-ui, and whether any balance went negative.

The version comparison alone answers a question that came up repeatedly — a
panel that was updated but never restarted keeps serving the old code, and every
symptom then looks like "the fix did not work".

### Fixed — The agent restarted before reporting, so updates looked stuck

`update_self` ran `systemctl restart` and returned. The restart killed the
process before the result was posted, so the job sat on `taken` forever and the
panel showed an update that never finished — job 1840 in the reported log.

The result goes out first now; the restart happens after.

### Fixed — The intrusion page took over a minute to open

The owner lookup I added in 1.5.1 made one blocking DNS query per address, up to
sixty of them at 1.5 seconds each. The page now renders from cache immediately
and fills the names in behind it, so the second visit has them and the first
does not wait.

### Changed — Long lists are paginated, not expanded

"Show more" made the list longer, which is the problem it was meant to solve.
Numbered pages keep the page height fixed no matter how much data there is, with
first/last and a window around the current page once there are more than seven.

### Added — Export attacking addresses straight into the blocklist

Two buttons on the intrusion page: every attacker, or only the unknown ones —
the second skips tunnels and addresses your own clients connect from, because
blocking those means cutting your own customers. The file is in the format the
bulk blocker reads.

### Changed — The accounting date filters are Jalali too

The client filter's date range used bare Gregorian text fields. Anyone working
in a Persian panel holds the range in their head as Jalali.

## [1.8.1]

### Changed — The bot's messages now use the layout you asked for

A bold title under an emoji anchor, a rule beneath it, then sections with their
own thin rule, label-value rows, and copyable values in monospace. The delivery
and renewal screens lead the way.

The separator lines are back, but fixed at twenty characters. That was the
original objection and it was a real one — a long decorative line wraps on a
narrow phone and shreds the message. Twenty fits the narrowest screen there is.

### Fixed — A subscription showed the inbound's name instead of what was bought

`plan_id` is declared `ON DELETE SET NULL`, so the moment a plan is edited away
or replaced, the subscription loses the name of the thing the customer actually
paid for — and the label falls back to the inbound's remark, a server name the
customer never chose.

The plan name is written onto the subscription at purchase now and preferred
everywhere the subscription is named. Existing rows are backfilled from the
plans table on upgrade, for as long as those plans still exist.

## [1.8.0]

### Fixed — The monitoring report was cut in half before it arrived

`job-result` truncated the payload to 4000 characters and then parsed the
truncated string as JSON. A full server snapshot is larger than that, so the
parse always failed — and the failure was swallowed by a bare `except`, which
recorded the job as **successful** while storing nothing.

The agent log told the story: `sysmon` jobs returning real data with
`"level": "crit"`, and a page that stayed empty. The report arrived, was
destroyed on the doorstep, and the error went in the bin.

The full result is parsed now; only the copy stored in the jobs table is
shortened. The agent stops truncating structured output at all, and says so
explicitly if a result ever exceeds its limit instead of sending half a JSON
document.

### Fixed — Agents could never update themselves

`update_agent` kept failing with "این آدرس خارج از پنل مجاز نیست". The panel
runs behind nginx, so `request.base_url` gives `http://127.0.0.1:8100` — the
internal address, not the domain the agent knows. The agent rejects any URL that
does not start with its own panel address, and it is right to.

The URL is built from the forwarding headers now, so it matches what the agent
expects.

### Added — Blocking addresses in bulk, and proving they are blocked

A scan arrives with a hundred addresses and typing them one at a time is neither
practical nor error-free. Paste a list or load a file, and every address reports
its own result — "97 of 100" is useless without knowing which three.

The list exports in the same format it imports, so one server's blocklist loads
straight into another.

And each blocked address has a **verify** button that asks the kernel directly.
"I saw a success message" is not the same as "it is blocked", and only the
routing table settles it.

### Changed — Every date in accounting is Jalali, and picked not typed

The reseller start date, the settled-until date, and payment dates all use the
Jalali picker now. A date typed by hand into a Gregorian field is one mental
conversion away from being wrong, on numbers that end up on an invoice.

### Changed — Long lists stop taking the whole page

`LongList` shows the first few rows, keeps the rest behind a button, and adds a
search box once a list is long enough for that to matter. Applied first to the
service and process lists in monitoring, where a real server produces dozens of
rows and the two that are actually broken were buried among them.

## [1.7.2]

### Fixed — The monitoring report was only ever requested by hand

The agent log from a live server showed twenty-three `health` jobs in a row,
five minutes apart, and not a single `sysmon`. That is the whole explanation for
a page that never filled in: the scheduler queued health checks automatically
but `sysmon` was only ever queued when an admin pressed a button.

So the page was empty until the first click, and stale immediately after — which
looks exactly like a broken feature, because in every way that matters it was
one.

`sysmon` is queued in the same loop as the health check now, skipped while the
last report is under five minutes old so the queue does not fill with redundant
work. Open the page and the numbers are already there.

## [1.7.1]

### Fixed — Rejecting an order handed out free coins

The rejection path refunded `coins_used` back to the customer. Coins were never
taken at that point — they were only deducted when an order was **approved** —
so every rejected order credited coins that had never left the balance.

Sending a deliberately bad receipt was therefore a coin printer: order with 100
coins of discount, get it rejected, receive 100 coins, repeat. Unlimited coins
means unlimited discounts.

### Fixed — The same coins could pay for two orders

Because the deduction waited for approval, nothing stopped a customer creating a
second order while the first was still pending. Both stored the same
`coins_used`, both got approved, and both deducted from a balance that had only
ever covered one — with no condition on the UPDATE, so the balance simply went
negative.

Coins are now **reserved when the order is created** and released on rejection,
expiry or cancellation. Reserving makes both faults impossible at once: a second
order cannot reserve what is already held, and the refund on rejection now
returns something that was genuinely taken.

`spend_coins()` is atomic in the same way `spend_balance()` became in 1.6.1 —
the row only updates while the balance covers the amount, and a failed reserve
writes no transaction. Releases are marked so the same hold cannot be returned
twice.

`bot/test_wallet.py` now covers both currencies: ten threads race for coins that
cover three reservations, and exactly three win.

## [1.7.0]

### Added — Blocking an address without turning the firewall on

Enabling ufw on a remote server is a real decision, and treating it with caution
is right. But "this address is eating my server and I want it gone now" should
not have to wait for that decision.

A blackhole route does not involve the firewall at all: the kernel drops every
packet destined for that address, so the TCP handshake never completes and the
connection dies at the first step. It takes effect immediately, touches no
service, and is undone with one command.

Its limit is stated on the page rather than hidden: it kills the reply, it does
not filter the inbound flood. Enough for a scanner or a password guesser; a
volumetric attack still needs the firewall.

Blocked addresses persist across reboots — a routing table lives in memory, so
without a saved list an address the admin blocked would quietly reopen after the
next restart while they believed it was still shut.

### Added — Why a node sends no report

Twice I guessed at this and twice I was wrong, because nothing showed where the
job stopped. The queue has four stages — recorded, taken, executed, returned —
and any of them can be the one that stalls.

The diagnose panel now shows all four with timestamps and the actual error text,
plus the last fifteen jobs. A job stuck at "queued" means the agent is not
connected; stuck at "taken" means it is running and not answering; "failed"
shows what it said. Each state comes with the command to run next.

### Added — Jalali date picker

`<input type="date">` shows a Gregorian calendar. Someone recalling when they
started working with a reseller remembers it as "اول مهر ۱۴۰۳", not
"2024-09-22" — and converting in your head is where the error enters, on a field
that feeds straight into an invoice.

The picker is Jalali; the value sent to the backend is still ISO Gregorian, so
nothing changed on that side. The conversion is tested against known dates
including leap years and a thousand consecutive days round-tripped.

The first leap-year formula I wrote was wrong for 1403 — which is a leap year —
and the test caught it immediately.

### Fixed — Two focus rings stacked on every search box

A global `input:focus-visible` outline applied to the input inside the search
box, which already shows focus on its container. The result was a soft ring plus
a hard 2px outline a few pixels inside it. One indicator now, not two.

### Fixed — Number fields showed the browser's spinner arrows

They sit in the corner, match no theme, and in a right-to-left layout land
exactly where the eye expects the number. Where stepping is genuinely useful,
NumberStepper has its own buttons that match the rest of the panel.

## [1.6.2]

### Fixed — Accounting was throwing on every page load

Adding the month-source breakdown in 1.6.0 needed a `sources` field on each
group row. That edit silently failed and I did not notice, so the aggregation
wrote into a key that was never created: `KeyError: 'sources'` on every call to
the billing overview. The whole accounting section was down.

Two more faults were underneath it, both found by the end-to-end test written
to reproduce the first:

`app.py` imports the regex module as `_re`. Two functions added in 1.6.0 wrote
`re.match` instead — which compiles fine, because Python resolves free names at
call time, and only throws `NameError` when a user actually presses the button.

And the bulk start-date call asked the billing overview for its group list while
holding an open write connection. The overview opens its own connection and
writes to `client_seen`, so SQLite deadlocked and the page stopped with
"database is locked".

### Fixed — `۱۴۰۳-۰۶-۱۰` was accepted as a date

`\d` in Python matches Persian and Arabic digits too, so a Jalali date passed
the format check and was stored as if it were Gregorian. Every calculation on
it afterwards was meaningless. The pattern is `[0-9]` now.

### Fixed — The start-date banner disappeared after one visit

Once the panel records that it has seen a client, the month source changes from
"default" to "first seen" — but first-seen is today, which still gives one
month. The banner only looked for "default", so it vanished after the first page
load while the billing stayed wrong. Both sources count now.

### Fixed — The firewall could not see ports bound to a specific address

A port counted as public only when bound to `0.0.0.0` or `*`. Anything bound to
the server's own address — which is how tunnels are usually configured — was
treated as unreachable and skipped by both the suggestions and the pre-flight
check. The ports that mattered most were the ones missing.

Only loopback is exempt now. A private address is not: ufw filters those too, so
calling them safe would have let the pre-flight miss a service that enabling the
firewall then cut.

### Fixed — `nexora fix-xui` printed scrambled output

Seven of its messages had Persian and English mixed on one line, and the Persian
had been mangled in the file itself — leaving fragments like `ok "mode from and"`
and `ok " Service "`. Rewritten in English, the same way slow-doctor was.

## [1.6.1]

### Fixed — The wallet could be spent twice

Both places that take money used the same shape:

    fresh = db.get_user(tg_id)
    if fresh["balance"] < price: return
    db.add_balance(user_id, -price, ...)

Read, decide, then write — with nothing holding the balance still in
between. That was survivable while the bot processed one update at a time.
It stopped being survivable when the bot grew an eight-thread pool, and the
auto-renew scheduler runs in a thread of its own that the per-chat lock does
not cover. A customer buying a plan at the moment their own auto-renewal
fires is two deductions reading one balance.

Worse, the UPDATE had no condition on it, so the result was a negative
balance that appeared in no report and no alert.

`spend_balance()` makes the database hold the invariant instead:

    UPDATE users SET balance = balance - ?
     WHERE tenant_id=? AND id=? AND balance >= ?

No rows updated means not enough money — no transaction row is written and
the order is rejected with the reason. `add_balance` keeps its unconditional
behaviour for top-ups, refunds and admin corrections, where a negative
balance is a legitimate thing to record.

`bot/test_wallet.py` spends real money down real threads: ten of them race
for a balance that covers exactly five purchases, and exactly five win.

## [1.6.0]

### Fixed — Why node monitoring never arrived

Three faults stacked on top of each other, and none of them printed anything.

The agent reported version `1.0.0` from the first release and never bumped it,
so the "this agent is too old" check could not fire. The update job then sent a
relative URL, which the agent rejected as outside its own panel — so it could not
update either.

The root cause was underneath both: the agent downloads `monitor.py` from the
panel and cached it forever. `if not mod_path.exists()` was the only condition.
When the panel later grew a `snapshot()` function, the agent still held a copy
from months earlier and answered "no such function" — a failure that reached a
row in the jobs table and stopped there.

The panel now sends its version with every check-in and the agent re-downloads
its modules whenever that version changes. `netid.py` ships alongside them, so
a remote server detects tunnels and mapped loopback the same way the main one
does.

### Added — Signed agent requests

The token alone was enough to impersonate a node if it ever leaked — from a log,
a backup, anywhere — and it travelled on every request. Requests now carry an
HMAC-SHA256 signature over the body and a timestamp, so the token itself is not
what goes over the wire and a recorded request cannot be replayed later. Clock
drift beyond five minutes is rejected, comparison is constant-time, and agents
that do not sign yet still work so nothing breaks overnight.

### Fixed — Accounting counted from today instead of from the start

`_months_for` now reports **where its number came from**: a logged renewal, the
client's creation date, the group's start date, or the first time the panel saw
the client. Groups show the mix, so "one month" is no longer a silent guess.

Two real errors fell out of that. A config that ran for two years and expired
yesterday was billed as one month, because the span was measured to the expiry
date and came out negative; it now measures to today. And the panel records the
first time it sees every client, so this cannot recur for anything added from
here on — the past cannot be reconstructed, but it can stop growing.

The dashboard now says how many groups have no start date and sets them all at
once, instead of expecting eleven visits to eleven settings pages.

### Changed — Charts are curves

Catmull-Rom through the real data points with a gradient fill underneath, a
crosshair, and the value under the cursor. The line still passes through every
measurement — nothing is smoothed away, it just stops looking like a saw.

## [1.5.2]

### Fixed — The firewall offered to cut the tunnel

On a real server the panel suggested closing port 7777 with the note "nothing to
do with your service — process: backpack". backpack **was** the tunnel. Ticking
that box would have cut every customer whose traffic comes through Iran.

The keep-list was a hardcoded set of process names that knew about xray and nginx
but nothing about tunnel engines. It now uses the same list as everything else,
and a tunnel port is treated like SSH: it cannot be ticked, and `apply_plan`
refuses to close it even when asked directly.

Suggestions now have a third bucket. A port with no process name — a UDP port on
this server, which could easily have been the tunnel — used to be confidently
filed under "close". Guessing on the admin's behalf is worse than saying nothing,
so those go to "we don't know what this is" with the command to find out.

### Added — Turning the firewall on without risking lockout

Enabling ufw on a remote server is a bet: one missing rule and the connection
dies with no way back except the provider's console. Most people therefore never
enable it, which is the worst outcome.

The new page removes the bet. It lists exactly what would be cut, refuses to
proceed while SSH or a tunnel is uncovered, and then enables with a rollback
timer. If the admin does not confirm within the chosen window — because they got
cut off — the server disables the firewall itself and everything returns to how
it was.

### Fixed — Blocked addresses did not appear in the list

`ufw status` prints no rules at all while the firewall is inactive, even though
rules added then are stored and take effect the moment it is enabled. So blocking
an address reported success and then showed an empty list. Those rules are read
from `ufw show added` now and marked as pending.

### Fixed — The panel went white again, for the same reason as before

`Power` was used in the navigation without being imported — exactly the shape of
the `Activity` bug from 1.5.0. A sixth seam test now compares every component and
icon used against what each file imports, so this class cannot reach a release a
third time.

## [1.5.1]

### Fixed — The black screen had a second, simpler cause

`ConfirmModal` called `createPortal` with no container argument at all. React
throws #200 — "Target container is not a DOM element" — and tears down the tree,
so the panel went black **every single time that dialog opened**. 1.5.0 fixed
the error-message path but not this, which is why blocking an IP still failed.

`tools/test-components.cjs` now mounts every UI component in a real DOM and
asserts none of them throw. Pattern-matching tests could never have caught this;
only running the code does. It found a second one immediately: `EmptyState`
crashed whenever its optional icon was omitted.

### Fixed — Node monitoring stayed empty

The agent had reported version `1.0.0` since the beginning and never bumped it,
so the "agent too old" check never fired. Worse, the update job sent a relative
URL that the agent rejected as outside its panel, so the update could not happen
either. The agent now reports the real version, builds the URL itself when the
panel does not supply one, and an agent reporting no version at all counts as
too old.

### Fixed — "undefined کانفیگ در ۱۱ گروه"

The accounting dashboard read `totalClients`, which only a different endpoint
ever returned. It is part of the overview response now, along with how many of
each group's clients came from the bot.

### Fixed — Resellers billed for one month regardless of history

Months came from the client's creation date, and older x-ui versions do not
record one — so every client counted as a single month and a reseller working
for two years was billed for one. A group's **start date** is now used as the
fallback, which gives the admin a lever for exactly this case.

### Added — Whose address is this?

Reverse DNS names the owner of an attacking address. `65.108.213.175` resolves
to `your-server.de` — Hetzner, a datacentre, not someone's home line. The page
labels each address as datacentre or consumer ISP, which is as close to
"identify the real user" as anything can honestly get: a VPN exit cannot be
traced back from outside, but knowing it is a datacentre tells you blocking it
is low-risk, and knowing it is Irancell tells you it is probably a person.

### Changed — Firewall suggestions no longer need scrolling

Search, a capped list, and a "show the rest" button.

### Changed — Spacing is a system, not a habit

Every pair of sibling blocks inside a section is spaced by one rule using a
shared scale, instead of the previous rule that only matched a few specific
class combinations and left everything else touching.

### Changed — The bot sales report looks like the invoice

Same palette, same header, summary cards, alternating rows, a total line, and a
daily trend chart — instead of a bare table. The funnel shows drop-off between
steps, and the daily chart has a mean line and per-day detail on hover.

## [1.5.0]

### Fixed — Blocking an IP blanked the whole panel

FastAPI returns a validation error's `detail` as an **array of objects**. Forty-five
places in the panel rendered `j.detail` straight into JSX, and React refuses to
render an object as a child — it tears down the tree and the screen goes black
until a refresh.

`errText()` now normalises any detail shape into a readable string, and every one
of those places goes through it. An `ErrorBoundary` wraps the page content as a
second line of defence, so the next unknown crash costs one section rather than
the whole panel, with the menu still usable.

### Fixed — "ناعدد" in the accounting pages

`Number("...").toLocaleString("fa-IR")` returns the literal string **ناعدد** when
the input is not a number, and that is what was printed where an amount belonged.

`faNum` now separates three cases: nothing recorded gives an em dash, a real
number is formatted, and a non-numeric string (a clock time, a kernel version)
just has its digits converted. No path reaches that word any more.

### Fixed — Boxes with no gap between them

Spacing came only from a hand-written `mb-4` on each card, so wherever it was
forgotten two boxes touched. Sibling blocks inside a section now get automatic
spacing; existing margins collapse with it rather than doubling, and grid cells
keep their own gap.

### Fixed — Loopback counted as the busiest customer

`ss` reports local traffic as `::ffff:127.0.0.1`, which the loopback filter did
not match. On the live server that single entry held **751 connections — 26.7%
of everything** — and sat at the top of the "busiest peers" list as though it
were a customer. Addresses are normalised before any comparison now, so mapped
IPv4, private ranges and link-local are all excluded.

### Fixed — Only some tunnels were recognised

Tunnel detection matched a short list of process names and depended on `ss`
reporting them, which needs privileges it often does not have. **backpack** was
missing entirely. `netid.py` now identifies tunnels from the process table,
falls back to the ports those processes own, and knows eighteen engines.

### Added — Is this attacker a customer of mine?

Many attacking addresses are themselves VPN exits. The real address behind a VPN
cannot be discovered from outside — by this panel or anything else — and the
page says so rather than pretending otherwise.

The useful question has an answer: every attempting address is checked against
the addresses your own clients connect from, drawn from x-ui's IP records, the
Xray access log, and current connections. A match means a customer is sitting
behind that VPN, and blocking it cuts them off.

While wiring this up: `_connected_ips()` read a key named `top` that
`connections()` has never returned, so customer detection had silently never
worked at all.

### Fixed — Node monitoring never arrived

The `sysmon` command shipped in 1.4.0, but a node running an older agent simply
leaves the job queued for ever — the panel showed an empty page and no reason.
It now reads the agent's reported version, says plainly that it is too old, and
offers a button to update it without opening an SSH session. A failed job's
error is shown too.

### Changed — Accounting is separate from the bot

Bot customers are your direct customers; accounting exists to bill resellers.
Clients the bot sold are identified from the bot's own database — not guessed
from a name pattern — and are never billed to a reseller, even if they end up
inside a group. Each group reports how many of its clients came from the bot.

## [1.4.2]

### Fixed — slow-doctor treated resellers as if they did not exist

The previous patch compared x-ui's client list against the bot's subscriptions
and reported everything else as unaccounted. That is wrong for anyone selling
through resellers: those clients are real, they belong to a group, and the
panel bills them through accounting. The script was calling normal business
"a problem".

It now breaks clients down per reseller group and only warns about clients that
belong to no group *and* were not sold by the bot — the only ones genuinely
outside every channel.

The connections-per-client figure had the same fault. Dividing 2250 connections
by the bot's 6 subscriptions produced 375 and a red BAD line, when the real
denominator is every enabled client in x-ui. The basis is now printed alongside
the number, and says so explicitly when x-ui cannot be read.

## [1.4.1]

### Fixed — slow-doctor printed its own separator lines as data

The script defined a shell function named `head`. A function shadows the
coreutil of the same name, so every `| head -8` in the script was calling that
function with "-8" as a title — printing a decorative line instead of the first
eight rows.

On a real server that meant the process list, the busiest peer IPs and the
whole Xray section came back as `-7`, `-1` and rows of `──────`, with no error
anywhere to explain it. The function is now called `section`.

`tools/test-slowdoctor.py` asserts that no function in the script shares a name
with a coreutil it pipes into, so this cannot come back.

### Added — slow-doctor compares x-ui against what the bot sold

A connection count that looks impossible usually has a dull explanation: x-ui
holds clients that were added by hand and the bot never sold. The script now
counts them, says how many are enabled beyond the bot's subscriptions, and
lists the heaviest clients by traffic — one client far above the rest is a
shared config, many clients evenly spread is simply more users than you thought.

## [1.4.0]

### Added — The bot uses what Telegram actually offers

Messages built HTML by hand inside f-strings. One unclosed tag makes Telegram
reject the whole message, so the customer sees nothing and the log holds only a
400. `bot/fmt.py` turns the tag vocabulary into functions that always escape
their input and always close, and `tg.py` validates every message before it
goes out — a malformed one is sent as plain text rather than vanishing.

What that bought, per the formatting request:

- monospace for everything copyable — card numbers, subscription links,
  referral codes, order ids
- blockquotes for asides, and **expandable** blockquotes for the install guide
  so a long explanation no longer buries the screen
- strikethrough on the old price beside the bold new one, so the customer sees
  the saving instead of reading "20% off"
- spoilers where curiosity is the point
- hyperlinks instead of raw URLs
- an emoji at the start of each line as a visual anchor

`bot/test_fmt.py` runs the validator over all 44 rendered screens, so a broken
tag fails the build rather than a customer's message.

### Fixed — "تمدید" meant nothing when you had three subscriptions

Three subscriptions produced three identical buttons. Every screen now names a
subscription by plan **and** by the inbound's own remark — the name you gave the
server in 3x-ui — so a customer with servers in two countries can tell them
apart. The welcome screen listed only the first subscription's days remaining
without saying which one it meant; it now lists up to three by name.

### Added — Who is knocking on SSH

A new firewall page reads the auth log and groups failed logins by IP. The
distinction that matters: IPs currently connected to your service are almost
certainly your own customers mistyping, and blocking one costs you a customer.
Those are separated, coloured differently, and skipped by batch blocking.

Each hardening gap — root login, password auth, port, fail2ban — comes with the
exact command to fix it, and the one that can lock you out says so.

### Fixed — The firewall rules table was twice as long as it needed to be

ufw lists every rule twice, IPv4 and IPv6. They are merged now, with filters by
action and scope, so the page stops being a scroll.

### Added — Expenses, and what they do to profit

Servers abroad, the Iran server, traffic top-ups and domains had nowhere to
live, which made "revenue" a number that feels like profit and isn't.

Expenses now have a page, split by category, with the recurring monthly total
called out. Euro costs convert through the free-market rate, but the toman
amount is **frozen at entry** — recomputing later would make last month's cost
move with the market. A manual rate always beats the live one. An expense that
cannot be converted is refused rather than stored as zero.

The ledger page answers the question directly: billed, received, outstanding,
spent, and profit from money actually received. Debtors are listed so a
reseller's payment lands against the right balance.

### Added — The Iran server, managed from this panel

The agent now runs the panel's own `monitor.py` and `firewall.py`, downloaded
from the panel at run time. A second copy inside the agent would drift within a
release and report different numbers for the same server; this way every node
reports with identical thresholds and field names. No second panel install.

### Added — Bot sales report as a PDF

Same house style as the reseller invoice. Its CSV download was also broken: the
link carried no auth header, so the browser saved a 401 body and called it a
spreadsheet.

### Fixed — Things found while building the above

`app.py` referenced a `log` that was never defined and had no module-level
`import logging`. The panel's node list came from an endpoint that does not
exist. `test-seams.py` only recognised `fetch` and `call()`, so a helper named
`useJson` made four new routes look uncalled.

CI now also runs on Python 3.10: `install.sh` uses whatever `python3` the distro
ships, and testing only 3.12 would let syntax that breaks on Ubuntu 22.04 ship
silently.

### Changed — App.jsx was 11,418 lines in one file

Every panel change meant searching a file with 110 components in it. That is the
reason the firewall suggestion UI stayed missing for two releases: the backend
endpoint existed, nothing called it, and nobody could see the gap.

It is now 24 modules in four layers, each importing only downward:

    lib/        constants, formatting, shared hooks
    ui/         base components, no domain logic
    sections/   one file per domain — bot/ is split again by page
    shell/      the workspace switch
    App.jsx     334 lines: state, routing, layout

The split was done mechanically rather than by hand — a script mapped every
top-level declaration, worked out which identifiers each module actually uses,
and generated the imports from that map. Two bugs in the script were caught by
the tests before anything was committed: the app component being swallowed into
the wrong module, and the first icon in the lucide import list going missing,
which the build did not catch but `test-panel-runtime.js` did.

Behaviour is unchanged; the bundle is the same size to within a kilobyte, and
all 487 tests pass.

### Added — CI, so the tests actually run

The project had 475 tests that only ran when someone typed the command by hand.
`.github/workflows/ci.yml` now runs all of them on every push and pull request:
Python suites, seam tests, the bot text preview, the frontend build, and the three
Node suites. Tags additionally run the release gate.

### Added — `tools/test-seams.py`

Every bug that reached a customer in this project was a wiring bug, not a logic bug.
One half of the code declared something and the other half never heard about it: a
button with no dispatcher branch, a query against a table that was never created, a
backend route with no caller, a method defined twice.

Ordinary tests never caught these, because a test exercises a *function* and a seam
is not a function. This file counts both sides of five seams and diffs them:

- backend routes against frontend callers
- SQL table names against the real schema
- settings keys the bot reads against fields the panel can write
- duplicate function definitions in one scope
- handler names referenced but never defined

It found twelve real problems on its first run.

### Fixed — Eight bot settings the panel could never set

`trial_enabled`, `ask_phone`, `support_username`, `order_ttl_minutes`, `email_prefix`,
`sub_base_url`, `help_text` and `admins` were all read by the bot and written by
nothing. Each had a sensible fallback, so nothing crashed — the features were simply
frozen at their defaults forever. The free trial in particular could not be switched
on by any means.

The bot settings page now has a "رفتار ربات" section for all eight.

### Fixed — Error alerts you can act on

When an update failed, the admin group got the update id and "details are in the
server log", which meant opening an SSH session to learn anything. The alert now
carries the exception type, its message, and the button or command that triggered it —
none of which is sensitive. Customer message text is deliberately still excluded.

### Added — `tools/release-check.py`

The release checklist used to live in someone's memory. It is now a command that
verifies VERSION, `package.json` and the git tag agree, that CHANGELOG has a
non-empty section for the version, that the tree is clean and on `main`, and that
every suite passes. It exits non-zero if anything is missing, and CI runs it on tags.

### Known — Four backend routes with no caller

`test-seams.py` reports these as open debt rather than failing on them:
`/api/admin/reset-defaults` (resets all config with no confirmation),
`/api/admin/billing/overview`, `DELETE /api/admin/billing/payments/{id}` (the panel can
record a payment but not remove one), and `/api/admin/health/local`.

## [1.3.0]

### Fixed — Topping up the wallet crashed every time

`wallet_topup` and `wallet_topup_amount` both ran `SELECT * FROM cards`. There is no
`cards` table — bank cards live in the tenant's settings JSON, and always have. Every
customer who pressed "شارژ کیف پول" hit an `OperationalError` that the dispatcher
swallowed, so the button simply did nothing and nobody was told why.

Both call sites now read `ctx.s["cards"]` and pick one through `core.pick_card()`, the
same path the purchase flow has always used.

This was found by building a preview that renders every bot screen. The two screens had
never been exercised by a test because no test had ever pressed that button.

### Added — Every date the bot shows is now Jalali

The bot told an Iranian customer their subscription expires on `2027-01-15`. Nobody
reads that and knows whether it is next week or next year.

`core.fa_date()` and `core.fa_datetime()` convert through `jdatetime`, so expiry now
reads `۲۵ دی ۱۴۰۵` and an order timestamp reads `۱۴۰۵/۰۶/۲۰ — ۱۶:۵۱`. If `jdatetime`
is missing or the stored value is malformed, the old Gregorian string comes back
unchanged — showing a date must never be able to kill a message.

### Added — Firewall rule suggestions in the panel

The backend has had `/api/admin/firewall/suggest` and `/apply-plan` since 1.1.0 with
nothing in the panel calling them. The endpoints were dead code.

The firewall workspace now reads what is actually listening on the server and, for each
service, says whether the port should stay open or be closed, and why. Close suggestions
arrive pre-ticked because they are the reason the section exists; keep suggestions do
not, so nobody accidentally opens a door that was shut. Nothing is applied without an
explicit confirm, and SSH is still refused as a close target.

### Added — `bot/preview_texts.py`

Renders all forty bot screens to a text file without touching Telegram or a real panel:
the purchase flow, wallet, coins, referrals, tickets, renewals, the expiry reminder, the
automatic-renewal success and failure notices, every admin page, and the daily group
report.

It exists so bot copy can be reviewed as a whole instead of one screen at a time — and
it earned its keep immediately by surfacing the wallet crash and a Gregorian date that
had been missed.

### Added — `tools/test-bot-buttons.py`

Walks every `callback_data` the bot can emit and asserts the dispatcher has a branch for
it. Two buttons had shipped with no handler at all: "تمدید" and the admin ticket-reply
button. Both were dead on arrival and neither was noticed by a human.

### Fixed — `slow-doctor.sh` printed garbled IP addresses

The Persian labels inside `awk printf` collided with the terminal's bidirectional text
handling, and the connection parser read a fixed `$5` where the column position varies
by `ss` version. Rewritten in English, parsing `$NF`.

## [1.2.0]

### Fixed — Every bot message re-read the settings 31 times

`ctx.s` was a property that opened a fresh SQLite connection, ran a query and parsed JSON
on **every access** — and handlers touch it in 31 places. One simple message meant dozens
of disk round-trips before the bot did any actual work.

Measured: 2ms per read on an SSD, 62ms of pure overhead per message, and that is a fast
disk. On a VPS with shared storage it is several times worse.

Settings and tenant data are now cached for two seconds — short enough that a change made
in the panel appears effectively instantly, long enough that a single message reads them
once. Overhead per message dropped from 70ms to under 0.1ms.

### Fixed — SQLite locked the whole database on every write

The bot opened its database in the default journal mode, where a write blocks every
reader. That stayed invisible while updates were processed one at a time, but 1.0.0
introduced a pool of eight workers, and this is exactly where they queued up behind each
other.

WAL is now enabled, with `synchronous=NORMAL` and a 20-second busy timeout. Eight threads
running 60 mixed read/write operations each complete with no lock errors. Filesystems
that cannot support WAL fall back silently to the previous behaviour.

### Added — `slow-doctor.sh`

The panel can only see so much from inside. This script measures the server itself and
says what each number means: CPU saturation relative to core count, memory and swap
pressure, real disk write speed, service restart counts, **latency to Telegram** — which
dominates how fast the bot feels — SQLite journal mode and size, and the busiest IPs.

Run it with `sudo bash slow-doctor.sh` and send the output.

### Changed

- Tunnel peer lookup is cached for two minutes. Detecting them cost an extra `ss` call on
  every sample, which on a page refreshing every eight seconds is work for nothing

### Reverted

A cache for the panel's `load_config` was written and then removed: measured against the
real config it was *slower* than reading the file, because the defensive deep copy cost
more than the read it replaced. Complexity that does not pay for itself does not ship.

## [1.1.0]

### Fixed — The referral link never told anyone anything

Every signup action — welcome bonus, admin group notice, telling the referrer — lived in
`cmd_start` behind an "if this user is new" check. But `_on_message` calls
`_get_or_create` first, so the user already existed by the time `cmd_start` ran and the
whole block was skipped. With the phone gate enabled, `cmd_start` does not run on first
contact at all. The code had been dead for a long time while looking entirely reasonable.

It now lives where the user is actually created. The referrer hears the moment someone
joins with their link, sees how many people they have invited, and is told the coin
arrives after that person's first purchase — rather than waiting for a reward that was
never coming yet.

### Fixed — The renew button did nothing

It was rendered in "my subscriptions", but no branch in the callback dispatcher handled
it. The callback fell through and returned `None`, so pressing it produced no reply and
no log line.

Renewal now opens a page naming the plan, its volume and duration, days remaining and the
config name. Someone holding three subscriptions could not otherwise tell which one they
were about to pay for. Auto-renewal messages name the plan for the same reason.

### Fixed — Tunnels counted as suspicious traffic

The Iranian node carries every customer's traffic, so it always topped "unusual usage"
and buried real warnings underneath itself. Tunnel peers are now identified from the
tunnel database and from processes belonging to known engines, shown separately as your
own infrastructure, and excluded from the heavy-usage check.

### Added — The firewall proposes rules

Reads what is actually listening and suggests what to do with each port: keep SSH and the
panel's own services, with the reason attached to each; close anything public that
belongs to neither; ignore loopback-only ports; skip ports that already have a rule.
Nothing is applied without an explicit confirmation.

### Changed

- Maintenance renamed to "تازه‌سازی خودکار سرویس", with a description that explains why
  an admin would want it and says plainly that a healthy server does not need it
- More guidance text moved into blockquotes, which Telegram renders with a side rule so
  an explanation reads as an explanation rather than another instruction

## [1.0.0] — First release

Nexora is a subscription manager for 3x-ui: a customer-facing subscription page, a
Telegram sales bot, reseller billing, tunnels, a firewall manager and server monitoring.

### Subscription page

A customer page with 32 appearance combinations, one-click add to Happ, v2rayNG and
V2Box, install guides per platform, four languages, and a live usage view fed from the
panel's own headers. Ships as a single self-contained HTML file with the IRANSansX
webfont embedded, so it renders identically wherever x-ui serves it from.

### Telegram bot

Sells plans, takes card-to-card receipts, and delivers configs. A receipt is only marked
approved once the config actually exists in the panel, so a failed provision leaves the
order in the queue to retry rather than stranding a paying customer.

Every config arrives with a QR image, a one-tap copy button for the link, and the raw
config URLs as a fallback if no subscription link could be built. Card numbers and
amounts get their own copy buttons, stripped of dashes so banking apps accept them.

Updates are processed concurrently — eight at a time by default, tunable with
`BOT_POOL_SIZE`. Messages from the same chat stay strictly ordered, so a slow config
build for one customer never blocks anyone else.

### Billing

Periodic invoices for resellers, per-plan and per-gigabyte rates, commission tracking for
affiliates on every purchase rather than only the first, and a sales report. Volume above
the highest configured rate falls back to that rate instead of silently billing zero.

### Tunnels

Connects an Iranian server to an overseas one with five engines — Backhaul, Chisel,
Rathole, GOST and FRP — with two-sided deploy, latency measurement and health checks.

### Firewall

Reads and manages ufw rules from the panel. Enabling the firewall is refused when no rule
would keep SSH reachable, automatic reboot needs an explicit confirmation, and rules
covering critical ports cannot be deleted without acknowledging what they protect.

### Monitoring

CPU, memory, disk, network, services and open ports, each with the threshold that makes
it dangerous. Open ports lead with counts by risk and filter by risk, exposure and
search. Active connections show which IPs hold the largest share, with tunnel traffic
recognised rather than flagged. Usage history samples every five minutes and reports the
quietest hour of the day, which is what the maintenance schedule should be set to.

### Testing

```
test_bot.py             73
test_flow.py            76
test_admin.py           39
test_xui.py             24   against a simulated 3x-ui v3 panel
test-billing.py         13
test-maintenance.py     24
test-firewall.py        23
test-bot-throughput.py  12
test-subpage.js         21
check-api-contract.py, test-render.cjs, test-serve.py
```
