# راهنمای کار روی نکسورا

این فایل برای عاملی است که روی این مخزن کار می‌کند. معماری و نصب در
`README.md` و `INSTALL.md` است؛ این‌جا فقط چیزهایی است که اگر ندانی،
اشتباه می‌کنی.

## پروژه

مدیریت اشتراک برای پنل 3x-ui (Xray)، چهار بخش:

| بخش | کجا |
|---|---|
| پنل مدیریت (FastAPI + React) | `backend/app.py` · `frontend/src/` |
| ربات فروش تلگرام | `bot/` |
| حسابداری و صورتحساب واسطه‌ها | داخل `backend/app.py` |
| تونل‌ها و مانیتورینگ | `backend/tunnels.py` · `backend/monitor.py` |

`backend/app.py` حدود ۱۰ هزار خط است. قبل از تغییر، همان ناحیه را بخوان —
تابع‌های کمکی معمولاً از قبل وجود دارند.

ریموت گیت **`newgh`** است، نه `origin`. مخزن **عمومی** است: هیچ توکن،
رمز، آی‌پی سرور یا داده‌ی مشتری نباید کامیت شود.

## قواعدی که زیرشان نمی‌زنیم

**حسابداری اولویت اول است.** هر عددی که به واسطه نشان داده می‌شود باید
قابل دفاع باشد. نرخ‌ها را مالک خودش در سیستم تعریف می‌کند — کار ما فقط
*درست حساب‌کردن* است، نه حدس‌زدن نرخ. یک ردیف که بی‌صدا صفر حساب شود،
پولِ از دست رفته است.

**خروجی ابزارهای تشخیصی انگلیسی است.** هر چیزی که در ترمینال لینوکس چاپ
می‌شود (`tools/billing-why.py`، `tools/reseller.py`، `tools/fix-flow.py`)
باید انگلیسی باشد. متن راست‌به‌چپ با خروجی چپ‌به‌راست ترمینال قاطی می‌شود
و خوانده نمی‌شود. رابط کاربری و کامنت‌ها فارسی می‌مانند.

**تاریخ‌ها شمسی و انتخابی‌اند.** هر جای حسابداری که تاریخ می‌گیرد، با
`JalaliDate` از `frontend/src/ui/jalali.jsx` — تایپ‌کردنی نه. تبدیل ذهنی
میلادی به شمسی یعنی خطا، و خطا این‌جا مستقیم روی صورتحساب می‌نشیند.

**وضعیتِ سفارش و پولِ سفارش یک چیزند.** `status` همان ستونی است که
همه‌ی آمارهای فروش از رویش شمرده می‌شوند، پس هر جا پول جابه‌جا می‌شود
وضعیت هم باید همان‌جا و با همان ادعا عوض شود — از راه
`db.close_order()`، نه یک `UPDATE orders` دستی. وقتی این دو جدا نوشته
شدند، در دو جهتِ مخالف از هم دور افتادند: خریدِ کیف پول هیچ‌وقت
`approved` نمی‌شد (فروشِ تحویل‌شده در هیچ آماری نبود) و تمدیدِ ناموفق
`approved` می‌ماند (پولِ برگشته «فروش» شمرده می‌شد). جمعِ دو خطا عددی
می‌ساخت که درست به نظر می‌رسید.

**سفارش تا وقتی کانفیگش ساخته نشده `approved` نمی‌شود.** نه قبلش. این
قاعده در مسیر کارت بود و در سه مسیر دیگر نبود: خرید با کیف پول، تمدید
خودکار، و تست رایگان. هر سه یک شکل داشتند و سه بار جدا پیدا شدند. حالا
همه از `close_order` می‌گذرند و `test_flow` اسکن می‌کند که مسیر چهارمی
دوباره خام `SET status='approved'` ننویسد.

**خرید یک هسته دارد: `wallet_purchase`.** پرداخت از کیف پول — چه از
داخل ربات، چه از داخل مینی‌اپ — از همین یک تابع رد می‌شود.
`wallet_pay` فقط پوسته‌ی تلگرامی‌اش است و `/api/mini/buy` فقط ورودی را
می‌سنجد. اگر مسیر تازه‌ای منطق را دوباره بنویسد، می‌شود مسیر چهارم — و
سه مسیر قبلی هر کدام یک‌بار یک قاعده را فراموش کردند.

`test_flow` دو چیز را می‌سنجد: هسته هر دو پرداخت (پورسانت و پاداش
معرف) را بدهد، و پوسته خودش `create_order`/`spend_balance` صدا نزند.

و **تستِ مسیر موفق کافی نیست.** وقتی ساخت کانفیگ موفق است، سفارش چه
`approved` را قبلش بنویسی چه بعدش، `approved` تمام می‌شود — پس تفاوت
دیده نمی‌شود. تستِ واقعی آن است که ساخت را عمداً بشکنی و ببینی سفارش
`approved` نمانده و پول برگشته.

**رنگ از توکن می‌آید، نه از `rgba()` خام.** پنج پله برای هر وضعیت:
`wash` (زمینه‌ی ردیف)، `soft` (چیپ)، `fill` (هاور)، `line` (خط دور)،
`edge` (فوکوس و خطا). به‌علاوه `--hair-1..4` برای سفید و
`--scrim-1..4` برای سیاه.

چرا قاعده شد: همین چهار وضعیت در ۳۲۸ جای JSX دستی نوشته شده بودند و
رنگ هشدار به‌تنهایی ۱۷ آلفای متفاوت داشت. `.05` و `.06` و `.07` را
هیچ طراحی عمداً انتخاب نمی‌کند. تستِ ui-safety هم rgbaی خام را رد
می‌کند و هم `var()`ای که به توکنِ تعریف‌نشده اشاره کند — که بی‌صدا
شفاف می‌شود. دو استثنا: انتخابگر پوسته و طراح صفحه اشتراک، که آن‌جا
مقدار رنگ خودِ داده است.

**هر سه مسیرِ خرید باید هر سه پرداخت را انجام دهند.** خرید با کارت،
خرید با کیف پول، و تمدید خودکار — هر کدام باید پورسانتِ همکار فروش و
پاداشِ معرف را بدهند. پورسانت هر سه را داشت و پاداشِ معرف فقط مسیر کارت
را، پس دوستی که با کیف پول می‌خرید هیچ سکه‌ای به معرفش نمی‌رساند — در
حالی که ربات وعده‌اش را داده بود. `test_flow` هر سه تابع را با ast
می‌سنجد که هر دو را صدا بزنند.

**کد تخفیف وصل نیست.** جدول `discounts`، تابع `validate_discount` و
ستون‌های `discount_code`/`discount_pct` وجود دارند ولی هیچ‌جا صدا زده
نمی‌شوند. قابلیتی نیمه‌کاره است، نه چیزی که کار کند — رویش حساب باز نکن.

**تست رایگان خرید نیست.** گرفتن تست، خودش یک سفارشِ `approved` با مبلغ
صفر می‌سازد. پس هر شمارشی که «چند نفر خریدند» را جواب می‌دهد باید
سفارش‌های پلنِ تست را کنار بگذارد (`COALESCE(p.is_trial,0)=0`)، وگرنه
هر کسی که دکمه‌ی تست را زده «خرید موفق» شمرده می‌شود — و بخشِ «فقط تست
گرفتند» هیچ‌وقت نمی‌تواند چیزی جز صفر باشد. مبلغ برای این کار کافی
نیست: تستِ صفر تومانی و سفارشِ صد درصد تخفیف‌خورده هر دو صفرند ولی یکی
تبدیل است و دیگری نه.

**در ربات، هر دکمه‌ای که نشان داده می‌شود باید کار کند.** متنش هم مهم است.
دکمه‌ی بی‌جواب از نبودِ دکمه بدتر است.

**صفحه‌بندی شماره‌دار، نه «نمایش بیشتر».** تست `test-ui-safety.py` این را
اجرا می‌کند.

**هیچ صفحه‌ی تازه‌ای پول را خودش حساب نمی‌کند.** چهار سطح جدا این کار را
می‌کردند و چهار عدد متفاوت می‌دادند. حالا هر چیزی که بدهی می‌شمارد از
`_period_share` می‌گیرد و مبلغ را با `_line_amount` می‌سازد — نه
`months × price`، که نرخ کاربر اضافه را نادیده می‌گیرد. و قاعده‌ی
`_billable_config` («ساخته شد و هرگز روشن نشد») روی همه‌شان اعمال می‌شود.

**«فروش» با «درآمد» یکی نیست.** سفارشی که از کیف پول پرداخت شده فروش
هست ولی پول تازه‌ای با آن نرسیده — آن پول موقع شارژ رسیده. هر عددی که
«چقدر گرفته‌ام» را جواب می‌دهد باید `paid_from='card'` را فیلتر کند و
فقط مستاجرهای ریشه را بشمارد؛ فروش ربات نماینده درآمد نماینده است.

**هیچ‌وقت `FROM tenants … LIMIT 1` بدون شرط.** این یک اشتباه در یک روز
پنج بار پیدا شد. به مالک می‌رسد فقط چون معمولاً کوچک‌ترین شناسه مال
اوست — و همین که ردیف مالک یک‌بار پاک و دوباره ساخته شود (اجرای دوباره‌ی
نصب)، شناسه‌اش از نماینده بزرگ‌تر می‌شود و همه چیز بی‌صدا به ردیف اشتباه
می‌رود: درآمد ربات صفر، هشدار سرور به گروه نماینده، همکار فروش زیر
مستاجر اشتباه. یا `WHERE parent_id IS NULL ORDER BY id LIMIT 1`، یا یک
شناسه‌ی مشخص. تست درز این را اجرا می‌کند.

**قاعده‌ای که دو جا نوشته می‌شود، تستِ برابری می‌خواهد.** پرتکرارترین
باگِ این مخزن همین است: یک قاعده در دو جا، اصلاح در یکی. تا امروز هفت
بار — قاعده‌ی «قابل صورتحساب»، پرچم باز/بسته‌ی نماینده، فیلتر مستاجر
ریشه، کشِ ماژول ایجنت، توقف سرویس پیش از بازگردانی، اعتبارسنجی بیلد، و
فهرست موتورهای تانل در نسخه‌ی جایگزین.

اگر مجبوری تکرارش کنی (مثل کلاس جایگزینِ `monitor` برای وقتی `netid`
نیست)، همان‌جا تستی بنویس که برابریِ دو نسخه را بسنجد. وگرنه درست همان
وقتی از هم جدا می‌شوند که کسی یکی را اصلاح می‌کند.

**مسیر خرابِ بی‌صدا ممنوع.** اگر چیزی کار نکرد یا ناقص کار کرد، باید
بگوید *چه چیزی* و *چرا*. بیشتر باگ‌های واقعی این پروژه از همین بوده‌اند:
کدی که کار می‌کرد ولی چیزی نمی‌گفت.

## سبک کد

کامنت‌ها فارسی‌اند و **چرا** را توضیح می‌دهند، نه **چه** را. اگر یک تکه کد
شکل عجیبی دارد، دلیلش — معمولاً باگی که یک‌بار دیده شده — باید بالایش
نوشته باشد. این قرارداد در سراسر مخزن رعایت شده؛ آن را ادامه بده.

چند الگو که تکرار می‌شوند:

- **ادعا با `UPDATE` شرطی و `cur.rowcount`** — هر جا دو مسیر ممکن است
  هم‌زمان یک چیز را بردارند (زمان‌بند در برابر قفلِ چت در `bot/run.py`).
  اول ادعا کن، بعد پول بده.
- **فهرست مجاز، نه فهرست ممنوع** — هر جا از راز یا داده محافظت می‌شود.
  یک قلم جاافتاده در فهرست ممنوع یعنی نشتِ همه چیز.
- **`_portal_*` یک سطح API کوچک و جداست** — نماینده هرگز به مسیرهای مدیر
  نمی‌رسد. تست درز این را اجرا می‌کند: هر مسیر `/api/portal/*` باید از
  `portal_tenant` رد شود.

## نگاه‌کردن به پنل

تا مدت‌ها تنها راهِ دیدنِ پنل، بالاآوردن بکند و دیتابیس و ربات بود؛
یعنی عملاً هیچ‌وقت به آن نگاه نمی‌شد و ایرادهای چیدمانی فقط وقتی پیدا
می‌شدند که مالک بازش می‌کرد. حالا:

```bash
cd frontend && NEXORA_SINGLE_BUNDLE=1 npx vite build
node tools/make-harness.js
cd frontend && npx vite preview --outDir dist-test --port 5180
```

| آدرس | چه چیزی |
|---|---|
| `/harness.html` | پنل مدیر |
| `/harness.html?as=portal` | پنل نماینده |
| `/harness.html?as=mini` | مینی‌اپ مشتری |

صفحه‌ی اشتراکِ مشتری جداست — آن را سرورِ 3x-ui با قالب Go رندر
می‌کند، نه باندلِ ما:

```bash
node tools/make-subpage-harness.js   # → frontend/dist-test/sub.html
```

**بعد از هر بیلد دوباره بسازش.** هر دو خروجی در `dist-test` می‌نشینند
و `vite build` آن پوشه را پاک می‌کند؛ اگر یادت برود، `/sub.html` بی‌صدا
به خودِ پنل برمی‌گردد (SPA fallback) و فکر می‌کنی صفحه خراب شده.

همان درس، یک‌بار دیگر: تا وقتی این ابزار نبود، آن صفحه را فقط روی
سرورِ واقعی با یک اشتراکِ واقعی می‌شد دید — یعنی عملاً هیچ‌وقت. اگر
قالبِ تازه‌ای به `sub-page-index.html` اضافه شد و این اسکریپت
نشناختش، با خطا می‌ایستد؛ مقدارش را به `VALS` اضافه کن.

داده‌ی ساختگی در `tools/harness-boot.js` است. **واقع‌نما نگهش دار:**
با آرایه‌های خالی، بیشتر صفحه‌ها شاخه‌ی «هنوز تنظیم نشده» را می‌گیرند
و سالم به نظر می‌رسند — شش کرش واقعی فقط وقتی پیدا شدند که داده‌ی
شبیهِ واقعیت داده شد.

و **اندازه بگیر، نگاه نکن.** چیزهایی که با چشم پیدا نشدند ولی با
`getBoundingClientRect` بلافاصله پیدا شدند: کارتی که ۱۹۰ پیکسل بود
به‌جای ۱۴۲، تولتیپی که پهنای کل صفحه را زیاد می‌کرد (کارت ۴۸۰،
محتوا ۵۲۱)، و نوار بالایی که اصلاً نمی‌چسبید.

## قبل از ریلیز

```bash
PYTHONIOENCODING=utf-8 python tools/release-check.py
```

این دروازه است: هماهنگی نسخه، وضعیت مخزن، تست‌های ربات و پنل و درزها، و
کل فرانت‌اند. سبز نشد، ریلیز نکن.

## روال ریلیز

۱. `VERSION` و `frontend/package.json` را هم‌زمان بالا ببر
۲. بخش `CHANGELOG.md` را بنویس — *چه چیزی خراب بود و چرا*، نه فهرست کامیت
۳. `release-check.py` سبز
۴. کامیت و `git push newgh main`
۵. **منتظر سبزشدن CI بمان** (`gh run list -R nexora-technology-v/nexora-subscription-manager`)
۶. `git tag -a vX.Y.Z` و `git push newgh vX.Y.Z`
۷. `gh release create`

**تگِ منتشرشده را جابه‌جا نکن.** `nexora-cli.sh` نسخه‌ی روی سرور را با تگ
مقایسه می‌کند؛ سروری که از قبل روی آن نسخه است پیام «آخرین نسخه‌اید»
می‌گیرد و اصلاح هرگز نمی‌رسد. به‌جایش شماره‌ی وصله را بالا ببر.

## دام‌های محیط (ویندوز)

- تست‌های پایتون: **`PYTHONIOENCODING=utf-8`** لازم است، وگرنه روی متن
  فارسی `UnicodeEncodeError` می‌دهند
- تست‌های `.cjs`: **`NODE_PATH=frontend/node_modules`**
- مسیر پیش‌فرض دیتابیس **نسبی** است (`../data/`) و به پوشه‌ی کاری
  وابسته. هر تستی که دیتابیس می‌سازد باید `BILLING_DB` و `BOT_DB` را به
  پوشه‌ی موقت خودش ببرد، وگرنه همه‌ی اجراها روی یک فایل بیرون از مخزن
  می‌نشینند و تست به ردِ اجرای قبلی تکیه می‌کند
- **heredoc گیت‌بش بک‌اسلش را می‌خورد** (`\n` تبدیل به خط واقعی می‌شود).
  اسکریپت‌های وصله‌ای که کاراکتر فرار دارند را با ابزار Write بنویس، نه
  با heredoc
- **ماژول `db` در ربات دو نسخه دارد.** `handlers.py` آن را با
  `import db as DB` برمی‌دارد و تست‌ها با `from bot import db`. اینها دو
  شیء ماژولِ جداست که فقط یک فایلِ دیتابیس مشترک دارند. وصله‌زدن به
  `db.TenantDB` هیچ اثری روی چیزی که `handlers` صدا می‌زند ندارد —
  `H.DB.TenantDB` را وصله بزن. (در اجرای واقعی بی‌ضرر است: این ماژول
  هیچ حالتِ سطح‌ماژولی ندارد.)
- PDF محلی رندر نمی‌شود (poppler نصب نیست). برای بررسی خروجی PDF، روی
  `drawString` جاسوس بگذار و متن کشیده‌شده را بخوان

## قواعدی که تست‌ها اجرا می‌کنند

اینها را تست‌ها رد می‌کنند، پس از اول رعایتشان کن:

- **هیچ `detail` خامی در JSX** — همیشه `errText(j.detail, "پیام پیش‌فرض")`.
  اسکنر فقط ۹ کاراکتر به عقب نگاه می‌کند، پس شکل درست دقیقاً همین است
  (نه `errText(j && j.detail, …)`)
- **هر مسیر API که فرانت صدا می‌زند باید واقعی باشد** — قالب مسیر باید
  تمیز بماند؛ کوئری را *بیرون* از قالب بچسبان (`fetch(url + q)`)، وگرنه
  تست درز یک قطعه‌ی جعلی می‌بیند
- **هر `createPortal` مقصد می‌خواهد** — و هر چیزی با `position: fixed` که
  داخل `.fx-anim` باشد باید از راه portal روی `document.body` برود؛ آن
  کلاس `transform` ماندگار دارد و `fixed` را در خودش حبس می‌کند

## پیش از ساختن یک قابلیت تازه

اول یک برگه‌ی کوتاه بنویس، بعد کد. الگو و دلیلش در
[`docs/spec-template.md`](docs/spec-template.md) است.

پنج دور نیازمندی بعد از ساخت — که در پنل نمایندگی اتفاق افتاد — با پانزده
دقیقه سؤال قبل از ساخت جمع می‌شد.
