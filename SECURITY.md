# امنیت

## گزارش مشکل امنیتی

مشکل امنیتی را **به‌صورت issue عمومی گزارش نکنید**. نکسورا روی سرورهایی نصب است که پول و اطلاعات مشتری واقعی دارند؛ یک باگ منتشرشده قبل از رفع، همان سرورها را در معرض قرار می‌دهد.

دو راه:

- [Security advisory خصوصی](https://github.com/nexoratech-v/nexora-subscription-manager/security/advisories/new) در همین ریپو
- تلگرام: [@crm_nexoravpn](https://t.me/crm_nexoravpn)

در گزارش، اگر می‌شود این‌ها را بنویسید: نسخه، مسیری که مشکل از آن قابل بهره‌برداری است، و اینکه مهاجم باید احراز هویت شده باشد یا نه.

## نسخه‌های پشتیبانی‌شده

| نسخه | وضعیت |
|:--|:--|
| ۱٫۹٫x | پشتیبانی می‌شود |
| ۱٫۸ و پایین‌تر | فقط ارتقا — رفع امنیتی جداگانه منتشر نمی‌شود |

به‌روزرسانی یک دستور است و قبلش snapshot گرفته می‌شود:

```bash
nexora update
```

## نسخه‌هایی که نباید استفاده شوند

| نسخه | چرا |
|:--|:--|
| ۱٫۱٫۶ | اگر اینباند پیش‌فرض تنظیم نشده باشد هیچ کانفیگی ساخته نمی‌شود |
| زیر ۱٫۱٫۹ | تحویل کانفیگ به مشتری ممکن است بی‌صدا شکست بخورد، و صورتحساب ممکن است بعضی ردیف‌ها را صفر حساب کند |

## آنچه نکسورا از خودش محافظت می‌کند

این‌ها تصمیم‌های عمدی‌اند، نه اتفاقی:

**فایروال نمی‌تواند شما را بیرون بیندازد.** روشن‌کردن فایروال وقتی هیچ قاعده‌ای پورت ۲۲ را باز نگذاشته، بدون تایید صریح رد می‌شود. قاعده‌ی SSH برچسب «حیاتی» می‌گیرد و حذفش تایید جدا می‌خواهد. شماره‌ی قواعد درست قبل از حذف دوباره خوانده می‌شود تا قاعده‌ای که جایش نشسته حذف نشود.

**ریبوت خودکار پیش‌فرض خاموش است** و جدا از ری‌استارت سرویس در نظر گرفته شده. تا وقتی `confirmedReboot` صریحاً فرستاده نشود، اجرا نمی‌شود.

**agent تانل رمز root نمی‌گیرد.** سرور ایران فقط یک agent دارد که خودش بیرون وصل می‌شود، هیچ پورتی باز نمی‌کند و فقط چند دستور مشخص را می‌شناسد. اگر پنل نفوذ شود، مهاجم دسترسی shell روی سرور ایران ندارد.

**خواندن از 3x-ui فقط‌خواندنی است.** حسابداری مستقیم از دیتابیس 3x-ui می‌خواند و چیزی در آن نمی‌نویسد.

**ماژول‌ها جدا هستند.** ربات، حسابداری و تانل سرویس و دیتابیس مستقل دارند. از کار افتادن یکی بقیه را نمی‌خواباند.

## چیزهایی که خودتان باید رعایت کنید

- رمز پنل را ساده نگذارید؛ پنل روی اینترنت باز است
- توکن API پنل 3x-ui را جای دیگری کپی نکنید
- قبل از انتشار عکس یا لاگ در issue، توکن ربات و شماره کارت را پاک کنید
- `nexora backup` را دوره‌ای بگیرید — بازگردانی از نداشتن بک‌آپ ساخته نمی‌شود
